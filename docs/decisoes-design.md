# Regem — Decisões de Design e Contratos de Dados

> Documento vivo. Toda decisão nova aprovada em sessão vai no changelog (§6). Fonte: mockups em `mockups/`. **Nota de nomenclatura:** nos contratos abaixo, `loja` = `unidade` no schema; `tenant` = `empresa`.

---

## 1. Identidade

- Conceito: "no comando de todo o negócio, do balcão ao balanço". Autoridade + operação saudável.
- Logo: monograma **R.O. em órbita** (Rodrigo Oliveira), anel dourado. (Nos mockups aparece "Ω" — placeholder Omera; usar sempre o `RegemMark`.)
- Assinatura visual do produto: **Linha do Tempo Operacional** — o dia da loja em faixas horizontais por setor, com marcador "AGORA" em tempo real.

## 2. Design tokens

### 2.1 Tema claro (app principal + app do colaborador) — classe `.app-light`

```css
.app-light{
  /* superfícies */
  --canvas:#EDF0F4;      /* fundo geral (cinza-azulado frio) */
  --surface:#FFFFFF;     /* cards */
  --surface-2:#F6F8FA;   /* áreas rebaixadas, tracks, inputs */
  --line:#DCE3E9;        /* bordas */
  --line-2:#C6D0D9;      /* bordas de inputs */
  /* tinta */
  --ink:#0F2230;         /* texto principal / sidebar (petróleo) */
  --ink-2:#3D5566;       /* secundário */
  --ink-3:#7A8FA0;       /* terciário, rótulos */
  /* AÇÃO PRIMÁRIA / MARCA = ÂMBAR (Regem). ⚠️ Âmbar é SEMPRE ação/marca —
     nunca cor de estado. (mockups Fable 2026-07-02) */
  --primary:#E8A845;     --primary-ink:#2A1D06;
  /* semânticas de status — dessincronizadas do âmbar de propósito */
  --ok:#1FA875;  --ok-soft:#E1F3EC;    /* verde: sucesso/operação saudável */
  --warn:#E06A3C; --warn-soft:#FCEAE1; /* laranja-avermelhado: atenção/pendência (NÃO é âmbar) */
  --danger:#D64545; --danger-soft:#FAE4E4; /* vermelho: crítico (dark: #E05252) */
  --info:#3A7FB8; --info-soft:#E4EFF7;
  /* forma e tipografia */
  --radius:10px; --radius-lg:14px;
  --font-display:'Archivo',sans-serif;    /* títulos, rótulos caps, marca */
  --font-body:'Figtree',sans-serif;       /* corpo */
  --font-mono:'JetBrains Mono',monospace; /* KPIs, horários, valores, códigos */
}
```

### 2.2 Tema escuro (KDS e Terminal de Ponto — leitura à distância)

```css
:root{
  --bg:#0B141B;     /* KDS */   /* Ponto: #0F2230 */
  --panel:#12202A;  /* Ponto: #16303F */
  --panel-2:#182B37;/* Ponto: #1D3B4D */
  --line:#24394699; /* Ponto: #2A495C */
  --text:#EAF2F7; --text-2:#8FA9BA;
  --ok:#19C08F; --warn:#FFB13D; --danger:#FF5A4E; --info:#4AA8E0;
}
```

### 2.3 Regras de uso

- **Dourado (`--primary`)** é a única cor de ação primária/CTA e de marca. **Verde (`--ok`)** = status de sucesso, nunca CTA. **Âmbar (`--warn`)** nunca é ação primária, só alerta/correção.
- Números sempre em `--font-mono` (KPIs, horários, NSR, preços, pontos).
- Rótulos de seção/setor: Archivo 700, caps, letter-spacing `.12em–.18em`, 10–11px.
- Status nunca por cor apenas: sempre acompanha texto na `Tag`.
- Avatares: iniciais sobre cor derivada do id do colaborador (paleta `#0E7C66 #2C6E9B #6B3FA0 #8A5A10 #C2453A`).

## 3. Matriz RBAC (view × perfil) — **aplicada no servidor**

Perfis (categoria da hierarquia): `presidente` > `gerente` > `supervisao` > `execucao`.

| View / recurso | presidente | gerente | supervisao | execucao |
|---|:--:|:--:|:--:|:--:|
| dashboard | ● | ● | ◐ (só seu setor) | ✕ |
| escalas | ● | ● CRUD | ◐ leitura do setor | ◐ só as próprias (app) |
| tarefas / checklists | ● | ● CRUD | ● CRUD no setor | ● concluir apenas |
| estoque / recebimento | ● | ● | ● registrar no setor | ✕ |
| fichas técnicas | ● | ● | ◐ leitura | ✕ |
| vistorias / desperdício | ● | ● | ● registrar no setor | ● quando designado |
| POP & guias | ● | ● CRUD | ● CRUD no setor | ◐ leitura |
| desempenho / gamificação | ● rede | ● loja | ◐ setor | ◐ só o próprio |
| pessoas & ponto (gerencial) | ● | ● aprova ajustes/extras | ✕ | ◐ espelho próprio (app) |
| fornecedores | ● | ● | ✕ | ✕ |
| mural & clima | ● rede | ● loja | ● setor | ◐ lê e responde |
| **visão C&O (relatórios rede)** | ● | ✕ **nunca** | ✕ | ✕ |
| **bot (editar regras)** | ● | ◐ consulta log | ✕ | ✕ |
| **módulos on/off** | ● | ✕ | ✕ | ✕ |
| wizard de configuração | ● | ● | ✕ | ✕ |
| log de auditoria | ● rede | ● loja | ✕ | ✕ |

Legenda: ● acesso/edição · ◐ só leitura (escopo indicado) · ✕ a API não retorna o recurso.

Adicionais: `gerente` pode ser a mesma pessoa que `presidente`. `supervisao` tem escopo = setor(es) atribuído(s). Sobrescrever responsável de tarefa: `gerente`+; sempre audita.

## 4. Contratos de dados (referência p/ a API)

### 4.1 Hierarquia base
```json
{ "empresa": {"id":"t_01","nome":"Grupo Vieira"},
  "unidade": {"id":"l_01","tenant_id":"t_01","nome":"Loja 01 — Centro","ramo":"food_service"},
  "setor":   {"id":"s_coz","unidade_id":"l_01","nome":"Brigada de Cozinha"},
  "funcao":  {"id":"f_sous","setor_id":"s_coz","nome":"Sous-chef","categoria":"supervisao"},
  "colaborador": {"id":"c_042","unidade_id":"l_01","funcao_id":"f_sous","nome":"Paulo Lima",
    "matricula":"00042","vinculo":"clt","escala_modelo":"6x1",
    "lgpd":{"consentimento_foto":true,"data_consentimento":"2026-01-10"}} }
```

### 4.2 Escala e turno
```json
{ "turno": {"id":"sh_9911","colaborador_id":"c_042","unidade_id":"l_01","setor_id":"s_coz",
  "data":"2026-07-02","inicio":"08:00","fim":"17:00","intervalo_previsto":"12:00",
  "modelo":"6x1","tipo":"regular"} }
```
Modelos: `6x1, 5x2, 12x36, 4x3, horista, diarista, pj, autonomo`. `tipo`: regular | plantao_12x36 | diaria | pj | folga | descoberto.

### 4.3 Timeline operacional — `GET /unidades/{id}/timeline?data=YYYY-MM-DD`
Formato do `TIMELINE_DATA` do mockup (horas decimais, janela 06–22 configurável):
```json
[ { "setor":"Cozinha", "blocos":[
  { "ini":10.8, "fim":16.2, "rot":"Linha do almoço", "quem":"Sous-chef P. Lima",
    "colaborador_id":"c_042", "st":"exec" } ] } ]
```
`st`: ok | exec | atencao | feito.

### 4.4 Tarefa e checklist
```json
{ "tarefa": {"id":"tk_501","setor_id":"s_coz","data":"2026-07-02","hora":"14:30",
  "titulo":"Higienização de bancadas pós-almoço",
  "responsavel":{"colaborador_id":"c_042","origem":"escala"},
  "pop_id":"POP-COZ-004","status":"pendente","ociosidade":false,
  "conclusao":{"quando":null,"por":null,"foto_url":null}} }
```
`origem`: escala | manual (manual audita). `status`: pendente | concluida | atrasada | impossivel.

### 4.5 Ponto (Portaria 671/2021)
```json
{ "marcacao": {"nsr":"000048292","equipamento_id":"PONTO-ENTRADA-01","colaborador_id":"c_042",
  "tipo":"entrada","timestamp":"2026-07-02T07:54:12-03:00","turno_id":"sh_9911",
  "hash_integridade":"sha256:…","foto":{"url":null,"consentimento":true,"data_expurgo":"2027-07-02"},
  "origem":"terminal","geoloc":null,"offline_sync":false} }
```
`tipo`: entrada | inicio_intervalo | fim_intervalo | saida. `origem`: terminal | celular_geolocalizado.

### 4.6 Estoque, PVPS e recebimento
```json
{ "item_estoque": {"id":"i_tom","nome":"Tomate italiano","unidade":"kg","lote":"L-2481",
    "quantidade":6,"vencimento":"2026-07-04","posicao_pvps":"frente"},
  "recebimento": {"id":"rc_301","fornecedor_id":"fo_01","nf":"48211","foto_nota_url":"…",
    "itens":[{"item_id":"i_tom","qtd_nf":10,"qtd_recebida":10,"status":"ok"},
             {"item_id":"i_ceb","qtd_nf":5,"qtd_recebida":0,"status":"nao_veio"}],
    "encaminhado_financeiro":{"quando":null,"formato":"json"}} }
```
Índice de faltas do fornecedor = `nao_veio / total_itens` acumulado. `posicao_pvps`: frente | fundo (fundo = inconformidade).

### 4.7 Ficha técnica (alimenta CMV e compras) — **implementado**
```json
{ "ficha": {"id":"FT-021","nome":"Molho base de tomate","setor_id":"s_coz",
  "rendimento":{"qtd":10,"unidade":"porcao_500ml"},"validade_pos_producao_dias":6,
  "ingredientes":[{"item_id":"i_tom","qtd_liquida":4.5,"fc":1.15,"custo_unit_ref":"ultimo_recebimento"}],
  "preco_venda":14.90,"meta_cmv_pct":31.5,"pop_id":"POP-COZ-002",
  "passos":[{"ordem":1,"texto":"…","midia_url":null}]} }
```
Cálculo: `custo_total = Σ(qtd × fc × custo_unit)`; `custo_porcao = custo_total / rendimento`; `cmv = custo_porcao / preco_venda`.

### 4.8 Gamificação — **implementado**
```json
{ "ocorrencia": {"colaborador_id":"c_042","tipo_id":"oc_cobriu_turno","pontos":8,
  "registrado_por":"c_gerente","origem":"manual"} }
```
Pode ficar negativa. Ranking é **opaco**: só o `presidente` vê (nem o gerente). Tipos/premiações predefinidos pelo presidente.

### 4.9 Auditoria (imutável, append-only)
```json
{ "auditoria": {"id":"au_88","tenant_id":"t_01","unidade_id":"l_01",
  "quando":"2026-07-02T13:40:00-03:00","quem":{"colaborador_id":"c_ger","perfil":"gerente"},
  "tipo":"ponto","acao":"aprovou_hora_extra",
  "detalhe":{"alvo":"c_jul","valor":"+1h10","ref":"2026-07-01"},"origem":"web"} }
```
`tipo`: escala | ponto | recebimento | checklist | modulos | vistoria | bot | mural. `origem`: web | mobile | app_colaborador | kds | terminal.

### 4.10 Bot de suporte
```json
{ "regra_bot": {"id":"rb_01","tipo":"pedido_atrasado",
  "gatilhos":["atraso","demorando","cadê meu pedido"],
  "resposta":"Seu pedido está em preparo prioritário. Tempo: {tempo_estimado}.",
  "canais":["delivery"],"escalar":{"modo":"condicional","condicao":"atraso_min > 15"},
  "ativa":true,"editavel_por":"presidente"} }
```

### 4.11 Módulos (feature flags)
```json
{ "modulos":{"app_colaborador":true,"kds":true,"terminal_ponto":true,"bot_ia":false},
  "escopo":"rede" }
```
`escopo`: rede | unidade:{id}. Alteração só por `presidente`, sempre audita.

## 5. Arquitetura dos módulos satélites

| Módulo | Plataforma real | Comunicação | Observações |
|---|---|---|---|
| **KDS** | app nativo/empacotado (Tauri/Electron Win · Android) | **WebSocket na rede local**; descoberta mDNS; MAC cadastrado no servidor | Web não acessa MAC/mDNS → mockup é referência de UI. Alertas entram no topo da fila com som |
| **Terminal de Ponto** | tablet Android ou desktop empacotado | HTTPS + fila offline local | NSR por equipamento; exporta AFD/AEJ |
| **App Colaborador** | PWA ou app (React Native) | HTTPS + IndexedDB offline | Só leitura + concluir tarefas; sem cadastros |
| **Integrações externas** | — | API pública (OpenAPI) + webhooks | Delivery, WhatsApp Business, contabilidade |

## 6. Changelog de decisões

| Data | Decisão | Motivo |
|---|---|---|
| 2026-07-02 | RBAC no servidor; matriz §3 aprovada | Hierarquia é pilar do produto |
| 2026-07-02 | Timeline usa contrato §4.3 (horas decimais, janela configurável) | Mockup já renderiza deste formato |
| 2026-07-02 | Offline: last-write-wins + log de conflito; IDs idempotentes no cliente | Simplicidade no MVP |
| 2026-07-02 | Ponto estruturado p/ Portaria 671/2021 (NSR/AFD/AEJ) desde o MVP | Evitar retrabalho legal |
| 2026-07-02 | KDS/Ponto como apps independentes com WebSocket local | Limitações do navegador |
| 2026-07-02 | **Marca Regem: ação primária = DOURADO `#E2A340`; verde `#0E7C66` vira status "ok"** (mockup Omera usava verde como primária) | Decisão de marca do usuário ("Dourado Regem") |
| 2026-07-02 | Nomenclatura: `loja`=`unidade`, `tenant`=`empresa` no schema | Alinhar contratos ao schema Drizzle existente |
| 2026-07-02 | **Landing pública em `/`; login movido para `/entrar`** (redirects de auth/logout atualizados) | Ter tela de marketing antes do login (ref. brand-kit "Topo de landing page") |
| 2026-07-02 | **Fase F-A: tempo real via socket.io.** Gateway NestJS com rooms por tenant/unidade; handshake por JWT (gestor) ou token de equipamento (device). Eventos `ponto:marcado`, `kds:alerta`, `device:status`. Tabela `equipamento` (migration 015) + **NSR agora por equipamento** (Portaria 671). Superfície de teste: `/kds` web (tema escuro). | Base dos apps satélites. Escolha do usuário (socket.io + NSR por equipamento já). Packaging nativo/mDNS = Fase F-B |
| 2026-07-02 | **Conflito de cor resolvido (mockups Fable):** âmbar `#E8A845` é SÓ marca/ação; atenção passa a laranja-avermelhado `#E06A3C`, crítico vermelho `#E05252`/`#D64545`, verde segue "ok". Fontes do tema escuro unificadas em Archivo/Figtree/JetBrains (era Sora/Inter). Tokens em `globals.css`. | Se alertas ficassem âmbar, o usuário não distinguiria "ação" de "algo errado" |
| 2026-08-12 | **Atacado por volume — Fase A (mig 184).** `produto.atacado_ativo` (toggle) + `produto_faixa_preco.desconto_pct` (reaproveita a tabela da Fase L1, antes preço/un fixo e nunca aplicada). Desconto % progressivo por quantidade ("a partir de N un → -X%"), aplicando a maior faixa `qtd_min ≤ qtd`. Ligado no PDV (`vendaBalcao`, `adicionarItem`/comanda) e no cardápio (`receberPedido` + faixas expostas no `menu`). Desconto calc. sobre a **quantidade total pedida**; helper `common/preco-atacado.ts`. Stacking: atacado é preço de linha; cupom/cashback incidem sobre o subtotal já descontado (empilham). | Pedido do usuário: cadastrar desconto de atacado por faixa, valendo em PDV + cardápio, para qualquer pedido que atinja a quantidade |
| 2026-08-12 | **Sinal + cancelamento da encomenda — migs 187–190 (S1–S5).** Regra de sinal por loja: base (`cardapio_config.encomenda_exige_sinal`/`sinal_pct`/`cancel_horas`) + faixas por quantidade (`encomenda_regra_sinal`, mig 187); resolve pela qtd de itens (faixa que contém a qtd, senão base). Sinal cobrado online via PIX reaproveitando o gateway (cobra só o valor do sinal; `pedido_externo.sinal_*`/`cancelavel_ate`, mig 188); confirmação central `aprovarPagamento` distingue "sinal pago" de "pago total". Aviso forte no checkout. Cancelamento: cliente cancela dentro do prazo com **estorno real** (`reembolsarPagamentoMP` refund / PagBank charge cancel) + fallback manual (`reembolso_pendente`); loja cancela → evento `encomenda.sinal.reembolsar`. n8n (mig 189): `encomenda_sinal_pago` (confirmação+prazo) na aprovação + cron `*/15` de lembrete (`avisado_cancelamento_em`). Recorrência LEVE (mig 190, sem assinatura): `encomenda_recorrencia` (dias da semana+hora) + `pedido_externo.recorrencia_id`; cron `40 5` gera cada ocorrência reusando `receberPedido` (flag `_sistema` pula validações) e manda o LINK do sinal por WhatsApp (`encomenda_sinal_link`); cliente pausa/cancela a recorrência. Pagamento por ocorrência (sem cartão salvo). | Marmitaria/bolos/kits: garantir a produção com sinal e reduzir no-show; reembolso justo dentro do prazo; recorrência sem a complexidade de billing recorrente |
| 2026-08-12 | **Modo Encomenda (marmitaria) — mig 186. OPT-IN por loja (nasce OFF).** `cardapio_config`: `encomenda_ativa` + `encomenda_antecedencia_horas` (mín., ex. 24) + `encomenda_horizonte_dias` (ex. 30) + `encomenda_corte` (time, opcional) + `encomenda_capacidade_dia` (opcional). Regra única `regrasEncomenda(cfg)` (janela [minData,maxData] hoje) exposta no `menu.loja.encomenda` e usada para validar no `receberPedido` (só food/varejo; serviços mantém agendamento de horário). Pedido de encomenda grava `pedido_externo.retirada_tipo='encomenda'` (via `delivery.ingest`) e passa mesmo com a loja fechada. Antecedência é em HORAS (permite **mesmo dia mais tarde**, não só data futura); checkout (`cart-sheet`) usa seletor **data+hora** (datetime-local, min/max pelas regras) atrás de `encomenda.ativa` — sem a flag, food não oferece dia/hora à frente. Operação: `GET /delivery/encomendas?data=` (`listarEncomendas` agrupado por data) + página `/encomendas` (agenda "o que produzir no dia"). **Sem ordem de produção automática** (decisão do usuário: só lista por data). Vale para retirada + entrega; pagamento segue a config da loja. | Marmitaria opera por encomenda; feature ligável só para quem usa, sem afetar as demais lojas |
| 2026-08-12 | **Encomenda de atacado — Fase B (mig 185).** `ordem_producao` ganha `comanda_id`/`origem`/`data_entrega`. Quando um item de atacado com ficha excede o estoque disponível e a venda manda `encomendaDataEntrega`, vende o disponível no ato e o excedente vira **ordem de produção agendada** (criada pós-commit, amarrada à comanda). Disponibilidade derivada do ledger (`movimento_estoque`) via explosão de ficha (piso do gargalo). Preview em `POST /vendas/atacado/preview`. UI no PDV: toggle "Agendar encomenda do que faltar" + data + preview; comprovante lista as encomendas. Sem ficha → não encomenda (vende como hoje). Cobrança da encomenda = na entrega (não modelada agora). | "Vende o que tem, resto vira encomenda"; reaproveita o módulo de ordem de produção (mig 130) em vez de criar fluxo novo |
| 2026-07-03 | **Hardening P1 (branch `hardening`):** `empresa.controller` protegido (só a empresa do token; POST/findAll removidos — vazamento cross-tenant sanado); `main.ts` com helmet + **fail-fast** de `JWT_SECRET` e de `CORS_ORIGIN` em produção; `@nestjs/throttler` global (120/min) + 5/min em `login`/`pin`/`register`; **lockout de PIN** (10 falhas/15 min por unidade) com falhas registradas na auditoria. | Exposição de dados em produção com auto-deploy ligado; anti brute-force/spam |
| 2026-07-03 | **CI/CD P2:** `.github/workflows/ci.yml` roda `npm ci` + `npm run build` (+ `npm test`) em backend e frontend a cada push/PR. Push em `main` exige CI verde (branch protection + PR). Documentado no CLAUDE.md. | Portão de qualidade antes do auto-deploy |
| 2026-07-03 | **Testes P3:** Jest no backend + regras críticas isoladas em `src/common/regras-negocio.ts` (usadas pelos services): custo médio ponderado, saldo do ledger, retry de NSR (23505), late-binding tarefa→escala, recorrência de títulos. 16 testes verdes. | Cobrir 100% as regras de cálculo de dinheiro/ponto |
| 2026-07-03 | **RBAC por escopo P4 (§3):** escopo `setor`/`unidade` resolvido no login (via `função→setor→unidade`, sem migration) e embutido no JWT; guard injeta `setorId`/`unidadeId`; `escopoPermiteSetor` (supervisor só o próprio setor) com testes. Filtro aplicado no `desperdicio.findAll` como referência — **rollout aos demais endpoints operacionais é incremental**. | Guard validava só a categoria, não o escopo |
| 2026-07-03 | **Auditoria P5:** migration 023 torna `audit_log` **append-only** (trigger aborta UPDATE/DELETE). Auditoria adicionada à conclusão de tarefa e submissão/publicação de checklist (onboarding template e alocação de escala já auditavam). **Ainda sem auditoria (rollout incremental, menor risco):** CRUD de colaborador/função e demais cadastros de configuração, override de tarefa, movimento manual de estoque. | Trilha imutável e cobertura das mutações sensíveis |
| 2026-07-03 | **Contrato de API P6:** `@nestjs/swagger` (plugin auto-anota DTOs no build); `/api/v1/docs` exposto (em prod só com `SWAGGER_ENABLED=true`); `npm run openapi` gera `docs/openapi.json` a partir do `dist`. Referenciado no CLAUDE.md. | Contrato versionado para integrações externas (Fase K) |
| 2026-07-03 | **Sessão no front P7:** qualquer 401 limpa o token e redireciona para `/entrar?expirada=1` (banner amigável); erro de rede distinto de erro de API. **Decisão consciente:** manter o JWT em `localStorage` no MVP (app e API em domínios distintos — `app.` × `api.`). **Plano:** migrar para cookie `httpOnly` quando houver domínio unificado (mitiga XSS). | Sessão previsível + decisão de segurança registrada |
| 2026-07-03 | **Portão de CI = por disciplina (opção B):** branch protection não é aplicada em repo privado no plano grátis do GitHub (exige Team/Enterprise). Regra criada em `main` mas "not enforced"; adotado o fluxo branch→PR→CI verde→merge por convenção. Migrar para Team quando o time crescer. | Limitação de plano do GitHub |
| 2026-07-03 | **Fase ERP passo 1+2** (ver `logica-negocio.md`): ledger de estoque ganha `ref_tipo`/`ref_id` (idempotência via índice único) + `POST /producao` (explosão de ficha: baixa insumos ao custo médio, entrada do produto ao custo teórico). Migration 024. Fichas aninhadas ficam de fora (schema não referencia sub-ficha) e `vendas.baixaFicha` ainda sem `ref_*` (follow-up). | Destravar o motor de estoque/custo sem depender de PDV |
| 2026-07-03 | **ERP passo 3** (ver `logica-negocio.md`): ROP real (dias_seguranca + qtd sugerida) e view de **validades FEFO** no `/estoque`; **jobs** 06:00 (ponto de pedido) e 06:10 (validades) emitindo alerta em tempo real ao KDS. Sem migration. | Automatiza compra e reduz perda |
| 2026-07-03 | **ERP passo 4** (ver `logica-negocio.md`): snapshot de estoque (endpoint + cron mensal) + **CMV real × teórico → desvio** (`/estoque/cmv`) com card no `/estoque`. Sem migration (usa `estoque_snapshot` da 024). "Furo" (desvio − desperdício) pendente pois desperdício não liga a item/custo. | O KPI de ouro que diferencia o produto |
| 2026-07-03 | **ERP passo 6 (jornada)** + **Furo de CMV**: espelho de ponto ganha prévia gerencial (extra 50/100%, noturno 22h–5h, DSR estimado; sem migration); desperdício vinculável a item baixa estoque e fecha **furo = desvio − desperdício** (migration 025). Ver `logica-negocio.md`. | Fecha jornada e o ciclo de custo |
| 2026-07-03 | **Mural & Clima** (migration 026): comunicados com **leitura rastreável** (`comunicado`/`comunicado_leitura`) + **pesquisa de clima anônima**. **Decisão de privacidade (LGPD):** `clima_resposta` não guarda `colaborador_id`; a contagem "N/total" e a trava de voto duplo vêm de `clima_participacao` (separada), sem ligar pessoa→resposta; API só devolve o consolidado. Publicar/criar = gestor; ler/responder = qualquer autenticado. Tela `/mural`. | Engajamento de equipe com anonimidade garantida no schema |
| 2026-07-03 | **Bot de Suporte** (migration 027): bot de **regras por palavra-chave** (não IA generativa) — `bot_regra` (tipo, gatilhos, resposta, escala nunca/sempre/condicional) + `bot_atendimento` (log → métrica). Casamento é função pura `casarRegraBot` (normaliza acento/caixa). Editar regras = **presidente** (mockup: "só Proprietário / C&O"); listar/métricas = gestor; perguntar = qualquer autenticado. Sem match ou regra 'sempre' → escala para humano. Tela `/bot` (tabela + chat de teste). | Suporte de 1º nível automatizável sem custo de LLM |
| 2026-07-03 | **Wizard por ramo (versão rica)** (sem migration — blueprints hardcoded em `onboarding/templates.ts`): 4 ramos (food_service/varejo/industria_leve/servicos) com setores+funções sugeridos. Wizard `/wizard` em 4 passos (ramo → setores → funções → escalas + resumo) com chips selecionáveis; `POST /onboarding/wizard` cria **só o selecionado** (setores/funções/etiquetas) em transação + auditoria `aplicou_wizard`. Reusa `setor`/`funcao`/`etiqueta`; `/inicio` e `POST /onboarding/template` (pacote completo) seguem intactos. Escalas = informativas. RBAC presidente/gerente. | Ativação com menos atrito, customizável por ramo |
| 2026-07-03 | **Fichas técnicas aninhadas** (migration 028: `ficha_ingrediente.sub_ficha_id`): um ingrediente pode ser item de estoque, **sub-receita** (outra ficha) ou avulso. Custo é **recursivo** (sub-ficha entra pelo custo/porção — pura `custoTotalFicha` com memo+guarda de ciclo); explosão de `POST /producao` é **recursiva** até os itens-raiz (agrega consumo, `visitados` anti-ciclo). Ciclo (direto/transitivo) barrado na escrita (`fichaAlcancavel`) + safety net na leitura (ramo repetido = 0). Front `/fichas` com seletor de sub-receita. | Aprofunda o motor de CMV/explosão p/ food service real |
| 2026-07-03 | **Revisão geral** (migration 029): corrigido bug — `vendas.baixaFicha` não explodia sub-fichas (venda com sub-receita não baixava insumos aninhados → CMV distorcido), agora recursiva. **Mural escopado por unidade** (`colaborador.unidade_id`; feed rede+loja; "alvo" por comunicado). **Venda idempotente** (`comanda.idempotency_key` único por tenant; PDV gera chave estável). **Wizard idempotente** (reusa setor/função por nome). **Clima** só revela consolidado com ≥3 respostas (anti-deanonimização). **Bot** métrica "hoje" no fuso America/Sao_Paulo. | Robustez multi-loja + integridade dos números |
| 2026-07-04 | **P-UI1 — Shell responsivo** (migration 030: `colaborador.ui_prefs jsonb`): prefs `{sidebar:expanded\|collapsed, side:left\|right}` no **localStorage `regem_ui`** (paint) + **servidor** (`GET/PATCH /colaborador/me/prefs`, fonte da verdade entre dispositivos; puras `sanitizeUiPrefs`/`mergeUiPrefs` testadas). Shell via **CSS Grid** com `[data-side]`/`[data-collapsed]` e `order` (markup único, 240/72px, `prefers-reduced-motion`); recolhido = ícones+tooltip, rótulos viram divisores, logo→monograma, botão recolher com `aria-expanded` + atalho **`[`**; **<860px = drawer** off-canvas do lado escolhido (Esc/clique-fora, **foco preso**, hambúrguer no mesmo lado). Menu da conta com **Preferências** (destro/canhoto + recolher). Toasts espelhados: adiado (sem toast global hoje). | Fundação responsiva/ergonômica (base do P-UI2/3) |
| 2026-07-04 | **Toast global** (sem migration): `lib/toast.ts` (pub/sub singleton — chamável de qualquer lugar) + `<Toaster>` no `layout.tsx`, `aria-live`, auto-dismiss. **Espelha para o lado oposto à sidebar** (lê `regem_ui.side`) — não nasce sob o polegar que navega (fecha o detalhe adiado do P-UI1). Adotado em `/estoque` (snapshot/alertas) e `/pdv` (venda); adoção nas demais telas é incremental. | Feedback consistente + ergonomia canhoto/destro |
| 2026-07-04 | **Alertas de estoque persistidos** (migration 031: `alerta_estoque`): ROP e FEFO deixam de ser só eventos efêmeros — os jobs (06:00/06:10) **gravam** um alerta por (tenant, tipo) via upsert-por-aberto (índice único parcial), **auto-resolvem** quando a condição some, e ainda emitem o evento em tempo real. `GET /estoque/alertas` + `POST /estoque/alertas/:id/resolver` (gestor); card no `/estoque` com "Resolver". | Alertas confiáveis (sobrevivem, listam, resolvem) |
| 2026-07-04 | **NOVA ROTA — Arquitetura Edge** (`docs/arquitetura-edge.md`): substitui "satélites nativos/mDNS" por **local-first** (servidor local = PDV com Postgres + sync direcional com a nuvem). **Slice 1 (sem migration):** config direcional `TABELAS_SYNC` (sobe/desce/ambos + cursor), função pura **`venceLWW`** (last-write-wins + desempate por id, testada) e **`GET /sync/pull?desde=`** (deltas de controle por cursor, tenant do token, whitelist de tabelas). v2: `POST /sync/push` + sequência monotônica (migration) + tombstones + token de serviço. | Operar offline na loja; sync/backup na nuvem |
| 2026-07-04 | **Sync Edge slice 2** (sem migration): **`POST /sync/push`** (ingestão do operacional local→nuvem, append-only). **Segurança 1ª classe:** autenticação por **token de dispositivo** (`equipamento.tipo='servidor_local'`, guard `SyncTokenGuard`, header `x-sync-token`) em vez de JWT de presidente; **`tenant_id` FORÇADO** ao do token (ignora o da linha); **whitelist de tabelas** (`TABELAS_PUSH`) + **colunas por introspecção** (só colunas reais); identificadores via `sql.identifier`; idempotente (`on conflict (id) do nothing` + trata 23505); limites de payload. **Redação de segredos no pull** (`colaborador.senha_hash/pin_hash` nunca saem — PIN 4 díg. é brute-forçável offline). v3: LWW-update ('ambos'), sequência monotônica, tombstones. | Sync seguro sem confiar no cliente |
| 2026-07-04 | **Sync Edge slice 3 — LWW-update** (sem migration): `POST /sync/push` passa a aceitar as tabelas **`ambos`** (`item_estoque`, `fornecedor`) com upsert **last-write-wins**: `on conflict (id) do update` só se a linha recebida for **mais nova** (`updated_at`) **e do mesmo tenant** (`where <t>.tenant_id = excluded.tenant_id`) — nunca sobrescreve `id`/`tenant_id`. Fecha o caminho bidirecional das tabelas mutáveis. Config `modoPush(tabela)` → append\|lww\|null. v4: sequência monotônica (cursor à prova de clock-skew, migration) + tombstones (hard-deletes/soft-delete que não bumpa `updated_at`). | Bidirecional seguro do estoque/custo |
| 2026-07-04 | **Sync Edge slice 4 — soft-deletes** (sem migration): `GET /sync/pull` passa a incluir no delta as linhas com `deleted_at > desde` (por introspecção, só onde a coluna existe) → **exclusões propagam** mesmo sem bump de `updated_at`. **Decisão de segurança:** sequência monotônica + tombstones de **hard-delete** exigem **triggers nas tabelas centrais** e **não serão deployados sem ambiente de teste** (trigger com erro derruba todos os writes) — **adiado para a Fase 2** (Postgres local valida o CDC antes). | Excluir propaga sem risco; CDC perigoso fica pra ambiente testável |
| 2026-07-04 | **Fase 2 (edge) — PWA quiosque** (sem migration): `/kds` e `/terminal/ponto` viram **PWAs instaláveis** em tela cheia. Manifests próprios (`kds.webmanifest`/`ponto.webmanifest`, `display:fullscreen`, orientação, tema), ícones PNG on-brand gerados (círculo âmbar+órbita sobre navy, 192/512/maskable), **service worker** (`/sw.js`) com **shell offline** (network-first na navegação, SWR nos assets) que **NÃO intercepta a API** (outra origem → dados vão direto e o app trata a falha). Layouts de rota (`app/kds/layout.tsx`, `app/terminal/ponto/layout.tsx`) setam metadata/viewport + registram o SW. | Satélites instaláveis/tela-cheia com UI offline, testável no fluxo web |
| 2026-07-04 | **Fase 2 (edge) — servidor local + daemon de sync, VALIDADO localmente** (sem migration): `ConfigModule` prioriza `.env.local` (edge/dev, gitignored) → backend roda contra **Postgres local**. **`edge/sync-daemon.mjs`**: pull (controle nuvem→local) + push (operacional local→nuvem), cursores em `sync_state`, resiliente a **ordem de FK** (retry). Testado ponta a ponta com 2º banco fazendo o papel da nuvem. **2 bugs de sync corrigidos:** (a) `categoria_produto` sem `updated_at` → cursor defensivo (cai p/ `created_at`, não derruba o pull); (b) `empresa` (tenant) faltava no sync e filtra por `id` → adicionada com `escopo:'id'`. `scripts/apply-all-local.mjs` aplica as 31 migrations no banco local. Ver `backend/edge/README.md`. | Servidor local funcionando + sync validado com banco de teste real |
| 2026-07-05 | **Polimento mobile — Fase 1 (velocidade percebida)** (sem migration): (1) as 3 fontes saíram do `@import` bloqueante do `globals.css` para **`next/font/google`** (self-host, `display:'swap'`), ligadas às variáveis já existentes `--font-sans`/`--font-display`/`--font-mono` no `<html>` (tailwind.config inalterado). (2) `export const viewport` no layout raiz com **`viewportFit:'cover'`** (safe-areas) + **`themeColor`** por esquema (navy `#0D1A2B` escuro / base `#EDF0F4` claro). (3) Novos **`components/ui/skeleton.tsx`** (Skeleton/Text/Card/List) e **`empty-state.tsx`** (ícone+título+descrição+ação); os "Carregando…" das 10 telas mais usadas (meu-dia, escala, estoque, pedidos, kds, pdv, cadastros, pessoas, financeiro, produtos) viraram **skeletons no formato do conteúdo real** — `ResponsiveTable` ganhou skeleton de linhas/cards (cobre estoque e demais tabelas); PDV ganhou flag `carregado` p/ separar carregando de vazio; KDS usa skeleton no tema escuro enquanto checa a sessão. (4) `globals.css`: **`-webkit-tap-highlight-color: transparent`** + **`overscroll-behavior-y: none`** no `body`. Tokens/paleta/tipografia preservados; nenhuma funcionalidade removida. Build verde. | 85% dos usuários é Android — reduzir tela em branco e travar comportamentos touch |
| 2026-07-05 | **Polimento mobile — Fase 2 (BottomNav Material 3)** (sem migration): `components/app-shell/bottom-nav.tsx` — (1) **filtro por perfil** via `getCategoria()` do JWT (mesmo padrão da sidebar): item Cadastros ganha `roles:['presidente','gerente','supervisao']` → **Execução não vê Cadastros**; (2) estado ativo corrigido para `path === href` ou `path.startsWith(href + '/')` (cobre sub-rotas); (3) **`min-h-[56px]`** + `justify-center` nos itens (alvo de toque M3); (4) **`navigator.vibrate?.(10)`** ao trocar de aba (não vibra ao retocar a atual); (+) `pb-[env(safe-area-inset-bottom)]` p/ não ficar sob a home-bar (efeito do `viewportFit:'cover'` da F1). **Descoberta:** o componente não estava montado (só um import morto em `docs/page.tsx`) — **decisão do Rodrigo: montar na Shell**, `<BottomNav/>` renderizado no fim do `shell-content` e escondido `min-[860px]:hidden` (mesmo breakpoint do drawer) → nav-topo no rodapé só no mobile, drawer/sidebar seguem no desktop. Nenhuma funcionalidade removida; tokens preservados. Build verde. | Navegação mobile ergonômica (polegar) para os destinos-topo |
| 2026-07-05 | **Polimento mobile — Fase 3 (saúde do código)** (sem migration, **zero mudança visual**): quebra dos dois maiores arquivos de tela em componentes por seção + hooks de dados. **`produtos/page.tsx` 953 → 376 linhas** (orquestrador): novos `components/produtos/` — `types.ts` (Variacao/Combo/vazio/SELOS/brl/selectCls), `use-produtos-data.ts` (hook de carga), e por seção `categorias-card`, `produto-form` (compõe `loja-fields` + `variacoes-editor` + `combo-editor` + `fiscal-fields`), `destinos-card`, `faixas-card`, `complementos-card` (+ `grupo-complemento` movido), `produtos-lista`. **`cadastros/page.tsx` 698 → 83 linhas**: novos `components/cadastros/` — `constants.ts` (CATEGORIAS/VINCULOS/DIAS/META/Lists), `use-cadastros-data.ts` (hook), `build-secoes.ts` (as 8 seções de cadastro), `cadastros-hub`, `secao-detalhe`, `cadastros-skeleton` (`entity-form.tsx` intacto). **Prova de não-regressão visual:** o multiconjunto de `className` (estáticas + dinâmicas multi-linha) é byte-idêntico antes/depois — produtos **153/153**, cadastros **52/52** (via `diff` do JSX extraído). Também removido um import morto de `BottomNav` em `docs/page.tsx`. Build verde. | Manutenibilidade: telas de 900+/700 linhas viram orquestradores enxutos |
| 2026-07-05 | **Cardápio digital fiel ao mockup `regem-loja.html`** (migration 046). **F1 (backend):** `pedido_externo` ganhou **endereço estruturado** (`endereco_rua/numero/referencia/bairro` + `cliente_telefone2`) e `produto.destaque` (upsell). `receberPedido` aceita `rua/numero/referencia/bairroId/telefone2`, resolve o bairro (nome+taxa) das **áreas de atuação** (`cardapio_bairro`, já cadastradas na config), **compõe** o texto `endereco` p/ impressão e grava as partes via `delivery.ingest`; `menu()` expõe `destaque`; cadastro de produto ganhou toggle "Produto em destaque". e2e 14/14 + regressão 21/21. **F2 (frontend):** reescrita de `/c/[token]` seguindo o passo a passo do mockup, **componentizada** (`components/loja/`: `tipos`, `item-sheet`, `cart-sheet`): cards mobile (foto/selos/de-por/parcela/＋), **item-sheet** com etapas obrigatórias (radio/checkbox com marca) + **stepper de quantidade** + observação, **cart-sheet** com linhas (qtd/remover), **barra de progresso de frete grátis**, **upsell "peça também"** (produtos `destaque`), cupom, seg **entrega/retirada** + **agendamento** (quando/datetime), e **form de endereço estruturado** (rua, número, referência, **bairro puxando as áreas de atuação**, telefone 1 e 2, nome). **Cliente lembrado no aparelho** (localStorage) + prefill do nome pelo telefone quando a fidelidade está ativa. Timeline mantida. ⚠️ **Merge exige aplicar a migration 046 no Supabase.** | Paridade com iFood/Anota AI no cardápio digital, por ramo |
| 2026-07-05 | **Cardápio digital: KDS + senha + formas de cartão** (migration 047). **① Cai no KDS automático:** `cardapio.receberPedido` (entrega/retirada) agora **auto-aceita** (config `auto_kds`, default on) → `delivery.aceitar` → `venderExterno` cria comanda + produção e **emite ao KDS na hora**. **② Senha:** o cardápio ganhou **contador próprio** (`pedido_externo.display_id` sequencial por tenant) e, ao entrar no PDV, `venderExterno` **puxa a senha LOCAL** (`proximaSenha`, gravada na comanda) e repassa `plataforma`+`senhaPlataforma` ao `producao_pedido`; o **KDS mostra a senha local grande + selo "🛵 Cardápio · #<senha da plataforma>"**. **③ Formas de cartão:** `cardapio_config.formas_cartao` (lista livre de rótulos, ex.: "Crédito Visa", "Débito", "VR Alelo") + toggle `auto_kds`, editáveis em `/cardapio-config`; o menu expõe `formasCartao`; no store, escolher **Cartão** abre as sub-opções e a escolhida vai como `pedido_externo.bandeira` (obrigatória quando há formas cadastradas). e2e 10/10 + regressão PDV A–C 21/21. ⚠️ **Merge exige rodar a migration 047 no Supabase.** | Pedido do cardápio visível na cozinha, com senha local + origem, e cartão detalhado |
| 2026-07-05 | **Revisão conceitual — Etapa 1: entrada/login** (sem migration). **Bug crítico corrigido:** o login mandava todo mundo para `/painel`, mas o dashboard é `@Roles('presidente','gerente')` → supervisão/execução tomavam **403** ao entrar. Novo helper `rotaInicial(cat)` (gestor→`/painel`, demais→`/meu-dia`) usado no `/entrar` e no `/pin`; `/painel` agora **protege a rota** (sem token→`/entrar`; perfil sem dashboard→`/meu-dia`). **Login por senha agora é auditado** (`login`/`login_falhou` em `audit_log` — antes só falha de PIN era). **"Esqueci minha senha"** deixou de ser link morto: nova página `/recuperar-senha` (honesta — **não há infra de e-mail**, então orienta reset pelo gestor) + endpoint `POST /colaboradores/:id/senha` (RBAC presidente/gerente, auditado `senha_redefinida`) e card **"Acesso & senha"** em `/pessoas` (gestor define/redefine a senha de um colaborador). **"Manter conectado"** virou real: `setToken(t, persist)` usa `localStorage` (persiste) ou `sessionStorage` (cai ao fechar) — antes o checkbox era decorativo. **PIN pad do login** parou de descartar o PIN digitado (handoff via `sessionStorage` → prefill no `/pin`). Login usa `replace` (não volta ao login) e quem já está logado é redirecionado; erro de login com `aria-live`. e2e auth 10/10 + regressão PDV A–C 21/21. | Destrava o login de 2 perfis + recuperação de senha real + trilha de auditoria |
| 2026-07-06 | **Revisão conceitual — landing + linha do tempo operacional + remoção do PIN de login** (sem migration). **Landing (`/`):** a vitrine "Módulos que crescem com você" foi atualizada dos 4 antigos para 12 módulos atuais (PDV & Comandas, Cardápio digital, KDS, NFC-e, TEF, Delivery & iFood, Fichas & CMV, Estoque/Validade/Compras, Fornecedores & Recebimento, Terminal de Ponto, Mural & Clima, Bot). **Linha do tempo operacional (dashboard):** reestruturada de uma barra única (que quase sempre aparecia vazia por depender de `turno` genérico) para **linhas por setor** puxando da **escala do dia** (`escala_alocacao` × `turno` × `etiqueta`→`setor` × `colaborador`) — mostra quem está escalado, blocos coloridos por status relativo ao **marcador "agora"** (em dia/em execução/concluído/atenção-sem-responsável), tarefas com horário e janelas de pico como overlay. `GET /dashboard/timeline` passou a devolver `{ setores[], picos[], agora }`; componente `Timeline` reescrito (por setor, scroll horizontal). **PIN removido do login:** `/entrar` virou só e-mail+senha (aba PIN e o pad removidos); rota `/pin` (login por PIN) excluída — o **backend `pinLogin` e o Terminal de Ponto (`/terminal/ponto`) permanecem** (bater ponto por PIN é da Portaria 671). e2e timeline 8/8 + auth/PDV regressão 21/21. | Landing atual, timeline com valor real (da escala) e login simplificado |
| 2026-07-06 | **Revisão conceitual — Dashboard (`/painel`)** (sem migration). **Lacuna corrigida:** o dashboard só mostrava operação (tarefas/escala/estoque/desperdício) e ignorava o lado comercial que já existe. `dashboard.resumo` passou a devolver `vendas { total, faturado, ticketMedio }` (comandas fechadas do dia, no fuso SP) + `deliveryPendentes` (pedidos externos ativos); o front ganhou uma **linha de KPIs comerciais** (Vendas hoje R$, Ticket médio, Delivery pendente) acima da operacional. **Fuso corrigido:** `hoje()` e os defaults do controller passaram a usar `America/Sao_Paulo` (antes data UTC divergia do marcador "agora" da timeline perto da meia-noite). Verificado que o card "Tarefas em atraso" já casava os campos (`titulo`/`setorNome`/`colaboradorNome`). e2e dashboard 7/7 + regressão PDV A–C 21/21. | O gestor vê o dinheiro do dia, não só a operação |
| 2026-07-06 | **Revisão conceitual — PDV (`/pdv`)** (sem migration, só frontend). Três correções de workflow: (1) **status do caixa visível** — o PDV busca `caixaAberta` no load, mostra banner "caixa aberto/fechado", permite **abrir o caixa** dali (valor de abertura) e **bloqueia o botão de finalizar** sem caixa (antes o operador só descobria no erro do submit); (2) **produto simples entra em 1 toque** — `tap()` só abre o seletor quando há variação/complemento; sem isso, adiciona direto (balcão rápido, antes abria modal só com "observação" pra tudo); (3) **troco no dinheiro** — campo "valor recebido" calcula troco/falta ao vivo quando a forma é dinheiro. Regressão PDV A–C 21/21; build verde. | Menos fricção e menos erro no caixa/balcão |
| 2026-07-06 | **PDV épico — Fase 1: Turno & Caixa** (migration 048). A sessão de caixa virou **turno**: `caixa_sessao.turno_numero` (sequencial por dia, fuso SP) atribuído ao abrir; `GET /financeiro/caixa` (`caixaAtual`) devolve `turnoNumero` + `operadorNome`. **Trava de operador:** `exigeDonoDoTurno` — só quem **abriu** (ou gerente/presidente override) pode **sangrar/suprir/fechar**; "troca de operador" = fechar o turno e abrir outro. Novo componente `components/pdv/caixa-panel.tsx` no PDV: abrir turno, **suprimento**, **sangria** e **fechar (conferência cega:** contado × esperado = abertura+entradas−saídas em dinheiro, com diferença e resumo por forma — backend `movimentarCaixa`/`fecharSessao` já existiam, faltava a UI + o nº do turno + a trava). e2e turno 11/11 + regressão PDV A–C 21/21. **Merge exige migration 048.** *(Próximas fases do épico: 2 produtos por categoria+fotos, 3 cancelar/alterar cupom, 4 formas de pagamento + dividir conta.)* | Operação de caixa por turno/operador, com fechamento conferido |
| 2026-07-06 | **PDV épico — Fase 2: produtos por categoria + fotos** (sem migration). A lista `/produtos` passou a devolver `imagemRef`; o grid do PDV agora é **agrupado por categoria** (cabeçalho com nome + contagem) e cada produto é um **card com foto** (`imagemRef`, com emoji de fallback), preço e "escolher tamanho" quando variável. As abas de categoria seguem como filtro rápido. Regressão PDV A–C 21/21; builds verdes. | Balcão mais rápido de reconhecer o produto (foto + organização) |
| 2026-07-06 | **PDV épico — Fase 3 (cancelar/alterar cupom) + Fase 4 (formas de pagamento + dividir conta)** (migration 049). **F3:** `GET /vendas/cupons/busca?senha=` acha um cupom pela senha; componente `buscar-cupom.tsx` no PDV mostra itens/total/status e **cancela** (com estorno de estoque+caixa, que já existia). "Alterar" = cancelar + refazer (decisão: edita só enquanto aberto; balcão nasce fechado). `listarCupons` passou a expor `senha`. **F4 (migration 049):** tabela **`forma_pagamento`** (cadastro no Financeiro, com auto-seed de dinheiro/pix/crédito/débito na 1ª leitura) — CRUD (`/financeiro/formas-pagamento`) + card em `/financeiro`; PDV e cardápio **puxam essa lista** em vez das 3 formas fixas. Tabela **`comanda_pagamento`** + `vendaBalcao` aceita `pagamentos[]` (**dividir conta / multi-pagamento**): valida que a soma bate com o total, grava uma linha por forma e **um lançamento de caixa por forma** (fechamento "por forma" correto); `comanda.forma='multiplo'`. UI de "Dividir conta" no PDV (linhas forma×valor com restante ao vivo); TEF automático só no pagamento único por cartão/pix. e2e 12/12 + regressão PDV A–C 21/21. **Merge exige migration 049.** | Fecha o épico do PDV: cancelamento por senha + pagamento flexível |
| 2026-07-06 | **Fix — cancelar cupom não avisava o KDS** (sem migration). Ao cancelar uma venda, os pedidos de produção continuavam ativos na fila do KDS. Novo `ProducaoPedidoService.cancelarPorComanda` marca **todos** os `producao_pedido` da comanda como `cancelado` e emite `producao.evento` (tipo `cancelado`) por destino; `vendas.cancelar` passou a chamá-lo após o estorno. No **KDS**, o handler de `producao:atualizado` com `tipo:'cancelado'` **avisa a cozinha** (alerta vermelho "❌ CANCELADO — Senha/Mesa/Plataforma" + som) antes do pedido sair da fila. e2e cancel→KDS 6/6 + regressão PDV A–C 21/21. | Cozinha não prepara pedido cancelado |
| 2026-07-06 | **Revisão — Mesas (`/mesas`)** (sem migration, só frontend). Alinhado ao épico do PDV: o pagamento das mesas passou a **puxar as formas do cadastro** (`/financeiro/formas-pagamento`) em vez das 3 fixas (`fecharComanda`/`fecharMesa` aceitam a forma livre); o "Carregando…" da lista virou **skeleton**. *(Mesa em modo "comandas por cliente" já cobre pagar por pessoa; multi-forma numa mesma comanda fica como extensão futura do fechar.)* Build verde. | Consistência das formas de pagamento entre balcão e mesas |
| 2026-07-06 | **Revisão — Garçom (`/garcom`) + `SeletorProduto`** (sem migration, só frontend). O `SeletorProduto` (compartilhado por **garçom e mesas**) recebeu as melhorias da Fase 2 do PDV: **produtos agrupados por categoria** (cabeçalho + contagem), **card com foto** (`imagemRef`, fallback emoji) e **1-toque para produto simples** (sem abrir o modal quando não há variação/complemento). Lista de mesas do garçom: "Carregando…" → skeleton. Build verde. | Consistência de catálogo (fotos/categoria/1-toque) entre PDV, mesas e garçom |
| 2026-07-06 | **Reposicionamento — `/pedidos` vira "Painel de produção do gestor"** (sem migration). Deixou de duplicar o KDS (que é da cozinha) e virou supervisão gerencial: `filaPdv` enriquecida com **`setorNome`** (join `setor`) e **vínculo à comanda** (`cliente`, `total`, `mesa`, `status`). A tela passou a: **consolidar por setor** (todas as telas de produção num lugar), **resumo** (em produção / **atrasados** / prontos), **destaque de atraso/SLA** (decorrido × `tempoPreparoMin`, card vermelho quando estoura), **valor da venda** por pedido, itens e cancelamento (já existia). *(Nota: `producao_pedido.setorId` só vem preenchido quando o setor tem destino/KDS configurado; sem isso agrupa em "Sem setor" — melhoria de roteamento fica para depois.)* e2e painel 6/6 + regressão PDV A–C 21/21. | Supervisão da produção (multi-setor, atraso, vínculo com a venda) em vez de cópia do KDS |
| 2026-07-06 | **Delivery 2.0 — kanban configurável + caixa próprio** (migration 050). **(a) Correções:** cancelar delivery agora chama `producao.cancelarPorComanda` no `estornarVendaExterna` (a cozinha parava de ser avisada — mesmo bug do balcão); aceite automático que falha marca `auto_aceite_falhou` (some silencioso → destaque na coluna Chegada). **(b) Kanban:** 4 colunas (Chegada / Produção / Em rota / Finalizado) mapeadas a `status`; visibilidade por coluna em `delivery_config.colunas` (jsonb); **botão "⚙️ Configurar quadro" FORA do quadro** (barra superior) para reativar coluna oculta; **filtro por coluna em menu ancorado** (entregador/bairro/forma/nº) — escolha do usuário sobre o toast; card exibe os dados ricos já ingeridos (pago × na entrega, troco, taxa, bairro, agendamento, entregador); mover card = **botões de avançar** (Aceitar → Marcar pronto → Despachar → Concluir); despacho de entrega abre modal de **entregador** (`pedido_externo.entregador_id/nome`). **(c) Caixa do delivery separado do PDV** via `caixa_sessao.origem` ('pdv' ou 'delivery') — abrir/sangria/suprimento/fechar cego reusando `CaixaPanel`; **dinheiro na entrega** amarra o lançamento da venda à sessão do delivery ao concluir (fechamento confere a gaveta). `listar` passou a trazer finalizados das últimas 24h. e2e delivery 20/20 + regressão PDV 21/21. | Delivery vira central de entregas (padrão de mercado), com correções e controle de caixa próprio |
| 2026-07-06 | **Delivery — detalhe do pedido, entregadores e estoque na conclusão** (migration 051). **(a) Card clicável → painel de detalhe** com 3 ações: 🖨 **Reimprimir** vias · ✏️ **Alterar** · ✕ **Cancelar**. **(b) Cancelamento com trava:** confirma → motivo → **senha de login de um gestor** (`autorizarPorSenha`, bcrypt contra presidentes/gerentes; atendente pode cancelar informando a senha, auditado com quem autorizou). Vai para Finalizados/cancelado; no **KDS de entregas** o pedido fica **riscado** + "CANCELADA" (a fila de entrega passou a incluir cancelados recentes). **(c) Estoque baixa só na CONCLUSÃO** (entrega): `venderExterno` não baixa no aceite; `avancar→concluido` chama `baixarEstoqueExterno`; cancelado antes disso **não movimenta estoque**. **(d) Alterar itens** (enquanto não despachado): `alterarItensExterno` adiciona/remove itens na comanda 'fechada', recalcula total + ajusta o lançamento no financeiro, reemite ao KDS, marca `alterado`, **reimprime as vias** e põe **obs "ALTERADO"** nos pedidos do KDS. **(e) Entregadores** = colaboradores com função cujo nome contém "Entregador" (+ `colaborador.telefone`); o despacho abre a seleção (fallback **"Nenhum"**) e "em rota" mostra 🛵 nome + 📞 telefone (`pedido_externo.entregador_telefone`). **(f) "Retornar à produção"** desfaz um despacho errado (despachado→confirmado). **(g) Rename** coluna "Chegada"→"Novo"; filtro abre em "Nº do pedido". **Fix de segurança/contabilidade:** `sessaoAbertaId` (venda de balcão) ficou **origem-aware** ('pdv') — um caixa de delivery aberto não satisfaz nem recebe mais uma venda de balcão. e2e delivery 20/20 + rodada 2 16/16 + regressão PDV 21/21. | Central de entregas completa (padrão de mercado) + estoque/caixa corretos |
| 2026-07-06 | **Delivery — quadro estilo iFood Gestor** (migration 052; ref. mockup enviado). **(a) Filtro geral por tipo** na barra: Todos / 🛵 Delivery / 🏪 Retirada (filtra o quadro inteiro por `pedido_externo.tipo`). **(b) ➕ Novo pedido manual** (delivery ou retirada) — `criarManual` resolve **preço no servidor** a partir do cadastro e reaproveita a ingestão (canal 'manual'). **(c) ⏸ Pausar loja** por **30 min / 1 h / 12 h** + motivo (`pausadoAte`/`pausaMotivo`); **reativa sozinha** (`pausado` é computado) e mostra banner "pausada até HH:MM". **(d) Faixas de tempo editáveis** (`prep_balcao_min/max`, `prep_delivery_min/max`) num card no topo da coluna, que alimentam o **contador "Prepare em até HH:MM:SS"** do card (vermelho quando estoura). **(e) Card** ganhou **nº sequencial do dia** (`pedido_externo.numero`, o "#284"), **ícone 📍 de rota** (Google Maps), **botão NF** (emite NFC-e da comanda via `fiscal.emitir`), tipo (🛵/🏪) e total realçado. Coluna "Novo" renomeada para **"Em análise"** (decisão do usuário: manter as 4 colunas). e2e delivery r1 20/20 + r2 16/16 + r3 13/13 + regressão PDV 21/21. | Paridade visual/funcional com os líderes de mercado (iFood Gestor) |
| 2026-07-06 | **Delivery — alterar endereço (taxa por bairro) + refinos de card** (sem migration). **(a) "Alterar" agora edita também o endereço:** ao trocar o bairro, puxa a **taxa de entrega do cadastro de bairros** (`cardapio_bairro`, "área de atendimento") por `bairroId` ou nome e **recalcula o total** (subtotal itens + taxa − desconto); `alterar` aceita `endereco` e há `GET /delivery/bairros` para o editor escolher o bairro (que já mostra a taxa). *(Taxa por distância — geocoding — fica como follow-up.)* **(b) Nome do cliente** no card em **negrito e maior**. **(c) Chip da plataforma colorido pela marca:** iFood vermelho `#EA1D2C`, WhatsApp `#25D366`, Totem, Uber/Rappi/99, e **Cardápio na cor da marca Regem** (dourado); demais canais caem no neutro. e2e delivery r4 9/9 (+ r1/r2/r3 e PDV verdes). | Correção de pedido com recálculo de frete + leitura mais rápida do card |
| 2026-07-06 | **Delivery — refino visual do quadro** (só frontend). Títulos das colunas maiores (`text-base`, negrito) com **contador em pílula** na cor da coluna; cada coluna ganhou **fundo suave** na própria cor (`hsl(var(--cor) / 0.05)`) e cabeçalho tonalizado (`/0.14`) — os cards brancos seguem legíveis por cima. O **caixa do delivery** (turno/sangria/suprimento) foi **integrado à barra superior** (novo prop `embedded` no `CaixaPanel`, sem borda/fundo próprios) e a barra foi **afinada** (`px-3 py-2`) para dar destaque ao quadro. Build verde. | Hierarquia visual mais clara e mais espaço para os pedidos |
| 2026-07-06 | **Delivery — fix de hidratação + botões afinados** (só frontend). A categoria (`getCategoria()`, lê `localStorage`) passou a ser lida em `useEffect` (estado `cat` inicia null) para o 1º render do cliente bater com o do servidor — corrige "Hydration failed… matching `<span>`" que vinha do `CaixaPanel` renderizado condicionalmente. Botões **Novo pedido / Pausar / ⚙️ / Simular** reduzidos para `h-8` (Button `sm` era `h-11`), na altura do filtro de tipo. Build verde. | Erro de hidratação visível + barra mais compacta |
| 2026-07-07 | **Delivery — painel de configurações (menu lateral), 1ª leva** (migration 053). O ⚙️ do quadro abre um **painel com menu lateral** (`ConfigPanel`) no lugar do modal simples de colunas. Seções entregues: **Quadro** (colunas), e o grupo **Cardápio digital** — **Loja** (nome, **logo por imagem** via `ImageUpload`, CPF/CNPJ, responsável+contato, contato da loja, WhatsApp, Instagram, site, pedido mínimo), **Endereço** (CEP/rua/nº/bairro/cidade/UF/ref/compl), **Horários** (por dia da semana), **Tipos de pedido** (Delivery/Retirar/Consumir no local — 3 flags independentes) e **Área de atendimento** (bairro+taxa com **liga/desliga**; `delivery/bairros` passou a listar só ativos). Reusa `cardapio_config` (novos campos) + `cardapio_bairro.ativo`. Abas **Banners/Impressoras/Integrações/Robô** aparecem no menu marcadas "em breve" (2ª leva). **Bloqueios registrados:** detecção de impressoras do SO exige o edge; integrações reais precisam de credenciais+edge; robô com prompt exige LLM. e2e config 10/10 + delivery r4 9/9 + PDV 21/21. | Centralizar as configurações do delivery/cardápio num só lugar, estilo mercado |
| 2026-07-07 | **Config — área por raio (ou bairro) + Banners** (migration 054, 2ª leva parte 1). **Área de atendimento** ganhou modo **exclusivo** `cardapio_config.area_modo` ('bairro' ou 'raio'): por **raio** usa faixas `raios` (jsonb `[{ateKm,taxa}]`, ordenadas, faixa 0km descartada) a partir do endereço da loja; por **bairro** segue a lista existente. **Registrado:** o cálculo da distância no checkout precisa de **geocoding** (colunas `end_lat/end_lng` já criadas para quando a integração de mapas entrar) — a config já persiste. **Banners** (tabela nova `banner`): upload de imagem + título/link/ordem (↑↓)/ativo, CRUD replace-all em `/cardapio/banners`. No painel, as abas Banners e Área saíram do "em breve"; restam **Impressoras/Integrações/Robô**. e2e config r2 10/10 + r1 10/10. | Frete por raio (padrão de mercado) + banners do cardápio |
| 2026-07-07 | **Config — Impressoras (cadastro manual)** (migration 055, 2ª leva parte 2). Seção Impressoras no painel: cadastra impressora reusando `equipamento` (tipo `impressora`) com **direcionamento** — Caixa (`papel='cupom'`) ou Cozinha (`papel='producao'`, opcional por **setor**), **IP:porta** e **nº de vias** (`equipamento.vias`, migration 055). Endpoints dedicados `GET/PUT/DELETE /equipamento/impressoras` (upsert individual, não replace-all, para não quebrar tokens de KDS). **Detecção automática das impressoras do SO fica para o edge** (aviso na tela). Menu: sobra só Integrações/Robô no "em breve". e2e impressoras 6/6. | Direcionar impressão caixa/cozinha por setor + vias, sem depender do edge |
| 2026-07-07 | **Config — Integrações (credenciais)** (migration 056, 2ª leva parte 3). Tabela `integracao` (por tenant+canal: iFood/Uber Eats/Rappi/99Food) com merchant/client id + **client_secret/token (secrets)**. Seção no painel: por canal, ativo + credenciais. **Segurança:** o **GET nunca retorna os secrets** (só flags `temSecret`/`temToken`); o **PUT preserva** o secret quando o campo vem vazio (só troca com valor novo). Ativação real (receber pedidos) roda no **edge** com essas credenciais — aviso na tela. Endpoints `GET/PUT /delivery/integracoes` (presidente/gerente). No menu, sobra só **Robô** no "em breve". e2e integrações 8/8 (inclui não-vazamento de secret). | Cadastro seguro de credenciais dos apps externos |
| 2026-07-07 | **Config — Robô de atendimento (mensagens) + fim da 2ª leva** (migration 057). Seção Robô no painel: **ativo**, **saudação**, **mensagem de ausência**, **respostas prontas** (gatilho→resposta) e um campo de **base de conhecimento** já persistido (`robo_prompt`) para o futuro LLM. Campos em `cardapio_config` (`robo_ativo/saudacao/ausencia/prompt/mensagens`). **O "cérebro" com IA (respostas livres) fica para etapa dedicada** (precisa de provedor de IA/custo — aviso na tela); as mensagens já funcionam sem IA. **Com isso o painel de configurações está completo** (Quadro · Cardápio digital: Loja/Endereço/Horários/Tipos/Área[bairro ou raio] · Banners · Impressoras · Integrações · Robô). e2e robô 7/7 + config r1 10/10 + r2 10/10. | Fecha o módulo de configurações do delivery/cardápio |
| 2026-07-07 | **Integração WhatsApp/n8n — webhook de status + contexto pro robô** (sem migration; reusa `integracao` e `pedido_externo.cliente_telefone`). **(a) Webhook de status:** a cada mudança de status do delivery (aceito/pronto/em rota/concluído/cancelado), o Regem faz `POST` na URL do n8n (`dispararWebhook`, fire-and-forget) com `{evento,pedidoId,numero,status,tipo,cliente,telefone,total,entregador}` e **assinatura HMAC-SHA256** no header `X-Regem-Signature`. Config no painel ⚙️ Integrações → card **"WhatsApp / n8n"** (URL no `merchantId`, segredo no `clientSecret` mascarado; canal `n8n`). **(b) Menu público** (`GET /publico/cardapio/{token}`) passou a expor **robô** (saudação/ausência/mensagens/prompt), **horários** e **tipos** — o agente lê tudo com o token. **(c) "Cadê meu pedido?":** `GET /publico/cardapio/{token}/pedidos?telefone=` casa pelos **últimos 8 dígitos** (ignora formatação/DDI) e devolve pedidos recentes com status. Diretriz do fluxo: robô manda o **link do cardápio** no principal; pedido por texto é plano B (via `/publico/cardapio/{token}/pedido`). e2e webhook 13/13 (inclui disparo real + HMAC conferido por servidor local) + integrações 8/8 + delivery 20/20. | Liga o robô de WhatsApp (n8n do usuário) ao Regem: status proativo + contexto + status por telefone |
| 2026-07-07 | **Correções do fluxo cardápio/robô (pós-teste)** (sem migration). **(a) Endereço da loja** agora vem no menu público (`loja.endereco{cep,rua,…,texto}`) — o robô responde "onde fica". **(b) Horário de funcionamento é validado:** `estaAberta(cfg)` (toggle `aberto` + `horarios` por dia no fuso SP, trata virada de meia-noite); o menu expõe `abertaAgora` e o `pedidoPublico` **bloqueia** (400) fora do horário (pedido agendado passa). **(c) Formas de pagamento do cardápio** ganharam UI no painel (Loja → `pagamentos`: pix/cartão/na entrega) — sem isso o checkout não mostrava pagamento. **(d) Storefront respeita os tipos** (só mostra Entrega/Retirada habilitados; 1 tipo vira rótulo fixo) e o badge ABERTO/FECHADO usa `abertaAgora`; checkout **bloqueado com a loja fechada**. **(e) Prefill por link:** `/c/{token}?nome=…&tel=…` preenche nome/telefone — é o que faz o pedido nascer com o **mesmo telefone do WhatsApp**, destravando o "cadê meu pedido" e o **webhook** (que precisam do telefone). **(f) Status** agora traz `statusTexto` + `estimativaMin` (tempo de entrega) pro robô. e2e loja 9/9 + webhook 13/13 + config 10/10 + delivery 9/9. | Fechar as pontas do pedido via WhatsApp/cardápio que apareceram no teste real |
| 2026-07-07 | **Fix — link de delivery abria envio-direto (modo mesa)** (sem migration, só frontend). O storefront decidia o fluxo por `menu.modo === 'mesa'`: uma loja em **modo Mesa (QR na mesa)** fazia o link de delivery **enviar direto**, sem checkout (tipo/pagamento/endereço) — parecia "venda de balcão". Corrigido: o envio-direto só ocorre quando o link é de **mesa de verdade** (`?mesa=` presente); o link de delivery **sempre abre o checkout**, independente do `modo` da loja. O QR de mesa (`/c/{token}?mesa=5`) segue com envio direto. | Delivery e QR-de-mesa convivem na mesma loja sem a config de `modo` atrapalhar |
| 2026-07-07 | **Handoff automático (robô → humano): chamados de atendimento** (migration 058). Novo módulo `atendimento` + tabela `atendimento_chamado`. O robô (n8n) abre um chamado por **endpoint público** `POST /publico/cardapio/{token}/atendimento` (`{tipo: mudanca\|erro\|humano, cliente, telefone, pedidoNumero, mensagem}`) quando o cliente quer **mudar o pedido**, **relatar erro no recebimento** ou **falar com atendente**. O Regem grava e **dispara alerta em tempo real** (`atendimento.novo` → gateway → socket `atendimento:novo`, room do tenant). No app (tela de Delivery) há um **🔔 sino com contador** + painel **"Atendimento"** listando os chamados abertos com **"Responder no WhatsApp"** (wa.me com o +55) e **"Resolver"** (`GET /atendimento`, `POST /atendimento/{id}/resolver`, gestor/atendente). *(Status do robô: passou a vir por um nó de fetch dos pedidos do cliente no contexto — mais robusto que a tool que dependia do LLM preencher o telefone.)* e2e atendimento 6/6 + loja 9/9 + webhook 13/13. | Cliente que precisa de gente cai numa fila visível e rastreável, não some no WhatsApp |
| 2026-07-08 | **Escala — Fase 1: modelo & cores** (migration 059). Fundação da reforma da Escala. **(a) `setor.cor`** (editável por presidente/gerente via `PATCH /setores/:id`) — cor própria do setor. **(b) N:N `colaborador_funcao`** (backfill do `colaborador.funcao_id` atual como principal): um operador cobre **várias funções**; `POST /colaboradores` aceita `funcaoIds[]` (só funções do tenant; principal = 1ª válida) e `GET /colaboradores` devolve `funcaoIds`. **(c) Cor da vaga 100% pela hierarquia** (`funcao.categoria`): paleta fixa de 4 níveis (Presidente/C&O · Gerente · Supervisão · Execução) em `frontend/src/lib/hierarquia.ts` (sem override por etiqueta); `GET /etiquetas` e a escala passam a retornar `categoria` + `setorCor`. **(d) Cadastros:** `EntityForm` ganhou tipos `color` e `multiselect` (cor no setor; multi-função no colaborador). Bug de fuso corrigido: `hoje()` da escala usava UTC → agora `America/Sao_Paulo`. Smoke 11/11 no `regem_local`; builds verdes. **Merge exige migration 059 no Supabase.** *(Fase 2: grade colorida + remover/editar alocação; Fase 3: gerar etiqueta na função; Fase 4: visões dia/mensal + regras CLT.)* | Menu crítico: modelo alinhado (N:N, hierarquia, cor de setor) antes das telas |
| 2026-07-08 | **Escala — Fase 2: grade por hierarquia + remover/editar alocação** (sem migration). **(a) Cores:** a vaga na grade agora é colorida **pela hierarquia da função** (`corHierarquia(categoria)` de `lib/hierarquia.ts`) — dot + faixa lateral; cabeçalho de cada setor usa a **`setor.cor`**; **legenda** dos 4 níveis no topo. Grupos passam a derivar de `etiqueta.setorNome/setorCor` (dropou a chamada a `/setores`). **(b) Remover/editar (lacuna que não existia):** `DELETE /escala/:id` (soft-delete `deleted_at`) e `PATCH /escala/:id` (trocar responsável/reabrir vaga/tipo, com checagem de conflito de colaborador no mesmo turno/dia) — ambos **auditados** (`removeu_alocacao`/`alterou_alocacao`). No front, clicar na célula lista as alocações existentes com **select de responsável** (troca 1-clique) + **lixeira** (remover), acima do form de adicionar. **(c) Responsável filtrado:** o form de alocação só lista colaboradores que **cobrem a função da vaga** (usa o N:N da Fase 1); fallback "mostra todos" com aviso se ninguém tem a função. Smoke 8/8 no `regem_local`; builds verdes. | Escala legível por nível + finalmente dá pra corrigir/trocar/remover alocação |
| 2026-07-08 | **Escala — Fase 3: gerar etiqueta ao criar função** (sem migration). No cadastro de **Função**, `POST /funcoes` aceita `gerarEtiqueta` (bool) + `sigla?`: se ligado e a função tem **setor**, cria automaticamente a **etiqueta (vaga)** vinculada — herda `setorId`/`unidadeId` do setor, sigla = informada ou **abreviada do nome** (`gerarSigla`: sem acento, só letras, 4 chars — "Aux. Cozinha" → `AUXC`), e **contador** = próximo livre para aquela sigla na unidade (evita colisão do índice único). Sem setor, cria só a função. Cadastro de função ganhou "Gerar etiqueta?" (Sim/Não) + "Sigla (opcional)". Smoke 8/8 no `regem_local`; builds verdes. | Menos atrito: a vaga nasce junto da função, pronta pra escala |
| 2026-07-08 | **Escala — Fase 4A: tipos de jornada + regras CLT** (migration 060). Base da conformidade trabalhista na montagem da escala. **Modelo:** `colaborador.jornada_tipo` (5x2/12x36/4x3/horista/outro — mescláveis) e `turno.pausa_inicio/pausa_fim` (intervalo intrajornada). **Regras puras testadas** (`common/regras-escala.ts`, 14 testes): intervalo intrajornada por jornada bruta (art. 71: >6h→1h, 4–6h→15min), **interjornada 11h** (art. 66) entre dias adjacentes, folgas semanais por tipo (5x2→aviso >5 dias; 4x3→aviso >4). **Aplicação** (decisão do usuário: *avisar + bloquear ilegais*): `criar`/`alterar` alocação **bloqueiam** (400) o ilegal — jornada >6h **sem** intervalo e interjornada <11h — e retornam os demais como **avisos** (toast ⚠️ no front, não travam). Cadastros: tipo de jornada no colaborador + pausa no turno. Smoke 7/7 + jest 66/66; builds verdes. **Merge exige migration 060 no Supabase.** *(Fase 4B: dias importantes; 4C: visões dia/timeline/mensal; 4D: avisos no modal.)* | Escala em conformidade com a CLT já na montagem (sem gerar erro trabalhista) |
| 2026-07-08 | **Escala — Fase 4B: dias importantes** (migration 061). Nova tabela `dia_especial` (tenant, unidade opcional=rede, colaborador opcional=férias/folga de 1 pessoa, `data`, `data_fim` opcional=período, `tipo` feriado/ferias/evento/folga/outro, nome, descrição). Módulo `dia-especial`: `GET /dias-especiais?de=&ate=` (sobreposição por `coalesce(data_fim,data)`), `POST`/`DELETE` (soft) — listar = qualquer autenticado; criar/remover = presidente/gerente/supervisão. Cadastros ganhou seção **Dias importantes**. A **grade da escala** marca os dias no cabeçalho (emoji por tipo 🎉🏖️📅😴 + nome no title), desktop e mobile. Smoke 6/6; builds verdes. **Merge exige migration 061 no Supabase.** *(Fase 4C: visões dia/timeline/mensal — última da Escala.)* | Cadastrar feriados/férias/eventos e vê-los na escala |
| 2026-07-08 | **Escala — Fase 4C: visões Dia (timeline) e Mês** (sem migration; fecha a reforma da Escala). Alternador **Semana · Dia · Mês** no topo. **Backend:** `enriched` da escala passou a expor horas do turno (`turnoInicio/turnoFim/pausaInicio/pausaFim`) e novo `GET /escala/periodo?de=&ate=` (alocações enriquecidas num intervalo). **Dia** (`TimelineDia`): cada colaborador escalado vira uma **barra no eixo de horas** agrupada por setor, colorida pela hierarquia, com a **pausa marcada** (faixa hachurada) — o gestor vê os intervalos e organiza a cobertura; nav por dia + selos de dias especiais. **Mês** (`GradeMensal`): tabela colaboradores × dias do mês, célula = sigla da vaga (colorida) ou vazia (folga), cabeçalho marca dias especiais; nav por mês. Builds verdes. | Fecha a Escala: além da grade semanal, timeline do dia (com pausas) e panorama mensal |
| 2026-07-08 | **Revisão — Operação (`/operacao`)** (sem migration, só frontend). Hub operacional (Estoque · Recebimento · Validades FEFO · Desperdício · Vistorias). **2 bugs corrigidos:** (a) o estado de carregamento renderizava um "Carregando…" **fora do Shell** → sidebar/topbar sumiam durante a carga; agora usa `Shell` + `SkeletonList` (consistente com as demais telas). (b) `hoje` (default das datas de movimento/desperdício/vistoria/recebimento) usava `toISOString()` (**UTC**) → à noite no Brasil o campo já vinha com a data de amanhã; agora `America/Sao_Paulo` (na página e no `RecebimentoForm`). Sem erros de sintaxe (build verde). *(Observação: a tela usa cores cruas do Tailwind nos badges em vez dos tokens semânticos — divergência cosmética, não corrigida.)* | Não perder a navegação na carga + data correta no fuso certo |
| 2026-07-08 | **Operação — badges nos tokens** (sem migration, só frontend). Follow-up da revisão: os badges que usavam cores cruas do Tailwind (`bg-red-100`/`emerald-100`/`amber-100`/`slate-100`) passaram para os **tokens semânticos** — `bg-destructive/10 text-destructive`, `bg-ok/10 text-ok`, `bg-warn/10 text-warn`, `bg-secondary text-muted-foreground`. Alinha ao design system. Build verde. | Consistência de cor com o resto do app |
| 2026-07-08 | **Estoque — E1: renomear + hub + insumo rico** (migration 062). O menu **"Operação" virou "Estoque"** e a tela (antes tudo empilhado) virou um **hub por seções** (estilo Cadastros): 📦 Insumos · 🧮 Contagem · 🛒 Compras · 🚚 Recebimento · 🗓️ Validades · ♻️ Desperdício · ✔️ Vistorias, + atalho **Inteligência** (`/estoque`). **Cadastro rico de insumo** (`InsumoForm`): categoria como **cadastro próprio** (`categoria_item` + CRUD, criar inline), **fornecedor** (select + criar inline), **unidade tipada** (un/caixa/kg/L/fardo/fita… + custom), estoque mínimo e **conversões personalizadas** (`item_conversao`: 1 fardo = 400 un). `item_estoque` ganhou `fornecedor_id` + `categoria_item_id`; `POST/PATCH /estoque/itens` gravam tudo (conversões replace-all); `listItens` devolve categoria (nome+cor), fornecedor e conversões. Contagem/Compras entram como "em breve" (E2/E3). Badges já nos tokens. Smoke 9/9; builds verdes. **Merge exige migration 062 no Supabase.** *(E2 Contagem · E3 Compras · E4 Painel do estoque.)* | Estoque deixa de ser "um cadastro só jogado junto" e ganha estrutura + insumo completo |
| 2026-07-08 | **Estoque — E2: Contagem** (migration 063). Seção **Contagem** do hub: **listas personalizadas** (`contagem_lista` + `_item`): nome, produtos, **recorrência** (diária/semanal/mensal/avulsa), **horário de alerta**, **delegar** a colaborador, flags **avisar KDS / dashboard**. **Executar contagem** (`contagem_execucao` + `contagem_item`): ao "Contar", faz **snapshot do saldo do sistema** por item; o operador informa o **contado**; ao salvar com "aplicar ajuste", lança um **movimento `ajuste`** da diferença (reconcilia o estoque). `listListas` traz nº de produtos, última contagem e `pendenteHoje` (recorrência × dia no fuso SP). **Alerta por horário:** `@Cron` horário emite `kds.alerta.sistema` (→ KDS) para as listas que vencem naquela hora/dia (com o responsável no texto). Módulo `contagem` (listar/iniciar/salvar = autenticado; criar/remover = gestão). Smoke 8/8; builds verdes. **Merge exige migration 063 no Supabase.** *(E3 Compras · E4 Painel.)* | Contagem cíclica com delegação, alerta e ajuste automático do estoque |
| 2026-07-08 | **Estoque — E3: Compras** (migration 064). Seção **Compras** do hub: **gerar lista** (`compra_lista` + `compra_item`) — nome, **fornecedor**, **data de recebimento**, **delegar** o recebimento, flags KDS/dashboard; seleção de produtos com **filtro** (busca) + **quantidade** e custo/un por item; botão **"sugerir do estoque baixo"** (`GET /compras/sugestao` = itens abaixo do mínimo, qtd = mínimo − saldo). **Receber** (`POST /compras/listas/:id/receber`): lança **movimento `entrada`** por item (com custo), **atualiza o custo médio ponderado** (`custoMedioPonderado`), marca `recebida` e avisa KDS. Módulo `compras` (listar/receber = autenticado; criar/remover = gestão). Smoke 9/9; builds verdes. **Merge exige migration 064 no Supabase.** *(Falta E4 — Painel do estoque.)* | Fecha o ciclo compra→recebimento→estoque com sugestão e custo médio |
| 2026-07-08 | **Estoque — E4: Painel** (sem migration; fecha o épico). Nova 1ª aba **📊 Painel** do hub: KPIs (**valor em estoque**, nº de insumos, **abaixo do mínimo**, **zerados**), card **CMV do mês** (real × teórico → desvio, via `/estoque/cmv`) e **valor por categoria** (barras coloridas pela cor da categoria, ordenado). Tudo derivado do `listItens` já carregado + `/estoque/cmv` — sem endpoint novo. **Épico do Estoque completo:** E1 hub+insumo rico · E2 Contagem · E3 Compras · E4 Painel. Build verde. | Visão gerencial do estoque num relance, fechando a reforma |
| 2026-07-08 | **Revisão — Fichas Técnicas: ingrediente ligado ao Estoque** (sem migration; só frontend). **Gap corrigido:** o form de ficha só mandava `insumoNome` (texto livre) + custo digitado à mão — nunca o `item_id` — então **produzir a ficha NÃO baixava o estoque** dos insumos (a explosão em `producao.service` só baixa quando o ingrediente tem `item_id`) e o custo ficava manual/estático. Agora o seletor de ingrediente tem 3 opções: **Insumo avulso** (texto), **Insumo do estoque** (optgroup com os itens cadastrados → envia `itemId`, custo automático pelo **custo médio**, unidade travada) e **Sub-receita** (como já era). Fecha o ciclo **ficha → produção → baixa de estoque → CMV real** e conecta o épico do Estoque às Fichas. Smoke da cadeia 4/4 (produzir 3× baixou o insumo 10→4); build verde. | Sem o `item_id`, produzir não consumia o estoque nem custava certo |
| 2026-07-08 | **Fichas — meta de CMV editável + editar ficha + skeleton** (sem migration). (1) **Meta de CMV por ficha** deixou de ser fixa (31,5%): campo editável no card de custo, usada no CMV/preço sugerido e enviada no salvar (`meta_cmv` já existia no schema). (2) **Editar ficha:** botão ✏️ no card carrega a ficha no form (campos + ingredientes) e salva via `PATCH /fichas/:id`; o `update` do backend passou a **substituir os ingredientes** quando enviados (replace-all, com validação de **ciclo** de sub-receita via `validarSubFicha`) — `UpdateFichaDto` ganhou `ingredientes[]`. (3) **Skeleton** na listagem enquanto carrega (`SkeletonList`), no lugar do flash "Nenhuma ficha". Smoke edit 6/6 (meta persiste, replace de ingredientes, ciclo bloqueado); builds verdes. | Ajustar receita sem reccriar + meta de margem por prato |
| 2026-07-08 | **Fichas dentro do Estoque** (sem migration). Organização: o item **"Fichas Técnicas" saiu da sidebar** e passou a ser acessado **pelo hub de Estoque** (botão "🧾 Fichas técnicas" nas ações, ao lado de "Inteligência"). `/fichas` ganhou eyebrow "Estoque · produção" + botão **← Estoque** pra voltar ao hub. Faz sentido: ficha consome insumo do estoque (baixa na produção). | Agrupar o que é do mesmo domínio (insumo→ficha→produção) |
| 2026-07-14 | **Segurança/robustez do cardápio — Etapa 1: pedido idempotente + upload seguro** (migration 086). **(a) Idempotência:** `pedido_externo` ganhou `client_ref` + índice único parcial `(tenant_id, client_ref)`. O front do cardápio (`c/[token]`) gera um UUID quando o carrinho é montado e reenvia o **mesmo** `clientRef` em qualquer retry; `cardapio.service.receberPedido` devolve o pedido existente (200, `reenvio:true`) sem recriar/recobrar, e `delivery.ingest` grava o ref + trata corrida (violação 23505 → devolve o existente). Teste `cardapio.idempotencia.spec` prova 2 chamadas c/ mesmo ref = 1 pedido. **(b) Upload:** `FileInterceptor` com `limits.fileSize = 8 MB` (corta no Multer) + validação de **magic bytes** (JPEG/PNG/WebP/GIF) no `midia.service` — usa o tipo **detectado** (não o mimetype do cliente, forjável) para extensão/Content-Type; rejeição amigável. Builds verdes; testes 2/2. **Merge exige migration 086.** | Retry/duplo-toque não gera pedido duplicado; upload não aceita arquivo forjado ou gigante |
| 2026-07-14 | **Segurança do cardápio — Etapa 2: OTP protegido** (migration 087). O código de verificação do telefone (WhatsApp) deixou de ser gerado com `Math.random` e de ser gravado em **texto puro**. Agora: geração com `crypto.randomInt(100000, 1000000)` (uniforme, imprevisível); o banco (`cliente_otp`) guarda **apenas o hash SHA-256** (`codigo_hash`; `codigo` virou nullable e não é mais escrito); a confirmação compara **hash com hash**. O código em claro existe **só no payload do webhook de envio** (n8n/Evolution). Expiração (5 min) e limite de **5 tentativas** mantidos. Builds verdes. **Merge exige migration 087.** | Vazamento do banco não expõe códigos OTP; código forte e não adivinhável |
| 2026-07-14 | **Robustez do cardápio — Etapa 3: dinheiro em centavos** (sem migration). Novo `src/util/dinheiro.ts` (`paraCentavos`, `paraReais`, `somarCentavos`, `percentualCentavos`, `formatarReais`): toda conta interna roda em **centavos inteiros** (sem drift de float), convertendo para reais só na fronteira (resposta/coluna numeric). Migrados: o **total do pedido público** (`cardapio.receberPedido` — subtotal dos itens, cupom+prêmio+cashback e total final agora em centavos) e o **fechamento de caixa** (`financeiro.fecharSessao` — esperado/informado/diferença em centavos). 3 testes de arredondamento (`dinheiro.spec`): 3× R$ 9,99 −10% = R$ 26,97; taxa 6,90 + cashback parcial; 7 itens com promoção — todos batem ao centavo. Builds verdes; testes 5/5. | Elimina centavos "fantasma" de float em pedido e caixa |
| 2026-07-14 | **Financeiro — Etapa 4: fechamento de caixa cego por forma** (migration 088). No fechamento, o operador informa o **contado por forma** (dinheiro, cartão, Pix) **sem ver o esperado**; só após confirmar o sistema revela o comparativo **contado × esperado × diferença por forma** + total, gravando tudo (`caixa_sessao.valores_informados / esperado_por_forma / diferenca_por_forma`). Sangrias/suprimentos entram no esperado (já são lançamentos). **Diferença acima de um limite** (config por unidade `limite_diferenca_caixa`, padrão **R$ 5,00**) gera **ocorrência automática** (tipo de sistema "Diferença de caixa", get-or-create) + auditoria. Novo **relatório de fechamentos** (`GET /financeiro/caixa/fechamentos`, gerente/presidente) na rota `/caixa/fechamentos` com `ResponsiveTable` (data/turno/operador/esperado/contado/diferença, tooltip da diferença por forma) + item na sidebar. `fecharSessao` aceita `valoresInformados` (compat com `valorInformado`). Builds verdes. **Merge exige migration 088.** | Conferência honesta (cega) e rastreável; desvio de caixa vira ocorrência automática |
| 2026-07-14 | **Cardápio — Etapa 5: UX no nível dos líderes** (mig 089 tema + 090 sugestão). **Skeleton** no formato do conteúdo (hero+categorias+cards) no lugar de "Carregando…". **"Peça também"** com motor: sugestões **vinculadas ao produto** (cadastro, prioridade) → senão **mais pedidos** (`comanda_item`), excluindo carrinho/indisponíveis (`produto_sugestao` + `GET /peca-tambem`); consumido na seção que já existia no `cart-sheet`; cadastro no form do produto (chips+select). **Modo escuro** configurável: `cardapio_config.tema` (claro/escuro/`auto`) + toggle do cliente (localStorage por token) + `auto` segue `prefers-color-scheme`; CSS escopado `.tema-escuro` no `<body>` (sobrepõe as classes claras sem tocar no modo claro; âmbar preservado). *(Barra de frete grátis e categorias sticky já existiam no `cart-sheet`; página já era componentizada em `@/components/loja/*`.)* Builds verdes. **Merge exige migrations 089 + 090.** | Cardápio mais rápido de ler, sugestão inteligente e tema à escolha da loja/cliente |
| 2026-07-14 | **Cardápio — Etapa 6: acompanhamento + alerta de pedido novo** (sem migration). **(a)** Página de status **standalone** e reabrível `c/[token]/pedido/[id]?ref=` com timeline (Recebido → Em preparo → Pronto/Saiu → Entregue) e atualização por **polling leve** (8s, para ao finalizar). O backend `statusPedido` valida o `ref` contra `client_ref` (não vaza pedido de outro trocando o ID). O cardápio mostra o **link "Acompanhar pedido"** ao finalizar. **(b)** **Alerta sonoro** de pedido novo no painel de delivery: beep (Web Audio, sem asset) + **destaque visual** (pulse) quando a contagem de "novo" sobe; botão **🔔 liga/desliga** persistido (localStorage) com aviso "clique para ativar o som" (o navegador exige o gesto). Builds verdes. **Fecha a rodada de segurança/robustez do cardápio (6 etapas).** | Cliente acompanha sem refazer o carrinho; loja não perde pedido que chega |
| 2026-07-14 | **Impressão automática na cozinha — Etapa 1 (ESC/POS via edge)** (migration 092). Constrói **sobre** a infra existente (`impressao_job`, impressoras em `equipamento`, geração de vias em `producao-pedido`), sem remover nada. **(a) Impressora mais rica:** `equipamento` ganhou `largura` (58/80 mm) e `setores_atendidos` (jsonb) — uma impressora pode servir vários setores. **(b) Roteamento upgrade** (`producao-pedido.criarPedidos`): além do roteamento por produto/setor→equipamento que já existia, o item também vai para impressoras cujos `setores_atendidos` batem com o setor do produto; item **sem setor** cai na **impressora padrão** da unidade (`padrao`); dedupe por (impressora,item). **(c) Worker do edge** (`edge/impressao-daemon.mjs` + `edge/escpos.mjs`, serviço `RegemEdgeImpressao`): estratégia **LISTEN/NOTIFY do Postgres (imediato) + poll de 3s (rede de segurança)** — supera long-poll (sem conexão presa em proxy) e socket.io (sem dependência nova); envia por **TCP RAW/9100** com **retry/backoff (3 tentativas)**; converte o texto do ticket em ESC/POS (título/senha em **fonte dupla+negrito+centro**, OBS em **negrito**, complementos indentados, **corte + bipe**, acentos transliterados p/ ASCII, largura 32/48 col). Gatilho `trg_impressao_nova` na mig 092. **(d) Painel** (`/equipamentos`): impressora com largura + setores atendidos + "impressora padrão"; botão **"imprimir teste"** por impressora (`POST /impressao/impressoras/:id/teste`); **fila de impressão** com status e **reimpressão** de falhas (`GET /impressao/fila`). **(e) Teste:** `npm run impressora:fake` (impressora TCP falsa que decodifica o ticket) — pipeline validado ao vivo (ESC/POS + TCP + corte/bipe). Builds verdes. **Merge exige migration 092.** *(Pendências: propagação do daemon no auto-update fica pro instalador completo — como o sync-daemon hoje; teste de roteamento é manual via 2 fake printers, pois o CI não tem DB.)* | Pedido confirmado sai sozinho na impressora certa da cozinha/bar, com layout legível e reimpressão |
| 2026-07-14 | **Mapa de calor de entregas por bairro — Etapa 3** (sem migration). Novo `GET /delivery/mapa-calor?dias=7\|30\|90` (**presidente/gerente**, RBAC no servidor) que agrega `pedido_externo` de **entrega não cancelada** no período (**todos os canais**) por `endereco_bairro` (nulo → "Sem bairro"): nº de pedidos, receita (Σ total), **ticket médio**, **taxa média** (avg frete) e **% de participação**; devolve também os totais gerais. Nova tela `/relatorios/delivery` (**Mapa de calor de entregas**): seletor de período (7/30/90d), **cartões de insight** (bairro campeão, pedidos, ticket médio, taxa média) e `ResponsiveTable` com **barra de calor CSS** por linha (intensidade pela participação em pedidos, na cor âmbar da marca). Atalho "🗺️ Mapa de calor por bairro" na aba Delivery de `/relatorios` (complementa a lista "Por região" simples que já existia). **Sem lat/lng no pedido** → visão por bairro (o modo raio/scatter por coordenada fica como evolução futura, pois `pedido_externo` não guarda coordenada). Builds verdes. **Fecha a rodada de 3 etapas (impressão ESC/POS · cupom 1ª compra já existente · mapa de calor).** | Ver de quais bairros vêm os pedidos, com receita/ticket/taxa — decisão de raio de entrega e marketing local |
| 2026-07-14 | **Atualização do edge: botão manual + verificação por horário de abertura + auto-update propaga `edge/`** (sem migration). **(A)** `atualizar.ps1` agora faz **cópia overlay** dos arquivos de `edge/` (daemons `.mjs` + `.ps1`) do pacote — arquivo por arquivo, sem apagar a pasta — e um bloco **"garantir serviços"** que registra `RegemEdgeImpressao` se faltar; resolve a pendência (a impressão automática passa a chegar por auto-update, não só na reinstalação). Também **atualiza `APP_VERSION` no `.env.local`** ao concluir (e reverte no rollback) — evita rebaixar o mesmo pacote em loop. **(B)** A **verificação** deixou de ser "1×/hora" e passou a rodar só nas **janelas de abertura da loja** (nos 10 primeiros min após abrir e ~30 min depois), lendo `cardapio_config.horarios` do banco local — **fallback 04:00** se não houver horário; marca a janela do dia em `sync_state`. `cardapio_config` entrou no sync `desce` (config da loja disponível offline). A tarefa `RegemEdgeUpdate` **perdeu o gatilho diário 04:00** (não aplica mais sozinha) e virou **sob demanda**. **(C)** No app (`/servidor`, só quando `NEXT_PUBLIC_EDGE=1`, gestão): card **Atualização do servidor** com **Verificar atualização** (força o update-check ao vivo na nuvem) e **Instalar atualização** (dispara `schtasks /run /tn RegemEdgeUpdate` — a tarefa SYSTEM aplica com backup+rollback). Endpoints `GET/POST /edge/atualizacao/{status,verificar,aplicar}` (gate `EDGE_MODE=true` + presidente/gerente). Builds verdes; PS parseado (PS1 em ASCII — `→`/`—` viravam aspas tipográficas sem BOM). | Loja atualiza quando quer (é avisada ao abrir, instala com 1 clique) e a impressão automática passa a chegar pelo update |
| 2026-07-14 | **Sistema híbrido Nuvem/Local — consciência de modo, escape hatch e restauração** (sem migration). Concretiza a filosofia offline-first (local opera sem internet; só integrações dependem de rede). **F1 — consciência de modo:** `ModoOperacao` (selo 🖥️ Local / ☁️ Nuvem no cabeçalho, decidido pelo build `NEXT_PUBLIC_EDGE`, sem toggle em runtime) + `ServidorOfflineAviso` (só no edge: se o servidor local cai com o app aberto, banner "usar modo nuvem?" → abre `app.dmsregem.com`). **F2 — atalhos:** o instalador cria atalhos separados na área de trabalho — **PDV/KDS/Ponto/Garçom locais** (`localhost:3001/…`) + **Regem (nuvem)** como escape hatch de boot. **F3 — restaurar do estado da nuvem** (voltar ao local após operar na nuvem): botão em `/servidor` (só edge + presidente) grava flag em `sync_state`; o `sync-daemon` faz **2 tempos** — (1) empurra o operacional local pendente pra nuvem, (2) puxa as **tabelas transacionais** (`comanda`, `comanda_item`, `producao_pedido(_item)`, `pedido_externo`, `caixa_sessao`, `lancamento_caixa`, `movimento_estoque`) por delta e faz **upsert por id** (aditivo, nunca apaga o que é só local; retry de FK). Novo `GET /sync/restore` (mesma mecânica do pull, whitelist `TABELAS_RESTORE`) + `POST /edge/restaurar` / `GET /edge/restaurar/status` (gate `EDGE_MODE` + presidente). Builds verdes. **Pendências:** (a) as tabelas transacionais ainda **não sobem** continuamente pra nuvem (só o restore-pull desce) — o UP contínuo é a v2 do sync; (b) restore não testado ponta a ponta (precisa nuvem+edge reais). Impressão no modo nuvem (F4) fica para depois. Ver memória [[modo-hibrido-nuvem-local]]. | Loja escolhe Nuvem/Local com clareza, tem escape se o local cai, e volta pro local sem perder dado |
| 2026-07-14 | **Híbrido F4 — impressão no modo nuvem (escape hatch por navegador)** (sem migration). Método A escolhido (recomendado): novo `lib/print-cupom.ts` (`imprimirCupomNavegador`) renderiza o cupom em HTML com CSS de bobina (58/80mm) e chama `window.print()` — funciona com qualquer impressora instalada no Windows (inclusive térmica com driver), **sem depender do worker do edge**. Botão **"🌐 Imprimir (navegador)"** no modal de cupom (`/cupons`), ao lado da reimpressão térmica (que virou "🖨 Reimprimir (térmica)", via servidor local). É aditivo e seguro (só acrescenta um caminho de impressão que roda offline do edge); se um dia quiser térmica silenciosa em loja 100% nuvem, a ponte na bandeja (Método B, reusa `escpos.mjs`) entra como adição. Fecha as fases do híbrido (F1–F4); o menu por perfil fica com o dono do projeto (amarrado ao `perfil_acesso` configurável). Build verde. | Dá pra imprimir o cupom operando na nuvem, sem o servidor local |
| 2026-07-14 | **Catálogo completo de permissões + editor do presidente** (migration 093). O RBAC configurável (`perfil_acesso`) ganhou um **catálogo completo** de permissões (30+ chaves) alinhado ao menu novo: `permissoes.ts` expõe a interface `Permissoes` ampliada + `CATALOGO_PERMISSOES` (chave/rótulo/grupo/tipo) + `podeAcessar()` + `PERFIS_PADRAO` com os defaults por nível (Presidente tudo · Gerência sem financeiro/fiscal/relatórios/C&O e dashboard sem R$ · Supervisão operação do setor · Execução PDV/mesas/cupons/pedidos+mural, escala visual, conclui checklist, meu_dia não). `PermSpec` do `@RequirePerm` ampliado para qualquer chave booleana (o `PermissoesGuard` já era genérico). Módulo `perfil` ganhou **criar** (`POST /perfis`), **remover** (`DELETE /perfis/:id`, bloqueia presidente e perfil em uso) e **catálogo** (`GET /perfis/catalogo`). A tela **/config/acessos** virou **dirigida pelo catálogo** (toggles agrupados por área, ações CRUD para ponto/estoque) + **adicionar/remover perfil**. **Migration 093:** backfill do `perfil_acesso.permissoes` (jsonb `default || existente` — preenche chaves novas por nível preservando edições). Builds verdes. **Menu (shell) fica com o dono** (pendura `perm:'<chave>'` por item); **server enforcement (`@RequirePerm` nos controllers) é o follow-up que anda junto com o menu.** **Merge exige migration 093.** | Presidente monta perfis por permissão (não por nível fixo), cobrindo todo o menu novo, sem quebrar categorias operacionais |
| 2026-07-15 | **RBAC — enforcement no servidor (`@RequirePerm`) das telas P-only** (sem migration). Casando com o catálogo (mig 093): `relatorios` (classe → `relatorios_vendas`) e `auditoria` (classe → `auditoria`) passam a exigir a permissão além do `@Roles` — efeito: gerência/supervisão **perdem** relatórios e auditoria (matriz nova = presidente). Endpoints pontuais: `vendas GET /remocoes` → `@RequirePerm('cancelamentos')` (+ RolesGuard/PermissoesGuard no método) e `financeiro GET /caixa/fechamentos` → `@RequirePerm('turnos')`. Telas **operacionais compartilhadas** (PDV/mesas/cupons/pedidos/TEF etc.) ficam **sem** enforcement por área (quebraria uso cruzado entre perfis) — a visibilidade delas é só no menu. Build verde. *(Nota: enquanto o menu novo não é publicado, a gerência ainda vê o item antigo de relatórios/auditoria no shell e recebe 403 ao clicar — resolve quando o shell for religado às permissões.)* | Servidor bloqueia de fato as telas sensíveis por permissão, não só o menu esconde |
| 2026-07-15 | **Menu religado por permissão + estrutura nova (14 grupos + submenus)** (sem migration). O `shell.tsx` deixou de filtrar por `roles` (4 níveis) e passou a filtrar por **permissão do catálogo** (`getPermissoes()`), casando com o editor/mig 093. NAV virou 2 níveis (`NavNode` + `children`): Dashboard · PDV-Balcão [Mesas · Cupons] · Delivery [Pedidos] · Meu Dia · Escalas · Estoque · Checklist & registros · Mural & clima · Financeiro [Notas · TEF · Configuração] · Cadastros · Gerenciamento de ponto · Configurações [Produção & KDS · Config. por ramo · Planos · Acessos & perfis · Servidor local] · Relatórios [Turnos · Relatórios de vendas · Cancelamentos de itens] · Auditoria · Visão C&O. Cada item tem `perm:'<chave>'`; presidente vê tudo (bypass); `estoque` usa `.ver`. **Garçom e Bot saíram do menu** (acessados à parte). Fidelidade/Cashback ficam de fora até terem tela própria (sem rota). Builds verdes. **⚠️ Transição:** quem já está logado (não-presidente) precisa **relogar** para o token carregar as permissões novas (senão o menu aparece vazio). | Menu e servidor agora falam a mesma língua (permissão), na organização nova |
| 2026-07-15 | **RBAC — enforcement estendido às telas P-only de gestão** (sem migration). Casando com o catálogo: `fiscal GET /config` → `@RequirePerm('fiscal_config')` (notas seguem em `fiscal`); `tef GET/PUT /config` → `@RequirePerm('tef')` (os `pagamentos` do PDV seguem abertos — TEF é operacional); `licenca GET /planos` + `POST /assinatura/checkout` → `@RequirePerm('planos')` (`licenca/status` segue aberto p/ o paywall). Com isso, todas as telas **P-only** ficam bloqueadas no servidor: notas(`fiscal`), config fiscal(`fiscal_config`), config TEF(`tef`), financeiro(`financeiro`), planos, turnos, relatórios, cancelamentos, auditoria. Build verde. *(Apontado: **Acessos & perfis** está no menu como P+G, mas `perfil.controller` é presidente-only — recomendado manter só presidente; aguardando decisão do dono.)* | Só o presidente acessa as telas sensíveis, de fato no servidor |
| 2026-07-15 | **Menu em acordeão** (sem migration, só frontend). Os grupos do `shell.tsx` (PDV-Balcão, Delivery, Financeiro, Configurações, Relatórios) viraram **clicáveis**: o pai expande/recolhe a lista de subitens (seta ▾/▸), deixando o menu mais curto/arrumado — em vez de tudo sempre aberto. Abre sozinho no grupo da tela ativa; estado por grupo. PDV-Balcão e Delivery viraram grupos (a tela própria virou o 1º subitem: "Balcão" `/pdv`, "Painel" `/delivery`). Build verde. | Menu mais limpo e curto, com submenus escondidos até clicar |
| 2026-07-15 | **Segurança — fail-fast em segredos + bcrypt 12** (sem migration). Revisão de segurança validada: **(1)** `cliente-token.ts` (link mágico do cliente, HMAC) não usa mais o fallback `'regem-dev-secret'` — **lança erro** se `JWT_SECRET`/`CLIENTE_TOKEN_SECRET` ausente/fraco (evitava forjar sessão de cliente = PII). **(2)** `lease.ts` (licença Ed25519) **recusa** gerar par efêmero em produção (`NODE_ENV=production`) — só dev, com aviso. **(5)** `bcrypt` subiu de **10 → 12** rounds em todos os hashes (senha + PIN); throttle de auth já estava apertado (`@Throttle 5/60s`). *(Achados 3 — perm/cat presos no JWT 12h — e 4 — lockout de PIN por unidade — validados como **trade-offs**, aguardando decisão de abordagem.)* | Fecha degradação silenciosa de segredos; hashes mais fortes |
| 2026-07-15 | **Sync v2 — tabelas transacionais sobem (edge → nuvem) por LWW** (migration 095). As vendas/produção/caixa (`comanda`, `comanda_item`, `producao_pedido(_item)`, `pedido_externo`, `caixa_sessao`) passam a **subir continuamente** para a nuvem, fechando o "espelho" e destravando o restore de verdade. Como mudam de estado (comanda fecha, pedido avança), a mig 095 adiciona **`updated_at`** (nas que faltavam) + gatilho `trg_bump_updated_at` (bumpa em todo UPDATE); o daemon (`PUSH_TABLES`, pais-primeiro) e o `TABELAS_SYNC` ('sobe', cursor `updated_at`) reenviam a linha a cada mudança, e o `POST /sync/push` aplica **LWW** (update-se-mais-nova) para qualquer tabela com `updated_at` — append puras (movimento_estoque) seguem imutáveis. Push tolera FK fora de ordem (23503 → ignora, volta no próximo ciclo). `TABELAS_RESTORE` também passou a cursor `updated_at` (traz mudanças de estado feitas na nuvem durante a queda). Builds verdes. **Merge exige migration 095.** | Nuvem vira espelho real das vendas do edge; restore fica completo |
| 2026-07-15 | **RBAC ao vivo (revalidação no banco) + lockout de PIN mais curto + enforcement do Gerenciamento de ponto** (sem migration). **#3:** `JwtAuthGuard` deixou de confiar cegamente no token — agora **revalida o colaborador no banco** (status + perfil.nivel + permissões) com **cache de 30s**: bloqueio/rebaixamento/mudança de permissão valem **na hora** (não em 12h), e — bônus — **mudança de permissão NÃO exige mais relogar**. Erro de banco → mantém o token (disponibilidade); `bloqueado` → 401. **#4:** janela de lockout de PIN por unidade **15 → 5 min** (recupera rápido, reduz DoS interno; brute-force já barrado pelo `@Throttle 5/60s`/IP). **Enforcement P+G:** `GET /ponto/pessoas` (Gerenciamento de ponto) → `@RequirePerm('ponto_gerencial')` (P+G, tira supervisão conforme a matriz). Builds verdes. *(Achados de segurança 1,2,3,4,5 agora endereçados; 3 e 4 fechados aqui.)* | Revogar/rebaixar vale na hora; sem relogar; PIN menos DoS |
| 2026-07-15 | **RBAC — fecha enforcement dos P+G (servidor + produção/KDS)** (sem migration). `edge` atualização/restauração-status → `@RequirePerm('servidor')`; config de **Produção & KDS** (`producao-pedido` PUT produtos/setores destinos, cores, senha/config) → `@RequirePerm('producao_kds')` — só as **escritas** (gestão); os GET seguem abertos (o KDS lê cores/senha). **Cadastros** fica **sem** enforcement por área — é um hub que agrega CRUDs usados operacionalmente (ex.: PDV lê produtos); a visibilidade fica só no menu + `@Roles` nos CRUDs. Builds verdes. Com isso o enforcement por permissão cobre todas as áreas de gestão (P-only e P+G). | Toggles de permissão do C&O valem no servidor também nas telas P+G |
| 2026-07-15 | **Multi-unidade (rede) — Fase 1 (gestão) + Fase 2 (isolamento por loja)** (migrations 097, 098; em `dev-local`). **Fase 1:** tela *Configurações → Unidades* (CRUD, reusa `POST/PATCH/DELETE /unidades`) + permissão `unidades` no catálogo RBAC (mig 097). **Fase 2 (escopo completo front+backend):** seletor de unidade no topbar + header **`X-Unidade-Id`** + decorator **`@UnidadeAtual`** (resolve o RBAC no servidor; o `tenant_id` de cada query protege contra id forjado). Passaram a filtrar pela unidade: **escala** (dia/semana/período), **colaboradores/setores/turnos**, **dashboard** (`resumo`+`timeline`, fragmentos `sql` nas 8 subqueries — todas as tabelas têm `unidade_id`) e **operacional** (produção/KDS, mesas, config fiscal — fallback pro `@UnidadeAtual` quando não vem `?unidadeId=`). **Restrição presidente-only** (pedido do usuário; mig 098): a visão/gestão de unidades e o seletor são **exclusivos do presidente/C&O** — `@Roles('presidente')` no `UnidadeController`, perm `unidades` revogada dos demais, seletor gated em `getCategoria()==='presidente'`. **Isolamento por loja:** o claim JWT `uni` passou a usar **`colaborador.unidade_id`** (fallback função→setor) → gerente/supervisão/execução ficam **travados na própria loja** (o `@UnidadeAtual` ignora o header de quem tem `uni`); presidente (`uni=null`) vê/alterna a rede. Comparativo entre lojas na Visão C&O já existia. **Merge exige migrations 097 e 098.** | Rede multi-loja: presidente gerencia/compara; demais isolados na própria unidade |
| 2026-07-15 | **PLANO APROVADO (a implementar) — Cadastro de Terminal de PDV.** Terminal = `equipamento tipo='pdv'` (reusa `unidadeId/nome/token/ativo`). **Identificação por pareamento por token OBRIGATÓRIO** (sem retrocompatibilidade — projeto ainda em teste antes da disponibilidade final): admin cadastra "Caixa 1" → token → o PC cola uma vez → `localStorage` → header **`X-Terminal-Id`**; decorator `@TerminalAtual` resolve terminal→unidade→caixa. **Caixa por terminal:** `sessaoAberta/abrirSessao/caixaAtual` passam a considerar `terminal_id` (a trava "1 caixa aberto" vira por terminal; fecha de brinde o gap do caixa multi-unidade na nuvem). **Impressão:** impressoras de **setor** (cabeadas na rede, **sem PC** — `equipamento tipo='impressora'` com `host:porta`, roteadas por `setoresAtendidos` pelo daemon do edge via **TCP 9100**) **NÃO mudam**; a **via do cliente (cupom)** passa a sair só na impressora do terminal (`impressora_padrao_id`) → fallback unidade → `padrao`, **corrigindo** o `enfileirarViaCliente` que hoje imprime em **TODAS** as `papel='cupom'` do tenant (errado em multi-balcão/multi-loja). **Migrations previstas:** `caixa_sessao.terminal_id`, `equipamento.impressora_padrao_id`. **Fases:** F1 (cadastro + pareamento), F2 (caixa por terminal + fechamento por terminal), F3 (impressora por terminal + fix do cupom). RBAC: cadastro/pareamento = presidente/gerente; operar = perm `pdv`. | Identidade real de caixa (1/2/3 por loja); caixa e impressora por terminal; corrige cupom multi-balcão/multi-loja |
| 2026-07-15 | **Terminal de PDV — F1 (cadastro + pareamento)** (sem migration; `dev-local`). `equipamento` passou a aceitar **`tipo='pdv'`** (reusa `unidadeId/nome/token/ativo` — **sem coluna nova**). Cadastro pela tela *Configurações → Equipamentos* (novo tipo "Terminal de PDV (caixa)"); o `token` aparece uma vez. **Pareamento OBRIGATÓRIO:** `POST /equipamento/parear {token}` (autenticado — o token é a prova; sem `@Roles`) valida device pdv **ativo do tenant** → devolve `{id,nome,unidadeId}`; o PC guarda em `localStorage` e envia **`X-Terminal-Id`** em todo request (`req()`). O `/pdv` fica bloqueado por **`<TerminalGate>`** até parear (indicador "Terminal: X · trocar"). Infra pronta p/ F2: decorator **`@TerminalAtual`** (id cru do header) + `EquipamentoService.terminalUnidade(tenant,id)` (resolve terminal→unidade validando tenant+pdv+ativo). Verificado: criar→token, parear→identidade (unidade bate), token inválido→404. Builds verdes. | Identidade de caixa por máquina; base do caixa/impressora por terminal (F2/F3) |
| 2026-07-15 | **Terminal de PDV — F2 (caixa por terminal)** (migration 099; `dev-local`). `caixa_sessao.terminal_id`. A sessão de **balcão** (origem 'pdv') virou **por terminal**: `sessaoAberta/abrirSessao/caixaAtual/movimentarCaixa/fecharSessao` passam a receber o `@TerminalAtual` (header `X-Terminal-Id`); a trava "1 caixa aberto" vira **por terminal** (N caixas simultâneos, 1 por balcão) e a **unidade da sessão deriva do terminal** — fechando o gap do caixa multi-unidade na nuvem (`caixaAtual` não filtrava unidade). A **venda** (`vendaBalcao` + `sessaoAbertaId`) também amarra ao terminal: a comanda **herda a unidade do terminal** e o pagamento cai na sessão daquele caixa. Delivery (origem 'delivery') segue sem terminal (gaveta única). Abrir/vender sem terminal pareado é bloqueado. Verificado: 2 terminais (Matriz/Filial) abrem caixas independentes; `caixaAtual` isola por terminal; reabrir no mesmo → "já existe caixa aberto neste terminal"; unidade herdada correta. Builds verdes. **Merge exige migration 099.** | Cada balcão tem seu caixa; corrige o caixa multi-unidade/multi-balcão |
| 2026-07-15 | **Terminal de PDV — F3 (impressora/cupom por terminal)** — fecha o épico (migration 100; `dev-local`). `equipamento.impressora_padrao_id` (terminal PDV → impressora de cupom). `enfileirarViaCliente` (via do cliente) deixou de imprimir em **TODAS** as `papel='cupom'` do tenant e passou à prioridade: **(1)** impressora amarrada ao terminal; **(2)** impressoras 'cupom' da **unidade** (ou de rede, `unidade_id` nula). O `terminalId` flui `vendaBalcao → imprimirViaCliente → enfileirarViaCliente`. Cadastro/edição do vínculo: campo no form de PDV (Configurações → Equipamentos) + `PATCH /equipamento/:id/impressora` + select inline na lista. **Impressoras de setor (produção, TCP 9100, roteadas por `setoresAtendidos` pelo daemon do edge) seguem inalteradas.** Verificado (roteamento): terminal amarrado→só a dele; sem terminal+unidade→cupom da unidade+rede (não de outra loja); sem unidade→todas. Builds verdes. **Merge exige migration 100.** *(Épico Terminal de PDV completo: F1 identidade, F2 caixa por terminal, F3 impressora/cupom.)* | Cupom sai só na impressora do balcão certo; corrige multi-balcão/multi-loja |
| 2026-07-15 | **Instalador edge — robustez + segurança do log + atalhos em tela cheia** (sem migration; `dev-local`; exige rebuild do .exe p/ 2 e 3). **(1) Provisionamento:** `instalarSelfService` falha CEDO com log claro no servidor (`Logger.error` — causa: `LICENSE_*` ausentes na regem-api) + **503 genérico** ao instalador, em vez do 500 opaco (lease.ts fail-fast). Novo `licencaConfigurada()` em lease.ts. Motivada por instalação real que deu 500 por falta de `LICENSE_PRIVATE_KEY_B64` na nuvem. **(2) Credenciais fora do log:** o `.iss` parou de passar `-Email/-Senha` na linha de comando (o `Start-Transcript` gravava a senha do C&O no `instalar-*.log`); agora escreve um **arquivo temporário** (`CurStepChanged`/`SaveStringToFile` em `{tmp}`) e passa só `-CredFile`; o `instalar-tudo.ps1` lê e **apaga na hora** (`-Email/-Senha` seguem como fallback manual). **(3) Atalhos "cara de programa":** os atalhos da área de trabalho viraram `.lnk` abrindo Edge/Chrome em **`--app=<url> --start-fullscreen`** (janela sem abas/barra, tela cheia); sem navegador → cai no `.url` simples. Tudo ASCII (parse-check PS 5.1 ok). | Diagnóstico claro no log (não no 500), senha do C&O não vaza no log, e os terminais parecem um app dedicado |
| 2026-07-15 | **Escala recorrente — F1 (motor) + F2 (regra + endpoints)** (migration 101; `dev-local`). **F1:** `common/regras-escala.ts` ganhou `gerarDiasTrabalho(regra)` (12x36/4x3 = ciclo rotativo da data-base; 5x2/6x1 = por dia da semana via `folgasSemana`; pula feriados), `pausaSugerida(entrada,saída)` (pausa CLT centralizada: >6h→1h, 4–6h→15min, ≤4h→nenhuma) e `precisaPerguntarFolga`. +12 testes (28 verdes). **F2:** tabela **`escala_regra`** (recorrência) + `escala_alocacao.regra_id` + **overrides por dia** (`hora_inicio/fim`, `pausa_inicio/fim`) lidos por `coalesce` no `enriched`. `POST /escala/gerar` (cria a regra + preenche o período; feriados de `dia_especial` pulados quando `feriadosFechar`; idempotente; avisos CLT sem bloquear) e `PATCH /escala/:id/horario {escopo}` (**dia** / **futuras** = mesma regra data≥ / **datas** = lista que o front calcula p/ o padrão mensal; bloqueia CLT ilegal). Turno reusado/criado por setor+horário. Verificado: 12x36 gera 16 dias alternados, 5x2 gera 21 úteis, editar "só dia" vs "futuras" isola/propaga certo. **Merge exige migration 101.** *(Falta F3: wizard no card do dia.)* | Escala vira "evento recorrente de calendário": preenche o período sozinho, pausa por CLT, edição com escopo |
| 2026-07-15 | **Escala — botão de feriado/evento + 4x3 confirmado** (sem migration; `dev-local`). **4x3** já estava completo (opção em `JORNADAS` "4x3 (4 dias, 3 folgas)" + motor `CICLOS {on:4,off:3}`), só confirmado. **Botão discreto "Feriado / evento"** no header da Escala (`actions`, só presidente/gerente) → `DiaEspecialModal` (tipo feriado/evento, nome, data, "até" p/ período, observação) usando o `dia_especial` que já existia; api.ts ganhou `criarDiaEspecial`. Feriados alimentam o "fechar nos feriados" da geração; eventos servem de aviso (troca de escala/reforço) no calendário. A grade da escala já marcava os dias especiais (emoji) — agora dá pra cadastrá-los ali. | Cadastrar feriados/eventos direto na escala, sem ir a outra tela; alimenta a geração recorrente |
| 2026-07-15 | **Escala recorrente — F3 (wizard) + Férias** — fecha o épico (sem migration; `dev-local`). **Wizard `GerarEscalaWizard`** (botão "Gerar escala" no header, presidente/gerente): colaborador (prefill do `jornadaTipo`) + vaga + tipo + entrada/saída → **pausa CLT sugerida ao vivo** (editável) → **folgas** (chips da semana; escondido em 12x36/4x3) → **feriados: fechar** (toggle) → **período** (1/3/6/12 meses) → resumo → `POST /escala/gerar`. **Férias** = 3ª aba do `DiaEspecialModal` (colaborador + período): grava `dia_especial tipo='ferias'` e o backend **DESMARCA** (soft-delete) as alocações do colaborador no intervalo — ele sai da escala nesses dias (vaga abre p/ cobertura) e o marcador 🏖️ aparece na grade. `dia-especial.service.create` devolve `desmarcadas`. Verificado: wizard gera 12x36/5x2 certo; férias remove 7 dias no período e preserva os de fora. **Épico escala recorrente completo (F1 motor+CLT, F2 regra+endpoints+overrides mig 101, F3 wizard+férias).** | Montar escala vira preencher um "evento recorrente"; férias tiram o colaborador da escala automaticamente |
| 2026-07-15 | **Escala — regra de folga inquebrável + rótulos de vínculo + filtro por função no wizard** (sem migration; `dev-local`). **(bug)** o wizard deixava marcar 3 folgas num 5x2 e gerava: agora `folgasSemanais(tipo)` (5x2=2, 6x1=1) é validado no **backend** (`gerarEscala` → 400 "exige exatamente N folgas") **e** no wizard (não deixa passar do limite com toast; avisa "faltam X" antes de gerar; apara ao trocar de tipo). **Rótulos de vínculo** ("Tipos"): `VINCULOS` viraram CLT / Diarista / Horista / Prestador de serviço / Autônomo (valores inalterados). **Filtro por função/setor** no `GerarEscalaWizard`: o dropdown de colaborador passou a mostrar só quem tem a função da vaga (etiqueta) — não lista gente de outro setor; reseta o colaborador se trocar p/ vaga incompatível. | Tipo de escala é regra que não se quebra; menos erro de alocação por setor |
| 2026-07-15 | **Escala — resumo "todos do dia" ao clicar num card preenchido** (sem migration; `dev-local`). Clicar num card **preenchido** (grade semana desktop e card mobile) deixa de abrir direto o form e abre um **resumo do dia** (`detalheDia`): lista todos os escalados daquele dia — **nome · vaga (sigla) · horário** (entrada–saída), ordenados por horário, com nota dos dias especiais e "editar" por linha (abre o form da vaga). Card **vazio** segue abrindo a alocação. Setor não repetido na lista (já é o agrupamento/linha da grade). | Ver quem está o dia inteiro num clique, sem varrer vaga por vaga |
| 2026-07-15 | **Revisão de modelo da escala — Fase A (cadastro) + Fase B (modelo)** (migration 102; `dev-local`). **A (sem migration):** Cadastro alinhado às regras: **Turnos** perde os campos de pausa (a pausa vem da regra da escala); **Funções** perde "gerar etiqueta" e "sigla" (etiqueta só no card Etiquetas — backend só gera etiqueta se `gerarEtiqueta===true`, não mais por padrão); **Etiquetas** não lista mais presidente/C&O no campo Função e o placeholder da sigla virou "AUXC1 · Auxiliar de cozinha 1"; **card "Dias importantes" removido** (já está no menu Escala). **B (mig 102):** `unidade.tipo` = **Matriz | Filial** (a mais antiga de cada tenant vira Matriz; select + badge na tela Unidades) e **função ↔ setor N:N** (`funcao_setor`; `funcao.setor_id` segue como primário; findAll devolve `setorIds`; cadastro de Funções vira multiselect de setores). **Merge exige migration 102.** *(Faltam Fase C: motor 6x1-ciclo/5x1 + limites CLT; Fase D: falta+comprovante+relatório.)* | Modelo alinhado à lógica de negócio revisada (turno≠escala, unidade Matriz/Filial, função multi-setor) |
| 2026-07-15 | **Escala — Fase C: motor + limites CLT** (sem migration; `dev-local`). **6x1 virou ciclo 6/1** (era por dia da semana) e **5x1 é novo** (ciclo 5/1) — `CICLOS` no `regras-escala.ts`; só **5x2** segue por dia da semana (2 folgas). **Jornada máxima por tipo** (`jornadaMaximaHoras`: 12x36=12h, 4x3=11h, 5x2/5x1=8.8h, 6x1=7.34h) → `validarAlocacao` emite aviso **`carga_horaria`** se a jornada passar do teto (ex.: 12h numa 5x2). **DSR** (`avisosDsr`): aviso quando um mês do período tem todos os domingos trabalhados (≥1 domingo de folga/mês). `POST /escala/gerar` ganhou **`respeitarClt`** (checkbox "Levar em conta regras da CLT vigente", ligado por padrão) que liga/desliga esses avisos; o wizard mostra o alerta **ao vivo** ao digitar horários fora do teto. JORNADAS ganhou 6x1/5x1. 36 testes verdes. | Tipos de escala legais completos + alerta de jornada fora da CLT |
| 2026-07-15 | **Escala — Fase D: Falta (presença + comprovante + relatório)** — fecha a revisão da escala (migration 103; `dev-local`). `escala_alocacao` ganhou **`presenca`** (prevista/presente/falta_justificada/falta_injustificada) + `comprovante_ref` + `presenca_obs` + `presenca_em`. `PATCH /escala/:id/presenca` (presidente/gerente/supervisão; comprovante só na justificada; auditado `marcou_presenca`) e `GET /escala/faltas?de&ate` (relatório: lista + **resumo por colaborador** justif/injust). Front: no resumo "Escalados do dia" cada linha tem um **chip de presença** → `PresencaModal` (3 status + upload do comprovante na justificada + obs); botão **"Faltas"** no header → `FaltasModal` (período + resumo + lista com link do comprovante). Verificado ponta-a-ponta. **Merge exige migration 103.** *(Revisão da escala A–D completa: A cadastro, B modelo mig 102, C motor/CLT, D falta mig 103.)* | Registrar presença/faltas com comprovante e relatório por colaborador |
| 2026-07-15 | **Escala — reclassificação folga: por dia da semana = soma 7** (sem migration; `dev-local`; correção da Fase C). Regra clara: os tipos cuja soma trabalho+folga = 7 (**5x2**=2 folgas, **6x1**=1, **4x3**=3) são **por dia da semana** → o usuário **escolhe os dias de folga** (com o nº exato validado). Só **12x36** (dia sim/não) e **5x1** (5 on/1 off, 6 dias, não fecha a semana) ficam em **ciclo automático**. Antes a Fase C tinha posto 6x1 e 4x3 como ciclo — o usuário apontou que só o 5x2 deixava escolher. `CICLOS` = {12x36, 5x1}; `folgasSemanais` = {5x2:2, 6x1:1, 4x3:3}; wizard `ehCiclo`/`folgasExigidas` idem. 34 testes verdes. Verificado: 6x1 exige 1 folga, 4x3 exige 3, 12x36/5x1 automáticos. | Escolher a folga bate com a CLT (6x1 folga no domingo, 4x3 3 dias) |
| 2026-07-15 | **Timeline: pico mais visível + janela de pico por intervalo de dias** (migration 104; `dev-local`). **(1)** No `Timeline` do dashboard a faixa de pico saiu do `bg-destructive/5` (quase invisível) para **`bg-warn/12` + bordas âmbar** e ganhou uma **faixa discreta no topo** com "🔥 nome" (descrição de status sutil). **(2)** `janela_pico.dia_semana_fim` — a janela de pico aceita **intervalo de dias** (ex.: seg→sex, com "volta" na semana): no cadastro (Cadastros → Janelas de pico) o select de dia ganhou **"Personalizar (intervalo)…"** que revela "Do dia"/"Até o dia" (via novo `showIf`/`fromRow` no `EntityForm`); o match no dashboard virou range-aware. Verificado: pico seg→sex aparece na quarta, some no domingo. **Merge exige migration 104.** | Pico legível na timeline + janelas por faixa de dias (não só 1 dia/todos) |
| 2026-07-15 | **Escala — unificação da alocação num só fluxo (Gerar escala)** (sem migration; `dev-local`). A tela antiga de "Nova alocação" (`nova-alocacao-form.tsx`) foi **removida**; tudo passa pelo **`GerarEscalaWizard`** (título "Gerar escala recorrente" → "**Gerar escala**"). Novo campo **Recorrência** com **"1 dia" como padrão** (dataInicio=dataFim → alocação avulsa; feriados/folga escondidos e não exigidos nesse modo — backend pula a validação de folga quando `dataInicio===dataFim`); períodos maiores = recorrente. O wizard aceita **colaborador/vaga pré-selecionados**: **card vazio** → abre com a vaga; **"editar"** no resumo do dia → abre com o colaborador já selecionado. O resumo do dia ganhou botão de **remover** (🗑, reusa `removerAlocacao`). Removidos `NovaAlocacaoForm`/`Select`/`elegiveis`/`trocarResp` órfãos. Novo `showIf`/`fromRow` do EntityForm já existia. | Um só fluxo pra alocar (dia ou recorrente), menos telas |
| 2026-07-15 | **Escala — folga editável destravada + fix do texto + dia avulso ignora folga** (sem migration; `dev-local`). Correção do wizard "Gerar escala": os **dias de folga** agora ficam **editáveis para 5x2/6x1/4x3 em qualquer período** (`perguntaFolga = !ehCiclo`, não mais só no recorrente); só **5x1 e 12x36** mostram "folga automática" — e o texto que aparecia errado ("4x3: folga automática" mesmo em 5x2) foi corrigido para 12x36/5x1. No modo **"1 dia"** a geração **ignora as folgas** (backend: `folgasSemana=[]` quando `dataInicio===dataFim`) → clicar em qualquer card (mesmo num sábado) sempre aloca; o label das folgas indica "· usados na recorrência". Verificado: dia avulso num sábado (folga do 5x2) cria 1. | Folga configurável em todas as escalas por dia da semana; alocar dia avulso nunca falha |
| 2026-07-16 | **Escala — editar do card carrega os colaboradores do dia** (sem migration; `dev-local`). No fluxo "editar" (resumo do dia → editar), o `GerarEscalaWizard` recebe `colaboradoresDoDia` (ids dos escalados daquele dia) e o dropdown de colaborador passa a listar **todos os do dia** (antes filtrava só pela função da vaga → aparecia só 1). Trocar de colaborador puxa a escala dele; a etiqueta não reseta o colaborador nesse modo. O filtro por função segue no card vazio (nova alocação). | Editar/reatribuir entre os escalados do dia sem ficar preso à função da vaga |
| 2026-07-16 | **RBAC de filial no servidor + seletor só-Matriz** (mig 105; `dev-local`). (1) Seletor de unidade no topbar perdeu a opção "Todas as lojas": padrão = **Matriz**, presidente escolhe matriz ou uma filial. (2) `@UnidadeAtual()` endurecido: **fail-closed** — não-presidente sem unidade recebe `NIL_UUID` (não vê a rede), só presidente vê `null`=rede/header. (3) Helper `common/filtro-unidade.ts` (`sqlUnidade`/`condUnidade` estrito; `sqlUnidadeOuRede`/`condUnidadeOuRede` p/ config herdável). (4) Filtro por unidade aplicado (leitura + escrita forçada) em **Estoque, Desperdício, Vistoria, Contagem, Recebimento, Financeiro (títulos/caixa/DRE/fechamentos), Ponto/Pessoas, Delivery**. (5) Mig 105 faz backfill de `unidade_id` NULL→matriz do tenant só nas tabelas **transacionais** (config/catálogo/plano ficam NULL=rede, herdado). (6) **Override travado:** nos endpoints que aceitavam `?unidadeId=` (produção fila/pedidos, vendas/mesas, fiscal/config), só o **presidente** pode escolher a unidade por query; os demais são forçados ao `@UnidadeAtual` (um operador não fura a própria loja via query param). (7) **Catálogo** (cardápio/produto/ficha/fidelidade/cashback) fica **compartilhado da rede** de propósito — é config administrativa gerida pelo presidente; herança por filial fica disponível (helper pronto) se um dia quiser cardápio por loja. Conceito confirmado pelo usuário: presidente/C&O = administrativo (não opera PDV/delivery/comandas); operação nas filiais é por login gerencial/execução travado numa unidade, sem filtro. | Gerente/supervisão/execução/operadores só enxergam a própria loja pela API; filtro de unidade é exclusivo do presidente |
| 2026-07-16 | **Lote de pendências #9/#10/#13/#14/#15/#17/#18** (mig 106; `dev-local`). **#14 Pagamento online real (Mercado Pago PIX):** `common/mercadopago.ts` (cria PIX via REST + consulta status), `pagarPedidoPublico` gera QR+copia-e-cola quando há token (integracao canal `mercadopago` ou env `MP_ACCESS_TOKEN`), fallback mock sem gateway; webhook `POST /publico/cardapio/pagamento/mercadopago/webhook` confirma; mig 106 `pedido_externo.gateway_payment_id`; storefront mostra QR. **#15 Frete por distância:** `common/geocode.ts` (Nominatim/OSM, sem chave) geocoda a loja ao salvar config e o endereço do cliente no checkout raio (sem GPS). **#9 Hub de relatórios:** nova aba "Operações de caixa" (`GET /relatorios/operacoes-caixa`: cancelamentos + sangrias/suprimentos por operador) — o resto do hub já existia. **#10 Enforcement de módulos ativáveis:** `@RequireModulo`+`ModuloGuard` no KDS (`/producao/fila`); inline no terminal de ponto (origem terminal) e no App do Colaborador (perfil execução em tarefas). **#13 Preview do template por ramo:** `/inicio` mostra o que será criado antes de aplicar (blueprint ganhou `tipos`). **#17 Snapshot de estoque DIÁRIO** (era mensal) → CMV preciso em qualquer período. **#18 baixa de venda idempotente** (`onConflictDoNothing` no índice `idx_movimento_ref`; `ref_*` já existia). **#16a banner** já estava pronto (mig 054); **#16b IA** adiado. Backend build/tsc + 91 testes + front tsc OK. | Fecha 8 pendências do levantamento; gateway real e frete por distância prontos |
| 2026-07-16 | **Tutorial do cliente na landing + download do certificado do edge** (`dev-local`, sem migration). O edge passou a **servir o `ca.pem` por URL**: `https://<servidor>:3001/ca.pem` (baixa `regem-ca.crt`, content-type `application/x-x509-ca-cert`), rota nova em `backend/edge/edge-web.mjs` — troca "copiar via USB" por "abrir o link e instalar". A página `/como-funciona` (já linkada na landing) ganhou no passo do certificado instruções separadas **Windows** (Instalar Certificado → Raiz Confiável) e **Android** (Ajustes → Segurança → Instalar certificado CA) + o link de download. | Loja consegue confiar o cert nos tablets KDS/PDV/ponto sozinha, sem técnico |
| 2026-07-16 | **Épico Controle de Tarefas** (mig 107; `dev-local`). **Cadastro** por **Setor (opcional) + Função (obrigatória)** no lugar da etiqueta; responsável = escolha de quem cria (um escalado da função/setor via `GET /tarefas-instancias/responsaveis`, ou "em aberto"). **Status** com política de foto do presidente: Concluída/Parcial exigem foto **se** o presidente ligar (`entitlement` `tarefa_foto_conclusao`/`tarefa_foto_parcial`, gear no card), Não concluída = só motivo; até **3 fotos**, `data_expurgo` = +30d + cron `expurgarFotosTarefa`. **Editar** (`PATCH`) e **Excluir com motivo** (`DELETE`, soft-delete + auditoria). **Dashboard**: data + aviso de pendência no header; "Tarefas em atraso ou atenção" (½) ao lado de "Ponto de hoje" (½), "Ranking da equipe" abaixo (largura cheia); card com botão **+** (nova tarefa em modal) e tarefas **clicáveis** → `TarefaModal` (view/status/foto/editar/excluir). **Timeline**: pontos de tarefa mostram o nome; seletor de **alinhamento** da grade (15/30/60/120 min, padrão 1h). Alertas ao C&O = as não-concluídas/parciais seguem no card com destaque. mig 107: `tarefa_def.funcao_id`, `tarefa_instancia.(funcao_id, setor_id, fotos, data_expurgo)`. Backend build + 91 testes + front tsc OK. | Controle de tarefas ponta-a-ponta com comprovação por foto configurável |
| 2026-07-16 | **RBAC de cadastro de colaborador + login por senha** (sem migration; `dev-local`). Regra de quem cria quem (servidor, `colaborador.service`): **presidente** cria qualquer nível (inclusive outro presidente — sociedades/multi-admin); **gerente** cria só supervisão/execução (NÃO gerente nem presidente); nível-alvo = categoria da função principal (`podeCriarNivel`). **Login por e-mail+senha** só para gestão (presidente/gerente/supervisão): DTO ganhou `senha` (hash no create, exige e-mail; recusa para execução). **PIN** passa a ser rotulado como acesso ao **terminal de ponto**. Front (`build-secoes.ts`): opções de função filtradas pelo que o ator pode criar; campos e-mail+senha aparecem via `showIf` quando a função é de gestão; EntityForm ganhou tipo `password`. | Cadastro respeita a hierarquia no servidor; gestão entra por senha, operação por PIN |
| 2026-07-16 | **Loja RBAC + card renomeado + Gestão do cardápio com auto-pausa** (mig 108; `dev-local`). **(1)** Dados da Loja (`/loja`) exclusivos do presidente/C&O: front redireciona não-presidente, item de menu com `soPres`, e `setConfig` **ignora os campos de identidade da loja** (`CAMPOS_LOJA`) quando o ator não é presidente (gerente ainda salva a config operacional do Delivery). **(2)** Ponto: confirmado que **não há exclusão** de marcação (append-only, Portaria 671) — só ajuste com justificativa obrigatória, auditado; nenhuma mudança. **(3)** Card do dashboard "Tarefas em atraso ou atenção" → **"Gerenciamento de tarefas"**. **(4)** Submenu **Delivery → "Gestão do cardápio"** (→ `/produtos`, CRUD já existente). Pausa manual (`disponivel_cardapio=false`) agora **aparece como "Esgotado"** no cardápio em vez de sumir. **Auto-pausa por estoque** (mig 108 `produto.pausado_estoque`/`pausa_motivo`): `cardapio.service.sincronizarEsgotados` recomputa via `@OnEvent('estoque.baixado')` (emitido por `vendas.lancarSaidas` e `recebimento.confirmar`) → pausa+alerta geral (`kds.alerta.sistema` + `alertaEstoque`) ao esgotar, despausa sozinho ao repor; config `cardapio_auto_pausa` (entitlement, toggle em `/produtos`). Backend build + 91 testes + front tsc OK. | Dados da loja só do presidente; cardápio pausa/esgota sozinho com aviso |
| 2026-07-16 | **Reativar produto esgotado sem estoque (contagem negativa)** (mig 109; `dev-local`). O gestor pode despausar um produto esgotado **sem dar entrada**: `produto.permite_negativo` (mig 109) desliga o **bloqueio por estoque** (não pausa/esgota mais) mas mantém `controla_estoque=true`, então a baixa continua e o saldo do insumo fica **negativo** (contagem negativa, informativa). `computeEsgotados` ignora `permite_negativo`; `POST /produtos/:id/permite-negativo {ativo}` liga/desliga (limpa pausa + libera cardápio ao ligar). Front `/produtos`: botão **"Reativar"** (com aviso: controle de estoque será desativado + contagem negativa) nos esgotados por estoque, badge **"contagem negativa"** e **"Voltar a controlar"** para desmarcar. Backend build + 91 testes + front tsc OK. | Reativar item esgotado sem repor, ciente da contagem negativa |
| 2026-07-16 | **Sistema de temas do cardápio digital** (mig 110; `dev-local`; só apresentação). `cardapio_config.menu_theme` (`classic`|`fastfood`, aditiva, fallback classic) exposto no config e no menu público. Front: `data-menu-theme` + `--brand-primary` no root do storefront + hooks `lp-*`; **nenhum seletor CSS sem o data-attr** → o tema **classic fica pixel-perfect** (Regra de Ouro). Dark mode segue independente. **Fastfood:** grade de **Destaques**, **seções por categoria com scroll-spy** (IntersectionObserver; aba clica → rola), **item passo-a-passo** (cabeçalho de grupo sticky + pílula `x/y` + ✓ verde — dados `sel.grupos` já existiam), **bottom-nav com carrinho central** + **Buscar** (overlay filtrando os itens carregados), CSS de cards flutuantes. **Banner deep-link:** render abre `item:ID`/`category:ID`/`coupon:CODE`/URL (reusa campo `link`); **editor no cadastro** (Delivery→Banners) com "Ação ao tocar" (dropdown de produtos/categorias). Seletor "Estilo do cardápio" no admin. Zero API nova, toda a lógica/checkout compartilhada. Front tsc + backend build + 91 testes OK. | 2 temas de layout por loja sem tocar na lógica; classic intacto | 
| 2026-07-17 | **3º tema do cardápio: `grid`** (`dev-local`; só apresentação). `menu_theme` agora aceita `classic`/`fastfood`/`grid` (validação nas duas pontas). Grid = cabeçalho **curvo** na cor da loja, **chips circulares** de categoria, **grade de 2 colunas** (`gridCard`: foto `aspect-square`, nome centralizado, ＋ circular), **dock inferior flutuante** (pílula com carrinho central). Reusa a mecânica rica do item-sheet (gate `ff` estendido a grid) e o scroll-spy/`irCategoria` das seções. `classic` segue fallback pixel-perfect. Front tsc + backend build + 91 testes OK. | Terceiro layout de loja mantendo a Regra de Ouro (lógica compartilhada) |
| 2026-07-17 | **Storage de mídia local (modo offline/edge)** (sem migration; `dev-local`). `midia.service` ganhou **fallback em disco**: quando `SUPABASE_URL`/`SUPABASE_SERVICE_KEY` **não** estão definidas (caso do `.env.local`), o upload grava em `MIDIA_DIR` (padrão `backend/uploads/<tenant>/<uuid>.ext`) e devolve URL servida pela própria API em **`GET /api/v1/publico/midia/:tenant/:arquivo`** (`MidiaPublicoController`, público, sem auth, com validação anti-path-traversal: só uuid+ext, resolve dentro da raiz). `remover()` (expurgo LGPD) também apaga do disco. Base pública configurável por `MIDIA_PUBLIC_URL` (edge na LAN aponta p/ o IP). **Na nuvem, como as chaves do Supabase existem, o código continua usando o Storage do Supabase — comportamento inalterado.** `uploads/` no `.gitignore`. Backend build + 91 testes + E2E (login→upload→GET 200) OK. | Modo local 100% offline (imagem era a única dependência de nuvem restante) |
| 2026-07-17 | **Cardápio: aba "Pedidos" + Perfil + cancelamento/alteração pelo cliente** (mig 111; `dev-local`). Menu inferior **"Pedidos"** deixa de abrir o Perfil e abre um **`PedidosPanel`** próprio: lista os pedidos do cliente **incluindo o atual** — em andamento em **destaque** com status (**Aguardando aceite** = status `novo`, **Em preparo**, **Em rota**), finalizados em **fundo neutro** (clicáveis). Detalhe do pedido mostra hora, itens, **total + desconto**, número e forma de pagamento. **Pedido atual**: **Solicitar cancelamento** (enquanto não saiu p/ entrega) → abre chamado no sino tipo `cancelamento` ligado ao pedido; no Delivery a equipe **Aceita** (cancela de fato via `delivery.cancelar`, exige senha de gestor) ou **Recusa**. **Solicitar alteração** (endereço/pedido/pagamento) → só avisa no sino (tipo `mudanca`). **Pedido finalizado**: **Pedir de novo**, com **bloqueio** se foi resgate de fidelidade e o cliente não tem mais prêmio disponível. Cabeçalho: botão **"Meus dados" → "Perfil"** (sem histórico; histórico foi p/ a aba Pedidos). `perfil()` enriquecido (desconto/taxa/endereço/flag `resgate`). mig 111: `atendimento_chamado.(pedido_id, decisao)`. Front tsc+build + back build + 91 testes OK. | Cliente acompanha/gerencia o pedido pelo cardápio; equipe decide cancelamentos no sino |
| 2026-07-17 | **Cardápio: Editar tema + carrossel de banners + categorias scroll-spy + ajustes** (mig 112; `dev-local`). **(1) Ramo**: só "🍔 Bares e Restaurantes" selecionável; Varejo/Indústria/Serviços "(em breve)" desabilitados. **(2) Frete grátis = 0** passa a **desligar** a função (antes 0 dava frete grátis sempre) — checkout e cabeçalho tratam `<= 0` como off. **(3) Banners**: viram **carrossel automático** (`BannerCarousel`, troca a cada N s, default **2**, só com 2+; indicadores) — antes ficava parado no 1º; cadastro ganhou campo "segundos" e **trava de máx. 3** (`setBanners` corta em 3). **(4) Botão "Editar tema"** (ao lado de Parcelas): modal com **cor primária** (paleta do código + custom) e **toggles** de Destaques/Banner/Últimos pedidos → gravados em `cardapio_config.tema_config` (mig 112, JSONB aditivo); storefront usa `temaConfig.corPrimaria` como accent e respeita os toggles. **(5) Categorias**: removido o chip **"Todos"**; o **Clássico** passou a usar **seções por categoria com scroll-spy** (estilo iFood, como fastfood/grid já faziam) — clicar rola até a seção e o scroll acompanha a categoria. **(6)** Barra de rolagem das faixas de categoria/destaques **escondida** (`.no-scrollbar`, arrasta sem barra). mig 112: `cardapio_config.tema_config jsonb`. Front tsc+build + back build + 91 testes OK. | Personalização do tema pelo lojista + navegação de categorias unificada nos 3 temas |
| 2026-07-17 | **Cardápio: responsividade + enquadramento de imagem + cores do cabeçalho** (sem migration; `dev-local`). **(1) Responsivo (3 temas):** o storefront virou coluna central **`max-w-2xl` centrada** (full-width no celular, centrada no desktop) — `<main>` virou wrapper `.lp-menu` full-bleed + `<main>` interno com a coluna; barra do carrinho e bottom-nav em `max-w-2xl`; grade do tema grid **`grid-cols-2 sm:grid-cols-3`**. **(2) Imagens enquadradas ao centro (3 temas):** caixas de imagem com **aspect fixo + `overflow-hidden` + `object-cover object-center`** (cardProduto square `absolute inset-0`; gridCard; item-sheet com `aspect-[16/9]`) — não vazam mais do card. **(3) Cores do cabeçalho** no "Editar tema": **cor de fundo** e **cor do texto** do cabeçalho (JSONB `tema_config.corCabecalho`/`corTextoCabecalho`, null=padrão); o header aplica via `style` e os textos/botões herdam a cor. Tailwind (não Bootstrap — mantém a stack). Front tsc OK + dev servers 200. | Cardápio se adapta a celular e desktop; imagens centradas; lojista controla as cores do topo |
| 2026-07-17 | **Cardápio: correções e ajustes (lote)** (sem migration; `dev-local`). **(1)** Config-panel: **Exportar QR** — botões "Exportar PNG" (512px via `qrcode`) e "Imprimir / PDF" (janela de impressão → salvar como PDF). **(2)** Bottom-nav fastfood/grid: label **"Perfil" → "Promo"** (o botão sempre foi da aba promos). **(3)** Busca (fastfood/grid): resultados **não clicáveis** corrigido — o item-sheet (`z-30`) abria atrás do overlay de busca (`z-40`); `abrirItem()` fecha a busca ao abrir o item. **(4)** Bug do **seletor de cor** do "Editar tema": `CorLinha`/`ToggleTema` eram recriados a cada render (fechavam o color picker nativo) → movidos ao escopo do módulo. **(5)** Menu de config do Delivery: **"Cardápio digital" vira o 1º** (default ao abrir), título **"Quadro" removido** e **"Colunas do quadro" movido p/ "Operação"** após Banners. **(6)** Painel Delivery: **botão "Simular" removido** + método `simularDelivery` órfão retirado do front. Front tsc+build + back build + 91 testes OK. | Correções de UX do cardápio reportadas em teste |
| 2026-07-17 | **Épico Catálogo — Fase 1: Categoria rica + storefront minimalista** (mig 113; `dev-local`). **Decisão de arquitetura:** o catálogo vai para o modelo de 3 níveis reutilizável (Categoria → Produto → Complemento(etapa) → Opção), **Opção A** (Complementos/Opções como entidades próprias N:N, materializadas sobre o motor `complemento_grupo/opcao` atual); Cadastro→"Produtos e Catálogo" abrirá a nova tela ao final; produto terá seletor de uso **PDV balcão / cardápio online** (já há `disponivel_balcao/disponivel_cardapio`). **Fase 1 (feita):** `categoria_produto` ganhou **foto (`imagem_ref`), descrição e disponibilidade** (`jsonb` de janelas `{dias,inicio,fim}`; vazio=sempre); `menu()` filtra categoria pela janela atual (`categoriaDisponivelAgora`) e expõe foto/descrição; backend `atualizar/reordenar/excluir` categoria (rotas em `/produtos/categorias`); front `categorias-card` com **editar (foto/descrição/disponibilidade), excluir e arrastar p/ reordenar** (HTML5 DnD, sem lib); storefront mostra a descrição sob o título da seção. **Storefront minimalista:** cabeçalho e barra de categorias mais compactos; **logo sempre em círculo preenchido** (`rounded-full` + object-cover). Front tsc+build + back build + 91 testes OK. | Base do novo catálogo (categoria rica) + enxugar o cardápio |
| 2026-07-17 | **Épico Catálogo — Fase 2: Opções reutilizáveis** (mig 114; `dev-local`). Nova tabela **`opcao`** (entidade reutilizável): `tipo` **simples / ficha / insumo**, `codigo_pdv`, `preco_custo`, `imagem_ref`, `descricao`, `controla_estoque`, `ativo` (invisível=false) e `esgotado` (pausa), com `ficha_id`/`item_id`/`produto_ref_id` conforme o tipo. Backend em produto.service (`listarOpcoes`, `criarOpcaoCatalogo`, `atualizarOpcao`, `excluirOpcao`) + rotas `/produtos/opcoes` (GET/POST/PATCH/DELETE, declaradas **antes de `:id`**). Front `opcoes-card.tsx` (lista com busca, badge de tipo, custo, toggle esgotado, editar/excluir) + modal (imagem, nome, código PDV, custo, descrição, seletor de tipo→ficha/insumo, controle de estoque, visível/esgotado), na página `/produtos`. Ligação opção↔complemento vem na Fase 3. Front tsc+build + back build + 91 testes OK. | Opções do catálogo viram item reutilizável (bebidas/adicionais/acompanhamentos) |
| 2026-07-17 | **Épico Catálogo — Fase 3: Complementos reutilizáveis** (mig 115; `dev-local`). Tabelas **`complemento`** (etapa reutilizável: `regra` uma/varias_sem_repeticao/varias_com_repeticao, `obrigatorio`, `min`/`max`, `canais` jsonb [delivery/balcao/mesa_publico/mesa_interno], `ativo`) e **`complemento_item`** (N:N complemento↔opção com `preco`/`ordem`). Backend em produto.service (`listarComplementos` com itens via `json_agg`, `criarComplemento`/`atualizarComplemento`/`excluirComplemento`, `salvarItensComplemento` que substitui os vínculos); `listarOpcoes` passou a trazer **`usado`** (nº de complementos que usam a opção). Rotas `/produtos/complementos-catalogo` (nome distinto do motor per-product `/produtos/.../complementos`). Front `complementos-catalogo-card.tsx` (lista + modal: nome, regra de escolha, obrigatório, min/máx, canais, e picker de opções reutilizáveis com preço), em `/produtos`. Materialização nos produtos vem na Fase 4. Front tsc+build + back build + 91 testes OK. | Etapas reutilizáveis que juntam opções (base do "Usado N vezes") |
| 2026-07-17 | **Épico Catálogo — Fase 4: Produto liga complementos + materialização (ÉPICO COMPLETO)** (mig 116; `dev-local`). `produto_complemento` (produto↔complemento N:N, ordem) + marcadores `complemento_grupo.origem_complemento_id`/`complemento_opcao.origem_opcao_id`. **Materialização** (`materializarProduto`): regenera os grupos/opções do MOTOR per-product (`complemento_grupo`/`complemento_opcao`, já lidos por `menu()`/comanda/KDS) a partir dos complementos reutilizáveis ligados ao produto — mexe **só** nos grupos com `origem_complemento_id` (grupos manuais do editor antigo ficam intactos); mapeia opção `insumo`→`itemId`, `simples`/`ficha`→`produtoRefId`. **Re-materializa** ao editar complemento (`reMaterializarPorComplemento`) e ao editar opção (`reMaterializarPorOpcao`) → propagação de verdade. Endpoints `/produtos/:id/complementos-catalogo` (GET/PUT). Front: `produto-etapas-card` (anexa complementos por checkbox, salva na hora) no editar; **canais balcão/cardápio já existiam** no form; `/produtos` ganhou **abas Produtos/Complementos/Opções** (base da tela "Produtos e Catálogo"). Front tsc+build + back build + 91 testes OK. | Etapas reutilizáveis viram etapas reais no cardápio/PDV via materialização, sem quebrar o motor |
| 2026-07-17 | **Shell viewport-fixo + reorg do gestor de cardápio (layout)** (`dev-local`). **Shell (global):** `.shell` passou de `min-height` para **`height:100dvh; overflow:hidden`**; `.shell-content` vira flex-column e `.shell-content > main` ganha `flex:1; overflow-y:auto` → **o cabeçalho fica estático e só o `<main>` rola**; o **menu lateral** (`nav flex-1 overflow-y-auto`) rola sozinho quando é grande. Antes a página inteira rolava (cabeçalho sumia = sensação amadora). **`/produtos`:** largura `max-w-3xl`→**`max-w-6xl`** (usa mais o espaço); layout vira `flex h-full` com **abas/sidebar de categorias fixos e a lista rolando sozinha** (`lg:overflow-y-auto` por coluna); auto-pausa virou toggle enxuto na barra de ações. Front tsc+build OK. | Chrome do app estático + só o conteúdo/lista rola (padrão de app, como a referência) |
| 2026-07-17 | **P1 — pedido online chega e é processado no edge** (sem migration; `dev-local`). `pedido_externo` virou **`ambos`** no sync (pedidos online descem por LWW). A **nuvem adia** a materialização quando a loja tem **edge ativo** (`EDGE_MODE`=false + `edge_heartbeat` do tenant nos últimos 3 min): `receberPedido` (cardápio) e `ingest` (marketplace/Open Delivery) deixam o pedido `novo` sem chamar `aceitar`. No **edge**, `delivery/edge-pedidos.processor.ts` (`@Interval` 15s, só `EDGE_MODE=true`) pega `pedido_externo` `novo`+`comanda_id IS NULL` e chama `aceitar` → comanda + produção + baixa de estoque **local** (KDS/garçom). Idempotente. | Pedido online cai na nuvem e o edge "puxa e joga localmente" — sem serviço novo (sync + processador) |
| 2026-07-17 | **P2 — complementos do produto sincronizam para o edge** (mig 117; `dev-local`). O motor `complemento_grupo`/`complemento_opcao` ganhou **`deleted_at`** (soft-delete) e virou **`desce`** no sync (o PDV/garçom local passa a ter as etapas). Como o sync só propaga exclusão por `deleted_at`, `materializarProduto` e `removerGrupo/removerOpcao` passaram a **soft-deletar** (regeração não deixa duplicata no edge); leituras filtram `deleted_at IS NULL` (`menu()`, editor per-produto), exceto `resolverOpcoes` (lookup por id do pedido). Tabelas reusáveis (opcao/complemento) ainda não sincronizam — edge recebe só o materializado (catálogo gerenciado na nuvem; **P3** decide). Back build + 91 testes OK. mig 117: `complemento_grupo/opcao.deleted_at` + índice parcial. | Edge tem os complementos p/ operar local; exclusão propaga sem duplicar |
| 2026-07-17 | **P3-parcial — catálogo sobe rápido para o cardápio online (híbrido, local-prioritário)** (sem migration; `dev-local`). Decisão do usuário: modo **híbrido** (local prioritário, nuvem = espelho p/ cardápio online + marketplaces). `produto` e `cardapio_config` viraram **`ambos`** no sync (edições/esgotado SOBEM por LWW — `updated_at` já bumpado); daemon `PUSH_TABLES` ganhou os dois. **Flash-sync** (`edge-flash-sync.service.ts`, `@Global`): push **imediato** do produto p/ a nuvem fora do ciclo (~30s), só no edge (`EDGE_MODE`+`CLOUD_API`+`SYNC_TOKEN`), best-effort — chamado no **esgotado automático**, na **edição** e no **reativar** → o cardápio online marca "esgotado" em segundos e **bloqueia novos pedidos**. Escopo: só produto+config (categoria/complementos reusáveis ficam p/ o P3 completo, que exige migration de `updated_at`). Back build + 91 testes OK. | Cardápio online reflete disponibilidade/estoque local em segundos, evitando pedidos de item esgotado |
| 2026-07-17 | **PIX online via Iugu (Modelo A — conta própria do lojista, BYO)** (mig 119; `dev-local`). Espelha o Mercado Pago: novo `common/iugu.ts` (`criarPixIugu`/`consultarFaturaIugu`, auth Basic base64(token:), invoice `payable_with:['pix']`). `cardapio.service`: `resolveGateway` escolhe o provedor ativo — **prioridade Iugu → Mercado Pago → mock** (sem regressão p/ quem usa MP); `pagar()` ramifica; `webhookIugu` re-consulta a fatura (não confia no corpo). Novo endpoint público `POST pagamento/iugu/webhook`. `pedido_externo.gateway_provider` (mig 119) diz ao webhook qual API consultar. Canal `iugu` adicionado à lista de integrações (card só-token no config-panel, com URL do webhook e onde pegar a Live API Token). Env fallback `IUGU_API_TOKEN`. Token é secret (não retorna no GET); tenant por correlação do `gateway_payment_id`. Back type-check + 91 testes OK. | Lojista recebe PIX direto na conta dele; Regem nunca toca no valor |
| 2026-07-18 | **Lote edge/onboarding/integrações** (mig 120; `dev-local`). **(a) Impressora local/USB**: `equipamento.conexao`+`dispositivo` (mig 120); worker do edge imprime ESC/POS cru na impressora do Windows por nome via spooler RAW (`raw-print.ps1`, winspool WritePrinter) — preserva negrito/fonte/corte; UI com seletor Rede×Local. **(b) Onboarding**: `register` passa a criar a unidade MATRIZ (corrige "não puxa loja no 1º login local") + campo endereço opcional. **(c) Rollback manual do edge**: `reverter.ps1` (restaura dist/web do último backup; banco só com `-ComBanco`) + tarefa `RegemEdgeRollback` + endpoint `edge/atualizacao/reverter` + botão no `/servidor` com confirmação; `atualizar.ps1` passa a backupar `web` e registrar a tarefa; msg "com a versão mais recente". **(d) Integrações**: tira Uber Eats, add Anota Aí + Keeta; add Mercado Pago como card BYO (antes só env) ao lado da Iugu. Back+front tsc + 91 testes OK. | Impressora USB no PDV; loja aparece no 1º login; reverter update problemático; MP/Iugu configuráveis por lojista |
| 2026-07-18 | **Proteção do appliance edge — Fase 1** (`dev-local`; runbook em `docs/seguranca-edge.md`). **Não vazar fonte/lógica:** `package.mjs` passa a remover `.map`/`.d.ts`/`.md` do pacote (sourcemaps reconstroem o TS) + **guard** que falha o build se algum vazar; comentários já saíam (`removeComments`). **Segredos:** JWT_SECRET confirmado único por loja; regra "nunca segredo mestre da nuvem no edge" documentada (P4 mídia deve ir por endpoint na nuvem); **ACL** do `.env.local` (SYSTEM+Admin) no instalador; **DPAPI opt-in** (`proteger-env.ps1` cifra segredos → `enc:`; `secure-env.ts`/`carregarEnvSeguro` decifra no boot, NO-OP sem `enc:`; `instalar-tudo.ps1 -ProtegerSegredos`). **Clone inerte:** auditado — lease preso a CNPJ+fingerprint na 1ª ativação + `LicenseInterceptor` corta escrita sem lease. **Ofuscação descartada** (quebra DI do Nest). Back build + 91 testes OK; round-trip DPAPI validado. | Menos superfície de clonagem/leitura no PC da loja, sem atrapalhar o funcionamento |
| 2026-07-18 | **Frente A — telemetria de erro local → nuvem** (mig 122; `dev-local`). Tabela `telemetria_evento` (dedup por hash, ocorrências). `edge.service.registrarTelemetria` (upsert + alerta `DIST_ALERT_WEBHOOK` na 1ª ocorrência/`fatal`) + `listarTelemetria`. Endpoints: `POST /edge/telemetria` (SyncTokenGuard → tenant seguro) e `GET /edge/telemetria` (C&O vê os erros do seu edge). **Coletores no edge:** `TelemetriaInterceptor` (APP_INTERCEPTOR, só edge, posta 5xx e re-lança sem mudar a resposta); `sync-daemon.mjs` posta erros de sync (dedup 5min) — pega erros de DB tipo coluna faltando; `atualizar.ps1` posta falha de update com x-sync-token. A distribuição é alertada de erros locais → repara → publica update. Back build + 91 testes OK. | Ver erros locais das lojas e agir rápido (reparo + update) |
| 2026-07-18 | **Fase 1 (segredos) 100% + `@cloud-only`** (`dev-local`). **P4 mídia via nuvem:** o edge não usa mais a service key do Supabase — `MidiaReconcileProcessor` posta a imagem local em `POST /midia/edge/upload` (SyncTokenGuard) e a NUVEM sobe ao Supabase; edge só precisa de CLOUD_API+SYNC_TOKEN (`temCloud`). **`@cloud-only`:** `apply-all-local` pula migrations marcadas quando `EDGE_MODE=true` (tabelas da distribuição, ex.: `telemetria_evento` mig 122, não são criadas no banco da loja). Back build + 91 testes OK. | Nenhum segredo mestre da nuvem no edge; dados da distribuição não descem à loja |
| 2026-07-18 | **Console da Distribuição — Fase 1 (auth + perfis)** (mig 123 `@cloud-only`; `dev-local`). Realm de auth SEPARADO das lojas: `usuario_distribuicao` + `distribuicao_auditoria`; módulo `distribuicao` (login com secret próprio `DIST_JWT_SECRET`/escopo `distribuicao`, `DistribuicaoGuard`+`PerfilDistGuard`+`@PerfilDist`, auditoria append-only, bootstrap do 1º diretoria por `DIST_BOOTSTRAP_SECRET`). Front `/distribuicao/login` + `/distribuicao` (home, gestão de usuários p/ diretoria + placeholders). Decisões: rota no Next atual (não app à parte); sem MFA nesta fase. Isento no LicenseInterceptor. Smoke-test ponta a ponta OK; back build+91 testes + front tsc OK. | Base do controle da distribuição (frota/telemetria/licenças/financeiro nas próximas fases) |
| 2026-08-08 | **Design: sync espelhado + modos Local/Nuvem** (doc `sync-espelho-e-modos.md`; **ainda não codado** — plano por fases). **Prioridade LOCAL** (80–90% da operação no app do edge; impressão/equipamentos são locais); **nuvem = presidente** (visualização/config/lançamento de emergência; acessar turno aberto por outro = só presidente). Sync passa a **ESPELHAR**: transacional (`comanda(_item)`, `caixa_sessao`, `producao_pedido(_item)`, `lancamento_caixa`, `movimento_estoque`) vira `ambos`/desce; **pull inicial completo**; **deltas bidirecionais a 60s** (era 30s); janela **60d** p/ transacional pesado (configurável no menu **Financeiro** via `empresa.mirror_dias`; só afeta o que o LOCAL puxa — a nuvem mantém histórico integral); **LWW+exceção** (comanda `fechada`/`paga` local vence delta velho). **RBAC de modo:** `colaborador.pode_nuvem` (default por perfil; só presidente edita) barra login na nuvem p/ execução/gerente sem liberação. **Impressão nuvem→edge:** `producao_pedido` desce, job carrega o **papel** (caixa/cupom/produção-setor), edge **re-resolve** a impressora local (nuvem não conhece a USB) — conserta "Comanda não encontrada" e permite a venda online sair no caixa da loja. Fases S1–S5. | Espelho robusto nuvem↔local; presidente opera online sem quebrar o local; retirada/impressão corretas |
| 2026-08-13 | **Segurança — assinatura do webhook MP por LOJA** (sem migration). A verificação da assinatura do webhook do Mercado Pago deixou de depender do env global `MP_WEBHOOK_SECRET` e passou a ser **config da loja** (BYO): guardada na coluna `integracao.client_secret` do canal `mercadopago`, cadastrável em Delivery · Integrações → Mercado Pago (campo "Assinatura secreta do webhook — opcional"). O `webhookMercadoPago` **correlaciona o pedido primeiro** (descobre o tenant pelo `gateway_payment_id`), depois valida a assinatura com o segredo **daquele** tenant; sem segredo cadastrado, mantém só a re-consulta ao gateway. Env vira override global. | Cada loja tem a conta MP dela — o segredo é dela, não da plataforma |
| 2026-08-13 | **Segurança — SSL `verify-full` condicional na conexão Postgres** (sem migration). [`drizzle.module.ts`] passou a decidir o TLS por `DB_SSL` (`disable`/`require`/`verify-full`), com **inferência pelo host**: `*.supabase.co`/`*.pooler.supabase.com` → `verify-full` (padrão nuvem); `localhost`/IP → `disable` (**edge intacto**, Postgres local sem CA). O `verify-full` confia na **CA da Supabase embutida** ([`supabase-ca.ts`], cert público) **＋ as raízes públicas do sistema** (`tls.rootCertificates`) — valida tanto a conexão direta (self-signed Supabase) quanto o pooler (cert de CA pública), e rejeita MITM. Escape hatch `DB_SSL=require`/`disable`. | Fechar downgrade/MITM na conexão com o banco sem quebrar o edge |
| 2026-08-13 | **Segurança — RLS por tenant (mecanismo entregue, INERTE)** (mig 191 `@cloud-only`; doc [`rls-multitenant.md`]). Defesa em profundidade: se o código esquecer um `WHERE tenant_id`, o **banco** recusa linhas de outro tenant. **Gate `RLS_ENABLED` (default false → passthrough puro).** Ligado: interceptor mais externo abre 1 transação/request, fixa `set_config('app.tenant', …, true)` (LOCAL — exigido pelo pooler em modo transação) e um **proxy do DRIZZLE** roteia todo `this.db.*` para essa transação → **zero mudança nos serviços**. Migration 191 cria policy `tenant_isolation TO regem_rls` (tabelas com `tenant_id`) e `app_global_read TO regem_rls` (globais) — escopadas ao role, então a API anônima segue bloqueada; **inertes** sob `regem_app` (BYPASSRLS). Helper `comTenant()` para jobs/pollers e rotas públicas (token→tenant). Ativação = runbook encenado (criar role `regem_rls` sem bypass → aplicar 191 → trocar `DATABASE_URL` + `RLS_ENABLED=true`), com rollback de 1 clique. Fases 0–5 no código; cutover é operação de nuvem do usuário. | Isolar tenant no nível do banco sem reescrever centenas de queries nem arriscar produção no merge |
| 2026-08-13 | **Segurança — auditoria de cadastros/estoque + RBAC no pareamento** (#310, sem migration). Trilha imutável (append-only + hash-chain) nas mutações que não gravavam quem/perfil/ação: estoque (`item_criado/editado`, `movimento_manual`), colaborador (`criado/editado/removido`), unidade, funcao, setor. Ator threaded do `@CurrentUser`; `registrar()` engole erro e nunca quebra a operação. RBAC: `POST /equipamento/parear` ganhou `@Roles('presidente','gerente')` + `@RequirePerm('servidor')` (era chamável por qualquer autenticado). | Fechar lacunas da varredura de cobertura (auditoria concentrava as lacunas; RBAC quase 100%) |
| 2026-08-13 | **Segurança — sessão em cookie httpOnly, Fase A** (doc [`auth-cookie-migracao.md`], sem migration). Tira o JWT do `localStorage` (XSS) para um cookie httpOnly na NUVEM; edge/integrações seguem no Bearer. **Fase A (aditiva, zero mudança de comportamento):** `login`/`register` também setam `regem_sess` (httpOnly, SameSite=Lax, Secure em prod, `Domain` via `COOKIE_DOMAIN`); guard aceita Bearer OU cookie; `POST /auth/logout` + `GET /auth/me`; CORS `credentials:true` na lista fixa da nuvem; front manda `credentials:'include'` na nuvem, `estabelecerSessao()` vira a costura única do login e `sair()` apaga o cookie no servidor. O Bearer segue conduzindo → cookie fica estabelecido e VERIFICÁVEL antes do cutover. **Fase B (próxima):** separar `getToken()` (presença) de `getJwt()` (token real) p/ os 53 gates seguirem intactos, socket lê o cookie no handshake, e `estabelecerSessao` vira cookie-only na nuvem. | Mitigar roubo de sessão por XSS; faseado porque o front decodifica o JWT e checa presença em 53 telas + socket — cutover não-testável localmente não pode ir de uma vez |
| 2026-08-13 | **Segurança — sessão em cookie httpOnly, Fase B (cutover cookie-only, AUTO-PROTEGIDO)** (doc [`auth-cookie-migracao.md`], sem migration). Na NUVEM o JWT sai do `localStorage`: no login, sonda `GET /auth/me` sem Bearer — se o cookie `regem_sess` funciona, o token **não** é guardado (fica só no cookie httpOnly → XSS não rouba a sessão) e a identidade vai p/ `regem_me` (só-exibição); se falhar, cai no Bearer (sem lockout). Separou `getJwt()` (token real: decode/Bearer/socket) de `getToken()` (presença = `getJwt() ?? (lerMe()?'cookie':null)`) → os 53 gates `if(!getToken())` ficam intactos. `uploadFile`/`pontoEspelhoPdfUrl` passam a `getJwt()`+`credentials`. Socket: `rt.ts` manda `withCredentials` e jwt-se-houver; `RealtimeGateway` lê `auth.jwt` OU o cookie do handshake (+CORS WS `credentials:true` na lista fixa). Edge/terminal/distribuição inalterados (Bearer). | Concluir a mitigação de roubo de sessão por XSS; auto-protegido (só vira cookie-only quando o cookie comprovadamente vai) para não arriscar produção |
| 2026-08-26 | **WhatsApp — provedor POR LOJA e API oficial da Meta (Cloud API), F2a** (mig 214, **NÃO** `@cloud-only`: `cardapio_config` existe no edge). O Regem passa a falar WhatsApp por dois caminhos — **Evolution** (não oficial, QR) e **Cloud API** (oficial) — e cada loja escolhe o seu em `cardapio_config.provedor` (default `evolution`, então **nenhuma loja existente muda de comportamento**). **`wa_cloud_phone_id` é a chave multi-tenant do lado da Meta:** a URL de callback do webhook é **uma só** para todas as lojas, e quem desambigua é o `metadata.phone_number_id` do payload — mesmo papel que `evolution_instancia` já faz. Índice único **parcial** impede duas lojas reivindicarem o mesmo número (uma leria as conversas da outra). **Recebimento** ([`whatsapp-cloud.service`]): resolve a loja, aplica 3 portões — provedor correto (evita **resposta duplicada** numa migração meio-feita), robô/loja ativos, conversa não pausada — e encaminha um **payload normalizado** ao n8n (`N8N_BOT_CLOUD_WEBHOOK_URL`; vazio = inerte). Responde 200 **antes** de processar: a Meta tem timeout curto e desativa a assinatura de quem demora. **Envio:** `POST /publico/bot/cloud/enviar` (auth `BOT_RESOLVER_SECRET`, loja pelo `phoneNumberId`) — **dois workflows separados** no n8n (um por provedor), então quem chega no endpoint da Cloud já é Cloud, sem roteamento. Nada do Evolution foi alterado. 9 testes novos (101 no total). | Migrar loja a loja para a API oficial sem downtime e sem resposta em dobro; o oficial não substitui o Evolution, convive |
| 2026-08-27 | **WhatsApp — escolha do provedor pela loja, com aceite auditado (F2c)** (sem migration; usa as colunas da mig 214). Em Delivery · Config · Robô, dois cards: **Conexão por QR Code** (grátis, não oficial) e **API oficial da Meta** (paga pelo lojista direto à Meta). **O texto do termo mora no BACKEND** ([`whatsapp-provedor.service`], `TERMO_PROVEDOR` versionado) e é servido pelo mesmo endpoint que a tela consome — o gestor lê exatamente o que será gravado; duplicar o texto no front deixaria os dois divergirem num deploy e o aceite não provaria nada. O aceite grava na **auditoria imutável** o texto INTEIRO (não só a versão), com quem, perfil e horário. Versão do termo é conferida no POST: aba aberta há dias com texto velho é recusada. **Sem sobreposição:** trocar de provedor exige desvincular o atual (dois ativos = cliente recebe resposta em dobro). `DELETE /whatsapp/cloud/vinculo` é o equivalente ao "Desconectar" do Evolution. RBAC presidente/gerente + `@RequirePerm(bot)`; leitura também para supervisão. No edge as rotas são @CloudOnly → o bloco simplesmente não renderiza e o fluxo do QR segue igual. | Vender os dois modelos com o risco de cada um por escrito e aceito — é o aceite, não o aviso na tela, que protege quando um lojista tiver o número banido |
| 2026-08-27 | **WhatsApp — histórico, modelos e conferência do número (F2b + F2d + Fase 3 parcial)** (mig 215 `whatsapp_mensagem` **@cloud-only**). **F2b:** a Meta NÃO guarda histórico — entrega o evento uma vez e esquece — então sem tabela própria a loja migrada perderia o chat do `/delivery`. Gravamos as duas direções; `wamid` com único por tenant é **idempotência real** (a Meta REENVIA o webhook quando não recebe 200 a tempo). **A entrada é gravada ANTES dos portões do robô**: o painel precisa mostrar o que o cliente disse justamente quando o robô está pausado e um humano atende. Leitura reproduz o **contrato exato do Evolution** (`telefone/jids/nome/naoLidas/ultimaMensagem/timestamp/pausada`), e o despacho por provedor vive no `whatsapp-provedor.service` → **o front não mudou uma linha**. "Não lidas" = mensagens do cliente depois da nossa última resposta (quem está esperando), não o contador do WhatsApp. **F2d:** `enviarTemplate` — fora da janela de 24h a Meta só aceita modelo aprovado, e é o que faz o aviso de pedido existir; `POST /publico/bot/cloud/template` para o workflow de avisos e `GET /whatsapp/cloud/templates` para ver o que já foi aprovado. **Campanha com texto livre é BARRADA na criação** quando a loja está na Cloud (melhor que rodar e falhar contato a contato, deixando o lojista achar que o número foi bloqueado). **Fase 3 (parcial):** botão "Conferir na Meta" valida o Phone Number ID antes de vincular e avisa se já pertence a outra loja — sem isso, um id digitado errado é aceito em silêncio e só aparece quando a mensagem não chega. Descobrir automaticamente as WABAs dos lojistas continua **bloqueado no App Review** (exige acesso avançado). 116 testes. | Migrar loja real sem perder o chat do painel; enviar aviso de pedido no único formato que a Meta aceita |
| 2026-08-27 | **Cardápio público ~3x mais rápido: consultas agrupadas por dependência** (sem migration). Medido em produção: endpoint SEM banco 0,19–0,59s; o cardápio 1,88–2,07s. **A causa não era query lenta nem N+1** (conferido: os helpers usam consultas set-based) e sim **~14 idas e voltas SEQUENCIAIS** ao Postgres, que está em `us-west-2` (Oregon) — ~115ms por viagem. As mesmas consultas foram agrupadas em **3 níveis de dependência** com `Promise.all`: nível 1 (só precisa do tenant: categorias, produtos, regras de sinal, formas de pagamento), nível 2 (precisa dos ids: atacado, esgotados, grupos, opções, variações), nível 3 (precisa de grupos/opções: regras, imagens). `computeEsgotados` também passou de 3 consultas sequenciais para 1 `Promise.all`. **`Promise.all` e não disparar solto para aguardar depois**: no Node 22 uma promessa rejeitada que ninguém aguardou derruba o processo. O guard `grupos.length` de `opcoes` foi preservado APÓS a consulta, para o resultado ficar idêntico. O trabalho total no banco é o mesmo — muda só a concorrência; pool do `pg` segue no padrão (10). | Cardápio lento é carrinho abandonado, e o gargalo era viagem, não banco |
| 2026-08-27 | **Cardápio: cache curto do estoque + índice de cobertura** (mig 216, NÃO @cloud-only). Depois de paralelizar as consultas (#382), o cardápio caiu de ~1,95s para ~1,19s — e sobraram ~450ms que **não eram trajeto**: com a viagem amortizada, o nível paralelo passa a demorar o que demora a consulta mais lenta. Era a soma de saldo, que **agrega todo o histórico de movimentações da loja, sem filtro de data**, a cada abertura do cardápio. **(A) Cache por loja** dos MAPAS de estoque (não do conjunto final de esgotados — assim a resolução produto→insumo continua exata para qualquer lista), TTL 30s ajustável por `CARDAPIO_ESTOQUE_TTL_MS` (0 desliga), com varredura de expirados acima de 100 lojas e `invalidarEstoque(tenantId)` para quem quiser refletir na hora. **A defasagem já existia e era MAIOR**: o cliente vê o cardápio no load e leva minutos montando o pedido, e o checkout não revalida — o cache estende em segundos uma janela que já era de minutos. **(B) Índice de cobertura** para o Postgres responder a soma sem tocar no heap; ajuda também o módulo de estoque do edge, que faz a mesma consulta. **Nenhum dos dois resolve a causa** — a soma continua proporcional ao histórico; o que resolve de vez é **saldo materializado por item, via trigger** (não pelo código: um caminho de escrita esquecido vira saldo errado silencioso), decidido para quando o volume justificar. 6 testes novos (122). | Tirar do caminho quente uma consulta que cresce para sempre, sem mexer no módulo de estoque ainda |
| 2026-09-14 | **FATURAMENTO passa a ter uma definição ÚNICA, e a gorjeta fica FORA dela** (sem migration; `backend/src/common/faturamento.ts`). Definição do dono: *"total de venda de produtos ou serviços; as taxas que são serviços integram o faturamento"* — e, na sequência, *"gorjeta não entra no faturamento"*. Logo: **entra** produto vendido + taxa de entrega quando é da loja + taxas de serviço cobradas no pedido; **não entra** gorjeta (repasse ao funcionário, Lei 13.419/2017) nem taxa da plataforma (receita dela); **reduz** só o desconto bancado pela LOJA — o do marketplace não reduz, porque a loja recebe cheio no repasse. O que isso corrigiu: (a) **`comanda.total` é gravado como `subtotal × (1 + taxa_servico_pct/100)`**, então TODO relatório que somava `comanda.total` contava a gorjeta do garçom como receita da empresa — Vendas, Curva ABC, atendentes, faturamento por mês, painel e Visão C&O; (b) o painel mostrava "Faturamento hoje" (`comanda.total`) e "Delivery faturado" (`pedido_externo.total`) como se fossem bases iguais, quando o delivery **já estava dentro** do primeiro e os dois números vinham de definições diferentes — agora delivery é um RECORTE do total; (c) a Visão C&O somava só comandas, deixando a taxa de entrega da loja de fora; (d) **o desconto de FRETE bancado pela loja estava sendo subtraído DUAS vezes** no 99food (a `taxa_entrega` já chega líquida — o canal grava `delivery_price`, que é a taxa *após* a promoção): a exclusão do alvo `DELIVERY_FEE` fecha a diferença de R$ 1.090,50 que aparecia na conferência. O caixa **não muda**: a gaveta continua recebendo o valor cheio e fechando por ele; a gorjeta agora aparece em linha própria no cupom do turno, no painel, na Visão C&O e na Conferência. Pedido anterior à mig 241 cai no `total` antigo em vez de virar zero, e a **cobertura** (% de pedidos com o detalhe por origem) é exposta na tela — misturar as duas bases em silêncio é o que faz relatório mentir. 27 testes travam a definição. | Três telas davam três faturamentos para o mesmo dia; e chamar de receita um dinheiro que é do garçom infla margem e imposto |
| 2026-09-14 | **NFC-e passa a declarar frete e desconto — rateados por item** (sem migration). `vFrete` e `vDesc` saíam **sempre `0.00`**, mesmo em pedido com entrega cobrada e cupom: a nota declarava valor maior do que a operação. Não bastava preencher o total — a SEFAZ valida que `total/ICMSTot/vDesc` é o **somatório** dos `det/prod/vDesc` (W16-10) e o mesmo para `vFrete` (W14-10); declarar só no total **rejeita** a nota. Por isso o rateio é proporcional ao valor da linha e feito em **CENTAVOS** (`common/rateio.ts`, método do maior resto): em float a soma não fecha e um centavo derruba a autorização. `vNF` e `vPag` (YA09) acompanham, `modFrete` vira `0` quando há frete, e a base de PIS/COFINS passa a ser a líquida (`vProd − vDesc + vFrete`) — usar o bruto inflava o imposto de quem tributa. Entram só o frete **da loja** (`taxa_entrega_dono = 'loja'`) e o desconto que a **loja** bancou: o desconto do marketplace não é desconto fiscal, a loja recebe o valor cheio, e o desconto de frete fica fora porque a taxa já chega líquida. Base sem as colunas da mig 241 cai no comportamento antigo em vez de a nota parar de sair. 12 testes de invariante fiscal. | Nota com valor diferente da operação é exposição fiscal, e o rateio por item é o que a SEFAZ exige para aceitar |
| 2026-09-14 | **Margem por produto passa a descontar o que a loja bancou** (sem migration). A Curva ABC somava `quantidade × preço_unitário` do item — preço **cheio** —, então produto vendido só em promoção aparecia como o mais lucrativo do cardápio. Agora o desconto da loja é **rateado nos itens da comanda**, proporcional ao valor da linha, numa única consulta set-based com window function; a tela mostra `preço cheio → desconto → faturamento`. Só o desconto da LOJA rateia: o do marketplace volta no repasse, então a loja recebeu mesmo o preço cheio por aquele item. O desconto de frete não rateia em produto. | Margem calculada sobre preço cheio é a que faz promoção parecer lucrativa |
| 2026-09-14 | **O canal PRÓPRIO (cardápio Regem) passa a gravar o detalhe por origem** (sem migration; usa as colunas da mig 241). Os marketplaces mandam o detalhe pronto no payload e o `adaptarGenerico` não tinha nada para repassar — resultado: **justamente o canal onde a loja mais dá desconto** (cupom, resgate de fidelidade, cashback, frete grátis) era o único a aparecer com desconto **zero** em todos os relatórios, e a margem dos seus produtos ficava inflada. O checkout agora monta os `descontos[]` na hora, quebrados por origem, e o valor final é **rateado entre as parcelas** para a soma do detalhe sempre fechar com o total gravado (a trava contábil limita o desconto ao subtotal). Frete grátis entra com alvo `DELIVERY_FEE`, igual aos marketplaces. | Sem isso a conferência comparava marketplace contra um canal próprio que reportava desconto zero |
| 2026-09-14 | **"A receber" do entregador sai do detalhe de pagamento** (sem migration; `entregador/cobranca.ts`). O app mostrava `total` sempre que `pago` era false, e isso errava em dois casos caros: pedido de marketplace **pago online** cujo `pago` não chegou `true` → o entregador **cobrava de novo** de quem já tinha pagado; e **pagamento dividido** (parte no PIX, parte em dinheiro), em que um booleano não tem como estar certo. Agora soma só o que **não** é pré-pago em `pagamentos[]` (mig 241), com `pago` e o total antigo como reserva — todos os caminhos só **reduzem** o valor cobrado, então na dúvida o entregador pede menos, nunca duas vezes. Vai junto o troco a levar e quanto já foi pago online (sem essa linha, "A receber: R$ 40" num pedido de R$ 100 parece defeito). 13 testes. | Cobrar duas vezes na porta do cliente é o erro mais caro que o app pode cometer |
| 2026-09-15 | **Estoque no edge: a cadeia da ficha passa a sincronizar e o ledger deixa de ser janelado** (mig 242, **NÃO** `@cloud-only`). Dois defeitos que, juntos, faziam o módulo de estoque **não funcionar** no servidor local. **(1)** `ficha_ingrediente`, `produto_variacao` e `produto_combo_item` **nunca estiveram** em `TABELAS_SYNC`. A explosão (`acumularProduto`/`acumularFicha`) lê exatamente essas três: no edge a ficha descia VAZIA, `lancarSaidas` não inseria movimento nenhum e — porque `consumo.size === 0` devolve `disponivel: null`, tratado como **ilimitado** — a loja vendia sem baixar estoque, com CMV zero e sem nada esgotar. **Adicionar à whitelist não bastava:** o sync é delta POR LINHA e as três não tinham cursor de mudança nem marca de exclusão; como a ficha é salva por `delete + insert`, o edge receberia as linhas novas e **ficaria com as antigas para sempre**, trocando "baixa zero" por "baixa duplicada" — pior de diagnosticar. A mig 242 dá às três o mesmo contrato das demais tabelas sincronizadas (`updated_at` + gatilho como cursor, `deleted_at` para a exclusão propagar, padrão do projeto desde a mig 118) e os serviços passaram a **soft-delete** com leitura filtrando `deleted_at is null`. **Lookup POR ID continua sem filtro de propósito** (preço da variação já escolhida): um pedido em aberto não pode quebrar porque a variação foi apagada depois — é justamente o que o soft-delete preserva e o delete físico destruía. **(2)** `movimento_estoque` estava em `TABELAS_JANELA_MIRROR` junto com comanda/caixa. Para transacional de EVENTO a janela é inócua; para o LEDGER não: **o saldo é a soma de todo o ledger** (`estoque.service` e `vendas.saldoItem`) e não há saldo materializado, então o edge nascia com saldo errado pelo tamanho do histórico cortado — no banco de desenvolvimento, **505 dos 509 movimentos** estavam fora da janela de 60 dias. Em cadeia: alerta de reposição do catálogo inteiro, disponibilidade de atacado zerada e snapshot diário contaminado. Removido da janela — vale para o pull contínuo **e** para o snapshot do restore, que usa o mesmo conjunto. **(3)** `TABELAS_DESDE_ZERO`: tabela adicionada ao sync depois cairia no **piso global** do cursor, que num edge já instalado está em "agora" — o histórico nunca desceria e a loja seguiria sem baixar estoque até alguém reeditar cada ficha. Enquanto não houver cursor próprio da tabela, o delta começa do zero; no ciclo seguinte o edge grava o seu e a exceção deixa de valer sozinha. Isso **não rebobina cursor existente**, então o `sync-daemon` ganhou `repararCursores` (marca de uma vez) para o ledger. Conferido ponta a ponta no banco real: edge com cursor em "agora" recebe os ingredientes de 200 dias atrás, o ciclo seguinte não repete, a exclusão desce com `deleted_at`, a explosão passa a contar 3 em vez de 5, e a janela de 60 dias segue intacta para comanda. 18 testes travam a config. | Três tabelas fora de uma whitelist faziam a loja vender sem baixar estoque, e nada no sistema reclamava |
| 2026-09-15 | **Documentos de estoque passam a sincronizar entre o servidor local e a nuvem** (mig 243, **NÃO** `@cloud-only`). `recebimento(_item)`, `lote`, `desperdicio`, `contagem_*` (4), `compra_lista/_item` e `titulo_financeiro` **nunca estiveram** em `TABELAS_SYNC`, embora os módulos que as escrevem (Recebimento, Contagem, Compras, Desperdício) sejam `EDGE_CORE`. O `movimento_estoque` subia, então o SALDO batia na nuvem — mas a **origem sumia**: entrava estoque sem dizer de qual nota, de qual contagem, de qual perda. Pior em `titulo_financeiro`: `recebimento.confirmar()` cria a **conta a pagar ao fornecedor**, e ela nunca chegava ao Financeiro — quem abria a tela não via a dívida. Para poder sincronizar faltava o contrato do transacional: `updated_at` + **gatilho que bumpa em todo UPDATE**. Das 11, **10 não tinham gatilho**, 5 não tinham `updated_at` e 2 não tinham timestamp algum. Sem o bump, a MUDANÇA DE ESTADO não propaga — é a razão já escrita na mig 095 ("sem isso, o append-only só capturaria a criação"): `contagem_execucao` aberta→fechada e `titulo_financeiro` aberto→**pago** ficariam invisíveis do outro lado, e a nuvem mostraria a conta em aberto para sempre depois de a loja pagar. Direção `'ambos'` (LWW): o documento nasce dos dois lados. **Nenhuma sofre DELETE físico** (conferido), então não precisam de `deleted_at`. 🔑 **Descoberta estrutural:** a lista de push do edge é **HARDCODED** em `sync-daemon.mjs` (`PUSH_TABLES`), **separada** da whitelist do backend — foi assim que 11 tabelas ficaram de fora sem nada falhar, porque nada quebra quando as duas divergem: o dado só para de subir. Um teste novo lê o daemon e quebra o CI se as listas voltarem a divergir. Consequência prática: o **pull** (nuvem→edge) funciona só com autodeploy; o **push** (edge→nuvem) exige o `.zip`. Conferido no banco real: documento de 200 dias atrás desce para edge com cursor em "agora", ordem do payload é segura para FK, e título pago / contagem fechada / recebimento confirmado descem com o status NOVO. 38 testes de config. | O saldo batia e o documento sumia — e a dívida com o fornecedor ficava presa no servidor da loja |
| 2026-09-15 | **Idempotência do estoque: recebimento, compra e contagem param de duplicar** (sem migration). Três operações que mexem em saldo — e duas que mexem em DINHEIRO — aceitavam ser executadas duas vezes. `compras.receber()` não tinha transação nenhuma (cada insert solto: falha no meio do laço deixava metade dos itens no estoque com a lista ainda pendente) e `contagem.salvarContagem()` idem, além de não ter guarda de status. `recebimento.confirmar()` **tinha** transação — a auditoria errou aqui —, mas o problema era outro: **check-then-act**. Lia o status, e só depois atualizava; dois cliques simultâneos liam 'rascunho' ao mesmo tempo, os dois passavam pela guarda, e entravam em dobro o estoque **e a conta a pagar ao fornecedor**. Correção em três camadas: (1) `select … for update` trava a linha antes de ler o status — o segundo espera, relê a versão nova e cai no "já confirmado"; (2) transação envolvendo tudo; (3) 🔑 **o `ref` no movimento**: o índice único `(tenant_id, ref_tipo, ref_id, item_id)` da **mig 024 já existia desde sempre** e nenhum dos três o usava — agora o próprio banco recusa a segunda entrada (23505), independente de trava. **O ref aponta a LINHA, não o documento**: usando o id do recebimento, duas linhas do MESMO item na mesma nota colidiriam entre si. Fecha também a auditoria ausente (`contagem` recebia `atorId` e ignorava, apesar de ajustar saldo) e o duplo clique no front, que era a primeira linha de defesa. Conferido com **execução concorrente real** no banco: de 2 chamadas simultâneas exatamente 1 passa, 1 movimento, 1 título. | Duas operações duplicavam dinheiro, e o mecanismo que impedia isso estava no banco desde a mig 024 sem ninguém usar |
| 2026-09-15 | **Contagem avisa o que se moveu durante a janela** (sem migration; opção 3 de 3, decidida com o dono). A auditoria apontou que o ajuste é calculado contra o saldo da ABERTURA e que isso "desconta duas vezes o que vendeu durante a contagem". **A matemática mostra que não é tão simples:** com saldo 100 na abertura e 20 vendidos durante a contagem, se o operador contou ANTES das vendas o ajuste pela abertura acerta (0 → saldo final 80) e pelo fechamento erra; se contou DEPOIS, inverte. **Nenhuma das duas bases está certa sozinha** — o que decide é QUANDO o item foi fisicamente contado, e o sistema não sabe. Em vez de trocar uma base errada por outra em silêncio, a contagem passa a EXPOR o movimento: `getExecucao` devolve, por item, o movimento líquido e o nº de movimentos desde a abertura (o ajuste da própria contagem é excluído, senão ele se auto-contaria); a tela marca o item enquanto se conta e mostra faixa no topo; ao salvar, a resposta traz os `suspeitos` (item, movimento, ajuste lançado), o toast avisa, o alerta do KDS sobe para prioridade média com "confira", e a auditoria guarda a lista. O ajuste segue lançado contra o snapshot da abertura — que é a base que o operador viu na tela. Decisão do dono: opção 3 agora, **opção 2 (carimbar a hora de cada item contado e calcular o saldo naquele instante) em seguida**. | Trocar uma base errada por outra em silêncio não resolveria; quem sabe a hora em que contou é o operador, e agora ele é avisado |
| 2026-09-15 | **O ajuste da contagem passa a usar o saldo do INSTANTE em que cada item foi contado** (mig 244, **NÃO** `@cloud-only`; opção 2 de 3, decidida com o dono depois da opção 3). Contexto da operação real, descrito pelo dono: o inventário roda **durante o expediente**; **hambúrguer industrializado** sai a cada venda E fica em dois lugares (câmara frigorífica + freezer da chapa), então conta-se andando entre eles enquanto a chapa consome; **molho cheddar** só sai na PRODUÇÃO, que roda fora do horário, e não corre risco numa contagem diurna; e parte dos lojistas conta com o expediente parado. `contagem_item` ganha `contado_em`, o front carimba a hora **a cada digitação** (recontar depois do aviso move a base junto) e o servidor calcula `ajuste = contado − saldo(contado_em)` numa única consulta que pareia item × instante (um SELECT por item seriam N viagens numa contagem de 200). A mesma conta acerta os três casos: item que se move ganha a base do seu próprio momento; item parado tem base idêntica à da abertura; loja parada não tem movimento para diferenciar. 🔒 A hora vem do CLIENTE, então é **limitada ao intervalo [abertura, agora]** — relógio adiantado, atrasado ou adulterado não escolhe base de estoque. Sem hora (app antigo / digitação em lote) cai em "agora". O aviso da opção 3 continua, agora como rede de segurança em vez de muleta. Junto: o endpoint de salvar deixou de usar `@Body() dto: any` — com `any` o ValidationPipe global não valida nada e o DTO que já existia nunca era usado. Prova no banco com o cenário do dono: 100 hambúrgueres, 30 vendidos durante a contagem, operador conta 68 → **ajuste −2 (a perda real), não −32**; base usada 70, não 100. | Contar 68 depois de vender 30 lançava −32 e apagava do estoque o que tinha sido legitimamente vendido |
| 2026-09-15 | **Adicional e variação de canal externo passam a baixar estoque, pelo CÓDIGO PDV** (sem migration). `baixarEstoqueExterno` passava, LITERALMENTE, `[]` de complementos e `1` de fator de ficha para `acumularProduto` — todo adicional vendido no iFood/99food/Anota Aí/Cardápio Web saía do estoque sem registro, e a variação de 1L baixava a mesma ficha da de 500ml. A causa estava antes: **os 5 adaptadores faziam `.map((o) => o.name)`**, jogando fora o código da opção e guardando só o texto para o KDS. Regra do dono: *"nos canais externos os produtos são linkados pelo código PDV; caso não estejam linkados não terá como dar baixa ou até mesmo ter custo e associados a fichas técnicas"* — então **casar por NOME foi descartado** (era a minha proposta inicial; seria heurística onde já existe chave). Os adaptadores passam a devolver `complementosItens: {nome, codigo, quantidade}`, o `venderExterno` resolve contra `complemento_opcao.codigo_pdv` **escopado ao grupo DAQUELE produto** (código repetido em outro produto não pode baixar o insumo errado), persiste em `comanda_item_complemento` — a baixa do delivery só ocorre na CONCLUSÃO e relê dali — e a conclusão passa os complementos e o `fator_ficha` reais. A **variação** resolve pelo mesmo caminho: no marketplace cada tamanho é um SKU próprio, então o código do item pode apontar para `produto_variacao`, e daí sai o produto pai + o fator. Opção sem código, ou com código que não existe no catálogo, **não baixa** — e o motivo vai para o log com o nome do adicional, para o lojista saber o que linkar. Quantidade final = a da opção × a do canal ("2x bacon" numa opção de 2 fatias = 4). Conferido no banco: burger grande + 2× bacon → pão −2 (fator da variação), bacon −100 g. | A baixa do canal externo descartava adicional e variação em duas constantes literais, e o código que ligava as pontas era jogado fora no adaptador |
| 2026-09-16 | **Três erros silenciosos na baixa de estoque** (sem migration). Verificando as pendências da auditoria contra o código atual, três se confirmaram e têm a mesma assinatura: o estoque erra sem ninguém ver. **(1) `saldoItem` filtrava por `movimento_estoque.unidade_id` — coluna que NÃO EXISTE** (conferido no schema, nas 5 migrations que citam a tabela e no dump real; reproduzido no banco: 42703). Com unidade preenchida derrubava dois caminhos: `POST /vendas/atacado/preview` dava 500 (o front engolia no `catch` e o preview nunca aparecia — degradação silenciosa) e a venda de balcão com encomenda de atacado estourava DENTRO da transação, revertendo comanda, itens e caixa **com o dinheiro já cobrado**. O parâmetro `unidadeId` foi removido da cadeia inteira: quem carrega a unidade é `item_estoque`, e a query já filtra por `item_id` — manter um argumento que não faz nada é convite para o mesmo bug voltar. **(2) Três `.catch(() => {})` engoliam a falha de `baixarEstoqueExterno`**: o pedido concluía e o insumo não saía, sem uma linha de log. Agora registram o MOTIVO com pedido e comanda (o pedido segue concluindo — não dá para segurar a entrega por causa do estoque). **(3) `hojeISO()` devolvia a data em UTC** e alimentava `movimento_estoque.data`, `lancamento_caixa.data` e o ponto: no Brasil (UTC−3) toda operação a partir das 21h caía no DIA SEGUINTE. Havia **quatro cópias privadas** da função; uma (ponto.controller) já tinha sido corrigida para o fuso da operação e as outras três seguiram em UTC — o conserto ficou preso num arquivo. Virou `common/data.ts`, com teste de comportamento (21h30 em SP é o mesmo dia) e **guarda de deriva** que quebra o CI se alguém redefinir `hojeISO` fora dali. | O padrão do dia: o erro não estava na lógica, estava no silêncio — coluna inexistente engolida por catch, falha de baixa sem log, e um conserto que ficou preso numa cópia |
| 2026-09-16 | **Produto de revenda e combo passam a esgotar; o checkout respeita a auto-pausa** (sem migration). Três defeitos na mesma função, todos no caminho "produto sem estoque continua vendendo". **(1)** `computeEsgotados` espelhava só DOIS dos três ramos que a venda usa (`acumularProduto`): combo e ficha. Faltava o terceiro — **produto de REVENDA**, ligado direto a `produto.item_id` pela mig 148 (a lata de refrigerante). A venda baixava o item, mas o esgotamento não olhava para ele: o saldo ia a zero e o produto seguia vendendo para sempre, empurrando o estoque para negativo. O `comboMap` tinha o mesmo furo um nível abaixo (`if (!r.fichaId) continue` descartava o componente industrializado) e ainda ignorava `controla_estoque` do componente — podendo pausar o combo por um item que a venda nem consome — e o soft-delete da mig 242. Agora a função espelha os três ramos, componente por componente. **(2)** O **checkout público não validava `pausado_estoque`**: a trava era checada só na MONTAGEM do menu, então quem estava com a página aberta desde antes de esgotar — ou quem chama a API direto — passava batido e o pedido entrava. A auto-pausa era cosmética. Agora recusa nomeando o produto. **(3)** O alerta de esgotado dava **23505** a partir do segundo disparo: a mig 031 tem índice ÚNICO parcial `(tenant_id, tipo) where resolvido_em is null`, e o erro sumia no `.catch` do gatilho. Passa a ATUALIZAR o alerta aberto (o painel mostra os produtos de agora, não os da primeira vez) e só insere quando não há nenhum, com `onConflictDoNothing` para a corrida entre dois gatilhos. Conferido no banco com o cenário da lata: revenda e combo pausam, despausam sozinhos quando entra estoque, o segundo disparo não estoura, e o checkout recusa com "…está sem estoque no momento". | A auto-pausa existia para impedir a venda sem estoque — e o produto industrializado, que é o mais fácil de acabar, era justamente o que ela não enxergava |
| 2026-09-16 | **Ordem de produção: a conclusão deixa de dividir pelo rendimento duas vezes e de rodar sem trava** (sem migration). As quatro pendências da auditoria neste módulo foram conferidas contra o código atual e **as quatro se confirmaram** — uma delas pior do que estava classificada. **(1) A baixa saía `rendimento` vezes menor.** `multiplicadorFicha` convertia a quantidade da ordem dividindo por `rendimento`, e a explosão da ficha (§1.2, `qtdBaixaExplosao`) divide por `rendimento` de novo — as duas metades da mesma conta moram em arquivos diferentes e ninguém viu. Conferido no banco: uma ficha que rende 1000 ml e consome 500 g de insumo, concluída inteira, **baixava 0,5 g em vez de 500 g**. O multiplicador passa a converter para a UNIDADE DE RENDIMENTO, que é o que o `produzir()` entende: com porção, `qtd × porcaoTamanho` (10 porções de 100 ml = os 1000 ml da ficha); sem porção, a ordem é múltiplo da ficha (mig 130) e vira `qtd × rendimento`. A entrada do produto acabado muda de escala junto — concluir uma ficha de 1000 ml dá entrada de 1000, não de 1. ⚠️ **As conclusões já lançadas estão subdimensionadas**; a correção retroativa do ledger é decisão do dono. **(2) `concluir()` era check-then-act sem transação e sem trava**, e a idempotência do banco não existia na prática: **`ref_id` nunca era gravado na criação da ordem**, então `produzir()` sorteava um UUID novo a cada chamada e o índice único da mig 024 (`tenant_id, ref_tipo, ref_id, item_id`) nunca via duplicata. Reproduzido no banco com o pool aquecido: **3 conclusões simultâneas, 3 aceitas, 3 movimentos** — estoque baixado em triplo por um duplo-clique. Agora a linha é travada (`select … for update`), a guarda de status é reavaliada SOB a trava, o `ref_id` é o **próprio id da ordem** (determinístico, então o índice da mig 024 volta a ser a rede de segurança do banco) e `produzir()` aceita a transação do chamador — baixa de insumo e encerramento da ordem no MESMO commit, senão haveria estoque movimentado com ordem em aberto. `liberar`, `iniciar` e `cancelar` entraram na mesma trava: cancelar e concluir ao mesmo tempo baixava o estoque de uma ordem que terminava cancelada. O PIN fica FORA da trava de propósito — é um bcrypt por colaborador do tenant, e segurar a linha durante isso serializaria a loja. **(3) e (4) Escopo por unidade:** `listar`/`relatorio` filtravam só por tenant e a unidade da ordem vinha crua do corpo do request — o gerente da loja A listava, concluía e cancelava ordem da loja B, e podia criar ordem carimbada em qualquer unidade. Adotado o padrão do mural: quem tem unidade no JWT vê e age só na própria (mais as de rede, sem unidade); quem não tem é rede e enxerga o tenant. Na criação a unidade do usuário **vence o corpo**, e quando a rede informa uma, ela é conferida contra o tenant. Junto: `gerarRecorrentes` e o corte de `pendencia_critica` usavam data em **UTC** — às 21h em SP a ordem recorrente de hoje nascia com a data de amanhã; passaram a usar `common/data.ts`. | As duas metades da conta do rendimento moravam em arquivos diferentes, e a trava de idempotência que já existia no banco nunca foi ligada: a ordem nunca gravava a própria referência |
| 2026-09-16 | **Recebimento passa a conferir o dono de tudo que vem do corpo do request** (sem migration). `itemId` e `fornecedorId` chegavam validados só como `@IsUUID()`: o id de **outro tenant** entrava na nota sem nenhuma checagem. O `desperdicio` já fazia essa conferência (tenant + unidade + soft-delete) — o recebimento era o fora-da-curva, e é justamente ele que **cria movimento de estoque, lote e CONTA A PAGAR**. Efeitos reprodutíveis no banco: nota da loja A dando entrada em insumo da loja B (estoque na loja errada); `recebimento_item` apontando para item de outro tenant, e como os `join`s de `findAll`/`findOne` não filtravam tenant, **o nome do fornecedor e do insumo do outro tenant apareciam na tela**; e título a pagar nascendo com `fornecedor_id` de fora. Agora: uma consulta por tabela (não uma por item — a nota tem dezenas de linhas) confere fornecedor e itens contra o tenant, o item contra a unidade da nota **ou nula** (catálogo da rede herdado pelas filiais), e o soft-delete. A nota com uma linha alheia é **recusada inteira**, não tem linha descartada em silêncio — o `compras.createLista` faz o contrário (`filter`), e nota com item sumido sem aviso vira estoque que não entrou e ninguém procura. A unidade da nota segue a regra da ordem de produção: a do usuário vence o corpo, e a informada pela rede é conferida. Os `join`s ganharam filtro de tenant para que o registro LEGADO — gravado antes desta conferência — pare de vazar nome na tela. | O módulo que cria estoque, lote e conta a pagar era o único que aceitava id de outro dono sem perguntar |
| 2026-09-16 | **Etiqueta de validade sincroniza; lote e validades passam a respeitar a loja** (mig 245, **NÃO** `@cloud-only`). F0 do épico de PVPS/FEFO — a parte que não depende de nenhuma decisão de modelo. **(1) Quarto furo da mesma classe das migs 242/243:** `EtiquetaValidadeModule` é EDGE_CORE, mas `etiqueta_validade` e `etiqueta_template` **nunca estiveram em `TABELAS_SYNC`**. A tabela existe nos dois lados e o módulo que a escreve roda no edge, então nada falhava — só não subia. A nuvem não enxergava o que estava aberto nem o que ia vencer (o job de alerta rodava sobre tabela vazia), a `perda()` subia como desperdício **sem a etiqueta que a originou**, e uma reinstalação levava junto todo o rastreio de validade em uso. As duas tabelas tinham `updated_at` desde a mig 136 mas **nenhum gatilho de bump** — a mesma lacuna que a mig 243 achou em 10 de 11 tabelas, e aqui é fatal porque o ciclo da etiqueta É uma sequência de UPDATEs (`fechado → em_uso → baixado → vencido/perda`): sem o bump a nuvem mostraria para sempre como fechada uma etiqueta que a loja já baixou. Conferido no banco: o UPDATE de status move o `updated_at`. **(2) Escopo por loja:** `GET /lotes` não filtrava unidade nenhuma (a loja A via lote da loja B), e a tela de validades filtrava com `= atual` puro, o que **escondia todo insumo de unidade nula** — a maioria em quem nunca separou catálogo por filial — enquanto o job das 06:10, que roda sem `atual`, alertava sobre ele. As duas passam a usar a mesma regra do recebimento: insumo DA LOJA **ou da REDE**. `lote` não tem `unidade_id` própria e herda a do insumo; a coluna entra na F1. | A etiqueta que a loja imprime nunca chegava à nuvem, e a tela de validade da loja mostrava lote de outra loja e escondia insumo da rede |
| 2026-09-16 | **A compra passa a ser CONFERIDA, e é daí que nasce o lote** (mig 246, **NÃO** `@cloud-only`). F1 do épico de PVPS/FEFO, **redirecionada para a porta certa**. O levantamento na base real matou a premissa da auditoria: `recebimento` = 0 notas e `lote` = 0 linhas. O PVPS nunca rodou uma vez desde a mig 012 — e não por desuso: **`lote` só nascia em `recebimento.confirmar()`, e o estoque entra por `compras.receber()`**, que nunca chamou aquele código. O épico inteiro estava pendurado numa porta que ninguém abre. **(1) `compras.receber()` não tinha conferência** e lançava no estoque a quantidade **PEDIDA**: pediu 10 caixas, chegaram 7, entravam 10 — estoque que não existe, com o custo médio ponderado calculado em cima dele. `qtd_recebida` e divergência só existiam em `recebimento_item`, do fluxo morto. Agora o endpoint exige a conferência linha a linha (antes não recebia corpo nenhum), grava o que chegou e lança **o recebido**. **(2) A validade nasce na conferência, não no cadastro** — ela está impressa na embalagem que a pessoa tem na mão, e cada compra do mesmo insumo chega com uma diferente; a data fixa de `item_estoque.validade` (mig 149) nunca poderia servir, e era justamente o que o motor de etiqueta exigia. Três estados, decididos com o dono: data preenchida → lote com validade e alertas; **indefinida** (o descartável) → registrada como ESCOLHA EXPLÍCITA, fora dos alertas; nenhum dos dois → **recusado**. O dono propôs o "indefinido" no lugar de um `controla_validade` no cadastro do insumo, e tem razão: ajuste no cadastro é invisível — alguém desliga uma vez e as perdas voltam —, enquanto a escolha por nota fica na mão de quem segura a caixa. Contra o risco de "indefinida" virar o clique rápido, a tela **lembra a última conferência daquele insumo** e pré-preenche (1 consulta com `distinct on`, não N): a decisão continua aparecendo, só não se redigita. **(3) O lote ganhou identidade** — `codigo` do fabricante, `fornecedor_id`, `unidade_id`, `compra_item_id`. Sem o código não dá para separar mercadoria numa troca ou num recall sem abrir embalagem, que é a razão do lote existir para o dono; e o mesmo insumo vindo de fornecedores diferentes era indistinguível. Nasce só quando há o que rastrear (validade **ou** código) — lote vazio de guardanapo seria ruído. **(4) `etiqueta_validade.lote_id`**: a etiqueta continua sendo impressa por uma PESSOA, na quantidade que ela decide (decisão do dono — 2000 ml de molho viram 4 potes ou 10; 7 caixas levam 7 etiquetas ou 84), mas passa a poder sair de um lote, herdando validade e fornecedor. É esse elo que faz o recall alcançar o que já foi ABERTO. Junto, dois buracos achados no caminho: `compra_lista` **nunca gravava a unidade** (o lote herdava a ausência e ficava fora do escopo de qualquer filial) e `createLista` **descartava item inválido em silêncio** (`filter`) — a lista saía menor do que o comprador montou, ele não compra o que sumiu e ninguém sabe por quê; agora recusa nomeando. Conferido no banco com 15 cenários; o mesmo roteiro contra a versão anterior entra **30 no estoque onde deviam entrar 7**. | O PVPS não estava meio implementado: estava inteiro, pendurado numa porta que nunca é aberta |
| 2026-09-16 | **A compra recebida passa a virar conta a pagar, com a data definida na criação ou no recebimento** (mig 247, **NÃO** `@cloud-only`). `compras.receber()` entrava com a mercadoria no estoque e **não criava dívida nenhuma** — só o `recebimento.confirmar()` criava, e a base mostra esse fluxo com ZERO notas. Ou seja: toda compra recebida virava estoque e **nunca** virava dívida no Financeiro, mesmo com o fornecedor e o valor já na mão (`compra_lista.fornecedor_id` e a soma das linhas). O valor do título usa a quantidade **CONFERIDA** (mig 246), não a pedida: pagar 10 caixas quando chegaram 7 é o mesmo erro do estoque, do lado do dinheiro — conferido no banco, sai R$ 14 e não R$ 20. Data de pagamento (decisão do dono): `compra_lista.vencimento` guarda a da criação, a conferência **sobrescreve**, e sem nenhuma das duas cai no prazo cadastrado do fornecedor (`fornecedor.prazo_pagamento_dias`, default 28) contado do recebimento — título sem vencimento não entra em alerta de contas a pagar e some do radar. Sem fornecedor ou sem custo informado, nenhum título é criado (mesma regra do `recebimento`). O Financeiro rotula a origem 'compra' como a de 'recebimento'. | O fornecedor e o valor estavam na mão o tempo todo, e a única porta de entrada que a loja usa não gerava a dívida |
| 2026-09-16 | **A etiqueta de validade passa a sair do LOTE** (sem migration — `etiqueta_validade.lote_id` veio na mig 246). Para etiquetar um insumo comprado, o motor exigia `item_estoque.validade`: uma data **FIXA no cadastro** (mig 149). Isso nunca poderia servir — cada compra do mesmo insumo chega com uma validade diferente —, e era exatamente por isso que o insumo comprado não tinha como ser etiquetado na prática. Agora o LOTE (criado na conferência da compra, mig 246) é uma quarta fonte, ao lado de produto, ficha e insumo: a validade é a **daquela entrega**, e a etiqueta guarda `lote_id`. É esse vínculo que faz um recall alcançar **o que já foi ABERTO** — antes, impossível. Regra do dono mantida: **a etiqueta não é gerada em massa a partir do documento** — nem do pedido de compra (que não garante a chegada) nem do de produção (que ainda vai produzir). Ela é impressa por uma PESSOA, que escolhe o produto e digita **quantas**, porque quantidade em estoque e número de embalagens são coisas diferentes (2000 ml de molho viram 4 potes ou 10; 7 caixas levam 7 etiquetas ou 84). O lote só entra como fonte pré-preenchida. O `item_id` vai junto na etiqueta, para o `abrir()` seguir recalculando a validade após aberto (mig 182). Lote de validade **indefinida** é recusado com motivo — `etiqueta_validade.validade` é NOT NULL e não há data a imprimir. `fontes()` passou a ser escopada por loja (insumo da loja OU da rede), que não era. | O insumo comprado não tinha como ser etiquetado: a única validade que o motor aceitava era uma data fixa que não existe na vida real |
| 2026-09-16 | **PVPS/FEFO passa a existir: a baixa consome o lote** (mig 248, **NÃO** `@cloud-only`). Última peça do épico. `lote.quantidade` era gravado na entrada e **nunca decrementado** — o próprio código admitia ("saídas ainda não decrementam lotes; usa lote.quantidade como saldo aproximado"). O alerta das 06:10 e a tela de validades mostravam a quantidade de quando a mercadoria chegou: o lote de 10 kg já todo consumido seguia avisando "vence em 2 dias, 10 kg". **Alerta que mente é pior do que alerta nenhum**, porque o lojista para de olhar. **Um motor único** (`common/lotes.ts`) atende os QUATRO pontos que dão saída — venda, produção, desperdício e ajuste negativo de contagem — mais os dois de estorno; espalhar a conta pelos seis repetiria o erro do `hojeISO`, que existia em quatro cópias. Regras: **FEFO** (vence primeiro sai primeiro; lote sem validade é o último da fila) e **nunca trava a operação** — se os lotes não cobrem, consome o que há e devolve a sobra, porque a venda não pode parar por causa do rastreio. **O saldo do lote é DERIVADO**, não um campo mutável: `lote.quantidade` passa a significar o que ENTROU e o consumo é a soma de `movimento_lote` (append-only). A razão é a mesma pela qual o saldo do estoque nunca foi um campo — `lote` sincroniza com LWW, e um saldo mutável perderia baixa concorrente: loja consome 3, nuvem consome 2, a última escrita vence e uma some. **Um bug real caiu no teste de concorrência:** o `for update` trava a linha do lote, mas um agregado calculado NO MESMO SELECT é avaliado no snapshot ANTIGO — a transação que esperava a trava acordava com o saldo de antes, e duas baixas de 6 num lote de 10 davam **saldo −2**. Resolvido em dois passos (travar, depois somar numa consulta separada, que em READ COMMITTED pega snapshot novo); conferido no banco: sobras [0,2] e o lote entrega exatamente 10. O **estorno** devolve ao lote de ORIGEM gravando o negativo — devolver só ao estoque geral deixaria o lote consumido para sempre e o alerta cego para mercadoria que voltou à prateleira. A baixa da venda herda de graça a idempotência do índice da mig 024: `returning` vazio = a baixa já existia, então não consome o lote de novo. | O lote existia desde a mig 012 e nunca foi decrementado uma vez: o alerta de validade sempre falou da mercadoria que chegou, não da que ainda está lá |
| 2026-09-16 | **Leitura do estoque volta a passar pelo RBAC; e a guarda do `hojeISO` estava cega para metade das cópias** (sem migration). **(1)** `GET /estoque/itens`, `/categorias-item` e `/movimentos` não tinham decorator NENHUM — nem `@Roles` nem `@RequirePerm`. Como o `PermissoesGuard` faz `if (!spec) return true`, **qualquer usuário autenticado lia o estoque inteiro**, incluindo `custo_medio` e o ledger de movimentos. Nos perfis padrão, gerente e supervisor têm `estoque.ver: true` (nada muda para eles), mas **execução tem `CRUD_NONE`** — o garçom lia o custo de tudo. Isso viola a regra nº 1 do projeto: *"RBAC no servidor, não no front. A API não pode sequer retornar dados de views não permitidas ao perfil."* As três ganharam `@RequirePerm('estoque','ver')`, o mesmo que `lote` e `recebimento` já exigem no mesmo domínio. As outras três (`inteligencia`, `validades`, `alertas`) tinham só `@Roles`, que ignora o pacote de permissões configurável; ganharam a permissão junto, o que é estritamente mais restritivo. **(2)** A guarda de deriva criada no #514 procurava `"function hojeISO"` — e havia **mais três cópias na forma arrow**, invisíveis para ela. Duas em **UTC**: `etiqueta-validade` (a validade da etiqueta impressa depois das 21h saía com **um dia a mais** — num controle que existe por causa da RDC 216) e `desligamento` (RH). A terceira, no cardápio, estava certa mas era deriva. A guarda passou a buscar as duas formas e a **ignorar linhas de comentário** (ao ampliá-la, o próprio comentário que a explicava a derrubava), com um meta-teste que quebra se alguém a estreitar de novo; provado criando uma cópia falsa em arrow e vendo o CI acusar. Junto, `addDias` da etiqueta montava a data em hora LOCAL e lia de volta em UTC — acertava por coincidência no Brasil e na nuvem, e erraria um dia em qualquer servidor a leste; passou a usar meio-dia UTC com aritmética em UTC. | Uma guarda que só enxerga metade das formas dá a sensação de estar protegido enquanto quatro cópias seguem erradas |
| 2026-09-16 | **Etiqueta que vira perda passa a baixar o estoque; o ciclo da etiqueta ganha guarda e trava** (sem migration). Quatro achados da auditoria no mesmo módulo. **(#48) A perda não mexia em nada.** `perda()` gravava o desperdício direto na tabela, com `quantidade: '1'` fixo e **sem movimento de estoque** — o comentário dizia "p/ CMV", mas o CMV lê o ledger, então a perda não aparecia em lugar nenhum e o insumo jogado fora seguia no estoque. Passou a usar o **mesmo caminho do desperdício manual** (`DesperdicioService`), que já fazia o certo: custo médio, movimento de saída e, desde o #522, consumo FEFO do lote de onde a etiqueta saiu. **A quantidade vem de quem registra**, porque a etiqueta não sabe quanto representa (um pote de 500 g ou de 2 kg) e inventar um número seria pior do que não baixar; sem quantidade, ou em etiqueta de ficha sem insumo resolvível, fica o registro textual — e a resposta diz que não baixou, para a tela avisar. **(#47 / #50) Sem guarda de estado:** etiqueta já CONSUMIDA virava perda (desperdício de algo usado), a mesma etiqueta virava perda duas vezes, e `finalizar` numa etiqueta que já tinha virado perda sobrescrevia `virou_perda: true` com `false` — o desperdício continuava lançado, mas a etiqueta passava a dizer que foi consumida. Agora só etiqueta viva (`fechado`/`em_uso`) aceita as duas ações, sob trava de linha e no mesmo commit do desperdício; conferido no banco com três cliques simultâneos → uma perda. **(#49) O controller ignorava o pacote de permissões configurável** — mesma classe do #523: tinha `@Roles` no nível da classe (a auditoria disse "sem guarda", mas execução já estava fora), sem `PermissoesGuard`. Leitura passa a exigir `estoque.ver` e o que muda estado, `estoque.editar`; nos perfis padrão, gerente e supervisor têm os dois. Junto, e necessário para o conserto funcionar: o `DesperdicioService` filtrava o insumo com `condUnidade` estrito, e insumo de unidade nula — a maioria — dava **404** para usuário de loja; passou à regra do recebimento (loja OU rede). | A perda de etiqueta existia para alimentar o CMV e não movia um grama do estoque |
| 2026-09-16 | **A produção deixa de baixar embalagem de delivery; #68 e #41 classificados como latentes** (sem migration). **(#63)** As linhas `somente_delivery` da ficha (mig 147 — embalagem de pedido externo) são puladas pela venda (`vendas.acumularFicha`), mas **a produção não pulava**: produzir uma ficha que tem caixa de delivery baixava a caixa do estoque e ainda somava o custo dela no custo unitário do item produzido. Conferido no banco com 10 unidades de uma ficha de queijo (R$ 2) + caixa (R$ 5): a versão anterior baixava 10 caixas e dava entrada no produzido a **R$ 7 em vez de R$ 2** — e esse custo inflado segue para o CMV de tudo que usa o item. A recursão de sub-ficha passa pelo mesmo laço, então vale em todos os níveis. **(#68 e #41) Latentes, não vivos.** A auditoria apontou que `lancarSaidas` é idempotente por (comanda, insumo) mas ignora a quantidade — se a comanda recebesse item depois da primeira baixa, o acréscimo nunca sairia (#68), e um pedido estornado e reconcluído ficaria com estoque inflado (#41). **O raciocínio está certo, mas os caminhos que disparam não existem hoje:** `adicionarItem` exige comanda `aberta` e nada reabre uma comanda fechada (os três `status: 'aberta'` são criação de comanda nova); o delivery faz `return` se o pedido já está `concluido` ou `cancelado`. O índice só é exercitado por re-execução com o MESMO total — duplo clique, delivery concluído duas vezes — e isso ele trata certo. Reescrever a baixa da venda, o caminho mais quente do sistema, para um cenário impossível seria risco sem retorno; ficou um aviso no ponto exato do código, com a armadilha da solução óbvia: não dá para resolver com UPDATE na quantidade, porque o ledger é append-only e sincroniza por `created_at` (o update não subiria) e `movimento_lote` aponta para a linha com quantidade fixa. | Um achado de auditoria com raciocínio certo pode descrever um caminho que o código não permite — conferir o gatilho antes de mexer no caminho mais quente do sistema |
| 2026-09-16 | **Alerta de estoque nasce por loja; compras respeitam a loja em todas as rotas** (mig 249, **NÃO** `@cloud-only`). **(#45) Alerta.** A mig 031 tinha índice único em `(tenant_id, tipo)`: um alerta aberto de ponto de pedido e um de validade **por empresa**, desenho de antes do multi-unidade. Os jobs calculavam sobre o tenant inteiro e gravavam sem unidade; a tela da loja filtrava por igualdade. **Em rede de uma loja isso não aparece** — o `UnidadeUnicaInterceptor` zera o filtro. **Em rede com várias lojas, o usuário de loja não via alerta nenhum.** A correção óbvia — deixar a loja ver o alerta sem unidade — seria pior: ele foi calculado sobre todas as lojas, e o gerente da A passaria a ler a lista de insumos da B. O conserto é o alerta nascer por loja: o índice passa a `(tenant_id, coalesce(unidade_id, zero), tipo)` e os dois jobs geram um alerta por loja em rede multi-loja (loja única segue com um só, igual antes). **Cada alerta usa exatamente o escopo da tela que a loja já vê** — o de ponto de pedido segue a de inteligência, o de validade segue a de validades — para o alerta nunca discordar da tela e para não pré-decidir o que é insumo de rede (#14, em aberto com o dono). Resolver a loja A não fecha mais o alerta da B; o alerta antigo sem unidade é fechado quando o tenant vira multi-loja. O evento de tempo real ia para a sala `tenant:` — **todo KDS da empresa tocava o alerta de uma loja**; alerta com loja passa a ir para `unidade:`. **(#6 / #33) Compras.** O #519 passou a gravar a loja na criação, mas listar, abrir, sugerir, remover e receber seguiam só por tenant: a lista da Filial A aparecia — e podia ser removida e recebida — na tela da B, e a sugestão trazia dois "Farinha de trigo" sem rótulo. As cinco rotas passaram a filtrar por loja, e a criação confere o insumo contra a loja da lista. **Lista antiga sem loja segue visível:** esconder uma lista em aberto seria pior do que mostrá-la, e ela nasceu da empresa inteira. Conferido no banco com duas lojas (15 cenários). | O sistema de alertas existia para quem tem uma loja e era invisível para quem tem várias — e a correção óbvia vazaria o estoque de uma loja para a outra |
| 2026-09-16 | **CMV real: as compras entram, o estoque inicial é a véspera, e o fechamento do dia tem hora configurável** (mig 250, **NÃO** `@cloud-only`). A auditoria apontou um erro (#15); havia três, e dois deles se escondiam um no outro. **(a) As compras nunca entravam no CMV.** A loja compra por `compras.receber()`, que grava `motivo: 'compra'`; o CMV e a coluna de compras da tela de inteligência só somavam `'recebimento'` — o fluxo com **zero notas** em produção. O CMV real virava estoque inicial menos final, sem compra nenhuma. **(b) O estoque final estava incompleto.** O snapshot rodava às 02:00 fixas e fotografava o dia ATUAL, recém-começado. **(c) O estoque inicial contava o primeiro dia duas vezes.** O snapshot do dia D soma os movimentos com data <= D, então usar o do próprio `inicio` punha as compras do 1º dia dentro do estoque inicial E dentro de `compras`. **Esse erro estava escondido pelo (b):** como o job das 02:00 fotografava um dia quase vazio, o snapshot de `inicio` saía por acaso perto do certo — **consertar só a hora, como a auditoria sugeria, faria o (c) aparecer.** Os três saíram juntos. Conferido no banco com números conhecidos (véspera R$ 500, compra R$ 300, fim R$ 500, consumo das fichas R$ 300): a versão anterior dava **compras R$ 0, estoque inicial R$ 600 e CMV real R$ 100 contra R$ 300 teórico — desvio de −R$ 200 que não existe**. Pior que número errado: o CMV parecia MELHOR que o consumo das fichas, o que esconde perda em vez de mostrá-la. Agora dá R$ 300 = R$ 300, desvio zero. **Decisão do dono sobre a hora:** configurável pelo presidente/C&O, padrão 06:00 (`empresa.snapshot_hora`, editável no Financeiro). O job passou a rodar a cada 15 min e, quando a hora de cada empresa chega, fotografa o dia ANTERIOR. Como o snapshot soma por data de movimento, a hora não muda o resultado — dá tempo para as vendas da noite sincronizarem do servidor local e aproxima o custo médio gravado do fechamento. Validado com o job varrendo as empresas dentro de uma transação desfeita: quem passou da hora ganha o snapshot de ontem, quem não chegou não é fotografado, e rodar de novo não duplica. Os helpers `somarDias` e `horaAgora` entraram no `common/data.ts`. | A auditoria viu um erro no CMV; havia três, e consertar só o que ela viu teria exposto o que estava escondido |
| 2026-09-16 | **Desperdício e ponto de baixa ganham permissão própria; o registro passa a gravar ponto e quem** (migs 251 e 252, **NÃO** `@cloud-only`). Decisões do dono: o desperdício é registrado em **pontos com leitor óptico** — PC à parte com leitor e tela, ou Android que escaneia o produto — **na mesma rede do servidor local**; **quem registra é o perfil de gestão que tiver a permissão**; a permissão **pode ser habilitada num perfil de execução quando necessário, mas não vem habilitada por padrão na execução**; e importam a **hora** e o **ponto** do registro. A foto do desperdício é do produto descartado, não de pessoa: **o #44 (LGPD) foi fechado sem mudança**. **O que o código fazia:** `POST /desperdicios` exigia `estoque.criar` — ampla demais (também cria insumo) e que nem a supervisão padrão tinha. O "quem" (#43) vinha do corpo do request e a tela nem o enviava. **O que mudou:** chave nova **`desperdicio`** no catálogo — aparece como interruptor em Acessos & Perfis —, ligada no padrão de gerente e supervisor e **deixada de fora da execução de propósito**. Ela trava o registro de desperdício e as cinco rotas do ponto de baixa (buscar, ler, abrir, finalizar, perda) e o item de menu; template, fontes, listagem e criação de etiqueta seguem com a gestão. **Perfis que já existem** recebem a chave pela **mig 252, conforme o nível** — porque o pacote salvo é usado como está, sem mesclar com o padrão. Sem ela, a chave ausente negaria o registro a toda a gerência no deploy; herdando de `estoque`, um perfil de execução que só VÊ estoque ganharia desperdício sem ninguém pedir (conferido no banco: essa execução fica desligada, e perfil já editado não é tocado). O rastro: a hora já existia (`created_at`, do servidor); entraram **`equipamento_id`** (o ponto, um equipamento do tipo novo **`ponto_baixa`**, conferido contra empresa e loja porque é o aparelho que se identifica) e **`registrado_por_id`**, do login. **A forma importa:** permissão declarada na CLASSE não se anula no método, então os decorators foram para cada rota, e um teste de CI roda os guards REAIS contra os handlers REAIS — inclusive o caso da execução com a permissão liberada; tirar a trava de uma rota faz o teste acusar (provado). O sync de `equipamento` é filtrado por tipo e ganhou `ponto_baixa` nos dois lados. **Limite do modelo, registrado:** `perfil_acesso` é único por `(tenant, nivel)` (mig 069) — existe UM perfil de execução por empresa, então liberar a permissão para a execução libera para todos os colaboradores de execução de todas as lojas; não há liberação por pessoa ou por loja. | Restringir por `estoque.criar` era amplo demais e travava a supervisão; a permissão certa é própria, com o padrão por nível gravado também nos perfis que já existem |
| 2026-09-17 | **Estoque por loja — fase A: todo movimento passa a pertencer a uma loja** (migs 253 e 254, **NÃO** `@cloud-only`). **Decisão do dono (separação total):** cada loja é uma unidade física diferente, com o próprio estoque, as próprias métricas e o próprio CMV; a loja é escolhida no login para separar as unidades por completo; o presidente decide loja por loja e **não há média de CMV**. O cadastro do insumo pode ser compartilhado, mas o controle é de cada loja. Hoje o sistema permite no máximo duas lojas. **O que estava errado:** `movimento_estoque` não tinha loja e o saldo é a soma do ledger por insumo — com o mesmo insumo em duas lojas, **as duas dividiam um saldo só**: a venda de uma baixava o estoque da outra. O inventário deu **10 lançamentos e 15 somas de saldo em 9 módulos**, e mostrou que **custo médio, estoque mínimo e dias de segurança** também são por insumo, não por loja. **Entrega em duas fases para reduzir risco:** esta (A) só GRAVA a loja — as leituras não mudam e o sistema se comporta igual; a B separa as leituras, o lote, o snapshot, o custo, o mínimo e o CMV. **Como a loja é descoberta:** uma função no banco (`regem_unidade_do_movimento`) resolve pela ORIGEM (comanda, lista de compras, nota, desperdício, lista de contagem, ordem de produção), depois pelo insumo exclusivo de uma loja, depois pela loja única da empresa; **senão fica nula — sem palpite** (decisão do dono), e a próxima contagem daquela loja restabelece o saldo. **Por que um gatilho e não só código:** até o `.zip` chegar às lojas, o servidor local continua gravando movimento sem loja e o envia assim; com o gatilho, **a nuvem resolve a loja desses movimentos quando eles chegam** (testado: INSERT sem a coluna, como o push do edge antigo, sai com a loja da comanda). A mesma função preenche o histórico (mig 254, separada da 253 pela lição da 244: se o UPDATE do ledger estourar o tempo, a coluna e o gatilho não são desfeitos). No banco de desenvolvimento, 258 de 267 movimentos ganharam loja; os 9 restantes são de empresas de teste sem nenhuma loja cadastrada. **O código passa a loja explícita** onde a origem não diz: ajuste manual e produção manual. **Lançar sem loja, em empresa de duas lojas, é recusado** ("escolha a loja") — o único caso é o presidente com "todas as lojas". Junto, conserto necessário ao modelo: o ajuste manual filtrava o insumo com loja estrita, e **a loja não conseguia ajustar insumo de cadastro compartilhado** ("Item inválido para esta unidade"); passou a aceitar insumo da loja ou compartilhado. 17 cenários no banco, dentro de transação desfeita. | Duas lojas dividindo um saldo só: a separação começa gravando de quem é cada movimento, antes de mudar qualquer leitura |
| 2026-09-17 | **Estoque por loja — fase B1: as quantidades passam a ser da loja** (migs 255 e 256, **NÃO** `@cloud-only`). Continuação da fase A (#529, cada movimento grava a loja). **O que muda:** toda leitura de saldo soma só os movimentos da loja escolhida no login — lista de estoque, inteligência (ponto de pedido, cobertura), sugestão de compra, painel (abaixo do mínimo), contagem (o `saldo_sistema` congelado, o movimento durante a contagem e o saldo no instante), disponibilidade de produto e preview de atacado. O presidente em "todas as lojas" vê a **soma** das lojas, nunca média. A Visão C&O conta o abaixo do mínimo **em cada loja**: antes o insumo compartilhado caía na linha "sem loja" com o saldo das duas juntas. **Lote:** a baixa só consome lote da loja do movimento (o FEFO da loja B não leva o lote da A que vence antes); lista de lotes e validades filtram pela loja do **lote**; lote antigo sem loja serve às duas. **Insumo compartilhado** passa a aparecer nas duas lojas (lista, contagem): o cadastro é comum, o saldo é de cada uma. **Snapshot e CMV:** o snapshot diário passa a ser por (empresa, **loja**, insumo, dia) — a chave primária vira índice único com `coalesce(unidade_id, uuid nulo)` em vez de `nulls not distinct`, porque o servidor local pode usar um Postgres anterior ao 15 já instalado no PC. O CMV pega o snapshot mais recente por insumo **e loja**, e compras/consumo/desperdício pela loja do **movimento**. A mig 256 reconstrói os snapshots antigos por loja — quantidade recontada pelo ledger, custo preservado como estava no dia (recalcular mudaria CMV de período fechado); **sem ela o CMV dobraria** (linha antiga sem loja + linha nova com loja na mesma data). No banco de desenvolvimento: 5616 → 5616 snapshots, zero chave duplicada, rodada de novo sem efeito. **Ainda NÃO é por loja (fase B2):** custo médio, estoque mínimo e dias de segurança continuam por insumo. 38 cenários no banco (duas lojas, insumo compartilhado e exclusivo), dentro de transação desfeita. | O cadastro é comum; o saldo, o lote, o snapshot e o CMV são de cada loja |
| 2026-09-17 | **Estoque por loja — fase B2: custo médio, estoque mínimo e dias de segurança por loja** (migs 257 e 258, **NÃO** `@cloud-only`). Fecha a separação total por loja (fases A #529 e B1 #530). **Tabela nova `item_estoque_unidade`:** uma linha por (insumo, loja); coluna nula = vale o do cadastro. Empresa de UMA loja não usa a tabela e segue exatamente como antes. **Id determinístico** `md5(item_id || unidade_id)::uuid`: a linha pode nascer ao mesmo tempo no servidor local (recebimento) e na nuvem (ajuste de mínimo); com id aleatório o sync, que casa por id, bateria no índice único do par — com o id do par as duas pontas geram a mesma linha e o LWW resolve. Sincroniza 'ambos'. **Custo:** compra, nota e produção ponderam o custo DA LOJA do movimento (helper `ponderarCustoDaEntrada`, chamado depois do INSERT: a loja é a que o gatilho da mig 253 gravou e o saldo "antes" é o da loja sem o próprio movimento — de quebra, a 2ª linha do mesmo insumo na mesma lista passa a ponderar sobre a 1ª). Entrada sem loja em empresa de duas lojas não pondera nada (sem palpite). Venda, desperdício e produção gravam na saída o custo da loja, resolvida pela MESMA função do gatilho (`regem_unidade_do_movimento`) antes do INSERT, para custo e loja gravados não divergirem. **Leituras:** lista, inteligência, snapshot, CMV, sugestão de compra, painel e Visão C&O expandem cada insumo nas lojas que o usam (`sqlLojasDoItem`) com saldo, custo e mínimo de cada uma. Em "todas": valor = Σ saldo × custo de cada loja; mínimo = soma dos mínimos; abaixo do mínimo = alguma loja em falta; sugestão = soma do que falta em cada loja (a sobra de uma não cobre a outra); movimentos sem loja continuam no total, valorizados pelo cadastro, sem mínimo próprio. **Edição do mínimo:** com loja escolhida grava o da loja, inclusive em insumo de cadastro compartilhado (a loja não edita o cadastro, só o mínimo dela); em "todas" a tela trava o campo (mostra a soma) e o servidor ignora o valor. **Ponto de partida (opção A):** a 258 dá a cada loja o custo, o mínimo e os dias de hoje — refazer a média pelo histórico exigiria custo em toda entrada, e carga inicial e parte dos recebimentos não têm. Na produção nenhuma empresa tem duas lojas: a 258 gravou 0 linhas. **Ficha técnica fora:** o custo dela é o gravado em cada ingrediente, não o custo médio vivo. 38 cenários no banco (duas lojas e uma loja) + os 38 da B1, em transação desfeita. | Cadastro comum; saldo, custo e mínimo de cada loja |
| 2026-09-17 | **Sync edge↔nuvem: reconciliação pós-atualização, fim do pingue-pongue do updated_at e descarga antes da reinstalação limpa** (mig 259, **NÃO** `@cloud-only`). Revisão do `sync-daemon` antes do corte de release; os defeitos foram **reproduzidos de ponta a ponta** no banco local (uma loja com o schema e o daemon da 1.29.0 contra a API local no papel de nuvem, depois a atualização com o daemon da `main` e com o corrigido). **1) Tabela/coluna que o edge ainda não tem:** o pull descartava a linha da tabela ausente (contando como aplicada) e aplicava sem as colunas ausentes — e o cursor AVANÇAVA; depois da atualização aquele histórico nunca mais descia (`movimento_lote`, código/loja do lote, exclusão de ingrediente da ficha, valor bruto do pedido, hora da contagem…). **2) `updated_at default now()` das migs 242/243** carimbava as linhas locais com a hora da atualização: o pull nunca corrigia o edge e o push das tabelas recém-incluídas **sobrescrevia a nuvem com vazio** (conferência da compra, vencimento, hora da contagem — os três viraram NULL na reprodução). **Correção (`edge/sync-reconciliacao.mjs`):** fila por tabela em `sync_state`; o pull rebobina a tabela uma vez e copia da nuvem só as colunas que o edge não tinha, nas linhas não editadas localmente depois da atualização; enquanto isso o push espera (quando o `updated_at` local é artificial) ou sobe sem essas colunas; ao terminar, o push volta ao instante da atualização. Lista fixa para a transição da 1.29.0 (cruzando migrations 224–258 com a whitelist; teste de deriva no `sync-config.spec`) e, para o futuro, detecção automática: tabela ausente não avança cursor e coluna ausente fica registrada e entra na fila quando a atualização a cria. **3) Pingue-pongue:** `bump_updated_at` (20 tabelas) e `set_updated_at` (31) carimbavam `now()` também quando o SYNC aplicava a linha → a linha voltava como mais nova, ia e vinha a cada ciclo e o carimbo falso podia vencer uma edição real. Mig 259: as duas funções respeitam a marca de sessão `regem.sync = on`, posta pelo daemon em toda conexão e pela nuvem na transação do push (com savepoint no fallback linha a linha). **4) Cursor do push em milissegundo** (Date do JS) contra timestamp em microssegundo: a última linha de cada página era reenviada em todo ciclo; o cursor passa a ser o texto do Postgres. Na reprodução, em repouso: antes 14 linhas subindo por ciclo, depois 0/0. **5) Reinstalação limpa (`.exe`)** apagava o banco da loja partindo da premissa de que tudo já subiu — falsa para as tabelas que entraram no push depois da versão instalada. O instalador agora roda o daemon novo em `--descarregar` contra o banco antigo (keyset por id quando falta a coluna de cursor) e só apaga com saída 0; senão reinstala PRESERVANDO o banco. Removida a restauração linha a linha aposentada em 01/09. | Sync que se corrige sozinho depois de uma atualização, sem perder dado de nenhum dos lados |
| 2026-09-17 | **Pausa por falta de estoque POR LOJA** (migs 260 e 261, **NÃO** `@cloud-only`). **Decisão do dono:** "a pausa por falta em estoque é só por unidade". Fecha o épico estoque por loja: a pausa era UM campo no produto (`produto.pausado_estoque`), calculado pelo saldo SOMADO da empresa — faltar insumo na loja B pausava o produto também na A, ou a B seguia vendendo sem insumo. **Tabela `produto_pausa_estoque`:** uma linha por (produto, loja), id `md5(produto_id || unidade_id)` (mesma regra da 257: edge e nuvem geram a mesma linha), nunca apagada (despausar grava `pausado = false`, porque exclusão física não sincroniza), sincroniza nos dois sentidos. **Cálculo:** o esgotado é recalculado com o saldo DA LOJA; o aviso do KDS e o alerta do painel vão para a loja. **Quem calcula cada loja:** o servidor local só a PRÓPRIA loja (`EDGE_UNIDADE_ID`); a nuvem só as lojas sem servidor local ativo (`edgeAtivo`) — se os dois calculassem a mesma loja, o de ledger atrasado venceria pela regra da mais nova. **Leituras:** cardápio público e checkout pela loja do cardápio (sem loja no cardápio, a única loja da empresa; cardápio de rede com várias lojas, o campo antigo); catálogo do totem pela loja do dispositivo; tela de produtos pela loja escolhida, e em "todas" mostra "esgotado em Loja B". Reativar sem estoque tira a pausa de todas as lojas. **`produto.pausado_estoque` continua** e passa a significar "pausado em TODAS as lojas": é o que lê o servidor local ainda sem o `.zip` e o que vale para empresa sem loja — errar para "todas" evita bloquear a venda da loja abastecida. A 261 dá a cada loja a pausa que o produto tem hoje (nada muda no ar ao aplicar). 22 cenários no banco, em transação desfeita. | Cada loja esgota e volta sozinha, pelo estoque dela |
| 2026-09-17 | **Sync: exclusão física e edição sem carimbo passam a sincronizar; complementos do item e pagamento dividido entram no sync** (mig 262, **NÃO** `@cloud-only`). Da análise do sync pós #532/#533. **1) Exclusão física não sincronizava:** o sync só leva linhas que existem — item removido de comanda aberta seguia na nuvem (produtos vendidos a mais), cliente "esquecido" pela LGPD na nuvem seguia no servidor local, comanda de mesa e impressora removidas voltavam na restauração, pagamento dividido refeito acumulava. **Decisão: registro de exclusão (tombstone), não soft-delete.** Um gatilho `AFTER DELETE` nas 52 tabelas sincronizadas com estado grava (empresa, tabela, id) em `sync_exclusao`, que sobe como anexo e desce pelo pull; quem recebe apaga a mesma linha — a nuvem só em tabela de `TABELAS_EXCLUIVEIS` e dentro da empresa do token, o edge depois das linhas da mesma resposta, com fila de retentativa para 23503 (ex.: cliente cujo pedido desvinculado ainda não chegou). Aplicação feita pelo sync tem a marca `regem.sync` (mig 259) e não gera registro de volta. Soft-delete foi descartado: exigiria filtrar `deleted_at` em toda leitura de comanda, nota e relatório. **2) Edição sem carimbo:** 15 tabelas sincronizadas sem gatilho de `updated_at`, e cinco pontos do código sem gravar a data — editar/excluir fornecedor, **excluir produto** (seguia ativo do outro lado), pausar canal do produto (a nuvem recusava o envio imediato), excluir complemento, excluir regra do bot. Gatilho ligado nelas (e `bump_atualizado_em` para `cliente`), com a marca de sessão da 259. **3) `comanda_item_complemento` e `pedido_externo_pagamento`** nunca sincronizaram; entram nos dois sentidos por `created_at` (linhas imutáveis), na janela do transacional e na restauração. Testes de deriva: a lista de gatilhos da 262 cobre `TABELAS_EXCLUIVEIS` e a exclusão é a última no push e no pull. **Reproduzido de ponta a ponta** (servidor local com o schema atual contra a API local): 12 cenários nos dois sentidos, incluindo o "esquecer" da LGPD; em repouso 0/0. | Apagou de um lado, some do outro |
| 2026-09-17 | **Sync: carga inicial 6× mais rápida e sem perda, relógio da loja vigiado** (sem migration). Medido com a carga inicial completa de uma loja de teste (1.762 comandas, 3.167 itens, 2.227 pedidos, 1.766 lançamentos) contra a API local. **Antes:** o primeiro ciclo não terminava em 150 s e, ao terminar, faltavam 480 lançamentos e 27 pedidos de produção. **Causas:** (1) no `pg.Pool`, consulta que FALHA descarta a conexão — cada linha com pai ainda ausente pagava uma reconexão (~250 ms no Windows); (2) linha filha que chegava antes do pai tinha só 3 tentativas NO MESMO ciclo e era descartada com o cursor já adiante; (3) a janela de 60 dias corta pela criação, e uma sessão de caixa ABERTA há 64 dias não descia; (4) na instalação nova tudo o que descia voltava a subir (6.900 linhas no 1º ciclo). **Correções:** a resposta do pull é aplicada numa conexão reservada, em lotes de 200 com divisão ao meio quando um lote falha; órfãos ficam em fila (`pull_orfaos`) e são retentados por até 20 ciclos; registro aberto (`JANELA_ABERTOS`: caixa e comanda) desce mesmo fora da janela; banco vazio sem cursor começa o push em "agora". **Depois:** carga completa em ~25 s, loja e nuvem iguais em todas as tabelas, sem eco. **Relógio:** a regra "a mais nova vence" depende do relógio do PC da loja; o daemon mede o desvio pelo cabeçalho `Date` da resposta do pull, guarda em `sync_state`, manda no heartbeat de saúde e avisa (log + telemetria) acima de 120 s; o console da distribuição e a frota mostram o selo "relógio". | Loja nova sincroniza em segundos e não perde linha |
| 2026-09-17 | **O servidor local baixa só o transacional da PRÓPRIA loja** (sem migration). **Decisão do dono:** "ele só baixa os dados da unidade caso a empresa tenha mais de uma; em casos de grande volume em cada unidade vai ser um tráfego de informações desnecessárias e riscos de bugs e erros". Até aqui o pull descia o transacional da empresa inteira: a filial baixava comandas, pedidos, caixa e movimentos da matriz — só para os processadores locais (escopados por `EDGE_UNIDADE_ID` desde a F2) ignorarem. **O que passa a ser filtrado:** o transacional e os documentos — comanda (e item e complemento), caixa e lançamento, produção (e item), pedido externo (e pagamento), movimento de estoque (e de lote), desperdício, recebimento (e item), lote, etiqueta de validade, contagem (lista, item, execução, item da execução), compra (lista e item) e título financeiro. Filha sem `unidade_id` é filtrada PELO PAI (`LOJA_PELO_PAI`) — sem isso ela desceria e quebraria por FK, engordando a fila de órfãos. **O que continua inteiro:** cadastro e configuração (pessoas, perfis, catálogo, fichas, complementos, insumos, equipamentos, configs, unidades, clientes) — são poucas linhas, valem para a rede, e filtrar gente/perfil arriscaria o login de quem atende as duas lojas e deixaria o PDV sem produto. **Linha SEM loja (da rede) sempre desce.** Só vale com **duas ou mais lojas** e com o edge tendo loja definida no equipamento; empresa de uma loja não muda nada. Mesmo filtro no `/sync/restore` e no arquivo do `/sync/snapshot`. Testes de deriva: todo transacional (e toda tabela janelada) tem filtro, e os cadastros não têm. **Provado de ponta a ponta:** empresa de duas lojas + edge da loja A — desceu o que é da A e o que é da rede, não desceu nada da B (nem as filhas), sem órfão na fila; empresa de uma loja, nada mudou. | A filial não baixa mais o movimento da matriz |
| 2026-09-18 | **Sync — bloco P0 da análise de mercado: exclusão vence, estado do servidor local em uma linha, expurgo com janela e reinicialização explícita** (migs 263, 264 e 265, **NÃO** `@cloud-only`). Da comparação com SymmetricDS, AppSync, Firestore, Couchbase, Atlas Device Sync, GoldenGate e SQL Data Sync. **1) Exclusão vence (defeito REPRODUZIDO):** a nuvem apagava uma linha, a loja editava a mesma linha antes de receber a exclusão e o push a **recriava na nuvem** — o dado apagado voltava a aparecer e a contar em relatório até a loja receber a exclusão e devolvê-la. Agora o push consulta `sync_exclusao` antes de inserir e descarta a linha (regra "delete always wins" do Atlas Device Sync e do GoldenGate; índice na mig 263). **2) Batida do servidor local:** cada batida INSERIA uma linha (2 por ciclo) — em 5.000 lojas são 14,4 milhões de linhas/dia (~14 GB pela medida de ~1 KB/linha). Agora o estado vive em `edge_status` (uma linha por servidor, atualizada) e o histórico é amostrado a cada 30 min; `edgeAtivo`, a Visão da frota e o console passam a ler o status (com o histórico no OR durante a transição, senão a loja ficaria "offline" no primeiro minuto após o deploy e a nuvem assumiria os pedidos dela). **3) Expurgo com janela declarada (30 dias):** job diário limpa `sync_exclusao`, `edge_heartbeat` e `edge_telemetria` (90 dias) — antes nada era limpo. O mercado sempre declara a janela: Couchbase expurga tombstone em 3 dias por padrão, o SQL Data Sync guarda 45 e o AppSync usa TTL na tabela delta. **4) Reinicialização explícita:** quem ficou mais tempo que a janela sem sincronizar perdeu exclusões e não tem como saber; o pull passa a responder `reinicializar`, e a loja **rebobina os cursores para o começo** e pede a restauração por arquivo (o push sobe o que é local antes, então nada se perde). Rebobinar é essencial: sem isso a nuvem pediria reinicialização a cada ciclo — laço de restauração, pego em teste. **5) Índices do sync (mig 263):** 38 das 59 tabelas do pull não tinham índice para `(tenant_id, cursor, id)`; o plano fazia `Sort` em memória e lia proporcionalmente ao tamanho da empresa. Medido: uma comanda passou de 62 para 10 blocos. **6) `sync_marcador` (mig 264)** — marcador de "esta tabela desta empresa mudou quando?", alimentado por gatilho POR COMANDO (não por linha), com 4 baldes para não serializar no commit; ainda não é lido (entra no bloco de desempenho). | O dado apagado não volta, e a telemetria para de crescer sem fim |
| 2026-09-18 | **Sync — bloco de DESEMPENHO: o pull deixa de perguntar às 59 tabelas, respostas comprimidas, menos requisições por ciclo e espalhamento** (sem migration nova; usa o `sync_marcador` da mig 264). **Medido antes:** um pull SEM NOVIDADE custava **56 transações, 16.546 linhas lidas e ~40 ms** (empresa com 1.762 comandas) — em 5.000 lojas projeta ~5.000 consultas/s e ~1,4 milhão de linhas/s para entregar zero. **Medido depois: 8 transações, 767 linhas e 6 ms.** **Como:** o gatilho da mig 264 mantém "esta tabela desta empresa mudou quando?" e o pull lê UMA linha por tabela e só consulta as que mudaram depois do cursor daquele servidor — é o que o SymmetricDS faz com `sym_data` e o AppSync com a tabela delta. Pula só quando o marcador é ESTRITAMENTE anterior ao cursor (na igualdade pode haver linha com o mesmo instante e id maior) e só quando existe marcador (silêncio nunca vira "não precisa sincronizar"); teste de deriva garante gatilho em toda tabela do pull, porque marcador semeado sem gatilho deixaria a tabela parada para sempre. **Compressão:** a primeira carga media **7,48 MB** e ia sem compressão mesmo com `Accept-Encoding: gzip` — agora **887 KB** (8,4×); em 5.000 lojas voltando juntas são ~37 GB contra ~4,3 GB. **Menos requisições:** comandos e aviso de atualização viajam na resposta da batida e a batida antecipada só acontece quando importa (restauração pendente ou ciclo anterior longo) — de 5 para 2 requisições por ciclo, ~250 req/s a menos em 5.000 lojas. **Espalhamento e recuo:** o ciclo deixa de ser `setInterval` fixo (±20% de variação, recuo progressivo até 5 min enquanto a nuvem não responde e atraso aleatório no boot), contra o efeito manada na volta de um incidente. | Loja parada custa quase nada; loja em movimento sincroniza igual |
| 2026-09-18 | **Sync — bloco de ROBUSTEZ: aplicação em transação, filas em tabela, limpeza do escopo, trava distribuída do push e relógio que barra o envio** (sem migration nova; usa `sync_fila` da mig 264). **1) Transação na aplicação do pull:** sem ela, quem consultava o banco local no meio da aplicação via estado pela metade (comanda sem itens, pedido sem pagamento) — é o checkpoint atômico do PowerSync e o lote transacional do SymmetricDS. Cada bloco e cada linha rodam em PONTO DE SALVAMENTO, senão a primeira linha ruim derrubaria a resposta inteira; erro volta tudo e o ciclo seguinte reaplica, com o cursor avançando só no fim. **2) Filas em tabela:** órfãos e exclusões pendentes saíram de um texto JSON dentro de UMA linha de `sync_state` (reescrito a cada ciclo, até 5.000 itens) para `sync_fila`, com índice e contagem. **3) Limpeza do escopo:** com o pull por loja (#536), o que desceu da outra loja antes continuava no banco; agora sai numa passada única por instalação — Electric e PowerSync também removem do cliente o que sai do recorte. A limpeza roda ANTES do envio (senão a loja empurraria linhas que vai apagar) e, por rodar na sessão marcada como sync, NÃO gera registro de exclusão: é limpeza local, não exclusão de negócio. **4) Trava do push no banco:** o controle de 1 push por dispositivo era um conjunto EM MEMÓRIA — com mais de uma réplica da API, dois envios do mesmo servidor passavam em paralelo; virou trava consultiva do Postgres, liberada sempre no fim. **5) Relógio:** desvio acima de 15 min SUSPENDE o envio (o recebimento continua) em vez de deixar a loja carimbar dados com hora errada e a regra da mais nova descartar a versão certa — medido e testado nos dois sentidos. | O servidor local nunca mostra meio estado, e relógio errado não corrompe o histórico |
| 2026-09-18 | **Sync — a trava de "um envio por servidor local" vira LINHA no banco** (mig 266, **NÃO** `@cloud-only`). Levantamento pós-implementação: a trava que eu havia trocado por `pg_try_advisory_lock` no bloco de robustez **não funciona com pool de conexões** — provado com 20 envios simultâneos do mesmo dispositivo, em que **todos os 20 passaram**. Trava consultiva é por SESSÃO e é REENTRANTE: duas requisições que peguem a mesma conexão do pool travam as duas; e o `unlock` pode cair noutra conexão, deixando a trava presa até aquela conexão morrer (o dispositivo ficaria em 429 para sempre). Agora é uma LINHA por dispositivo em `sync_push_lock`, adquirida com `insert ... on conflict do update where em < agora - 120s`: a disputa é resolvida pelo banco, independe de qual conexão atendeu e expira sozinha se a API morrer no meio do envio. Repetido o mesmo teste: **2 dos 20 recusados com 429**, nenhuma trava pendurada, envio seguinte normal. | A proteção contra tempestade volta a existir de verdade |
| 2026-09-18 | **Impressão — worker do servidor local volta a ler a fila; roteamento pela loja da IMPRESSORA e pela MÁQUINA do agente; espera longa no agente de nuvem** (mig 267, **NÃO** `@cloud-only`). **P0:** a consulta de reserva do `impressao-daemon.mjs` (desde o #436, na 1.29.0) ordenava por `c.criado_em` que o `returning` do CTE não devolvia → o Postgres recusava em todo ciclo e nada da fila local imprimia; o SQL saiu para `edge/impressao-fila.mjs` e é executado num Postgres de verdade no CI (`impressao-fila.spec.ts`, schema montado pelas migrations reais). **Roteamento:** o servidor local filtra também a loja da impressora (as impressoras de todas as lojas descem para todos os servidores; um job "da rede" imprimia no IP da outra loja — reproduzido); os que apontam para outra loja viram erro com motivo. No agente de nuvem, os caixas dividem o token da loja: o agente informa `maquina` + impressoras do Windows e o job USB só vai para a máquina dona (`equipamento.agente_maquina`, aprendida na 1ª impressão confirmada; não aprende quando outro caixa tem o mesmo nome; zera ao trocar o nome no Windows). **Escala:** consulta a cada 3 s (1.667 req/s em 5 mil lojas, ~10× o sync) → `POST /impressao/pendentes` com espera de até 25 s acordada pelo `pg_notify` que já existia (1 cliente LISTEN por processo; sem LISTEN reconsulta a cada 5 s). Limite por IP → token já validado sai do balde do IP nas rotas da fila e ganha 240/min por dispositivo (chavear pelo token cru abriria balde novo por token inventado). **Robustez:** reserva renovada job a job; lote da nuvem 5 (reserva 120 s × ~25 s por job com impressora fora); agente guarda os ids impressos (ACK perdido não reimprime); erro de configuração vai direto para `erro`; um LISTEN por vez no worker. **Escopo:** nuvem não enfileira cupom para loja com servidor local ativo (evita job órfão e cupom em dobro com agente instalado junto); painel e reimpressão da fila restritos à loja do usuário. Índice `idx_impressao_claim` (sem uso) trocado pelo parcial `idx_impressao_fila_viva`. | Análise da impressão (defeitos reproduzidos e medidos antes e depois). |
| 2026-09-18 | **Impressão — roteamento POR LOJA na criação do job + setor equivalente + DANFE numa impressora só + aviso na reimpressão** (mig 268, **NÃO** `@cloud-only`). Setor é por loja (`setor.unidade_id`), mas o catálogo é da rede e o produto aponta UM setor. Todas as escolhas de impressora na criação do job passam a aceitar só equipamento **da loja da venda ou sem loja** (`lojaOuRede`): destino do produto, padrão do setor, "impressora única" (conta as da loja), destinos de opção/complemento, KDS por etapa e a padrão do setor dele, impressoras do perfil do cupom, fallback da etiqueta de validade. **Setor equivalente:** setor de outra loja → setor desta loja com o mesmo nome (`lower(btrim)`); sem equivalente → o item conta como "sem setor" e cai na impressora padrão (não some). Setor desta loja sem impressora continua sem via (decisão da loja). O roteamento da produção virou UMA função (`rotearProducao`) usada pela venda local e pelo materializador do edge (eram duas cópias). **DANFE:** uma impressora — a que imprimiu o cupom da venda; senão uma `faz_cupom` da loja (padrão primeiro); antes todas as de `papel='cupom'` da empresa. **Mig 268:** o `pg_notify` também no UPDATE que devolve o job a `pendente` pronto (reimprimir), não na volta com espera. **CI:** banco com todas as migrations do zero (`EDGE_MODE=true`) para os testes de SQL real. | Reproduzido: 2 de 5 casos certos antes, 5 de 5 depois; 485 testes. |
| 2026-09-18 | **Impressão — uma fila por impressora com disjuntor, comandos remotos por servidor, DANFE pelo servidor local, estado e acentos por impressora** (mig 269, **NÃO** `@cloud-only`). **Filas:** o worker e o agente reservam excluindo impressoras ocupadas/fora do ar e cada impressora anda sozinha (padrão spooler do Windows/CUPS); disjuntor 30 s dobrando até 5 min, jobs presos voltam sem tentativa (`SQL_DEVOLVER` / `POST /impressao/:id/devolver`); conexão 3 s separada do envio 8 s. **`edge_comando` por destino:** uma linha por `servidor_local` (loja informada ou a empresa toda), `dados jsonb`; linha antiga sem destino vale para qualquer servidor — corrige rollback/teste chegando a um servidor só. **DANFE:** com servidor local ativo, a nuvem manda o texto por comando (`imprimir_danfe`) e o servidor escolhe a impressora com a mesma regra; QR Code desenhado (`@QR:`). **`impressora_status`** (fora do sync — não carimba `equipamento`): gravado pelo worker e pelas confirmações do agente; painel e saúde. **Acentos:** `equipamento.codepage` cp860/cp850 com `ESC t`, caractere fora da tabela cai sem acento; padrão = sem acento. **Escolha manual** de impressora validada no servidor (`garantirImpressoraDaLoja`). **Aviso** de produto com setor sem equivalente na loja (`GET /impressao/avisos-roteamento`). | Medido: cozinha 77,1 s → 0,3 s (servidor local) e 0,1 s (agente); 497 testes. |
| 2026-09-18 | **Impressão — o job vai para onde será impresso; vias por tipo; espera longa por contador** (sem migration nova). `gravarOuEncaminharImpressao` (common/impressao-destino.ts): no servidor local grava local; na nuvem, com o servidor da loja ativo, manda comando `imprimir` (mig 269) com impressora+texto — usado por etiqueta de validade, ordem de produção e etapa do KDS; `criarPedidos` na nuvem não grava vias/etiquetas para loja com servidor local (o `EdgeImpressaoProcessor` gera as de lá). Vias: `cliente`/`producao` com número próprio, `fiscal`/`etiqueta`/`teste` = 1, o resto `vias`. Impressora desativada → erro definitivo. Reimprimir zera `tentativas`. Espera longa: com LISTEN de pé espera o tempo todo; o "job entrou entre a consulta e a espera" é resolvido por CONTADOR de avisos por empresa lido antes da consulta (horário girava sem pausa no mesmo ms — pego pelo teste). Estado das impressoras: fila parada por impressora (pendentes + mais antigo); na nuvem, para loja com servidor local, vem de `edge_status.saude`. Devolver exige `claim_por` = máquina. | Reproduzido (etiqueta órfã), medido (3 vias; 6→2 consultas por espera); 512 testes. |
| 2026-09-18 | **API do servidor local — análise com a API real e correções; aviso de status do edge pedido à NUVEM** (sem migration). Método: banco montado como o instalador + dados reais pelo sync + 268 GETs + rotinas + venda/cancelamento/pedido online pela API. **Aviso de status no edge (opção b, decisão do dono):** a loja não tem `integracao` (não sincroniza) nem as tabelas do WhatsApp oficial; o edge pede à nuvem (`POST /delivery/aviso-da-loja`, x-sync-token, @CloudOnly), que só aceita pedido que conhece (409 até sincronizar), sem repetir o mesmo aviso em 10 min; fallback local só p/ provedor gratuito. **Níveis:** `auth/niveis.ts` — os únicos valores de categoria; `@OperadorDeCaixa()` (todos os níveis; supervisão/execução precisam da permissão `pdv`); travas de liberação com `ehGestor()`. **`ehServidorLocal()`** (common/modo.ts) substitui comparações de EDGE_MODE à mão. **Processo:** `unhandledRejection` registra e segue; `uncaughtException` sai p/ reiniciar. **Caixa:** cancelamento usa o caixa do terminal; estorno = uma saída por lançamento de entrada, mesma forma. **Impressão:** produção aceita impressora da rede. **Registro interno** `ERROS-CONHECIDOS.md` (local, fora do git) passa a ser consultado antes de codar. | Reproduzido na API real; 525 testes. |
| 2026-09-18 | **Validação de entrada nas rotas de escrita — corpo vazio não muda estado em silêncio** (sem migration). Varredura das 383 rotas POST/PUT/PATCH/DELETE com corpo vazio e ids inexistentes, em modo servidor local e em modo nuvem (cópia do banco, ambiente sem segredos de terceiros), e comparação da cópia com o original. **Campo liga/desliga obrigatório** (`common/exigir.ts`, `exigirBooleano`: ausente = 400; consentimento continua `=== true`); **salvar configuração com campo ausente mantém o valor atual** (Cardápio Web: ambiente e config preservados); **registro novo sem loja herda a loja atual** (comanda); **erro de formato do Postgres (22P02/22007/22008/22003/22001) = 400** com a causa registrada como aviso; fechar caixa exige os valores contados; PIX prioritário só com gateway conhecido; TEF confere a comanda da empresa. | 0 erros 500 nos dois modos; 533 testes. |
| 2026-09-18 | **Atualização do servidor local no modelo "monta ao lado e troca o conjunto" + assinatura obrigatória + distribuição escalonada** (migration 270, cloud-only). O `.zip` leva as dependências (`node_modules.tar`); a versão nova é montada inteira em `atualizacao-<versão>` com a loja operando e a troca/rollback é renomear pastas (dist, dependências, app, scripts, migrations, manifesto) — saúde = `/ping` na versão nova + serviços de pé por 30 s, senão volta sozinho. Com `edge/update-pub.pem` instalado, a assinatura v2 (`regem-edge-v2|versão|sha|url|validade`) é obrigatória; v1 segue emitida para a transição. Release com `percentual` (sorteio estável por empresa+versão), `lojas_piloto`, `pausado`, `recolhido`; a loja recebe a MAIOR versão liberada. Instalar exige loja parada (caixa aberto 16 h / pedido em produção 3 h) ou confirmação; agendamento executado pelo sync-daemon. | Análise comparada com Velopack/Squirrel, A/B do Android, Mender, TUF, Tauri/Sparkle (Ed25519 com chave embutida), Chrome/electron-builder/Sparkle (rollout escalonado), Intune (anéis/pausa), balena (update locks). Reproduzido: `.zip` falhava em toda loja protegida (migrations com `enc:`), `npm ci` apagava o `node_modules` sem internet, rollback parcial, assinatura nunca conferida (loja sem chave pública), barra de progresso nunca lida (BOM). 8 cenários numa loja simulada com `.env.local` cifrado. |
| 2026-09-19 | **Pedido de marketplace: confirmar ao canal só depois de gravar** (iFood/Open Delivery: ACK só do processado; Anota Aí: grava → visto → aceita; 99: errno≠0 pede reenvio; Cardápio Web: cursor não avança com falha, teto 2 h). **99Food:** código de entrega própria em `delivery_code` (int); retirada concluída por `/order/order/finish`; entrega da 99 não recebe `delivered`. **Fallback da nuvem:** só loja com servidor local cadastrado e sem batida em `edge_status` nem `edge_heartbeat`. Sem migration. | Doc oficial do iFood ("persista antes do acknowledgment") e da 99Food (reenvio com errno≠0; campos lidos no portal oficial); padrão de mercado inbox/idempotent consumer (Stripe, SQS). Reproduzido: Anota Aí aceitava no canal sem gravar; iFood perdia o pedido; nuvem aceitava pedido de loja com servidor vivo e de loja sem servidor. |
| 2026-09-19 | **Importação de clientes da Anota Aí (CSV) sem sobrescrever** (mig 271: `cliente.importacao` jsonb). Identidade = "Número Telefone" (a coluna WhatsApp da Anota Aí às vezes vem sem o 9); nome genérico ("cliente", ".", símbolo) entra vazio; só entram números novos — o cliente existente não é tocado (nem etiqueta); ordem recomendada ativos → inativos → potenciais (o primeiro segmento vale). Públicos novos no CRM/campanhas. Excel (.xlsx) e CSV: leitor de .xlsx próprio (ZIP + XML, deflate nativo, teto contra arquivo-bomba) — sem biblioteca nova no pacote da loja; conferido com as exportações reais (Excel e CSV dos inativos = os mesmos 9.999 contatos). PDF recusado (layout de impressão). A Anota Aí corta a exportação em 10.000 linhas. | Pedido do lojista: aproveitar a base da Anota Aí (~16 mil) para disparos sem perder o que o Regem já tem dos pedidos integrados. |
| 2026-09-21 | **NFC-e: emissão direta na SEFAZ (sem hub fiscal) e numeração por ponto de emissão** (mig 278). Sem certificado, a emissão é **recusada** — o transmissor simulado passa a exigir homologação + `FISCAL_SIMULADO=true`, e a nota simulada fica marcada e sai com tarja. Série própria por origem ('loja' e 'nuvem', nunca 0), contador que se recupera do maior número já emitido e índice único de reforço. `fiscal_config` desce para a loja e `nota_fiscal` volta. Corrigidos `dhEmi` (fuso da UF, não UTC) e `enderEmit` (município, bairro, número, CEP). | Hub em nuvem é **síncrono** e não emite com a internet da loja caída; os dois que emitem offline (Focus, TecnoSpeed) fazem isso instalando binário de terceiro no servidor da loja — e a Nuvem Fiscal foi desligada em 31/07/2026, o que tira o risco de fornecedor do campo teórico. Séries distintas são expressamente permitidas (Ajuste SINIEF 19/16, cl. 4ª, §1º) e não se comunicam ao Fisco. Pesquisa, base legal, o que varia por UF e o calendário da reforma: `docs/cupom-fiscal.md`. |
| 2026-09-22 | **Certificado A1 e CSC: só cifrados, e fora do sincronismo** (mig 279). AES-256-GCM com `SEGREDOS_CHAVE` **por servidor** (nuvem no EasyPanel; cada loja no `.env.local` protegido por DPAPI). A tabela `fiscal_credencial` não sincroniza; a loja receberá a sua cópia pelo canal autenticado do sync, recifrada com a chave dela. Sem chave, nada é guardado. Um CSC por ambiente. Cadastro só-nuvem e só do presidente. | Uma chave única compartilhada faria uma loja comprometida abrir o certificado de todas as outras. E o e-CNPJ assina em nome da empresa em qualquer sistema do governo, não só na NFC-e — é o segredo mais sensível que guardamos. GCM para que dado adulterado seja recusado em vez de usado. |
| _adicionar novas linhas aqui_ | | |
