# Regem — Decisões de Design e Contratos de Dados

> Documento vivo. Toda decisão nova aprovada em sessão vai no changelog (§6). Fonte: mockups em `mockups/`. **Nota de nomenclatura:** nos contratos abaixo, `loja` = `unidade` no schema; `tenant` = `empresa`.

---

## 1. Identidade

- Conceito: "no comando de todo o negócio, do balcão ao balanço". Autoridade + operação saudável.
- Logo: monograma **R.O. em órbita** (Rodrigo Oliveira), anel dourado. (Nos mockups aparece "Ω" — placeholder Omera; usar sempre o `RegemMark`.)
- Assinatura visual do produto: **Linha do Tempo Operacional** — o dia da loja em faixas horizontais por setor, com marcador "AGORA" em tempo real.

## 2. Design tokens

### 2.1 Tema claro (app principal + app do colaborador) — classe `.app-light`

```css
.app-light{
  /* superfícies */
  --canvas:#EDF0F4;      /* fundo geral (cinza-azulado frio) */
  --surface:#FFFFFF;     /* cards */
  --surface-2:#F6F8FA;   /* áreas rebaixadas, tracks, inputs */
  --line:#DCE3E9;        /* bordas */
  --line-2:#C6D0D9;      /* bordas de inputs */
  /* tinta */
  --ink:#0F2230;         /* texto principal / sidebar (petróleo) */
  --ink-2:#3D5566;       /* secundário */
  --ink-3:#7A8FA0;       /* terciário, rótulos */
  /* AÇÃO PRIMÁRIA / MARCA = ÂMBAR (Regem). ⚠️ Âmbar é SEMPRE ação/marca —
     nunca cor de estado. (mockups Fable 2026-07-02) */
  --primary:#E8A845;     --primary-ink:#2A1D06;
  /* semânticas de status — dessincronizadas do âmbar de propósito */
  --ok:#1FA875;  --ok-soft:#E1F3EC;    /* verde: sucesso/operação saudável */
  --warn:#E06A3C; --warn-soft:#FCEAE1; /* laranja-avermelhado: atenção/pendência (NÃO é âmbar) */
  --danger:#D64545; --danger-soft:#FAE4E4; /* vermelho: crítico (dark: #E05252) */
  --info:#3A7FB8; --info-soft:#E4EFF7;
  /* forma e tipografia */
  --radius:10px; --radius-lg:14px;
  --font-display:'Archivo',sans-serif;    /* títulos, rótulos caps, marca */
  --font-body:'Figtree',sans-serif;       /* corpo */
  --font-mono:'JetBrains Mono',monospace; /* KPIs, horários, valores, códigos */
}
```

### 2.2 Tema escuro (KDS e Terminal de Ponto — leitura à distância)

```css
:root{
  --bg:#0B141B;     /* KDS */   /* Ponto: #0F2230 */
  --panel:#12202A;  /* Ponto: #16303F */
  --panel-2:#182B37;/* Ponto: #1D3B4D */
  --line:#24394699; /* Ponto: #2A495C */
  --text:#EAF2F7; --text-2:#8FA9BA;
  --ok:#19C08F; --warn:#FFB13D; --danger:#FF5A4E; --info:#4AA8E0;
}
```

### 2.3 Regras de uso

- **Dourado (`--primary`)** é a única cor de ação primária/CTA e de marca. **Verde (`--ok`)** = status de sucesso, nunca CTA. **Âmbar (`--warn`)** nunca é ação primária, só alerta/correção.
- Números sempre em `--font-mono` (KPIs, horários, NSR, preços, pontos).
- Rótulos de seção/setor: Archivo 700, caps, letter-spacing `.12em–.18em`, 10–11px.
- Status nunca por cor apenas: sempre acompanha texto na `Tag`.
- Avatares: iniciais sobre cor derivada do id do colaborador (paleta `#0E7C66 #2C6E9B #6B3FA0 #8A5A10 #C2453A`).

## 3. Matriz RBAC (view × perfil) — **aplicada no servidor**

Perfis (categoria da hierarquia): `presidente` > `gerente` > `supervisao` > `execucao`.

| View / recurso | presidente | gerente | supervisao | execucao |
|---|:--:|:--:|:--:|:--:|
| dashboard | ● | ● | ◐ (só seu setor) | ✕ |
| escalas | ● | ● CRUD | ◐ leitura do setor | ◐ só as próprias (app) |
| tarefas / checklists | ● | ● CRUD | ● CRUD no setor | ● concluir apenas |
| estoque / recebimento | ● | ● | ● registrar no setor | ✕ |
| fichas técnicas | ● | ● | ◐ leitura | ✕ |
| vistorias / desperdício | ● | ● | ● registrar no setor | ● quando designado |
| POP & guias | ● | ● CRUD | ● CRUD no setor | ◐ leitura |
| desempenho / gamificação | ● rede | ● loja | ◐ setor | ◐ só o próprio |
| pessoas & ponto (gerencial) | ● | ● aprova ajustes/extras | ✕ | ◐ espelho próprio (app) |
| fornecedores | ● | ● | ✕ | ✕ |
| mural & clima | ● rede | ● loja | ● setor | ◐ lê e responde |
| **visão C&O (relatórios rede)** | ● | ✕ **nunca** | ✕ | ✕ |
| **bot (editar regras)** | ● | ◐ consulta log | ✕ | ✕ |
| **módulos on/off** | ● | ✕ | ✕ | ✕ |
| wizard de configuração | ● | ● | ✕ | ✕ |
| log de auditoria | ● rede | ● loja | ✕ | ✕ |

Legenda: ● acesso/edição · ◐ só leitura (escopo indicado) · ✕ a API não retorna o recurso.

Adicionais: `gerente` pode ser a mesma pessoa que `presidente`. `supervisao` tem escopo = setor(es) atribuído(s). Sobrescrever responsável de tarefa: `gerente`+; sempre audita.

## 4. Contratos de dados (referência p/ a API)

### 4.1 Hierarquia base
```json
{ "empresa": {"id":"t_01","nome":"Grupo Vieira"},
  "unidade": {"id":"l_01","tenant_id":"t_01","nome":"Loja 01 — Centro","ramo":"food_service"},
  "setor":   {"id":"s_coz","unidade_id":"l_01","nome":"Brigada de Cozinha"},
  "funcao":  {"id":"f_sous","setor_id":"s_coz","nome":"Sous-chef","categoria":"supervisao"},
  "colaborador": {"id":"c_042","unidade_id":"l_01","funcao_id":"f_sous","nome":"Paulo Lima",
    "matricula":"00042","vinculo":"clt","escala_modelo":"6x1",
    "lgpd":{"consentimento_foto":true,"data_consentimento":"2026-01-10"}} }
```

### 4.2 Escala e turno
```json
{ "turno": {"id":"sh_9911","colaborador_id":"c_042","unidade_id":"l_01","setor_id":"s_coz",
  "data":"2026-07-02","inicio":"08:00","fim":"17:00","intervalo_previsto":"12:00",
  "modelo":"6x1","tipo":"regular"} }
```
Modelos: `6x1, 5x2, 12x36, 4x3, horista, diarista, pj, autonomo`. `tipo`: regular | plantao_12x36 | diaria | pj | folga | descoberto.

### 4.3 Timeline operacional — `GET /unidades/{id}/timeline?data=YYYY-MM-DD`
Formato do `TIMELINE_DATA` do mockup (horas decimais, janela 06–22 configurável):
```json
[ { "setor":"Cozinha", "blocos":[
  { "ini":10.8, "fim":16.2, "rot":"Linha do almoço", "quem":"Sous-chef P. Lima",
    "colaborador_id":"c_042", "st":"exec" } ] } ]
```
`st`: ok | exec | atencao | feito.

### 4.4 Tarefa e checklist
```json
{ "tarefa": {"id":"tk_501","setor_id":"s_coz","data":"2026-07-02","hora":"14:30",
  "titulo":"Higienização de bancadas pós-almoço",
  "responsavel":{"colaborador_id":"c_042","origem":"escala"},
  "pop_id":"POP-COZ-004","status":"pendente","ociosidade":false,
  "conclusao":{"quando":null,"por":null,"foto_url":null}} }
```
`origem`: escala | manual (manual audita). `status`: pendente | concluida | atrasada | impossivel.

### 4.5 Ponto (Portaria 671/2021)
```json
{ "marcacao": {"nsr":"000048292","equipamento_id":"PONTO-ENTRADA-01","colaborador_id":"c_042",
  "tipo":"entrada","timestamp":"2026-07-02T07:54:12-03:00","turno_id":"sh_9911",
  "hash_integridade":"sha256:…","foto":{"url":null,"consentimento":true,"data_expurgo":"2027-07-02"},
  "origem":"terminal","geoloc":null,"offline_sync":false} }
```
`tipo`: entrada | inicio_intervalo | fim_intervalo | saida. `origem`: terminal | celular_geolocalizado.

### 4.6 Estoque, PVPS e recebimento
```json
{ "item_estoque": {"id":"i_tom","nome":"Tomate italiano","unidade":"kg","lote":"L-2481",
    "quantidade":6,"vencimento":"2026-07-04","posicao_pvps":"frente"},
  "recebimento": {"id":"rc_301","fornecedor_id":"fo_01","nf":"48211","foto_nota_url":"…",
    "itens":[{"item_id":"i_tom","qtd_nf":10,"qtd_recebida":10,"status":"ok"},
             {"item_id":"i_ceb","qtd_nf":5,"qtd_recebida":0,"status":"nao_veio"}],
    "encaminhado_financeiro":{"quando":null,"formato":"json"}} }
```
Índice de faltas do fornecedor = `nao_veio / total_itens` acumulado. `posicao_pvps`: frente | fundo (fundo = inconformidade).

### 4.7 Ficha técnica (alimenta CMV e compras) — **implementado**
```json
{ "ficha": {"id":"FT-021","nome":"Molho base de tomate","setor_id":"s_coz",
  "rendimento":{"qtd":10,"unidade":"porcao_500ml"},"validade_pos_producao_dias":6,
  "ingredientes":[{"item_id":"i_tom","qtd_liquida":4.5,"fc":1.15,"custo_unit_ref":"ultimo_recebimento"}],
  "preco_venda":14.90,"meta_cmv_pct":31.5,"pop_id":"POP-COZ-002",
  "passos":[{"ordem":1,"texto":"…","midia_url":null}]} }
```
Cálculo: `custo_total = Σ(qtd × fc × custo_unit)`; `custo_porcao = custo_total / rendimento`; `cmv = custo_porcao / preco_venda`.

### 4.8 Gamificação — **implementado**
```json
{ "ocorrencia": {"colaborador_id":"c_042","tipo_id":"oc_cobriu_turno","pontos":8,
  "registrado_por":"c_gerente","origem":"manual"} }
```
Pode ficar negativa. Ranking é **opaco**: só o `presidente` vê (nem o gerente). Tipos/premiações predefinidos pelo presidente.

### 4.9 Auditoria (imutável, append-only)
```json
{ "auditoria": {"id":"au_88","tenant_id":"t_01","unidade_id":"l_01",
  "quando":"2026-07-02T13:40:00-03:00","quem":{"colaborador_id":"c_ger","perfil":"gerente"},
  "tipo":"ponto","acao":"aprovou_hora_extra",
  "detalhe":{"alvo":"c_jul","valor":"+1h10","ref":"2026-07-01"},"origem":"web"} }
```
`tipo`: escala | ponto | recebimento | checklist | modulos | vistoria | bot | mural. `origem`: web | mobile | app_colaborador | kds | terminal.

### 4.10 Bot de suporte
```json
{ "regra_bot": {"id":"rb_01","tipo":"pedido_atrasado",
  "gatilhos":["atraso","demorando","cadê meu pedido"],
  "resposta":"Seu pedido está em preparo prioritário. Tempo: {tempo_estimado}.",
  "canais":["delivery"],"escalar":{"modo":"condicional","condicao":"atraso_min > 15"},
  "ativa":true,"editavel_por":"presidente"} }
```

### 4.11 Módulos (feature flags)
```json
{ "modulos":{"app_colaborador":true,"kds":true,"terminal_ponto":true,"bot_ia":false},
  "escopo":"rede" }
```
`escopo`: rede | unidade:{id}. Alteração só por `presidente`, sempre audita.

## 5. Arquitetura dos módulos satélites

| Módulo | Plataforma real | Comunicação | Observações |
|---|---|---|---|
| **KDS** | app nativo/empacotado (Tauri/Electron Win · Android) | **WebSocket na rede local**; descoberta mDNS; MAC cadastrado no servidor | Web não acessa MAC/mDNS → mockup é referência de UI. Alertas entram no topo da fila com som |
| **Terminal de Ponto** | tablet Android ou desktop empacotado | HTTPS + fila offline local | NSR por equipamento; exporta AFD/AEJ |
| **App Colaborador** | PWA ou app (React Native) | HTTPS + IndexedDB offline | Só leitura + concluir tarefas; sem cadastros |
| **Integrações externas** | — | API pública (OpenAPI) + webhooks | Delivery, WhatsApp Business, contabilidade |

## 6. Changelog de decisões

| Data | Decisão | Motivo |
|---|---|---|
| 2026-07-02 | RBAC no servidor; matriz §3 aprovada | Hierarquia é pilar do produto |
| 2026-07-02 | Timeline usa contrato §4.3 (horas decimais, janela configurável) | Mockup já renderiza deste formato |
| 2026-07-02 | Offline: last-write-wins + log de conflito; IDs idempotentes no cliente | Simplicidade no MVP |
| 2026-07-02 | Ponto estruturado p/ Portaria 671/2021 (NSR/AFD/AEJ) desde o MVP | Evitar retrabalho legal |
| 2026-07-02 | KDS/Ponto como apps independentes com WebSocket local | Limitações do navegador |
| 2026-07-02 | **Marca Regem: ação primária = DOURADO `#E2A340`; verde `#0E7C66` vira status "ok"** (mockup Omera usava verde como primária) | Decisão de marca do usuário ("Dourado Regem") |
| 2026-07-02 | Nomenclatura: `loja`=`unidade`, `tenant`=`empresa` no schema | Alinhar contratos ao schema Drizzle existente |
| 2026-07-02 | **Landing pública em `/`; login movido para `/entrar`** (redirects de auth/logout atualizados) | Ter tela de marketing antes do login (ref. brand-kit "Topo de landing page") |
| 2026-07-02 | **Fase F-A: tempo real via socket.io.** Gateway NestJS com rooms por tenant/unidade; handshake por JWT (gestor) ou token de equipamento (device). Eventos `ponto:marcado`, `kds:alerta`, `device:status`. Tabela `equipamento` (migration 015) + **NSR agora por equipamento** (Portaria 671). Superfície de teste: `/kds` web (tema escuro). | Base dos apps satélites. Escolha do usuário (socket.io + NSR por equipamento já). Packaging nativo/mDNS = Fase F-B |
| 2026-07-02 | **Conflito de cor resolvido (mockups Fable):** âmbar `#E8A845` é SÓ marca/ação; atenção passa a laranja-avermelhado `#E06A3C`, crítico vermelho `#E05252`/`#D64545`, verde segue "ok". Fontes do tema escuro unificadas em Archivo/Figtree/JetBrains (era Sora/Inter). Tokens em `globals.css`. | Se alertas ficassem âmbar, o usuário não distinguiria "ação" de "algo errado" |
| 2026-07-03 | **Hardening P1 (branch `hardening`):** `empresa.controller` protegido (só a empresa do token; POST/findAll removidos — vazamento cross-tenant sanado); `main.ts` com helmet + **fail-fast** de `JWT_SECRET` e de `CORS_ORIGIN` em produção; `@nestjs/throttler` global (120/min) + 5/min em `login`/`pin`/`register`; **lockout de PIN** (10 falhas/15 min por unidade) com falhas registradas na auditoria. | Exposição de dados em produção com auto-deploy ligado; anti brute-force/spam |
| 2026-07-03 | **CI/CD P2:** `.github/workflows/ci.yml` roda `npm ci` + `npm run build` (+ `npm test`) em backend e frontend a cada push/PR. Push em `main` exige CI verde (branch protection + PR). Documentado no CLAUDE.md. | Portão de qualidade antes do auto-deploy |
| 2026-07-03 | **Testes P3:** Jest no backend + regras críticas isoladas em `src/common/regras-negocio.ts` (usadas pelos services): custo médio ponderado, saldo do ledger, retry de NSR (23505), late-binding tarefa→escala, recorrência de títulos. 16 testes verdes. | Cobrir 100% as regras de cálculo de dinheiro/ponto |
| 2026-07-03 | **RBAC por escopo P4 (§3):** escopo `setor`/`unidade` resolvido no login (via `função→setor→unidade`, sem migration) e embutido no JWT; guard injeta `setorId`/`unidadeId`; `escopoPermiteSetor` (supervisor só o próprio setor) com testes. Filtro aplicado no `desperdicio.findAll` como referência — **rollout aos demais endpoints operacionais é incremental**. | Guard validava só a categoria, não o escopo |
| 2026-07-03 | **Auditoria P5:** migration 023 torna `audit_log` **append-only** (trigger aborta UPDATE/DELETE). Auditoria adicionada à conclusão de tarefa e submissão/publicação de checklist (onboarding template e alocação de escala já auditavam). **Ainda sem auditoria (rollout incremental, menor risco):** CRUD de colaborador/função e demais cadastros de configuração, override de tarefa, movimento manual de estoque. | Trilha imutável e cobertura das mutações sensíveis |
| 2026-07-03 | **Contrato de API P6:** `@nestjs/swagger` (plugin auto-anota DTOs no build); `/api/v1/docs` exposto (em prod só com `SWAGGER_ENABLED=true`); `npm run openapi` gera `docs/openapi.json` a partir do `dist`. Referenciado no CLAUDE.md. | Contrato versionado para integrações externas (Fase K) |
| 2026-07-03 | **Sessão no front P7:** qualquer 401 limpa o token e redireciona para `/entrar?expirada=1` (banner amigável); erro de rede distinto de erro de API. **Decisão consciente:** manter o JWT em `localStorage` no MVP (app e API em domínios distintos — `app.` × `api.`). **Plano:** migrar para cookie `httpOnly` quando houver domínio unificado (mitiga XSS). | Sessão previsível + decisão de segurança registrada |
| 2026-07-03 | **Portão de CI = por disciplina (opção B):** branch protection não é aplicada em repo privado no plano grátis do GitHub (exige Team/Enterprise). Regra criada em `main` mas "not enforced"; adotado o fluxo branch→PR→CI verde→merge por convenção. Migrar para Team quando o time crescer. | Limitação de plano do GitHub |
| 2026-07-03 | **Fase ERP passo 1+2** (ver `logica-negocio.md`): ledger de estoque ganha `ref_tipo`/`ref_id` (idempotência via índice único) + `POST /producao` (explosão de ficha: baixa insumos ao custo médio, entrada do produto ao custo teórico). Migration 024. Fichas aninhadas ficam de fora (schema não referencia sub-ficha) e `vendas.baixaFicha` ainda sem `ref_*` (follow-up). | Destravar o motor de estoque/custo sem depender de PDV |
| 2026-07-03 | **ERP passo 3** (ver `logica-negocio.md`): ROP real (dias_seguranca + qtd sugerida) e view de **validades FEFO** no `/estoque`; **jobs** 06:00 (ponto de pedido) e 06:10 (validades) emitindo alerta em tempo real ao KDS. Sem migration. | Automatiza compra e reduz perda |
| 2026-07-03 | **ERP passo 4** (ver `logica-negocio.md`): snapshot de estoque (endpoint + cron mensal) + **CMV real × teórico → desvio** (`/estoque/cmv`) com card no `/estoque`. Sem migration (usa `estoque_snapshot` da 024). "Furo" (desvio − desperdício) pendente pois desperdício não liga a item/custo. | O KPI de ouro que diferencia o produto |
| 2026-07-03 | **ERP passo 6 (jornada)** + **Furo de CMV**: espelho de ponto ganha prévia gerencial (extra 50/100%, noturno 22h–5h, DSR estimado; sem migration); desperdício vinculável a item baixa estoque e fecha **furo = desvio − desperdício** (migration 025). Ver `logica-negocio.md`. | Fecha jornada e o ciclo de custo |
| 2026-07-03 | **Mural & Clima** (migration 026): comunicados com **leitura rastreável** (`comunicado`/`comunicado_leitura`) + **pesquisa de clima anônima**. **Decisão de privacidade (LGPD):** `clima_resposta` não guarda `colaborador_id`; a contagem "N/total" e a trava de voto duplo vêm de `clima_participacao` (separada), sem ligar pessoa→resposta; API só devolve o consolidado. Publicar/criar = gestor; ler/responder = qualquer autenticado. Tela `/mural`. | Engajamento de equipe com anonimidade garantida no schema |
| 2026-07-03 | **Bot de Suporte** (migration 027): bot de **regras por palavra-chave** (não IA generativa) — `bot_regra` (tipo, gatilhos, resposta, escala nunca/sempre/condicional) + `bot_atendimento` (log → métrica). Casamento é função pura `casarRegraBot` (normaliza acento/caixa). Editar regras = **presidente** (mockup: "só Proprietário / C&O"); listar/métricas = gestor; perguntar = qualquer autenticado. Sem match ou regra 'sempre' → escala para humano. Tela `/bot` (tabela + chat de teste). | Suporte de 1º nível automatizável sem custo de LLM |
| 2026-07-03 | **Wizard por ramo (versão rica)** (sem migration — blueprints hardcoded em `onboarding/templates.ts`): 4 ramos (food_service/varejo/industria_leve/servicos) com setores+funções sugeridos. Wizard `/wizard` em 4 passos (ramo → setores → funções → escalas + resumo) com chips selecionáveis; `POST /onboarding/wizard` cria **só o selecionado** (setores/funções/etiquetas) em transação + auditoria `aplicou_wizard`. Reusa `setor`/`funcao`/`etiqueta`; `/inicio` e `POST /onboarding/template` (pacote completo) seguem intactos. Escalas = informativas. RBAC presidente/gerente. | Ativação com menos atrito, customizável por ramo |
| 2026-07-03 | **Fichas técnicas aninhadas** (migration 028: `ficha_ingrediente.sub_ficha_id`): um ingrediente pode ser item de estoque, **sub-receita** (outra ficha) ou avulso. Custo é **recursivo** (sub-ficha entra pelo custo/porção — pura `custoTotalFicha` com memo+guarda de ciclo); explosão de `POST /producao` é **recursiva** até os itens-raiz (agrega consumo, `visitados` anti-ciclo). Ciclo (direto/transitivo) barrado na escrita (`fichaAlcancavel`) + safety net na leitura (ramo repetido = 0). Front `/fichas` com seletor de sub-receita. | Aprofunda o motor de CMV/explosão p/ food service real |
| 2026-07-03 | **Revisão geral** (migration 029): corrigido bug — `vendas.baixaFicha` não explodia sub-fichas (venda com sub-receita não baixava insumos aninhados → CMV distorcido), agora recursiva. **Mural escopado por unidade** (`colaborador.unidade_id`; feed rede+loja; "alvo" por comunicado). **Venda idempotente** (`comanda.idempotency_key` único por tenant; PDV gera chave estável). **Wizard idempotente** (reusa setor/função por nome). **Clima** só revela consolidado com ≥3 respostas (anti-deanonimização). **Bot** métrica "hoje" no fuso America/Sao_Paulo. | Robustez multi-loja + integridade dos números |
| 2026-07-04 | **P-UI1 — Shell responsivo** (migration 030: `colaborador.ui_prefs jsonb`): prefs `{sidebar:expanded\|collapsed, side:left\|right}` no **localStorage `regem_ui`** (paint) + **servidor** (`GET/PATCH /colaborador/me/prefs`, fonte da verdade entre dispositivos; puras `sanitizeUiPrefs`/`mergeUiPrefs` testadas). Shell via **CSS Grid** com `[data-side]`/`[data-collapsed]` e `order` (markup único, 240/72px, `prefers-reduced-motion`); recolhido = ícones+tooltip, rótulos viram divisores, logo→monograma, botão recolher com `aria-expanded` + atalho **`[`**; **<860px = drawer** off-canvas do lado escolhido (Esc/clique-fora, **foco preso**, hambúrguer no mesmo lado). Menu da conta com **Preferências** (destro/canhoto + recolher). Toasts espelhados: adiado (sem toast global hoje). | Fundação responsiva/ergonômica (base do P-UI2/3) |
| 2026-07-04 | **Toast global** (sem migration): `lib/toast.ts` (pub/sub singleton — chamável de qualquer lugar) + `<Toaster>` no `layout.tsx`, `aria-live`, auto-dismiss. **Espelha para o lado oposto à sidebar** (lê `regem_ui.side`) — não nasce sob o polegar que navega (fecha o detalhe adiado do P-UI1). Adotado em `/estoque` (snapshot/alertas) e `/pdv` (venda); adoção nas demais telas é incremental. | Feedback consistente + ergonomia canhoto/destro |
| 2026-07-04 | **Alertas de estoque persistidos** (migration 031: `alerta_estoque`): ROP e FEFO deixam de ser só eventos efêmeros — os jobs (06:00/06:10) **gravam** um alerta por (tenant, tipo) via upsert-por-aberto (índice único parcial), **auto-resolvem** quando a condição some, e ainda emitem o evento em tempo real. `GET /estoque/alertas` + `POST /estoque/alertas/:id/resolver` (gestor); card no `/estoque` com "Resolver". | Alertas confiáveis (sobrevivem, listam, resolvem) |
| 2026-07-04 | **NOVA ROTA — Arquitetura Edge** (`docs/arquitetura-edge.md`): substitui "satélites nativos/mDNS" por **local-first** (servidor local = PDV com Postgres + sync direcional com a nuvem). **Slice 1 (sem migration):** config direcional `TABELAS_SYNC` (sobe/desce/ambos + cursor), função pura **`venceLWW`** (last-write-wins + desempate por id, testada) e **`GET /sync/pull?desde=`** (deltas de controle por cursor, tenant do token, whitelist de tabelas). v2: `POST /sync/push` + sequência monotônica (migration) + tombstones + token de serviço. | Operar offline na loja; sync/backup na nuvem |
| 2026-07-04 | **Sync Edge slice 2** (sem migration): **`POST /sync/push`** (ingestão do operacional local→nuvem, append-only). **Segurança 1ª classe:** autenticação por **token de dispositivo** (`equipamento.tipo='servidor_local'`, guard `SyncTokenGuard`, header `x-sync-token`) em vez de JWT de presidente; **`tenant_id` FORÇADO** ao do token (ignora o da linha); **whitelist de tabelas** (`TABELAS_PUSH`) + **colunas por introspecção** (só colunas reais); identificadores via `sql.identifier`; idempotente (`on conflict (id) do nothing` + trata 23505); limites de payload. **Redação de segredos no pull** (`colaborador.senha_hash/pin_hash` nunca saem — PIN 4 díg. é brute-forçável offline). v3: LWW-update ('ambos'), sequência monotônica, tombstones. | Sync seguro sem confiar no cliente |
| 2026-07-04 | **Sync Edge slice 3 — LWW-update** (sem migration): `POST /sync/push` passa a aceitar as tabelas **`ambos`** (`item_estoque`, `fornecedor`) com upsert **last-write-wins**: `on conflict (id) do update` só se a linha recebida for **mais nova** (`updated_at`) **e do mesmo tenant** (`where <t>.tenant_id = excluded.tenant_id`) — nunca sobrescreve `id`/`tenant_id`. Fecha o caminho bidirecional das tabelas mutáveis. Config `modoPush(tabela)` → append\|lww\|null. v4: sequência monotônica (cursor à prova de clock-skew, migration) + tombstones (hard-deletes/soft-delete que não bumpa `updated_at`). | Bidirecional seguro do estoque/custo |
| 2026-07-04 | **Sync Edge slice 4 — soft-deletes** (sem migration): `GET /sync/pull` passa a incluir no delta as linhas com `deleted_at > desde` (por introspecção, só onde a coluna existe) → **exclusões propagam** mesmo sem bump de `updated_at`. **Decisão de segurança:** sequência monotônica + tombstones de **hard-delete** exigem **triggers nas tabelas centrais** e **não serão deployados sem ambiente de teste** (trigger com erro derruba todos os writes) — **adiado para a Fase 2** (Postgres local valida o CDC antes). | Excluir propaga sem risco; CDC perigoso fica pra ambiente testável |
| 2026-07-04 | **Fase 2 (edge) — PWA quiosque** (sem migration): `/kds` e `/terminal/ponto` viram **PWAs instaláveis** em tela cheia. Manifests próprios (`kds.webmanifest`/`ponto.webmanifest`, `display:fullscreen`, orientação, tema), ícones PNG on-brand gerados (círculo âmbar+órbita sobre navy, 192/512/maskable), **service worker** (`/sw.js`) com **shell offline** (network-first na navegação, SWR nos assets) que **NÃO intercepta a API** (outra origem → dados vão direto e o app trata a falha). Layouts de rota (`app/kds/layout.tsx`, `app/terminal/ponto/layout.tsx`) setam metadata/viewport + registram o SW. | Satélites instaláveis/tela-cheia com UI offline, testável no fluxo web |
| 2026-07-04 | **Fase 2 (edge) — servidor local + daemon de sync, VALIDADO localmente** (sem migration): `ConfigModule` prioriza `.env.local` (edge/dev, gitignored) → backend roda contra **Postgres local**. **`edge/sync-daemon.mjs`**: pull (controle nuvem→local) + push (operacional local→nuvem), cursores em `sync_state`, resiliente a **ordem de FK** (retry). Testado ponta a ponta com 2º banco fazendo o papel da nuvem. **2 bugs de sync corrigidos:** (a) `categoria_produto` sem `updated_at` → cursor defensivo (cai p/ `created_at`, não derruba o pull); (b) `empresa` (tenant) faltava no sync e filtra por `id` → adicionada com `escopo:'id'`. `scripts/apply-all-local.mjs` aplica as 31 migrations no banco local. Ver `backend/edge/README.md`. | Servidor local funcionando + sync validado com banco de teste real |
| 2026-07-05 | **Polimento mobile — Fase 1 (velocidade percebida)** (sem migration): (1) as 3 fontes saíram do `@import` bloqueante do `globals.css` para **`next/font/google`** (self-host, `display:'swap'`), ligadas às variáveis já existentes `--font-sans`/`--font-display`/`--font-mono` no `<html>` (tailwind.config inalterado). (2) `export const viewport` no layout raiz com **`viewportFit:'cover'`** (safe-areas) + **`themeColor`** por esquema (navy `#0D1A2B` escuro / base `#EDF0F4` claro). (3) Novos **`components/ui/skeleton.tsx`** (Skeleton/Text/Card/List) e **`empty-state.tsx`** (ícone+título+descrição+ação); os "Carregando…" das 10 telas mais usadas (meu-dia, escala, estoque, pedidos, kds, pdv, cadastros, pessoas, financeiro, produtos) viraram **skeletons no formato do conteúdo real** — `ResponsiveTable` ganhou skeleton de linhas/cards (cobre estoque e demais tabelas); PDV ganhou flag `carregado` p/ separar carregando de vazio; KDS usa skeleton no tema escuro enquanto checa a sessão. (4) `globals.css`: **`-webkit-tap-highlight-color: transparent`** + **`overscroll-behavior-y: none`** no `body`. Tokens/paleta/tipografia preservados; nenhuma funcionalidade removida. Build verde. | 85% dos usuários é Android — reduzir tela em branco e travar comportamentos touch |
| 2026-07-05 | **Polimento mobile — Fase 2 (BottomNav Material 3)** (sem migration): `components/app-shell/bottom-nav.tsx` — (1) **filtro por perfil** via `getCategoria()` do JWT (mesmo padrão da sidebar): item Cadastros ganha `roles:['presidente','gerente','supervisao']` → **Execução não vê Cadastros**; (2) estado ativo corrigido para `path === href` ou `path.startsWith(href + '/')` (cobre sub-rotas); (3) **`min-h-[56px]`** + `justify-center` nos itens (alvo de toque M3); (4) **`navigator.vibrate?.(10)`** ao trocar de aba (não vibra ao retocar a atual); (+) `pb-[env(safe-area-inset-bottom)]` p/ não ficar sob a home-bar (efeito do `viewportFit:'cover'` da F1). **Descoberta:** o componente não estava montado (só um import morto em `docs/page.tsx`) — **decisão do Rodrigo: montar na Shell**, `<BottomNav/>` renderizado no fim do `shell-content` e escondido `min-[860px]:hidden` (mesmo breakpoint do drawer) → nav-topo no rodapé só no mobile, drawer/sidebar seguem no desktop. Nenhuma funcionalidade removida; tokens preservados. Build verde. | Navegação mobile ergonômica (polegar) para os destinos-topo |
| 2026-07-05 | **Polimento mobile — Fase 3 (saúde do código)** (sem migration, **zero mudança visual**): quebra dos dois maiores arquivos de tela em componentes por seção + hooks de dados. **`produtos/page.tsx` 953 → 376 linhas** (orquestrador): novos `components/produtos/` — `types.ts` (Variacao/Combo/vazio/SELOS/brl/selectCls), `use-produtos-data.ts` (hook de carga), e por seção `categorias-card`, `produto-form` (compõe `loja-fields` + `variacoes-editor` + `combo-editor` + `fiscal-fields`), `destinos-card`, `faixas-card`, `complementos-card` (+ `grupo-complemento` movido), `produtos-lista`. **`cadastros/page.tsx` 698 → 83 linhas**: novos `components/cadastros/` — `constants.ts` (CATEGORIAS/VINCULOS/DIAS/META/Lists), `use-cadastros-data.ts` (hook), `build-secoes.ts` (as 8 seções de cadastro), `cadastros-hub`, `secao-detalhe`, `cadastros-skeleton` (`entity-form.tsx` intacto). **Prova de não-regressão visual:** o multiconjunto de `className` (estáticas + dinâmicas multi-linha) é byte-idêntico antes/depois — produtos **153/153**, cadastros **52/52** (via `diff` do JSX extraído). Também removido um import morto de `BottomNav` em `docs/page.tsx`. Build verde. | Manutenibilidade: telas de 900+/700 linhas viram orquestradores enxutos |
| 2026-07-05 | **Cardápio digital fiel ao mockup `regem-loja.html`** (migration 046). **F1 (backend):** `pedido_externo` ganhou **endereço estruturado** (`endereco_rua/numero/referencia/bairro` + `cliente_telefone2`) e `produto.destaque` (upsell). `receberPedido` aceita `rua/numero/referencia/bairroId/telefone2`, resolve o bairro (nome+taxa) das **áreas de atuação** (`cardapio_bairro`, já cadastradas na config), **compõe** o texto `endereco` p/ impressão e grava as partes via `delivery.ingest`; `menu()` expõe `destaque`; cadastro de produto ganhou toggle "Produto em destaque". e2e 14/14 + regressão 21/21. **F2 (frontend):** reescrita de `/c/[token]` seguindo o passo a passo do mockup, **componentizada** (`components/loja/`: `tipos`, `item-sheet`, `cart-sheet`): cards mobile (foto/selos/de-por/parcela/＋), **item-sheet** com etapas obrigatórias (radio/checkbox com marca) + **stepper de quantidade** + observação, **cart-sheet** com linhas (qtd/remover), **barra de progresso de frete grátis**, **upsell "peça também"** (produtos `destaque`), cupom, seg **entrega/retirada** + **agendamento** (quando/datetime), e **form de endereço estruturado** (rua, número, referência, **bairro puxando as áreas de atuação**, telefone 1 e 2, nome). **Cliente lembrado no aparelho** (localStorage) + prefill do nome pelo telefone quando a fidelidade está ativa. Timeline mantida. ⚠️ **Merge exige aplicar a migration 046 no Supabase.** | Paridade com iFood/Anota AI no cardápio digital, por ramo |
| 2026-07-05 | **Cardápio digital: KDS + senha + formas de cartão** (migration 047). **① Cai no KDS automático:** `cardapio.receberPedido` (entrega/retirada) agora **auto-aceita** (config `auto_kds`, default on) → `delivery.aceitar` → `venderExterno` cria comanda + produção e **emite ao KDS na hora**. **② Senha:** o cardápio ganhou **contador próprio** (`pedido_externo.display_id` sequencial por tenant) e, ao entrar no PDV, `venderExterno` **puxa a senha LOCAL** (`proximaSenha`, gravada na comanda) e repassa `plataforma`+`senhaPlataforma` ao `producao_pedido`; o **KDS mostra a senha local grande + selo "🛵 Cardápio · #<senha da plataforma>"**. **③ Formas de cartão:** `cardapio_config.formas_cartao` (lista livre de rótulos, ex.: "Crédito Visa", "Débito", "VR Alelo") + toggle `auto_kds`, editáveis em `/cardapio-config`; o menu expõe `formasCartao`; no store, escolher **Cartão** abre as sub-opções e a escolhida vai como `pedido_externo.bandeira` (obrigatória quando há formas cadastradas). e2e 10/10 + regressão PDV A–C 21/21. ⚠️ **Merge exige rodar a migration 047 no Supabase.** | Pedido do cardápio visível na cozinha, com senha local + origem, e cartão detalhado |
| 2026-07-05 | **Revisão conceitual — Etapa 1: entrada/login** (sem migration). **Bug crítico corrigido:** o login mandava todo mundo para `/painel`, mas o dashboard é `@Roles('presidente','gerente')` → supervisão/execução tomavam **403** ao entrar. Novo helper `rotaInicial(cat)` (gestor→`/painel`, demais→`/meu-dia`) usado no `/entrar` e no `/pin`; `/painel` agora **protege a rota** (sem token→`/entrar`; perfil sem dashboard→`/meu-dia`). **Login por senha agora é auditado** (`login`/`login_falhou` em `audit_log` — antes só falha de PIN era). **"Esqueci minha senha"** deixou de ser link morto: nova página `/recuperar-senha` (honesta — **não há infra de e-mail**, então orienta reset pelo gestor) + endpoint `POST /colaboradores/:id/senha` (RBAC presidente/gerente, auditado `senha_redefinida`) e card **"Acesso & senha"** em `/pessoas` (gestor define/redefine a senha de um colaborador). **"Manter conectado"** virou real: `setToken(t, persist)` usa `localStorage` (persiste) ou `sessionStorage` (cai ao fechar) — antes o checkbox era decorativo. **PIN pad do login** parou de descartar o PIN digitado (handoff via `sessionStorage` → prefill no `/pin`). Login usa `replace` (não volta ao login) e quem já está logado é redirecionado; erro de login com `aria-live`. e2e auth 10/10 + regressão PDV A–C 21/21. | Destrava o login de 2 perfis + recuperação de senha real + trilha de auditoria |
| 2026-07-06 | **Revisão conceitual — landing + linha do tempo operacional + remoção do PIN de login** (sem migration). **Landing (`/`):** a vitrine "Módulos que crescem com você" foi atualizada dos 4 antigos para 12 módulos atuais (PDV & Comandas, Cardápio digital, KDS, NFC-e, TEF, Delivery & iFood, Fichas & CMV, Estoque/Validade/Compras, Fornecedores & Recebimento, Terminal de Ponto, Mural & Clima, Bot). **Linha do tempo operacional (dashboard):** reestruturada de uma barra única (que quase sempre aparecia vazia por depender de `turno` genérico) para **linhas por setor** puxando da **escala do dia** (`escala_alocacao` × `turno` × `etiqueta`→`setor` × `colaborador`) — mostra quem está escalado, blocos coloridos por status relativo ao **marcador "agora"** (em dia/em execução/concluído/atenção-sem-responsável), tarefas com horário e janelas de pico como overlay. `GET /dashboard/timeline` passou a devolver `{ setores[], picos[], agora }`; componente `Timeline` reescrito (por setor, scroll horizontal). **PIN removido do login:** `/entrar` virou só e-mail+senha (aba PIN e o pad removidos); rota `/pin` (login por PIN) excluída — o **backend `pinLogin` e o Terminal de Ponto (`/terminal/ponto`) permanecem** (bater ponto por PIN é da Portaria 671). e2e timeline 8/8 + auth/PDV regressão 21/21. | Landing atual, timeline com valor real (da escala) e login simplificado |
| 2026-07-06 | **Revisão conceitual — Dashboard (`/painel`)** (sem migration). **Lacuna corrigida:** o dashboard só mostrava operação (tarefas/escala/estoque/desperdício) e ignorava o lado comercial que já existe. `dashboard.resumo` passou a devolver `vendas { total, faturado, ticketMedio }` (comandas fechadas do dia, no fuso SP) + `deliveryPendentes` (pedidos externos ativos); o front ganhou uma **linha de KPIs comerciais** (Vendas hoje R$, Ticket médio, Delivery pendente) acima da operacional. **Fuso corrigido:** `hoje()` e os defaults do controller passaram a usar `America/Sao_Paulo` (antes data UTC divergia do marcador "agora" da timeline perto da meia-noite). Verificado que o card "Tarefas em atraso" já casava os campos (`titulo`/`setorNome`/`colaboradorNome`). e2e dashboard 7/7 + regressão PDV A–C 21/21. | O gestor vê o dinheiro do dia, não só a operação |
| 2026-07-06 | **Revisão conceitual — PDV (`/pdv`)** (sem migration, só frontend). Três correções de workflow: (1) **status do caixa visível** — o PDV busca `caixaAberta` no load, mostra banner "caixa aberto/fechado", permite **abrir o caixa** dali (valor de abertura) e **bloqueia o botão de finalizar** sem caixa (antes o operador só descobria no erro do submit); (2) **produto simples entra em 1 toque** — `tap()` só abre o seletor quando há variação/complemento; sem isso, adiciona direto (balcão rápido, antes abria modal só com "observação" pra tudo); (3) **troco no dinheiro** — campo "valor recebido" calcula troco/falta ao vivo quando a forma é dinheiro. Regressão PDV A–C 21/21; build verde. | Menos fricção e menos erro no caixa/balcão |
| 2026-07-06 | **PDV épico — Fase 1: Turno & Caixa** (migration 048). A sessão de caixa virou **turno**: `caixa_sessao.turno_numero` (sequencial por dia, fuso SP) atribuído ao abrir; `GET /financeiro/caixa` (`caixaAtual`) devolve `turnoNumero` + `operadorNome`. **Trava de operador:** `exigeDonoDoTurno` — só quem **abriu** (ou gerente/presidente override) pode **sangrar/suprir/fechar**; "troca de operador" = fechar o turno e abrir outro. Novo componente `components/pdv/caixa-panel.tsx` no PDV: abrir turno, **suprimento**, **sangria** e **fechar (conferência cega:** contado × esperado = abertura+entradas−saídas em dinheiro, com diferença e resumo por forma — backend `movimentarCaixa`/`fecharSessao` já existiam, faltava a UI + o nº do turno + a trava). e2e turno 11/11 + regressão PDV A–C 21/21. **Merge exige migration 048.** *(Próximas fases do épico: 2 produtos por categoria+fotos, 3 cancelar/alterar cupom, 4 formas de pagamento + dividir conta.)* | Operação de caixa por turno/operador, com fechamento conferido |
| 2026-07-06 | **PDV épico — Fase 2: produtos por categoria + fotos** (sem migration). A lista `/produtos` passou a devolver `imagemRef`; o grid do PDV agora é **agrupado por categoria** (cabeçalho com nome + contagem) e cada produto é um **card com foto** (`imagemRef`, com emoji de fallback), preço e "escolher tamanho" quando variável. As abas de categoria seguem como filtro rápido. Regressão PDV A–C 21/21; builds verdes. | Balcão mais rápido de reconhecer o produto (foto + organização) |
| 2026-07-06 | **PDV épico — Fase 3 (cancelar/alterar cupom) + Fase 4 (formas de pagamento + dividir conta)** (migration 049). **F3:** `GET /vendas/cupons/busca?senha=` acha um cupom pela senha; componente `buscar-cupom.tsx` no PDV mostra itens/total/status e **cancela** (com estorno de estoque+caixa, que já existia). "Alterar" = cancelar + refazer (decisão: edita só enquanto aberto; balcão nasce fechado). `listarCupons` passou a expor `senha`. **F4 (migration 049):** tabela **`forma_pagamento`** (cadastro no Financeiro, com auto-seed de dinheiro/pix/crédito/débito na 1ª leitura) — CRUD (`/financeiro/formas-pagamento`) + card em `/financeiro`; PDV e cardápio **puxam essa lista** em vez das 3 formas fixas. Tabela **`comanda_pagamento`** + `vendaBalcao` aceita `pagamentos[]` (**dividir conta / multi-pagamento**): valida que a soma bate com o total, grava uma linha por forma e **um lançamento de caixa por forma** (fechamento "por forma" correto); `comanda.forma='multiplo'`. UI de "Dividir conta" no PDV (linhas forma×valor com restante ao vivo); TEF automático só no pagamento único por cartão/pix. e2e 12/12 + regressão PDV A–C 21/21. **Merge exige migration 049.** | Fecha o épico do PDV: cancelamento por senha + pagamento flexível |
| 2026-07-06 | **Fix — cancelar cupom não avisava o KDS** (sem migration). Ao cancelar uma venda, os pedidos de produção continuavam ativos na fila do KDS. Novo `ProducaoPedidoService.cancelarPorComanda` marca **todos** os `producao_pedido` da comanda como `cancelado` e emite `producao.evento` (tipo `cancelado`) por destino; `vendas.cancelar` passou a chamá-lo após o estorno. No **KDS**, o handler de `producao:atualizado` com `tipo:'cancelado'` **avisa a cozinha** (alerta vermelho "❌ CANCELADO — Senha/Mesa/Plataforma" + som) antes do pedido sair da fila. e2e cancel→KDS 6/6 + regressão PDV A–C 21/21. | Cozinha não prepara pedido cancelado |
| 2026-07-06 | **Revisão — Mesas (`/mesas`)** (sem migration, só frontend). Alinhado ao épico do PDV: o pagamento das mesas passou a **puxar as formas do cadastro** (`/financeiro/formas-pagamento`) em vez das 3 fixas (`fecharComanda`/`fecharMesa` aceitam a forma livre); o "Carregando…" da lista virou **skeleton**. *(Mesa em modo "comandas por cliente" já cobre pagar por pessoa; multi-forma numa mesma comanda fica como extensão futura do fechar.)* Build verde. | Consistência das formas de pagamento entre balcão e mesas |
| 2026-07-06 | **Revisão — Garçom (`/garcom`) + `SeletorProduto`** (sem migration, só frontend). O `SeletorProduto` (compartilhado por **garçom e mesas**) recebeu as melhorias da Fase 2 do PDV: **produtos agrupados por categoria** (cabeçalho + contagem), **card com foto** (`imagemRef`, fallback emoji) e **1-toque para produto simples** (sem abrir o modal quando não há variação/complemento). Lista de mesas do garçom: "Carregando…" → skeleton. Build verde. | Consistência de catálogo (fotos/categoria/1-toque) entre PDV, mesas e garçom |
| 2026-07-06 | **Reposicionamento — `/pedidos` vira "Painel de produção do gestor"** (sem migration). Deixou de duplicar o KDS (que é da cozinha) e virou supervisão gerencial: `filaPdv` enriquecida com **`setorNome`** (join `setor`) e **vínculo à comanda** (`cliente`, `total`, `mesa`, `status`). A tela passou a: **consolidar por setor** (todas as telas de produção num lugar), **resumo** (em produção / **atrasados** / prontos), **destaque de atraso/SLA** (decorrido × `tempoPreparoMin`, card vermelho quando estoura), **valor da venda** por pedido, itens e cancelamento (já existia). *(Nota: `producao_pedido.setorId` só vem preenchido quando o setor tem destino/KDS configurado; sem isso agrupa em "Sem setor" — melhoria de roteamento fica para depois.)* e2e painel 6/6 + regressão PDV A–C 21/21. | Supervisão da produção (multi-setor, atraso, vínculo com a venda) em vez de cópia do KDS |
| 2026-07-06 | **Delivery 2.0 — kanban configurável + caixa próprio** (migration 050). **(a) Correções:** cancelar delivery agora chama `producao.cancelarPorComanda` no `estornarVendaExterna` (a cozinha parava de ser avisada — mesmo bug do balcão); aceite automático que falha marca `auto_aceite_falhou` (some silencioso → destaque na coluna Chegada). **(b) Kanban:** 4 colunas (Chegada / Produção / Em rota / Finalizado) mapeadas a `status`; visibilidade por coluna em `delivery_config.colunas` (jsonb); **botão "⚙️ Configurar quadro" FORA do quadro** (barra superior) para reativar coluna oculta; **filtro por coluna em menu ancorado** (entregador/bairro/forma/nº) — escolha do usuário sobre o toast; card exibe os dados ricos já ingeridos (pago × na entrega, troco, taxa, bairro, agendamento, entregador); mover card = **botões de avançar** (Aceitar → Marcar pronto → Despachar → Concluir); despacho de entrega abre modal de **entregador** (`pedido_externo.entregador_id/nome`). **(c) Caixa do delivery separado do PDV** via `caixa_sessao.origem` ('pdv' ou 'delivery') — abrir/sangria/suprimento/fechar cego reusando `CaixaPanel`; **dinheiro na entrega** amarra o lançamento da venda à sessão do delivery ao concluir (fechamento confere a gaveta). `listar` passou a trazer finalizados das últimas 24h. e2e delivery 20/20 + regressão PDV 21/21. | Delivery vira central de entregas (padrão de mercado), com correções e controle de caixa próprio |
| 2026-07-06 | **Delivery — detalhe do pedido, entregadores e estoque na conclusão** (migration 051). **(a) Card clicável → painel de detalhe** com 3 ações: 🖨 **Reimprimir** vias · ✏️ **Alterar** · ✕ **Cancelar**. **(b) Cancelamento com trava:** confirma → motivo → **senha de login de um gestor** (`autorizarPorSenha`, bcrypt contra presidentes/gerentes; atendente pode cancelar informando a senha, auditado com quem autorizou). Vai para Finalizados/cancelado; no **KDS de entregas** o pedido fica **riscado** + "CANCELADA" (a fila de entrega passou a incluir cancelados recentes). **(c) Estoque baixa só na CONCLUSÃO** (entrega): `venderExterno` não baixa no aceite; `avancar→concluido` chama `baixarEstoqueExterno`; cancelado antes disso **não movimenta estoque**. **(d) Alterar itens** (enquanto não despachado): `alterarItensExterno` adiciona/remove itens na comanda 'fechada', recalcula total + ajusta o lançamento no financeiro, reemite ao KDS, marca `alterado`, **reimprime as vias** e põe **obs "ALTERADO"** nos pedidos do KDS. **(e) Entregadores** = colaboradores com função cujo nome contém "Entregador" (+ `colaborador.telefone`); o despacho abre a seleção (fallback **"Nenhum"**) e "em rota" mostra 🛵 nome + 📞 telefone (`pedido_externo.entregador_telefone`). **(f) "Retornar à produção"** desfaz um despacho errado (despachado→confirmado). **(g) Rename** coluna "Chegada"→"Novo"; filtro abre em "Nº do pedido". **Fix de segurança/contabilidade:** `sessaoAbertaId` (venda de balcão) ficou **origem-aware** ('pdv') — um caixa de delivery aberto não satisfaz nem recebe mais uma venda de balcão. e2e delivery 20/20 + rodada 2 16/16 + regressão PDV 21/21. | Central de entregas completa (padrão de mercado) + estoque/caixa corretos |
| 2026-07-06 | **Delivery — quadro estilo iFood Gestor** (migration 052; ref. mockup enviado). **(a) Filtro geral por tipo** na barra: Todos / 🛵 Delivery / 🏪 Retirada (filtra o quadro inteiro por `pedido_externo.tipo`). **(b) ➕ Novo pedido manual** (delivery ou retirada) — `criarManual` resolve **preço no servidor** a partir do cadastro e reaproveita a ingestão (canal 'manual'). **(c) ⏸ Pausar loja** por **30 min / 1 h / 12 h** + motivo (`pausadoAte`/`pausaMotivo`); **reativa sozinha** (`pausado` é computado) e mostra banner "pausada até HH:MM". **(d) Faixas de tempo editáveis** (`prep_balcao_min/max`, `prep_delivery_min/max`) num card no topo da coluna, que alimentam o **contador "Prepare em até HH:MM:SS"** do card (vermelho quando estoura). **(e) Card** ganhou **nº sequencial do dia** (`pedido_externo.numero`, o "#284"), **ícone 📍 de rota** (Google Maps), **botão NF** (emite NFC-e da comanda via `fiscal.emitir`), tipo (🛵/🏪) e total realçado. Coluna "Novo" renomeada para **"Em análise"** (decisão do usuário: manter as 4 colunas). e2e delivery r1 20/20 + r2 16/16 + r3 13/13 + regressão PDV 21/21. | Paridade visual/funcional com os líderes de mercado (iFood Gestor) |
| 2026-07-06 | **Delivery — alterar endereço (taxa por bairro) + refinos de card** (sem migration). **(a) "Alterar" agora edita também o endereço:** ao trocar o bairro, puxa a **taxa de entrega do cadastro de bairros** (`cardapio_bairro`, "área de atendimento") por `bairroId` ou nome e **recalcula o total** (subtotal itens + taxa − desconto); `alterar` aceita `endereco` e há `GET /delivery/bairros` para o editor escolher o bairro (que já mostra a taxa). *(Taxa por distância — geocoding — fica como follow-up.)* **(b) Nome do cliente** no card em **negrito e maior**. **(c) Chip da plataforma colorido pela marca:** iFood vermelho `#EA1D2C`, WhatsApp `#25D366`, Totem, Uber/Rappi/99, e **Cardápio na cor da marca Regem** (dourado); demais canais caem no neutro. e2e delivery r4 9/9 (+ r1/r2/r3 e PDV verdes). | Correção de pedido com recálculo de frete + leitura mais rápida do card |
| 2026-07-06 | **Delivery — refino visual do quadro** (só frontend). Títulos das colunas maiores (`text-base`, negrito) com **contador em pílula** na cor da coluna; cada coluna ganhou **fundo suave** na própria cor (`hsl(var(--cor) / 0.05)`) e cabeçalho tonalizado (`/0.14`) — os cards brancos seguem legíveis por cima. O **caixa do delivery** (turno/sangria/suprimento) foi **integrado à barra superior** (novo prop `embedded` no `CaixaPanel`, sem borda/fundo próprios) e a barra foi **afinada** (`px-3 py-2`) para dar destaque ao quadro. Build verde. | Hierarquia visual mais clara e mais espaço para os pedidos |
| 2026-07-06 | **Delivery — fix de hidratação + botões afinados** (só frontend). A categoria (`getCategoria()`, lê `localStorage`) passou a ser lida em `useEffect` (estado `cat` inicia null) para o 1º render do cliente bater com o do servidor — corrige "Hydration failed… matching `<span>`" que vinha do `CaixaPanel` renderizado condicionalmente. Botões **Novo pedido / Pausar / ⚙️ / Simular** reduzidos para `h-8` (Button `sm` era `h-11`), na altura do filtro de tipo. Build verde. | Erro de hidratação visível + barra mais compacta |
| 2026-07-07 | **Delivery — painel de configurações (menu lateral), 1ª leva** (migration 053). O ⚙️ do quadro abre um **painel com menu lateral** (`ConfigPanel`) no lugar do modal simples de colunas. Seções entregues: **Quadro** (colunas), e o grupo **Cardápio digital** — **Loja** (nome, **logo por imagem** via `ImageUpload`, CPF/CNPJ, responsável+contato, contato da loja, WhatsApp, Instagram, site, pedido mínimo), **Endereço** (CEP/rua/nº/bairro/cidade/UF/ref/compl), **Horários** (por dia da semana), **Tipos de pedido** (Delivery/Retirar/Consumir no local — 3 flags independentes) e **Área de atendimento** (bairro+taxa com **liga/desliga**; `delivery/bairros` passou a listar só ativos). Reusa `cardapio_config` (novos campos) + `cardapio_bairro.ativo`. Abas **Banners/Impressoras/Integrações/Robô** aparecem no menu marcadas "em breve" (2ª leva). **Bloqueios registrados:** detecção de impressoras do SO exige o edge; integrações reais precisam de credenciais+edge; robô com prompt exige LLM. e2e config 10/10 + delivery r4 9/9 + PDV 21/21. | Centralizar as configurações do delivery/cardápio num só lugar, estilo mercado |
| 2026-07-07 | **Config — área por raio (ou bairro) + Banners** (migration 054, 2ª leva parte 1). **Área de atendimento** ganhou modo **exclusivo** `cardapio_config.area_modo` ('bairro' ou 'raio'): por **raio** usa faixas `raios` (jsonb `[{ateKm,taxa}]`, ordenadas, faixa 0km descartada) a partir do endereço da loja; por **bairro** segue a lista existente. **Registrado:** o cálculo da distância no checkout precisa de **geocoding** (colunas `end_lat/end_lng` já criadas para quando a integração de mapas entrar) — a config já persiste. **Banners** (tabela nova `banner`): upload de imagem + título/link/ordem (↑↓)/ativo, CRUD replace-all em `/cardapio/banners`. No painel, as abas Banners e Área saíram do "em breve"; restam **Impressoras/Integrações/Robô**. e2e config r2 10/10 + r1 10/10. | Frete por raio (padrão de mercado) + banners do cardápio |
| 2026-07-07 | **Config — Impressoras (cadastro manual)** (migration 055, 2ª leva parte 2). Seção Impressoras no painel: cadastra impressora reusando `equipamento` (tipo `impressora`) com **direcionamento** — Caixa (`papel='cupom'`) ou Cozinha (`papel='producao'`, opcional por **setor**), **IP:porta** e **nº de vias** (`equipamento.vias`, migration 055). Endpoints dedicados `GET/PUT/DELETE /equipamento/impressoras` (upsert individual, não replace-all, para não quebrar tokens de KDS). **Detecção automática das impressoras do SO fica para o edge** (aviso na tela). Menu: sobra só Integrações/Robô no "em breve". e2e impressoras 6/6. | Direcionar impressão caixa/cozinha por setor + vias, sem depender do edge |
| 2026-07-07 | **Config — Integrações (credenciais)** (migration 056, 2ª leva parte 3). Tabela `integracao` (por tenant+canal: iFood/Uber Eats/Rappi/99Food) com merchant/client id + **client_secret/token (secrets)**. Seção no painel: por canal, ativo + credenciais. **Segurança:** o **GET nunca retorna os secrets** (só flags `temSecret`/`temToken`); o **PUT preserva** o secret quando o campo vem vazio (só troca com valor novo). Ativação real (receber pedidos) roda no **edge** com essas credenciais — aviso na tela. Endpoints `GET/PUT /delivery/integracoes` (presidente/gerente). No menu, sobra só **Robô** no "em breve". e2e integrações 8/8 (inclui não-vazamento de secret). | Cadastro seguro de credenciais dos apps externos |
| 2026-07-07 | **Config — Robô de atendimento (mensagens) + fim da 2ª leva** (migration 057). Seção Robô no painel: **ativo**, **saudação**, **mensagem de ausência**, **respostas prontas** (gatilho→resposta) e um campo de **base de conhecimento** já persistido (`robo_prompt`) para o futuro LLM. Campos em `cardapio_config` (`robo_ativo/saudacao/ausencia/prompt/mensagens`). **O "cérebro" com IA (respostas livres) fica para etapa dedicada** (precisa de provedor de IA/custo — aviso na tela); as mensagens já funcionam sem IA. **Com isso o painel de configurações está completo** (Quadro · Cardápio digital: Loja/Endereço/Horários/Tipos/Área[bairro ou raio] · Banners · Impressoras · Integrações · Robô). e2e robô 7/7 + config r1 10/10 + r2 10/10. | Fecha o módulo de configurações do delivery/cardápio |
| 2026-07-07 | **Integração WhatsApp/n8n — webhook de status + contexto pro robô** (sem migration; reusa `integracao` e `pedido_externo.cliente_telefone`). **(a) Webhook de status:** a cada mudança de status do delivery (aceito/pronto/em rota/concluído/cancelado), o Regem faz `POST` na URL do n8n (`dispararWebhook`, fire-and-forget) com `{evento,pedidoId,numero,status,tipo,cliente,telefone,total,entregador}` e **assinatura HMAC-SHA256** no header `X-Regem-Signature`. Config no painel ⚙️ Integrações → card **"WhatsApp / n8n"** (URL no `merchantId`, segredo no `clientSecret` mascarado; canal `n8n`). **(b) Menu público** (`GET /publico/cardapio/{token}`) passou a expor **robô** (saudação/ausência/mensagens/prompt), **horários** e **tipos** — o agente lê tudo com o token. **(c) "Cadê meu pedido?":** `GET /publico/cardapio/{token}/pedidos?telefone=` casa pelos **últimos 8 dígitos** (ignora formatação/DDI) e devolve pedidos recentes com status. Diretriz do fluxo: robô manda o **link do cardápio** no principal; pedido por texto é plano B (via `/publico/cardapio/{token}/pedido`). e2e webhook 13/13 (inclui disparo real + HMAC conferido por servidor local) + integrações 8/8 + delivery 20/20. | Liga o robô de WhatsApp (n8n do usuário) ao Regem: status proativo + contexto + status por telefone |
| 2026-07-07 | **Correções do fluxo cardápio/robô (pós-teste)** (sem migration). **(a) Endereço da loja** agora vem no menu público (`loja.endereco{cep,rua,…,texto}`) — o robô responde "onde fica". **(b) Horário de funcionamento é validado:** `estaAberta(cfg)` (toggle `aberto` + `horarios` por dia no fuso SP, trata virada de meia-noite); o menu expõe `abertaAgora` e o `pedidoPublico` **bloqueia** (400) fora do horário (pedido agendado passa). **(c) Formas de pagamento do cardápio** ganharam UI no painel (Loja → `pagamentos`: pix/cartão/na entrega) — sem isso o checkout não mostrava pagamento. **(d) Storefront respeita os tipos** (só mostra Entrega/Retirada habilitados; 1 tipo vira rótulo fixo) e o badge ABERTO/FECHADO usa `abertaAgora`; checkout **bloqueado com a loja fechada**. **(e) Prefill por link:** `/c/{token}?nome=…&tel=…` preenche nome/telefone — é o que faz o pedido nascer com o **mesmo telefone do WhatsApp**, destravando o "cadê meu pedido" e o **webhook** (que precisam do telefone). **(f) Status** agora traz `statusTexto` + `estimativaMin` (tempo de entrega) pro robô. e2e loja 9/9 + webhook 13/13 + config 10/10 + delivery 9/9. | Fechar as pontas do pedido via WhatsApp/cardápio que apareceram no teste real |
| 2026-07-07 | **Fix — link de delivery abria envio-direto (modo mesa)** (sem migration, só frontend). O storefront decidia o fluxo por `menu.modo === 'mesa'`: uma loja em **modo Mesa (QR na mesa)** fazia o link de delivery **enviar direto**, sem checkout (tipo/pagamento/endereço) — parecia "venda de balcão". Corrigido: o envio-direto só ocorre quando o link é de **mesa de verdade** (`?mesa=` presente); o link de delivery **sempre abre o checkout**, independente do `modo` da loja. O QR de mesa (`/c/{token}?mesa=5`) segue com envio direto. | Delivery e QR-de-mesa convivem na mesma loja sem a config de `modo` atrapalhar |
| 2026-07-07 | **Handoff automático (robô → humano): chamados de atendimento** (migration 058). Novo módulo `atendimento` + tabela `atendimento_chamado`. O robô (n8n) abre um chamado por **endpoint público** `POST /publico/cardapio/{token}/atendimento` (`{tipo: mudanca\|erro\|humano, cliente, telefone, pedidoNumero, mensagem}`) quando o cliente quer **mudar o pedido**, **relatar erro no recebimento** ou **falar com atendente**. O Regem grava e **dispara alerta em tempo real** (`atendimento.novo` → gateway → socket `atendimento:novo`, room do tenant). No app (tela de Delivery) há um **🔔 sino com contador** + painel **"Atendimento"** listando os chamados abertos com **"Responder no WhatsApp"** (wa.me com o +55) e **"Resolver"** (`GET /atendimento`, `POST /atendimento/{id}/resolver`, gestor/atendente). *(Status do robô: passou a vir por um nó de fetch dos pedidos do cliente no contexto — mais robusto que a tool que dependia do LLM preencher o telefone.)* e2e atendimento 6/6 + loja 9/9 + webhook 13/13. | Cliente que precisa de gente cai numa fila visível e rastreável, não some no WhatsApp |
| 2026-07-08 | **Escala — Fase 1: modelo & cores** (migration 059). Fundação da reforma da Escala. **(a) `setor.cor`** (editável por presidente/gerente via `PATCH /setores/:id`) — cor própria do setor. **(b) N:N `colaborador_funcao`** (backfill do `colaborador.funcao_id` atual como principal): um operador cobre **várias funções**; `POST /colaboradores` aceita `funcaoIds[]` (só funções do tenant; principal = 1ª válida) e `GET /colaboradores` devolve `funcaoIds`. **(c) Cor da vaga 100% pela hierarquia** (`funcao.categoria`): paleta fixa de 4 níveis (Presidente/C&O · Gerente · Supervisão · Execução) em `frontend/src/lib/hierarquia.ts` (sem override por etiqueta); `GET /etiquetas` e a escala passam a retornar `categoria` + `setorCor`. **(d) Cadastros:** `EntityForm` ganhou tipos `color` e `multiselect` (cor no setor; multi-função no colaborador). Bug de fuso corrigido: `hoje()` da escala usava UTC → agora `America/Sao_Paulo`. Smoke 11/11 no `regem_local`; builds verdes. **Merge exige migration 059 no Supabase.** *(Fase 2: grade colorida + remover/editar alocação; Fase 3: gerar etiqueta na função; Fase 4: visões dia/mensal + regras CLT.)* | Menu crítico: modelo alinhado (N:N, hierarquia, cor de setor) antes das telas |
| 2026-07-08 | **Escala — Fase 2: grade por hierarquia + remover/editar alocação** (sem migration). **(a) Cores:** a vaga na grade agora é colorida **pela hierarquia da função** (`corHierarquia(categoria)` de `lib/hierarquia.ts`) — dot + faixa lateral; cabeçalho de cada setor usa a **`setor.cor`**; **legenda** dos 4 níveis no topo. Grupos passam a derivar de `etiqueta.setorNome/setorCor` (dropou a chamada a `/setores`). **(b) Remover/editar (lacuna que não existia):** `DELETE /escala/:id` (soft-delete `deleted_at`) e `PATCH /escala/:id` (trocar responsável/reabrir vaga/tipo, com checagem de conflito de colaborador no mesmo turno/dia) — ambos **auditados** (`removeu_alocacao`/`alterou_alocacao`). No front, clicar na célula lista as alocações existentes com **select de responsável** (troca 1-clique) + **lixeira** (remover), acima do form de adicionar. **(c) Responsável filtrado:** o form de alocação só lista colaboradores que **cobrem a função da vaga** (usa o N:N da Fase 1); fallback "mostra todos" com aviso se ninguém tem a função. Smoke 8/8 no `regem_local`; builds verdes. | Escala legível por nível + finalmente dá pra corrigir/trocar/remover alocação |
| 2026-07-08 | **Escala — Fase 3: gerar etiqueta ao criar função** (sem migration). No cadastro de **Função**, `POST /funcoes` aceita `gerarEtiqueta` (bool) + `sigla?`: se ligado e a função tem **setor**, cria automaticamente a **etiqueta (vaga)** vinculada — herda `setorId`/`unidadeId` do setor, sigla = informada ou **abreviada do nome** (`gerarSigla`: sem acento, só letras, 4 chars — "Aux. Cozinha" → `AUXC`), e **contador** = próximo livre para aquela sigla na unidade (evita colisão do índice único). Sem setor, cria só a função. Cadastro de função ganhou "Gerar etiqueta?" (Sim/Não) + "Sigla (opcional)". Smoke 8/8 no `regem_local`; builds verdes. | Menos atrito: a vaga nasce junto da função, pronta pra escala |
| 2026-07-08 | **Escala — Fase 4A: tipos de jornada + regras CLT** (migration 060). Base da conformidade trabalhista na montagem da escala. **Modelo:** `colaborador.jornada_tipo` (5x2/12x36/4x3/horista/outro — mescláveis) e `turno.pausa_inicio/pausa_fim` (intervalo intrajornada). **Regras puras testadas** (`common/regras-escala.ts`, 14 testes): intervalo intrajornada por jornada bruta (art. 71: >6h→1h, 4–6h→15min), **interjornada 11h** (art. 66) entre dias adjacentes, folgas semanais por tipo (5x2→aviso >5 dias; 4x3→aviso >4). **Aplicação** (decisão do usuário: *avisar + bloquear ilegais*): `criar`/`alterar` alocação **bloqueiam** (400) o ilegal — jornada >6h **sem** intervalo e interjornada <11h — e retornam os demais como **avisos** (toast ⚠️ no front, não travam). Cadastros: tipo de jornada no colaborador + pausa no turno. Smoke 7/7 + jest 66/66; builds verdes. **Merge exige migration 060 no Supabase.** *(Fase 4B: dias importantes; 4C: visões dia/timeline/mensal; 4D: avisos no modal.)* | Escala em conformidade com a CLT já na montagem (sem gerar erro trabalhista) |
| 2026-07-08 | **Escala — Fase 4B: dias importantes** (migration 061). Nova tabela `dia_especial` (tenant, unidade opcional=rede, colaborador opcional=férias/folga de 1 pessoa, `data`, `data_fim` opcional=período, `tipo` feriado/ferias/evento/folga/outro, nome, descrição). Módulo `dia-especial`: `GET /dias-especiais?de=&ate=` (sobreposição por `coalesce(data_fim,data)`), `POST`/`DELETE` (soft) — listar = qualquer autenticado; criar/remover = presidente/gerente/supervisão. Cadastros ganhou seção **Dias importantes**. A **grade da escala** marca os dias no cabeçalho (emoji por tipo 🎉🏖️📅😴 + nome no title), desktop e mobile. Smoke 6/6; builds verdes. **Merge exige migration 061 no Supabase.** *(Fase 4C: visões dia/timeline/mensal — última da Escala.)* | Cadastrar feriados/férias/eventos e vê-los na escala |
| 2026-07-08 | **Escala — Fase 4C: visões Dia (timeline) e Mês** (sem migration; fecha a reforma da Escala). Alternador **Semana · Dia · Mês** no topo. **Backend:** `enriched` da escala passou a expor horas do turno (`turnoInicio/turnoFim/pausaInicio/pausaFim`) e novo `GET /escala/periodo?de=&ate=` (alocações enriquecidas num intervalo). **Dia** (`TimelineDia`): cada colaborador escalado vira uma **barra no eixo de horas** agrupada por setor, colorida pela hierarquia, com a **pausa marcada** (faixa hachurada) — o gestor vê os intervalos e organiza a cobertura; nav por dia + selos de dias especiais. **Mês** (`GradeMensal`): tabela colaboradores × dias do mês, célula = sigla da vaga (colorida) ou vazia (folga), cabeçalho marca dias especiais; nav por mês. Builds verdes. | Fecha a Escala: além da grade semanal, timeline do dia (com pausas) e panorama mensal |
| 2026-07-08 | **Revisão — Operação (`/operacao`)** (sem migration, só frontend). Hub operacional (Estoque · Recebimento · Validades FEFO · Desperdício · Vistorias). **2 bugs corrigidos:** (a) o estado de carregamento renderizava um "Carregando…" **fora do Shell** → sidebar/topbar sumiam durante a carga; agora usa `Shell` + `SkeletonList` (consistente com as demais telas). (b) `hoje` (default das datas de movimento/desperdício/vistoria/recebimento) usava `toISOString()` (**UTC**) → à noite no Brasil o campo já vinha com a data de amanhã; agora `America/Sao_Paulo` (na página e no `RecebimentoForm`). Sem erros de sintaxe (build verde). *(Observação: a tela usa cores cruas do Tailwind nos badges em vez dos tokens semânticos — divergência cosmética, não corrigida.)* | Não perder a navegação na carga + data correta no fuso certo |
| 2026-07-08 | **Operação — badges nos tokens** (sem migration, só frontend). Follow-up da revisão: os badges que usavam cores cruas do Tailwind (`bg-red-100`/`emerald-100`/`amber-100`/`slate-100`) passaram para os **tokens semânticos** — `bg-destructive/10 text-destructive`, `bg-ok/10 text-ok`, `bg-warn/10 text-warn`, `bg-secondary text-muted-foreground`. Alinha ao design system. Build verde. | Consistência de cor com o resto do app |
| 2026-07-08 | **Estoque — E1: renomear + hub + insumo rico** (migration 062). O menu **"Operação" virou "Estoque"** e a tela (antes tudo empilhado) virou um **hub por seções** (estilo Cadastros): 📦 Insumos · 🧮 Contagem · 🛒 Compras · 🚚 Recebimento · 🗓️ Validades · ♻️ Desperdício · ✔️ Vistorias, + atalho **Inteligência** (`/estoque`). **Cadastro rico de insumo** (`InsumoForm`): categoria como **cadastro próprio** (`categoria_item` + CRUD, criar inline), **fornecedor** (select + criar inline), **unidade tipada** (un/caixa/kg/L/fardo/fita… + custom), estoque mínimo e **conversões personalizadas** (`item_conversao`: 1 fardo = 400 un). `item_estoque` ganhou `fornecedor_id` + `categoria_item_id`; `POST/PATCH /estoque/itens` gravam tudo (conversões replace-all); `listItens` devolve categoria (nome+cor), fornecedor e conversões. Contagem/Compras entram como "em breve" (E2/E3). Badges já nos tokens. Smoke 9/9; builds verdes. **Merge exige migration 062 no Supabase.** *(E2 Contagem · E3 Compras · E4 Painel do estoque.)* | Estoque deixa de ser "um cadastro só jogado junto" e ganha estrutura + insumo completo |
| 2026-07-08 | **Estoque — E2: Contagem** (migration 063). Seção **Contagem** do hub: **listas personalizadas** (`contagem_lista` + `_item`): nome, produtos, **recorrência** (diária/semanal/mensal/avulsa), **horário de alerta**, **delegar** a colaborador, flags **avisar KDS / dashboard**. **Executar contagem** (`contagem_execucao` + `contagem_item`): ao "Contar", faz **snapshot do saldo do sistema** por item; o operador informa o **contado**; ao salvar com "aplicar ajuste", lança um **movimento `ajuste`** da diferença (reconcilia o estoque). `listListas` traz nº de produtos, última contagem e `pendenteHoje` (recorrência × dia no fuso SP). **Alerta por horário:** `@Cron` horário emite `kds.alerta.sistema` (→ KDS) para as listas que vencem naquela hora/dia (com o responsável no texto). Módulo `contagem` (listar/iniciar/salvar = autenticado; criar/remover = gestão). Smoke 8/8; builds verdes. **Merge exige migration 063 no Supabase.** *(E3 Compras · E4 Painel.)* | Contagem cíclica com delegação, alerta e ajuste automático do estoque |
| 2026-07-08 | **Estoque — E3: Compras** (migration 064). Seção **Compras** do hub: **gerar lista** (`compra_lista` + `compra_item`) — nome, **fornecedor**, **data de recebimento**, **delegar** o recebimento, flags KDS/dashboard; seleção de produtos com **filtro** (busca) + **quantidade** e custo/un por item; botão **"sugerir do estoque baixo"** (`GET /compras/sugestao` = itens abaixo do mínimo, qtd = mínimo − saldo). **Receber** (`POST /compras/listas/:id/receber`): lança **movimento `entrada`** por item (com custo), **atualiza o custo médio ponderado** (`custoMedioPonderado`), marca `recebida` e avisa KDS. Módulo `compras` (listar/receber = autenticado; criar/remover = gestão). Smoke 9/9; builds verdes. **Merge exige migration 064 no Supabase.** *(Falta E4 — Painel do estoque.)* | Fecha o ciclo compra→recebimento→estoque com sugestão e custo médio |
| 2026-07-08 | **Estoque — E4: Painel** (sem migration; fecha o épico). Nova 1ª aba **📊 Painel** do hub: KPIs (**valor em estoque**, nº de insumos, **abaixo do mínimo**, **zerados**), card **CMV do mês** (real × teórico → desvio, via `/estoque/cmv`) e **valor por categoria** (barras coloridas pela cor da categoria, ordenado). Tudo derivado do `listItens` já carregado + `/estoque/cmv` — sem endpoint novo. **Épico do Estoque completo:** E1 hub+insumo rico · E2 Contagem · E3 Compras · E4 Painel. Build verde. | Visão gerencial do estoque num relance, fechando a reforma |
| 2026-07-08 | **Revisão — Fichas Técnicas: ingrediente ligado ao Estoque** (sem migration; só frontend). **Gap corrigido:** o form de ficha só mandava `insumoNome` (texto livre) + custo digitado à mão — nunca o `item_id` — então **produzir a ficha NÃO baixava o estoque** dos insumos (a explosão em `producao.service` só baixa quando o ingrediente tem `item_id`) e o custo ficava manual/estático. Agora o seletor de ingrediente tem 3 opções: **Insumo avulso** (texto), **Insumo do estoque** (optgroup com os itens cadastrados → envia `itemId`, custo automático pelo **custo médio**, unidade travada) e **Sub-receita** (como já era). Fecha o ciclo **ficha → produção → baixa de estoque → CMV real** e conecta o épico do Estoque às Fichas. Smoke da cadeia 4/4 (produzir 3× baixou o insumo 10→4); build verde. | Sem o `item_id`, produzir não consumia o estoque nem custava certo |
| 2026-07-08 | **Fichas — meta de CMV editável + editar ficha + skeleton** (sem migration). (1) **Meta de CMV por ficha** deixou de ser fixa (31,5%): campo editável no card de custo, usada no CMV/preço sugerido e enviada no salvar (`meta_cmv` já existia no schema). (2) **Editar ficha:** botão ✏️ no card carrega a ficha no form (campos + ingredientes) e salva via `PATCH /fichas/:id`; o `update` do backend passou a **substituir os ingredientes** quando enviados (replace-all, com validação de **ciclo** de sub-receita via `validarSubFicha`) — `UpdateFichaDto` ganhou `ingredientes[]`. (3) **Skeleton** na listagem enquanto carrega (`SkeletonList`), no lugar do flash "Nenhuma ficha". Smoke edit 6/6 (meta persiste, replace de ingredientes, ciclo bloqueado); builds verdes. | Ajustar receita sem reccriar + meta de margem por prato |
| 2026-07-08 | **Fichas dentro do Estoque** (sem migration). Organização: o item **"Fichas Técnicas" saiu da sidebar** e passou a ser acessado **pelo hub de Estoque** (botão "🧾 Fichas técnicas" nas ações, ao lado de "Inteligência"). `/fichas` ganhou eyebrow "Estoque · produção" + botão **← Estoque** pra voltar ao hub. Faz sentido: ficha consome insumo do estoque (baixa na produção). | Agrupar o que é do mesmo domínio (insumo→ficha→produção) |
| 2026-07-14 | **Segurança/robustez do cardápio — Etapa 1: pedido idempotente + upload seguro** (migration 086). **(a) Idempotência:** `pedido_externo` ganhou `client_ref` + índice único parcial `(tenant_id, client_ref)`. O front do cardápio (`c/[token]`) gera um UUID quando o carrinho é montado e reenvia o **mesmo** `clientRef` em qualquer retry; `cardapio.service.receberPedido` devolve o pedido existente (200, `reenvio:true`) sem recriar/recobrar, e `delivery.ingest` grava o ref + trata corrida (violação 23505 → devolve o existente). Teste `cardapio.idempotencia.spec` prova 2 chamadas c/ mesmo ref = 1 pedido. **(b) Upload:** `FileInterceptor` com `limits.fileSize = 8 MB` (corta no Multer) + validação de **magic bytes** (JPEG/PNG/WebP/GIF) no `midia.service` — usa o tipo **detectado** (não o mimetype do cliente, forjável) para extensão/Content-Type; rejeição amigável. Builds verdes; testes 2/2. **Merge exige migration 086.** | Retry/duplo-toque não gera pedido duplicado; upload não aceita arquivo forjado ou gigante |
| 2026-07-14 | **Segurança do cardápio — Etapa 2: OTP protegido** (migration 087). O código de verificação do telefone (WhatsApp) deixou de ser gerado com `Math.random` e de ser gravado em **texto puro**. Agora: geração com `crypto.randomInt(100000, 1000000)` (uniforme, imprevisível); o banco (`cliente_otp`) guarda **apenas o hash SHA-256** (`codigo_hash`; `codigo` virou nullable e não é mais escrito); a confirmação compara **hash com hash**. O código em claro existe **só no payload do webhook de envio** (n8n/Evolution). Expiração (5 min) e limite de **5 tentativas** mantidos. Builds verdes. **Merge exige migration 087.** | Vazamento do banco não expõe códigos OTP; código forte e não adivinhável |
| 2026-07-14 | **Robustez do cardápio — Etapa 3: dinheiro em centavos** (sem migration). Novo `src/util/dinheiro.ts` (`paraCentavos`, `paraReais`, `somarCentavos`, `percentualCentavos`, `formatarReais`): toda conta interna roda em **centavos inteiros** (sem drift de float), convertendo para reais só na fronteira (resposta/coluna numeric). Migrados: o **total do pedido público** (`cardapio.receberPedido` — subtotal dos itens, cupom+prêmio+cashback e total final agora em centavos) e o **fechamento de caixa** (`financeiro.fecharSessao` — esperado/informado/diferença em centavos). 3 testes de arredondamento (`dinheiro.spec`): 3× R$ 9,99 −10% = R$ 26,97; taxa 6,90 + cashback parcial; 7 itens com promoção — todos batem ao centavo. Builds verdes; testes 5/5. | Elimina centavos "fantasma" de float em pedido e caixa |
| 2026-07-14 | **Financeiro — Etapa 4: fechamento de caixa cego por forma** (migration 088). No fechamento, o operador informa o **contado por forma** (dinheiro, cartão, Pix) **sem ver o esperado**; só após confirmar o sistema revela o comparativo **contado × esperado × diferença por forma** + total, gravando tudo (`caixa_sessao.valores_informados / esperado_por_forma / diferenca_por_forma`). Sangrias/suprimentos entram no esperado (já são lançamentos). **Diferença acima de um limite** (config por unidade `limite_diferenca_caixa`, padrão **R$ 5,00**) gera **ocorrência automática** (tipo de sistema "Diferença de caixa", get-or-create) + auditoria. Novo **relatório de fechamentos** (`GET /financeiro/caixa/fechamentos`, gerente/presidente) na rota `/caixa/fechamentos` com `ResponsiveTable` (data/turno/operador/esperado/contado/diferença, tooltip da diferença por forma) + item na sidebar. `fecharSessao` aceita `valoresInformados` (compat com `valorInformado`). Builds verdes. **Merge exige migration 088.** | Conferência honesta (cega) e rastreável; desvio de caixa vira ocorrência automática |
| 2026-07-14 | **Cardápio — Etapa 5: UX no nível dos líderes** (mig 089 tema + 090 sugestão). **Skeleton** no formato do conteúdo (hero+categorias+cards) no lugar de "Carregando…". **"Peça também"** com motor: sugestões **vinculadas ao produto** (cadastro, prioridade) → senão **mais pedidos** (`comanda_item`), excluindo carrinho/indisponíveis (`produto_sugestao` + `GET /peca-tambem`); consumido na seção que já existia no `cart-sheet`; cadastro no form do produto (chips+select). **Modo escuro** configurável: `cardapio_config.tema` (claro/escuro/`auto`) + toggle do cliente (localStorage por token) + `auto` segue `prefers-color-scheme`; CSS escopado `.tema-escuro` no `<body>` (sobrepõe as classes claras sem tocar no modo claro; âmbar preservado). *(Barra de frete grátis e categorias sticky já existiam no `cart-sheet`; página já era componentizada em `@/components/loja/*`.)* Builds verdes. **Merge exige migrations 089 + 090.** | Cardápio mais rápido de ler, sugestão inteligente e tema à escolha da loja/cliente |
| 2026-07-14 | **Cardápio — Etapa 6: acompanhamento + alerta de pedido novo** (sem migration). **(a)** Página de status **standalone** e reabrível `c/[token]/pedido/[id]?ref=` com timeline (Recebido → Em preparo → Pronto/Saiu → Entregue) e atualização por **polling leve** (8s, para ao finalizar). O backend `statusPedido` valida o `ref` contra `client_ref` (não vaza pedido de outro trocando o ID). O cardápio mostra o **link "Acompanhar pedido"** ao finalizar. **(b)** **Alerta sonoro** de pedido novo no painel de delivery: beep (Web Audio, sem asset) + **destaque visual** (pulse) quando a contagem de "novo" sobe; botão **🔔 liga/desliga** persistido (localStorage) com aviso "clique para ativar o som" (o navegador exige o gesto). Builds verdes. **Fecha a rodada de segurança/robustez do cardápio (6 etapas).** | Cliente acompanha sem refazer o carrinho; loja não perde pedido que chega |
| 2026-07-14 | **Impressão automática na cozinha — Etapa 1 (ESC/POS via edge)** (migration 092). Constrói **sobre** a infra existente (`impressao_job`, impressoras em `equipamento`, geração de vias em `producao-pedido`), sem remover nada. **(a) Impressora mais rica:** `equipamento` ganhou `largura` (58/80 mm) e `setores_atendidos` (jsonb) — uma impressora pode servir vários setores. **(b) Roteamento upgrade** (`producao-pedido.criarPedidos`): além do roteamento por produto/setor→equipamento que já existia, o item também vai para impressoras cujos `setores_atendidos` batem com o setor do produto; item **sem setor** cai na **impressora padrão** da unidade (`padrao`); dedupe por (impressora,item). **(c) Worker do edge** (`edge/impressao-daemon.mjs` + `edge/escpos.mjs`, serviço `RegemEdgeImpressao`): estratégia **LISTEN/NOTIFY do Postgres (imediato) + poll de 3s (rede de segurança)** — supera long-poll (sem conexão presa em proxy) e socket.io (sem dependência nova); envia por **TCP RAW/9100** com **retry/backoff (3 tentativas)**; converte o texto do ticket em ESC/POS (título/senha em **fonte dupla+negrito+centro**, OBS em **negrito**, complementos indentados, **corte + bipe**, acentos transliterados p/ ASCII, largura 32/48 col). Gatilho `trg_impressao_nova` na mig 092. **(d) Painel** (`/equipamentos`): impressora com largura + setores atendidos + "impressora padrão"; botão **"imprimir teste"** por impressora (`POST /impressao/impressoras/:id/teste`); **fila de impressão** com status e **reimpressão** de falhas (`GET /impressao/fila`). **(e) Teste:** `npm run impressora:fake` (impressora TCP falsa que decodifica o ticket) — pipeline validado ao vivo (ESC/POS + TCP + corte/bipe). Builds verdes. **Merge exige migration 092.** *(Pendências: propagação do daemon no auto-update fica pro instalador completo — como o sync-daemon hoje; teste de roteamento é manual via 2 fake printers, pois o CI não tem DB.)* | Pedido confirmado sai sozinho na impressora certa da cozinha/bar, com layout legível e reimpressão |
| 2026-07-14 | **Mapa de calor de entregas por bairro — Etapa 3** (sem migration). Novo `GET /delivery/mapa-calor?dias=7\|30\|90` (**presidente/gerente**, RBAC no servidor) que agrega `pedido_externo` de **entrega não cancelada** no período (**todos os canais**) por `endereco_bairro` (nulo → "Sem bairro"): nº de pedidos, receita (Σ total), **ticket médio**, **taxa média** (avg frete) e **% de participação**; devolve também os totais gerais. Nova tela `/relatorios/delivery` (**Mapa de calor de entregas**): seletor de período (7/30/90d), **cartões de insight** (bairro campeão, pedidos, ticket médio, taxa média) e `ResponsiveTable` com **barra de calor CSS** por linha (intensidade pela participação em pedidos, na cor âmbar da marca). Atalho "🗺️ Mapa de calor por bairro" na aba Delivery de `/relatorios` (complementa a lista "Por região" simples que já existia). **Sem lat/lng no pedido** → visão por bairro (o modo raio/scatter por coordenada fica como evolução futura, pois `pedido_externo` não guarda coordenada). Builds verdes. **Fecha a rodada de 3 etapas (impressão ESC/POS · cupom 1ª compra já existente · mapa de calor).** | Ver de quais bairros vêm os pedidos, com receita/ticket/taxa — decisão de raio de entrega e marketing local |
| 2026-07-14 | **Atualização do edge: botão manual + verificação por horário de abertura + auto-update propaga `edge/`** (sem migration). **(A)** `atualizar.ps1` agora faz **cópia overlay** dos arquivos de `edge/` (daemons `.mjs` + `.ps1`) do pacote — arquivo por arquivo, sem apagar a pasta — e um bloco **"garantir serviços"** que registra `RegemEdgeImpressao` se faltar; resolve a pendência (a impressão automática passa a chegar por auto-update, não só na reinstalação). Também **atualiza `APP_VERSION` no `.env.local`** ao concluir (e reverte no rollback) — evita rebaixar o mesmo pacote em loop. **(B)** A **verificação** deixou de ser "1×/hora" e passou a rodar só nas **janelas de abertura da loja** (nos 10 primeiros min após abrir e ~30 min depois), lendo `cardapio_config.horarios` do banco local — **fallback 04:00** se não houver horário; marca a janela do dia em `sync_state`. `cardapio_config` entrou no sync `desce` (config da loja disponível offline). A tarefa `RegemEdgeUpdate` **perdeu o gatilho diário 04:00** (não aplica mais sozinha) e virou **sob demanda**. **(C)** No app (`/servidor`, só quando `NEXT_PUBLIC_EDGE=1`, gestão): card **Atualização do servidor** com **Verificar atualização** (força o update-check ao vivo na nuvem) e **Instalar atualização** (dispara `schtasks /run /tn RegemEdgeUpdate` — a tarefa SYSTEM aplica com backup+rollback). Endpoints `GET/POST /edge/atualizacao/{status,verificar,aplicar}` (gate `EDGE_MODE=true` + presidente/gerente). Builds verdes; PS parseado (PS1 em ASCII — `→`/`—` viravam aspas tipográficas sem BOM). | Loja atualiza quando quer (é avisada ao abrir, instala com 1 clique) e a impressão automática passa a chegar pelo update |
| 2026-07-14 | **Sistema híbrido Nuvem/Local — consciência de modo, escape hatch e restauração** (sem migration). Concretiza a filosofia offline-first (local opera sem internet; só integrações dependem de rede). **F1 — consciência de modo:** `ModoOperacao` (selo 🖥️ Local / ☁️ Nuvem no cabeçalho, decidido pelo build `NEXT_PUBLIC_EDGE`, sem toggle em runtime) + `ServidorOfflineAviso` (só no edge: se o servidor local cai com o app aberto, banner "usar modo nuvem?" → abre `app.dmsregem.com`). **F2 — atalhos:** o instalador cria atalhos separados na área de trabalho — **PDV/KDS/Ponto/Garçom locais** (`localhost:3001/…`) + **Regem (nuvem)** como escape hatch de boot. **F3 — restaurar do estado da nuvem** (voltar ao local após operar na nuvem): botão em `/servidor` (só edge + presidente) grava flag em `sync_state`; o `sync-daemon` faz **2 tempos** — (1) empurra o operacional local pendente pra nuvem, (2) puxa as **tabelas transacionais** (`comanda`, `comanda_item`, `producao_pedido(_item)`, `pedido_externo`, `caixa_sessao`, `lancamento_caixa`, `movimento_estoque`) por delta e faz **upsert por id** (aditivo, nunca apaga o que é só local; retry de FK). Novo `GET /sync/restore` (mesma mecânica do pull, whitelist `TABELAS_RESTORE`) + `POST /edge/restaurar` / `GET /edge/restaurar/status` (gate `EDGE_MODE` + presidente). Builds verdes. **Pendências:** (a) as tabelas transacionais ainda **não sobem** continuamente pra nuvem (só o restore-pull desce) — o UP contínuo é a v2 do sync; (b) restore não testado ponta a ponta (precisa nuvem+edge reais). Impressão no modo nuvem (F4) fica para depois. Ver memória [[modo-hibrido-nuvem-local]]. | Loja escolhe Nuvem/Local com clareza, tem escape se o local cai, e volta pro local sem perder dado |
| 2026-07-14 | **Híbrido F4 — impressão no modo nuvem (escape hatch por navegador)** (sem migration). Método A escolhido (recomendado): novo `lib/print-cupom.ts` (`imprimirCupomNavegador`) renderiza o cupom em HTML com CSS de bobina (58/80mm) e chama `window.print()` — funciona com qualquer impressora instalada no Windows (inclusive térmica com driver), **sem depender do worker do edge**. Botão **"🌐 Imprimir (navegador)"** no modal de cupom (`/cupons`), ao lado da reimpressão térmica (que virou "🖨 Reimprimir (térmica)", via servidor local). É aditivo e seguro (só acrescenta um caminho de impressão que roda offline do edge); se um dia quiser térmica silenciosa em loja 100% nuvem, a ponte na bandeja (Método B, reusa `escpos.mjs`) entra como adição. Fecha as fases do híbrido (F1–F4); o menu por perfil fica com o dono do projeto (amarrado ao `perfil_acesso` configurável). Build verde. | Dá pra imprimir o cupom operando na nuvem, sem o servidor local |
| 2026-07-14 | **Catálogo completo de permissões + editor do presidente** (migration 093). O RBAC configurável (`perfil_acesso`) ganhou um **catálogo completo** de permissões (30+ chaves) alinhado ao menu novo: `permissoes.ts` expõe a interface `Permissoes` ampliada + `CATALOGO_PERMISSOES` (chave/rótulo/grupo/tipo) + `podeAcessar()` + `PERFIS_PADRAO` com os defaults por nível (Presidente tudo · Gerência sem financeiro/fiscal/relatórios/C&O e dashboard sem R$ · Supervisão operação do setor · Execução PDV/mesas/cupons/pedidos+mural, escala visual, conclui checklist, meu_dia não). `PermSpec` do `@RequirePerm` ampliado para qualquer chave booleana (o `PermissoesGuard` já era genérico). Módulo `perfil` ganhou **criar** (`POST /perfis`), **remover** (`DELETE /perfis/:id`, bloqueia presidente e perfil em uso) e **catálogo** (`GET /perfis/catalogo`). A tela **/config/acessos** virou **dirigida pelo catálogo** (toggles agrupados por área, ações CRUD para ponto/estoque) + **adicionar/remover perfil**. **Migration 093:** backfill do `perfil_acesso.permissoes` (jsonb `default || existente` — preenche chaves novas por nível preservando edições). Builds verdes. **Menu (shell) fica com o dono** (pendura `perm:'<chave>'` por item); **server enforcement (`@RequirePerm` nos controllers) é o follow-up que anda junto com o menu.** **Merge exige migration 093.** | Presidente monta perfis por permissão (não por nível fixo), cobrindo todo o menu novo, sem quebrar categorias operacionais |
| 2026-07-15 | **RBAC — enforcement no servidor (`@RequirePerm`) das telas P-only** (sem migration). Casando com o catálogo (mig 093): `relatorios` (classe → `relatorios_vendas`) e `auditoria` (classe → `auditoria`) passam a exigir a permissão além do `@Roles` — efeito: gerência/supervisão **perdem** relatórios e auditoria (matriz nova = presidente). Endpoints pontuais: `vendas GET /remocoes` → `@RequirePerm('cancelamentos')` (+ RolesGuard/PermissoesGuard no método) e `financeiro GET /caixa/fechamentos` → `@RequirePerm('turnos')`. Telas **operacionais compartilhadas** (PDV/mesas/cupons/pedidos/TEF etc.) ficam **sem** enforcement por área (quebraria uso cruzado entre perfis) — a visibilidade delas é só no menu. Build verde. *(Nota: enquanto o menu novo não é publicado, a gerência ainda vê o item antigo de relatórios/auditoria no shell e recebe 403 ao clicar — resolve quando o shell for religado às permissões.)* | Servidor bloqueia de fato as telas sensíveis por permissão, não só o menu esconde |
| 2026-07-15 | **Menu religado por permissão + estrutura nova (14 grupos + submenus)** (sem migration). O `shell.tsx` deixou de filtrar por `roles` (4 níveis) e passou a filtrar por **permissão do catálogo** (`getPermissoes()`), casando com o editor/mig 093. NAV virou 2 níveis (`NavNode` + `children`): Dashboard · PDV-Balcão [Mesas · Cupons] · Delivery [Pedidos] · Meu Dia · Escalas · Estoque · Checklist & registros · Mural & clima · Financeiro [Notas · TEF · Configuração] · Cadastros · Gerenciamento de ponto · Configurações [Produção & KDS · Config. por ramo · Planos · Acessos & perfis · Servidor local] · Relatórios [Turnos · Relatórios de vendas · Cancelamentos de itens] · Auditoria · Visão C&O. Cada item tem `perm:'<chave>'`; presidente vê tudo (bypass); `estoque` usa `.ver`. **Garçom e Bot saíram do menu** (acessados à parte). Fidelidade/Cashback ficam de fora até terem tela própria (sem rota). Builds verdes. **⚠️ Transição:** quem já está logado (não-presidente) precisa **relogar** para o token carregar as permissões novas (senão o menu aparece vazio). | Menu e servidor agora falam a mesma língua (permissão), na organização nova |
| 2026-07-15 | **RBAC — enforcement estendido às telas P-only de gestão** (sem migration). Casando com o catálogo: `fiscal GET /config` → `@RequirePerm('fiscal_config')` (notas seguem em `fiscal`); `tef GET/PUT /config` → `@RequirePerm('tef')` (os `pagamentos` do PDV seguem abertos — TEF é operacional); `licenca GET /planos` + `POST /assinatura/checkout` → `@RequirePerm('planos')` (`licenca/status` segue aberto p/ o paywall). Com isso, todas as telas **P-only** ficam bloqueadas no servidor: notas(`fiscal`), config fiscal(`fiscal_config`), config TEF(`tef`), financeiro(`financeiro`), planos, turnos, relatórios, cancelamentos, auditoria. Build verde. *(Apontado: **Acessos & perfis** está no menu como P+G, mas `perfil.controller` é presidente-only — recomendado manter só presidente; aguardando decisão do dono.)* | Só o presidente acessa as telas sensíveis, de fato no servidor |
| 2026-07-15 | **Menu em acordeão** (sem migration, só frontend). Os grupos do `shell.tsx` (PDV-Balcão, Delivery, Financeiro, Configurações, Relatórios) viraram **clicáveis**: o pai expande/recolhe a lista de subitens (seta ▾/▸), deixando o menu mais curto/arrumado — em vez de tudo sempre aberto. Abre sozinho no grupo da tela ativa; estado por grupo. PDV-Balcão e Delivery viraram grupos (a tela própria virou o 1º subitem: "Balcão" `/pdv`, "Painel" `/delivery`). Build verde. | Menu mais limpo e curto, com submenus escondidos até clicar |
| 2026-07-15 | **Segurança — fail-fast em segredos + bcrypt 12** (sem migration). Revisão de segurança validada: **(1)** `cliente-token.ts` (link mágico do cliente, HMAC) não usa mais o fallback `'regem-dev-secret'` — **lança erro** se `JWT_SECRET`/`CLIENTE_TOKEN_SECRET` ausente/fraco (evitava forjar sessão de cliente = PII). **(2)** `lease.ts` (licença Ed25519) **recusa** gerar par efêmero em produção (`NODE_ENV=production`) — só dev, com aviso. **(5)** `bcrypt` subiu de **10 → 12** rounds em todos os hashes (senha + PIN); throttle de auth já estava apertado (`@Throttle 5/60s`). *(Achados 3 — perm/cat presos no JWT 12h — e 4 — lockout de PIN por unidade — validados como **trade-offs**, aguardando decisão de abordagem.)* | Fecha degradação silenciosa de segredos; hashes mais fortes |
| 2026-07-15 | **Sync v2 — tabelas transacionais sobem (edge → nuvem) por LWW** (migration 095). As vendas/produção/caixa (`comanda`, `comanda_item`, `producao_pedido(_item)`, `pedido_externo`, `caixa_sessao`) passam a **subir continuamente** para a nuvem, fechando o "espelho" e destravando o restore de verdade. Como mudam de estado (comanda fecha, pedido avança), a mig 095 adiciona **`updated_at`** (nas que faltavam) + gatilho `trg_bump_updated_at` (bumpa em todo UPDATE); o daemon (`PUSH_TABLES`, pais-primeiro) e o `TABELAS_SYNC` ('sobe', cursor `updated_at`) reenviam a linha a cada mudança, e o `POST /sync/push` aplica **LWW** (update-se-mais-nova) para qualquer tabela com `updated_at` — append puras (movimento_estoque) seguem imutáveis. Push tolera FK fora de ordem (23503 → ignora, volta no próximo ciclo). `TABELAS_RESTORE` também passou a cursor `updated_at` (traz mudanças de estado feitas na nuvem durante a queda). Builds verdes. **Merge exige migration 095.** | Nuvem vira espelho real das vendas do edge; restore fica completo |
| 2026-07-15 | **RBAC ao vivo (revalidação no banco) + lockout de PIN mais curto + enforcement do Gerenciamento de ponto** (sem migration). **#3:** `JwtAuthGuard` deixou de confiar cegamente no token — agora **revalida o colaborador no banco** (status + perfil.nivel + permissões) com **cache de 30s**: bloqueio/rebaixamento/mudança de permissão valem **na hora** (não em 12h), e — bônus — **mudança de permissão NÃO exige mais relogar**. Erro de banco → mantém o token (disponibilidade); `bloqueado` → 401. **#4:** janela de lockout de PIN por unidade **15 → 5 min** (recupera rápido, reduz DoS interno; brute-force já barrado pelo `@Throttle 5/60s`/IP). **Enforcement P+G:** `GET /ponto/pessoas` (Gerenciamento de ponto) → `@RequirePerm('ponto_gerencial')` (P+G, tira supervisão conforme a matriz). Builds verdes. *(Achados de segurança 1,2,3,4,5 agora endereçados; 3 e 4 fechados aqui.)* | Revogar/rebaixar vale na hora; sem relogar; PIN menos DoS |
| 2026-07-15 | **RBAC — fecha enforcement dos P+G (servidor + produção/KDS)** (sem migration). `edge` atualização/restauração-status → `@RequirePerm('servidor')`; config de **Produção & KDS** (`producao-pedido` PUT produtos/setores destinos, cores, senha/config) → `@RequirePerm('producao_kds')` — só as **escritas** (gestão); os GET seguem abertos (o KDS lê cores/senha). **Cadastros** fica **sem** enforcement por área — é um hub que agrega CRUDs usados operacionalmente (ex.: PDV lê produtos); a visibilidade fica só no menu + `@Roles` nos CRUDs. Builds verdes. Com isso o enforcement por permissão cobre todas as áreas de gestão (P-only e P+G). | Toggles de permissão do C&O valem no servidor também nas telas P+G |
| 2026-07-15 | **Multi-unidade (rede) — Fase 1 (gestão) + Fase 2 (isolamento por loja)** (migrations 097, 098; em `dev-local`). **Fase 1:** tela *Configurações → Unidades* (CRUD, reusa `POST/PATCH/DELETE /unidades`) + permissão `unidades` no catálogo RBAC (mig 097). **Fase 2 (escopo completo front+backend):** seletor de unidade no topbar + header **`X-Unidade-Id`** + decorator **`@UnidadeAtual`** (resolve o RBAC no servidor; o `tenant_id` de cada query protege contra id forjado). Passaram a filtrar pela unidade: **escala** (dia/semana/período), **colaboradores/setores/turnos**, **dashboard** (`resumo`+`timeline`, fragmentos `sql` nas 8 subqueries — todas as tabelas têm `unidade_id`) e **operacional** (produção/KDS, mesas, config fiscal — fallback pro `@UnidadeAtual` quando não vem `?unidadeId=`). **Restrição presidente-only** (pedido do usuário; mig 098): a visão/gestão de unidades e o seletor são **exclusivos do presidente/C&O** — `@Roles('presidente')` no `UnidadeController`, perm `unidades` revogada dos demais, seletor gated em `getCategoria()==='presidente'`. **Isolamento por loja:** o claim JWT `uni` passou a usar **`colaborador.unidade_id`** (fallback função→setor) → gerente/supervisão/execução ficam **travados na própria loja** (o `@UnidadeAtual` ignora o header de quem tem `uni`); presidente (`uni=null`) vê/alterna a rede. Comparativo entre lojas na Visão C&O já existia. **Merge exige migrations 097 e 098.** | Rede multi-loja: presidente gerencia/compara; demais isolados na própria unidade |
| 2026-07-15 | **PLANO APROVADO (a implementar) — Cadastro de Terminal de PDV.** Terminal = `equipamento tipo='pdv'` (reusa `unidadeId/nome/token/ativo`). **Identificação por pareamento por token OBRIGATÓRIO** (sem retrocompatibilidade — projeto ainda em teste antes da disponibilidade final): admin cadastra "Caixa 1" → token → o PC cola uma vez → `localStorage` → header **`X-Terminal-Id`**; decorator `@TerminalAtual` resolve terminal→unidade→caixa. **Caixa por terminal:** `sessaoAberta/abrirSessao/caixaAtual` passam a considerar `terminal_id` (a trava "1 caixa aberto" vira por terminal; fecha de brinde o gap do caixa multi-unidade na nuvem). **Impressão:** impressoras de **setor** (cabeadas na rede, **sem PC** — `equipamento tipo='impressora'` com `host:porta`, roteadas por `setoresAtendidos` pelo daemon do edge via **TCP 9100**) **NÃO mudam**; a **via do cliente (cupom)** passa a sair só na impressora do terminal (`impressora_padrao_id`) → fallback unidade → `padrao`, **corrigindo** o `enfileirarViaCliente` que hoje imprime em **TODAS** as `papel='cupom'` do tenant (errado em multi-balcão/multi-loja). **Migrations previstas:** `caixa_sessao.terminal_id`, `equipamento.impressora_padrao_id`. **Fases:** F1 (cadastro + pareamento), F2 (caixa por terminal + fechamento por terminal), F3 (impressora por terminal + fix do cupom). RBAC: cadastro/pareamento = presidente/gerente; operar = perm `pdv`. | Identidade real de caixa (1/2/3 por loja); caixa e impressora por terminal; corrige cupom multi-balcão/multi-loja |
| 2026-07-15 | **Terminal de PDV — F1 (cadastro + pareamento)** (sem migration; `dev-local`). `equipamento` passou a aceitar **`tipo='pdv'`** (reusa `unidadeId/nome/token/ativo` — **sem coluna nova**). Cadastro pela tela *Configurações → Equipamentos* (novo tipo "Terminal de PDV (caixa)"); o `token` aparece uma vez. **Pareamento OBRIGATÓRIO:** `POST /equipamento/parear {token}` (autenticado — o token é a prova; sem `@Roles`) valida device pdv **ativo do tenant** → devolve `{id,nome,unidadeId}`; o PC guarda em `localStorage` e envia **`X-Terminal-Id`** em todo request (`req()`). O `/pdv` fica bloqueado por **`<TerminalGate>`** até parear (indicador "Terminal: X · trocar"). Infra pronta p/ F2: decorator **`@TerminalAtual`** (id cru do header) + `EquipamentoService.terminalUnidade(tenant,id)` (resolve terminal→unidade validando tenant+pdv+ativo). Verificado: criar→token, parear→identidade (unidade bate), token inválido→404. Builds verdes. | Identidade de caixa por máquina; base do caixa/impressora por terminal (F2/F3) |
| 2026-07-15 | **Terminal de PDV — F2 (caixa por terminal)** (migration 099; `dev-local`). `caixa_sessao.terminal_id`. A sessão de **balcão** (origem 'pdv') virou **por terminal**: `sessaoAberta/abrirSessao/caixaAtual/movimentarCaixa/fecharSessao` passam a receber o `@TerminalAtual` (header `X-Terminal-Id`); a trava "1 caixa aberto" vira **por terminal** (N caixas simultâneos, 1 por balcão) e a **unidade da sessão deriva do terminal** — fechando o gap do caixa multi-unidade na nuvem (`caixaAtual` não filtrava unidade). A **venda** (`vendaBalcao` + `sessaoAbertaId`) também amarra ao terminal: a comanda **herda a unidade do terminal** e o pagamento cai na sessão daquele caixa. Delivery (origem 'delivery') segue sem terminal (gaveta única). Abrir/vender sem terminal pareado é bloqueado. Verificado: 2 terminais (Matriz/Filial) abrem caixas independentes; `caixaAtual` isola por terminal; reabrir no mesmo → "já existe caixa aberto neste terminal"; unidade herdada correta. Builds verdes. **Merge exige migration 099.** | Cada balcão tem seu caixa; corrige o caixa multi-unidade/multi-balcão |
| 2026-07-15 | **Terminal de PDV — F3 (impressora/cupom por terminal)** — fecha o épico (migration 100; `dev-local`). `equipamento.impressora_padrao_id` (terminal PDV → impressora de cupom). `enfileirarViaCliente` (via do cliente) deixou de imprimir em **TODAS** as `papel='cupom'` do tenant e passou à prioridade: **(1)** impressora amarrada ao terminal; **(2)** impressoras 'cupom' da **unidade** (ou de rede, `unidade_id` nula). O `terminalId` flui `vendaBalcao → imprimirViaCliente → enfileirarViaCliente`. Cadastro/edição do vínculo: campo no form de PDV (Configurações → Equipamentos) + `PATCH /equipamento/:id/impressora` + select inline na lista. **Impressoras de setor (produção, TCP 9100, roteadas por `setoresAtendidos` pelo daemon do edge) seguem inalteradas.** Verificado (roteamento): terminal amarrado→só a dele; sem terminal+unidade→cupom da unidade+rede (não de outra loja); sem unidade→todas. Builds verdes. **Merge exige migration 100.** *(Épico Terminal de PDV completo: F1 identidade, F2 caixa por terminal, F3 impressora/cupom.)* | Cupom sai só na impressora do balcão certo; corrige multi-balcão/multi-loja |
| 2026-07-15 | **Instalador edge — robustez + segurança do log + atalhos em tela cheia** (sem migration; `dev-local`; exige rebuild do .exe p/ 2 e 3). **(1) Provisionamento:** `instalarSelfService` falha CEDO com log claro no servidor (`Logger.error` — causa: `LICENSE_*` ausentes na regem-api) + **503 genérico** ao instalador, em vez do 500 opaco (lease.ts fail-fast). Novo `licencaConfigurada()` em lease.ts. Motivada por instalação real que deu 500 por falta de `LICENSE_PRIVATE_KEY_B64` na nuvem. **(2) Credenciais fora do log:** o `.iss` parou de passar `-Email/-Senha` na linha de comando (o `Start-Transcript` gravava a senha do C&O no `instalar-*.log`); agora escreve um **arquivo temporário** (`CurStepChanged`/`SaveStringToFile` em `{tmp}`) e passa só `-CredFile`; o `instalar-tudo.ps1` lê e **apaga na hora** (`-Email/-Senha` seguem como fallback manual). **(3) Atalhos "cara de programa":** os atalhos da área de trabalho viraram `.lnk` abrindo Edge/Chrome em **`--app=<url> --start-fullscreen`** (janela sem abas/barra, tela cheia); sem navegador → cai no `.url` simples. Tudo ASCII (parse-check PS 5.1 ok). | Diagnóstico claro no log (não no 500), senha do C&O não vaza no log, e os terminais parecem um app dedicado |
| 2026-07-15 | **Escala recorrente — F1 (motor) + F2 (regra + endpoints)** (migration 101; `dev-local`). **F1:** `common/regras-escala.ts` ganhou `gerarDiasTrabalho(regra)` (12x36/4x3 = ciclo rotativo da data-base; 5x2/6x1 = por dia da semana via `folgasSemana`; pula feriados), `pausaSugerida(entrada,saída)` (pausa CLT centralizada: >6h→1h, 4–6h→15min, ≤4h→nenhuma) e `precisaPerguntarFolga`. +12 testes (28 verdes). **F2:** tabela **`escala_regra`** (recorrência) + `escala_alocacao.regra_id` + **overrides por dia** (`hora_inicio/fim`, `pausa_inicio/fim`) lidos por `coalesce` no `enriched`. `POST /escala/gerar` (cria a regra + preenche o período; feriados de `dia_especial` pulados quando `feriadosFechar`; idempotente; avisos CLT sem bloquear) e `PATCH /escala/:id/horario {escopo}` (**dia** / **futuras** = mesma regra data≥ / **datas** = lista que o front calcula p/ o padrão mensal; bloqueia CLT ilegal). Turno reusado/criado por setor+horário. Verificado: 12x36 gera 16 dias alternados, 5x2 gera 21 úteis, editar "só dia" vs "futuras" isola/propaga certo. **Merge exige migration 101.** *(Falta F3: wizard no card do dia.)* | Escala vira "evento recorrente de calendário": preenche o período sozinho, pausa por CLT, edição com escopo |
| 2026-07-15 | **Escala — botão de feriado/evento + 4x3 confirmado** (sem migration; `dev-local`). **4x3** já estava completo (opção em `JORNADAS` "4x3 (4 dias, 3 folgas)" + motor `CICLOS {on:4,off:3}`), só confirmado. **Botão discreto "Feriado / evento"** no header da Escala (`actions`, só presidente/gerente) → `DiaEspecialModal` (tipo feriado/evento, nome, data, "até" p/ período, observação) usando o `dia_especial` que já existia; api.ts ganhou `criarDiaEspecial`. Feriados alimentam o "fechar nos feriados" da geração; eventos servem de aviso (troca de escala/reforço) no calendário. A grade da escala já marcava os dias especiais (emoji) — agora dá pra cadastrá-los ali. | Cadastrar feriados/eventos direto na escala, sem ir a outra tela; alimenta a geração recorrente |
| 2026-07-15 | **Escala recorrente — F3 (wizard) + Férias** — fecha o épico (sem migration; `dev-local`). **Wizard `GerarEscalaWizard`** (botão "Gerar escala" no header, presidente/gerente): colaborador (prefill do `jornadaTipo`) + vaga + tipo + entrada/saída → **pausa CLT sugerida ao vivo** (editável) → **folgas** (chips da semana; escondido em 12x36/4x3) → **feriados: fechar** (toggle) → **período** (1/3/6/12 meses) → resumo → `POST /escala/gerar`. **Férias** = 3ª aba do `DiaEspecialModal` (colaborador + período): grava `dia_especial tipo='ferias'` e o backend **DESMARCA** (soft-delete) as alocações do colaborador no intervalo — ele sai da escala nesses dias (vaga abre p/ cobertura) e o marcador 🏖️ aparece na grade. `dia-especial.service.create` devolve `desmarcadas`. Verificado: wizard gera 12x36/5x2 certo; férias remove 7 dias no período e preserva os de fora. **Épico escala recorrente completo (F1 motor+CLT, F2 regra+endpoints+overrides mig 101, F3 wizard+férias).** | Montar escala vira preencher um "evento recorrente"; férias tiram o colaborador da escala automaticamente |
| 2026-07-15 | **Escala — regra de folga inquebrável + rótulos de vínculo + filtro por função no wizard** (sem migration; `dev-local`). **(bug)** o wizard deixava marcar 3 folgas num 5x2 e gerava: agora `folgasSemanais(tipo)` (5x2=2, 6x1=1) é validado no **backend** (`gerarEscala` → 400 "exige exatamente N folgas") **e** no wizard (não deixa passar do limite com toast; avisa "faltam X" antes de gerar; apara ao trocar de tipo). **Rótulos de vínculo** ("Tipos"): `VINCULOS` viraram CLT / Diarista / Horista / Prestador de serviço / Autônomo (valores inalterados). **Filtro por função/setor** no `GerarEscalaWizard`: o dropdown de colaborador passou a mostrar só quem tem a função da vaga (etiqueta) — não lista gente de outro setor; reseta o colaborador se trocar p/ vaga incompatível. | Tipo de escala é regra que não se quebra; menos erro de alocação por setor |
| 2026-07-15 | **Escala — resumo "todos do dia" ao clicar num card preenchido** (sem migration; `dev-local`). Clicar num card **preenchido** (grade semana desktop e card mobile) deixa de abrir direto o form e abre um **resumo do dia** (`detalheDia`): lista todos os escalados daquele dia — **nome · vaga (sigla) · horário** (entrada–saída), ordenados por horário, com nota dos dias especiais e "editar" por linha (abre o form da vaga). Card **vazio** segue abrindo a alocação. Setor não repetido na lista (já é o agrupamento/linha da grade). | Ver quem está o dia inteiro num clique, sem varrer vaga por vaga |
| 2026-07-15 | **Revisão de modelo da escala — Fase A (cadastro) + Fase B (modelo)** (migration 102; `dev-local`). **A (sem migration):** Cadastro alinhado às regras: **Turnos** perde os campos de pausa (a pausa vem da regra da escala); **Funções** perde "gerar etiqueta" e "sigla" (etiqueta só no card Etiquetas — backend só gera etiqueta se `gerarEtiqueta===true`, não mais por padrão); **Etiquetas** não lista mais presidente/C&O no campo Função e o placeholder da sigla virou "AUXC1 · Auxiliar de cozinha 1"; **card "Dias importantes" removido** (já está no menu Escala). **B (mig 102):** `unidade.tipo` = **Matriz | Filial** (a mais antiga de cada tenant vira Matriz; select + badge na tela Unidades) e **função ↔ setor N:N** (`funcao_setor`; `funcao.setor_id` segue como primário; findAll devolve `setorIds`; cadastro de Funções vira multiselect de setores). **Merge exige migration 102.** *(Faltam Fase C: motor 6x1-ciclo/5x1 + limites CLT; Fase D: falta+comprovante+relatório.)* | Modelo alinhado à lógica de negócio revisada (turno≠escala, unidade Matriz/Filial, função multi-setor) |
| 2026-07-15 | **Escala — Fase C: motor + limites CLT** (sem migration; `dev-local`). **6x1 virou ciclo 6/1** (era por dia da semana) e **5x1 é novo** (ciclo 5/1) — `CICLOS` no `regras-escala.ts`; só **5x2** segue por dia da semana (2 folgas). **Jornada máxima por tipo** (`jornadaMaximaHoras`: 12x36=12h, 4x3=11h, 5x2/5x1=8.8h, 6x1=7.34h) → `validarAlocacao` emite aviso **`carga_horaria`** se a jornada passar do teto (ex.: 12h numa 5x2). **DSR** (`avisosDsr`): aviso quando um mês do período tem todos os domingos trabalhados (≥1 domingo de folga/mês). `POST /escala/gerar` ganhou **`respeitarClt`** (checkbox "Levar em conta regras da CLT vigente", ligado por padrão) que liga/desliga esses avisos; o wizard mostra o alerta **ao vivo** ao digitar horários fora do teto. JORNADAS ganhou 6x1/5x1. 36 testes verdes. | Tipos de escala legais completos + alerta de jornada fora da CLT |
| 2026-07-15 | **Escala — Fase D: Falta (presença + comprovante + relatório)** — fecha a revisão da escala (migration 103; `dev-local`). `escala_alocacao` ganhou **`presenca`** (prevista/presente/falta_justificada/falta_injustificada) + `comprovante_ref` + `presenca_obs` + `presenca_em`. `PATCH /escala/:id/presenca` (presidente/gerente/supervisão; comprovante só na justificada; auditado `marcou_presenca`) e `GET /escala/faltas?de&ate` (relatório: lista + **resumo por colaborador** justif/injust). Front: no resumo "Escalados do dia" cada linha tem um **chip de presença** → `PresencaModal` (3 status + upload do comprovante na justificada + obs); botão **"Faltas"** no header → `FaltasModal` (período + resumo + lista com link do comprovante). Verificado ponta-a-ponta. **Merge exige migration 103.** *(Revisão da escala A–D completa: A cadastro, B modelo mig 102, C motor/CLT, D falta mig 103.)* | Registrar presença/faltas com comprovante e relatório por colaborador |
| 2026-07-15 | **Escala — reclassificação folga: por dia da semana = soma 7** (sem migration; `dev-local`; correção da Fase C). Regra clara: os tipos cuja soma trabalho+folga = 7 (**5x2**=2 folgas, **6x1**=1, **4x3**=3) são **por dia da semana** → o usuário **escolhe os dias de folga** (com o nº exato validado). Só **12x36** (dia sim/não) e **5x1** (5 on/1 off, 6 dias, não fecha a semana) ficam em **ciclo automático**. Antes a Fase C tinha posto 6x1 e 4x3 como ciclo — o usuário apontou que só o 5x2 deixava escolher. `CICLOS` = {12x36, 5x1}; `folgasSemanais` = {5x2:2, 6x1:1, 4x3:3}; wizard `ehCiclo`/`folgasExigidas` idem. 34 testes verdes. Verificado: 6x1 exige 1 folga, 4x3 exige 3, 12x36/5x1 automáticos. | Escolher a folga bate com a CLT (6x1 folga no domingo, 4x3 3 dias) |
| 2026-07-15 | **Timeline: pico mais visível + janela de pico por intervalo de dias** (migration 104; `dev-local`). **(1)** No `Timeline` do dashboard a faixa de pico saiu do `bg-destructive/5` (quase invisível) para **`bg-warn/12` + bordas âmbar** e ganhou uma **faixa discreta no topo** com "🔥 nome" (descrição de status sutil). **(2)** `janela_pico.dia_semana_fim` — a janela de pico aceita **intervalo de dias** (ex.: seg→sex, com "volta" na semana): no cadastro (Cadastros → Janelas de pico) o select de dia ganhou **"Personalizar (intervalo)…"** que revela "Do dia"/"Até o dia" (via novo `showIf`/`fromRow` no `EntityForm`); o match no dashboard virou range-aware. Verificado: pico seg→sex aparece na quarta, some no domingo. **Merge exige migration 104.** | Pico legível na timeline + janelas por faixa de dias (não só 1 dia/todos) |
| 2026-07-15 | **Escala — unificação da alocação num só fluxo (Gerar escala)** (sem migration; `dev-local`). A tela antiga de "Nova alocação" (`nova-alocacao-form.tsx`) foi **removida**; tudo passa pelo **`GerarEscalaWizard`** (título "Gerar escala recorrente" → "**Gerar escala**"). Novo campo **Recorrência** com **"1 dia" como padrão** (dataInicio=dataFim → alocação avulsa; feriados/folga escondidos e não exigidos nesse modo — backend pula a validação de folga quando `dataInicio===dataFim`); períodos maiores = recorrente. O wizard aceita **colaborador/vaga pré-selecionados**: **card vazio** → abre com a vaga; **"editar"** no resumo do dia → abre com o colaborador já selecionado. O resumo do dia ganhou botão de **remover** (🗑, reusa `removerAlocacao`). Removidos `NovaAlocacaoForm`/`Select`/`elegiveis`/`trocarResp` órfãos. Novo `showIf`/`fromRow` do EntityForm já existia. | Um só fluxo pra alocar (dia ou recorrente), menos telas |
| 2026-07-15 | **Escala — folga editável destravada + fix do texto + dia avulso ignora folga** (sem migration; `dev-local`). Correção do wizard "Gerar escala": os **dias de folga** agora ficam **editáveis para 5x2/6x1/4x3 em qualquer período** (`perguntaFolga = !ehCiclo`, não mais só no recorrente); só **5x1 e 12x36** mostram "folga automática" — e o texto que aparecia errado ("4x3: folga automática" mesmo em 5x2) foi corrigido para 12x36/5x1. No modo **"1 dia"** a geração **ignora as folgas** (backend: `folgasSemana=[]` quando `dataInicio===dataFim`) → clicar em qualquer card (mesmo num sábado) sempre aloca; o label das folgas indica "· usados na recorrência". Verificado: dia avulso num sábado (folga do 5x2) cria 1. | Folga configurável em todas as escalas por dia da semana; alocar dia avulso nunca falha |
| 2026-07-16 | **Escala — editar do card carrega os colaboradores do dia** (sem migration; `dev-local`). No fluxo "editar" (resumo do dia → editar), o `GerarEscalaWizard` recebe `colaboradoresDoDia` (ids dos escalados daquele dia) e o dropdown de colaborador passa a listar **todos os do dia** (antes filtrava só pela função da vaga → aparecia só 1). Trocar de colaborador puxa a escala dele; a etiqueta não reseta o colaborador nesse modo. O filtro por função segue no card vazio (nova alocação). | Editar/reatribuir entre os escalados do dia sem ficar preso à função da vaga |
| 2026-07-16 | **RBAC de filial no servidor + seletor só-Matriz** (mig 105; `dev-local`). (1) Seletor de unidade no topbar perdeu a opção "Todas as lojas": padrão = **Matriz**, presidente escolhe matriz ou uma filial. (2) `@UnidadeAtual()` endurecido: **fail-closed** — não-presidente sem unidade recebe `NIL_UUID` (não vê a rede), só presidente vê `null`=rede/header. (3) Helper `common/filtro-unidade.ts` (`sqlUnidade`/`condUnidade` estrito; `sqlUnidadeOuRede`/`condUnidadeOuRede` p/ config herdável). (4) Filtro por unidade aplicado (leitura + escrita forçada) em **Estoque, Desperdício, Vistoria, Contagem, Recebimento, Financeiro (títulos/caixa/DRE/fechamentos), Ponto/Pessoas, Delivery**. (5) Mig 105 faz backfill de `unidade_id` NULL→matriz do tenant só nas tabelas **transacionais** (config/catálogo/plano ficam NULL=rede, herdado). (6) **Override travado:** nos endpoints que aceitavam `?unidadeId=` (produção fila/pedidos, vendas/mesas, fiscal/config), só o **presidente** pode escolher a unidade por query; os demais são forçados ao `@UnidadeAtual` (um operador não fura a própria loja via query param). (7) **Catálogo** (cardápio/produto/ficha/fidelidade/cashback) fica **compartilhado da rede** de propósito — é config administrativa gerida pelo presidente; herança por filial fica disponível (helper pronto) se um dia quiser cardápio por loja. Conceito confirmado pelo usuário: presidente/C&O = administrativo (não opera PDV/delivery/comandas); operação nas filiais é por login gerencial/execução travado numa unidade, sem filtro. | Gerente/supervisão/execução/operadores só enxergam a própria loja pela API; filtro de unidade é exclusivo do presidente |
| 2026-07-16 | **Lote de pendências #9/#10/#13/#14/#15/#17/#18** (mig 106; `dev-local`). **#14 Pagamento online real (Mercado Pago PIX):** `common/mercadopago.ts` (cria PIX via REST + consulta status), `pagarPedidoPublico` gera QR+copia-e-cola quando há token (integracao canal `mercadopago` ou env `MP_ACCESS_TOKEN`), fallback mock sem gateway; webhook `POST /publico/cardapio/pagamento/mercadopago/webhook` confirma; mig 106 `pedido_externo.gateway_payment_id`; storefront mostra QR. **#15 Frete por distância:** `common/geocode.ts` (Nominatim/OSM, sem chave) geocoda a loja ao salvar config e o endereço do cliente no checkout raio (sem GPS). **#9 Hub de relatórios:** nova aba "Operações de caixa" (`GET /relatorios/operacoes-caixa`: cancelamentos + sangrias/suprimentos por operador) — o resto do hub já existia. **#10 Enforcement de módulos ativáveis:** `@RequireModulo`+`ModuloGuard` no KDS (`/producao/fila`); inline no terminal de ponto (origem terminal) e no App do Colaborador (perfil execução em tarefas). **#13 Preview do template por ramo:** `/inicio` mostra o que será criado antes de aplicar (blueprint ganhou `tipos`). **#17 Snapshot de estoque DIÁRIO** (era mensal) → CMV preciso em qualquer período. **#18 baixa de venda idempotente** (`onConflictDoNothing` no índice `idx_movimento_ref`; `ref_*` já existia). **#16a banner** já estava pronto (mig 054); **#16b IA** adiado. Backend build/tsc + 91 testes + front tsc OK. | Fecha 8 pendências do levantamento; gateway real e frete por distância prontos |
| 2026-07-16 | **Tutorial do cliente na landing + download do certificado do edge** (`dev-local`, sem migration). O edge passou a **servir o `ca.pem` por URL**: `https://<servidor>:3001/ca.pem` (baixa `regem-ca.crt`, content-type `application/x-x509-ca-cert`), rota nova em `backend/edge/edge-web.mjs` — troca "copiar via USB" por "abrir o link e instalar". A página `/como-funciona` (já linkada na landing) ganhou no passo do certificado instruções separadas **Windows** (Instalar Certificado → Raiz Confiável) e **Android** (Ajustes → Segurança → Instalar certificado CA) + o link de download. | Loja consegue confiar o cert nos tablets KDS/PDV/ponto sozinha, sem técnico |
| 2026-07-16 | **Épico Controle de Tarefas** (mig 107; `dev-local`). **Cadastro** por **Setor (opcional) + Função (obrigatória)** no lugar da etiqueta; responsável = escolha de quem cria (um escalado da função/setor via `GET /tarefas-instancias/responsaveis`, ou "em aberto"). **Status** com política de foto do presidente: Concluída/Parcial exigem foto **se** o presidente ligar (`entitlement` `tarefa_foto_conclusao`/`tarefa_foto_parcial`, gear no card), Não concluída = só motivo; até **3 fotos**, `data_expurgo` = +30d + cron `expurgarFotosTarefa`. **Editar** (`PATCH`) e **Excluir com motivo** (`DELETE`, soft-delete + auditoria). **Dashboard**: data + aviso de pendência no header; "Tarefas em atraso ou atenção" (½) ao lado de "Ponto de hoje" (½), "Ranking da equipe" abaixo (largura cheia); card com botão **+** (nova tarefa em modal) e tarefas **clicáveis** → `TarefaModal` (view/status/foto/editar/excluir). **Timeline**: pontos de tarefa mostram o nome; seletor de **alinhamento** da grade (15/30/60/120 min, padrão 1h). Alertas ao C&O = as não-concluídas/parciais seguem no card com destaque. mig 107: `tarefa_def.funcao_id`, `tarefa_instancia.(funcao_id, setor_id, fotos, data_expurgo)`. Backend build + 91 testes + front tsc OK. | Controle de tarefas ponta-a-ponta com comprovação por foto configurável |
| 2026-07-16 | **RBAC de cadastro de colaborador + login por senha** (sem migration; `dev-local`). Regra de quem cria quem (servidor, `colaborador.service`): **presidente** cria qualquer nível (inclusive outro presidente — sociedades/multi-admin); **gerente** cria só supervisão/execução (NÃO gerente nem presidente); nível-alvo = categoria da função principal (`podeCriarNivel`). **Login por e-mail+senha** só para gestão (presidente/gerente/supervisão): DTO ganhou `senha` (hash no create, exige e-mail; recusa para execução). **PIN** passa a ser rotulado como acesso ao **terminal de ponto**. Front (`build-secoes.ts`): opções de função filtradas pelo que o ator pode criar; campos e-mail+senha aparecem via `showIf` quando a função é de gestão; EntityForm ganhou tipo `password`. | Cadastro respeita a hierarquia no servidor; gestão entra por senha, operação por PIN |
| 2026-07-16 | **Loja RBAC + card renomeado + Gestão do cardápio com auto-pausa** (mig 108; `dev-local`). **(1)** Dados da Loja (`/loja`) exclusivos do presidente/C&O: front redireciona não-presidente, item de menu com `soPres`, e `setConfig` **ignora os campos de identidade da loja** (`CAMPOS_LOJA`) quando o ator não é presidente (gerente ainda salva a config operacional do Delivery). **(2)** Ponto: confirmado que **não há exclusão** de marcação (append-only, Portaria 671) — só ajuste com justificativa obrigatória, auditado; nenhuma mudança. **(3)** Card do dashboard "Tarefas em atraso ou atenção" → **"Gerenciamento de tarefas"**. **(4)** Submenu **Delivery → "Gestão do cardápio"** (→ `/produtos`, CRUD já existente). Pausa manual (`disponivel_cardapio=false`) agora **aparece como "Esgotado"** no cardápio em vez de sumir. **Auto-pausa por estoque** (mig 108 `produto.pausado_estoque`/`pausa_motivo`): `cardapio.service.sincronizarEsgotados` recomputa via `@OnEvent('estoque.baixado')` (emitido por `vendas.lancarSaidas` e `recebimento.confirmar`) → pausa+alerta geral (`kds.alerta.sistema` + `alertaEstoque`) ao esgotar, despausa sozinho ao repor; config `cardapio_auto_pausa` (entitlement, toggle em `/produtos`). Backend build + 91 testes + front tsc OK. | Dados da loja só do presidente; cardápio pausa/esgota sozinho com aviso |
| 2026-07-16 | **Reativar produto esgotado sem estoque (contagem negativa)** (mig 109; `dev-local`). O gestor pode despausar um produto esgotado **sem dar entrada**: `produto.permite_negativo` (mig 109) desliga o **bloqueio por estoque** (não pausa/esgota mais) mas mantém `controla_estoque=true`, então a baixa continua e o saldo do insumo fica **negativo** (contagem negativa, informativa). `computeEsgotados` ignora `permite_negativo`; `POST /produtos/:id/permite-negativo {ativo}` liga/desliga (limpa pausa + libera cardápio ao ligar). Front `/produtos`: botão **"Reativar"** (com aviso: controle de estoque será desativado + contagem negativa) nos esgotados por estoque, badge **"contagem negativa"** e **"Voltar a controlar"** para desmarcar. Backend build + 91 testes + front tsc OK. | Reativar item esgotado sem repor, ciente da contagem negativa |
| 2026-07-16 | **Sistema de temas do cardápio digital** (mig 110; `dev-local`; só apresentação). `cardapio_config.menu_theme` (`classic`|`fastfood`, aditiva, fallback classic) exposto no config e no menu público. Front: `data-menu-theme` + `--brand-primary` no root do storefront + hooks `lp-*`; **nenhum seletor CSS sem o data-attr** → o tema **classic fica pixel-perfect** (Regra de Ouro). Dark mode segue independente. **Fastfood:** grade de **Destaques**, **seções por categoria com scroll-spy** (IntersectionObserver; aba clica → rola), **item passo-a-passo** (cabeçalho de grupo sticky + pílula `x/y` + ✓ verde — dados `sel.grupos` já existiam), **bottom-nav com carrinho central** + **Buscar** (overlay filtrando os itens carregados), CSS de cards flutuantes. **Banner deep-link:** render abre `item:ID`/`category:ID`/`coupon:CODE`/URL (reusa campo `link`); **editor no cadastro** (Delivery→Banners) com "Ação ao tocar" (dropdown de produtos/categorias). Seletor "Estilo do cardápio" no admin. Zero API nova, toda a lógica/checkout compartilhada. Front tsc + backend build + 91 testes OK. | 2 temas de layout por loja sem tocar na lógica; classic intacto | 
| 2026-07-17 | **3º tema do cardápio: `grid`** (`dev-local`; só apresentação). `menu_theme` agora aceita `classic`/`fastfood`/`grid` (validação nas duas pontas). Grid = cabeçalho **curvo** na cor da loja, **chips circulares** de categoria, **grade de 2 colunas** (`gridCard`: foto `aspect-square`, nome centralizado, ＋ circular), **dock inferior flutuante** (pílula com carrinho central). Reusa a mecânica rica do item-sheet (gate `ff` estendido a grid) e o scroll-spy/`irCategoria` das seções. `classic` segue fallback pixel-perfect. Front tsc + backend build + 91 testes OK. | Terceiro layout de loja mantendo a Regra de Ouro (lógica compartilhada) |
| 2026-07-17 | **Storage de mídia local (modo offline/edge)** (sem migration; `dev-local`). `midia.service` ganhou **fallback em disco**: quando `SUPABASE_URL`/`SUPABASE_SERVICE_KEY` **não** estão definidas (caso do `.env.local`), o upload grava em `MIDIA_DIR` (padrão `backend/uploads/<tenant>/<uuid>.ext`) e devolve URL servida pela própria API em **`GET /api/v1/publico/midia/:tenant/:arquivo`** (`MidiaPublicoController`, público, sem auth, com validação anti-path-traversal: só uuid+ext, resolve dentro da raiz). `remover()` (expurgo LGPD) também apaga do disco. Base pública configurável por `MIDIA_PUBLIC_URL` (edge na LAN aponta p/ o IP). **Na nuvem, como as chaves do Supabase existem, o código continua usando o Storage do Supabase — comportamento inalterado.** `uploads/` no `.gitignore`. Backend build + 91 testes + E2E (login→upload→GET 200) OK. | Modo local 100% offline (imagem era a única dependência de nuvem restante) |
| 2026-07-17 | **Cardápio: aba "Pedidos" + Perfil + cancelamento/alteração pelo cliente** (mig 111; `dev-local`). Menu inferior **"Pedidos"** deixa de abrir o Perfil e abre um **`PedidosPanel`** próprio: lista os pedidos do cliente **incluindo o atual** — em andamento em **destaque** com status (**Aguardando aceite** = status `novo`, **Em preparo**, **Em rota**), finalizados em **fundo neutro** (clicáveis). Detalhe do pedido mostra hora, itens, **total + desconto**, número e forma de pagamento. **Pedido atual**: **Solicitar cancelamento** (enquanto não saiu p/ entrega) → abre chamado no sino tipo `cancelamento` ligado ao pedido; no Delivery a equipe **Aceita** (cancela de fato via `delivery.cancelar`, exige senha de gestor) ou **Recusa**. **Solicitar alteração** (endereço/pedido/pagamento) → só avisa no sino (tipo `mudanca`). **Pedido finalizado**: **Pedir de novo**, com **bloqueio** se foi resgate de fidelidade e o cliente não tem mais prêmio disponível. Cabeçalho: botão **"Meus dados" → "Perfil"** (sem histórico; histórico foi p/ a aba Pedidos). `perfil()` enriquecido (desconto/taxa/endereço/flag `resgate`). mig 111: `atendimento_chamado.(pedido_id, decisao)`. Front tsc+build + back build + 91 testes OK. | Cliente acompanha/gerencia o pedido pelo cardápio; equipe decide cancelamentos no sino |
| 2026-07-17 | **Cardápio: Editar tema + carrossel de banners + categorias scroll-spy + ajustes** (mig 112; `dev-local`). **(1) Ramo**: só "🍔 Bares e Restaurantes" selecionável; Varejo/Indústria/Serviços "(em breve)" desabilitados. **(2) Frete grátis = 0** passa a **desligar** a função (antes 0 dava frete grátis sempre) — checkout e cabeçalho tratam `<= 0` como off. **(3) Banners**: viram **carrossel automático** (`BannerCarousel`, troca a cada N s, default **2**, só com 2+; indicadores) — antes ficava parado no 1º; cadastro ganhou campo "segundos" e **trava de máx. 3** (`setBanners` corta em 3). **(4) Botão "Editar tema"** (ao lado de Parcelas): modal com **cor primária** (paleta do código + custom) e **toggles** de Destaques/Banner/Últimos pedidos → gravados em `cardapio_config.tema_config` (mig 112, JSONB aditivo); storefront usa `temaConfig.corPrimaria` como accent e respeita os toggles. **(5) Categorias**: removido o chip **"Todos"**; o **Clássico** passou a usar **seções por categoria com scroll-spy** (estilo iFood, como fastfood/grid já faziam) — clicar rola até a seção e o scroll acompanha a categoria. **(6)** Barra de rolagem das faixas de categoria/destaques **escondida** (`.no-scrollbar`, arrasta sem barra). mig 112: `cardapio_config.tema_config jsonb`. Front tsc+build + back build + 91 testes OK. | Personalização do tema pelo lojista + navegação de categorias unificada nos 3 temas |
| 2026-07-17 | **Cardápio: responsividade + enquadramento de imagem + cores do cabeçalho** (sem migration; `dev-local`). **(1) Responsivo (3 temas):** o storefront virou coluna central **`max-w-2xl` centrada** (full-width no celular, centrada no desktop) — `<main>` virou wrapper `.lp-menu` full-bleed + `<main>` interno com a coluna; barra do carrinho e bottom-nav em `max-w-2xl`; grade do tema grid **`grid-cols-2 sm:grid-cols-3`**. **(2) Imagens enquadradas ao centro (3 temas):** caixas de imagem com **aspect fixo + `overflow-hidden` + `object-cover object-center`** (cardProduto square `absolute inset-0`; gridCard; item-sheet com `aspect-[16/9]`) — não vazam mais do card. **(3) Cores do cabeçalho** no "Editar tema": **cor de fundo** e **cor do texto** do cabeçalho (JSONB `tema_config.corCabecalho`/`corTextoCabecalho`, null=padrão); o header aplica via `style` e os textos/botões herdam a cor. Tailwind (não Bootstrap — mantém a stack). Front tsc OK + dev servers 200. | Cardápio se adapta a celular e desktop; imagens centradas; lojista controla as cores do topo |
| _adicionar novas linhas aqui_ | | |
